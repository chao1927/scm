package com.chaobo.scm.supplier.application.operations;

import java.math.BigDecimal;
import java.time.LocalDate;
import java.time.OffsetDateTime;

/**
 * OperationViews。
 *
 * <p>位于应用层，负责用例编排、事务边界、幂等处理和跨端口协作，核心不变量仍由领域对象保护。封装与其名称一致的业务或技术职责，并保持内部实现细节不向调用方泄露。该类型只在所属限界上下文内表达该语义，跨上下文协作应通过已声明的接口或事件完成。
 *
 * @author SCM Team
 * @since 0.1.0
 */
public final class OperationViews {

    /**
     * 创建 OperationViews。
     *
     * <p>构造阶段集中接收必需依赖或恢复对象状态，确保实例创建后即可安全参与所属用例。
     */
    private OperationViews() {
    }

    /**
     * WorkItem。
     *
     * <p>位于应用层，负责用例编排、事务边界、幂等处理和跨端口协作，核心不变量仍由领域对象保护。作为不可变数据载体集中表达一组相关业务参数或查询结果。该类型只在所属限界上下文内表达该语义，跨上下文协作应通过已声明的接口或事件完成。
     *
     * @author SCM Team
     * @since 0.1.0
     */
    public record WorkItem(long id, String workType, long supplierId, String businessType, long businessId, String businessNo, String title, int assigneeType, OffsetDateTime dueAt, int status, int version) {
    }

    /**
     * Warning。
     *
     * <p>位于应用层，负责用例编排、事务边界、幂等处理和跨端口协作，核心不变量仍由领域对象保护。作为不可变数据载体集中表达一组相关业务参数或查询结果。该类型只在所属限界上下文内表达该语义，跨上下文协作应通过已声明的接口或事件完成。
     *
     * @author SCM Team
     * @since 0.1.0
     */
    public record Warning(long id, String warningType, long supplierId, String businessType, long businessId, int level, String message, OffsetDateTime occurredAt, int status, int version) {
    }

    /**
     * FailedEvent。
     *
     * <p>位于应用层，负责用例编排、事务边界、幂等处理和跨端口协作，核心不变量仍由领域对象保护。表达已经发生的业务事实，载荷用于跨事务或跨上下文可靠传播。该类型只在所属限界上下文内表达该语义，跨上下文协作应通过已声明的接口或事件完成。
     *
     * @author SCM Team
     * @since 0.1.0
     */
    public record FailedEvent(long id, String sourceSystem, String eventCode, String eventType, String consumerName, int retryCount, String reason, OffsetDateTime updatedAt, String direction) {
    }

    /**
     * Reconciliation。
     *
     * <p>位于应用层，负责用例编排、事务边界、幂等处理和跨端口协作，核心不变量仍由领域对象保护。作为不可变数据载体集中表达一组相关业务参数或查询结果。该类型只在所属限界上下文内表达该语义，跨上下文协作应通过已声明的接口或事件完成。
     *
     * @author SCM Team
     * @since 0.1.0
     */
    public record Reconciliation(long id, String type, String targetSystem, LocalDate businessDate, long localCount, long remoteCount, BigDecimal localAmount, BigDecimal remoteAmount, String differenceDetail, int status, int version) {
    }

    /**
     * Dashboard。
     *
     * <p>位于应用层，负责用例编排、事务边界、幂等处理和跨端口协作，核心不变量仍由领域对象保护。作为不可变数据载体集中表达一组相关业务参数或查询结果。该类型只在所属限界上下文内表达该语义，跨上下文协作应通过已声明的接口或事件完成。
     *
     * @author SCM Team
     * @since 0.1.0
     */
    public record Dashboard(long pendingWork, long openWarnings, long failedInboundEvents, long failedOutboundEvents, long activeQualityIssues, long openReturns, long pendingReconciliations, BigDecimal latestAverageScore) {
    }

    /**
     * ExportTask。
     *
     * <p>位于应用层，负责用例编排、事务边界、幂等处理和跨端口协作，核心不变量仍由领域对象保护。作为不可变数据载体集中表达一组相关业务参数或查询结果。该类型只在所属限界上下文内表达该语义，跨上下文协作应通过已声明的接口或事件完成。
     *
     * @author SCM Team
     * @since 0.1.0
     */
    public record ExportTask(long id, String exportType, Long supplierId, String queryJson, int status, String fileUrl, String failReason, OffsetDateTime createdAt, OffsetDateTime updatedAt, int version) {
    }
}

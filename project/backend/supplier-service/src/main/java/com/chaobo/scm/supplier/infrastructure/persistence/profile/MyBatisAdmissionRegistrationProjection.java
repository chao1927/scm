package com.chaobo.scm.supplier.infrastructure.persistence.profile;import com.chaobo.scm.supplier.application.profile.AdmissionRegistrationProjectionPort;import org.springframework.stereotype.Repository;import java.time.OffsetDateTime;
@Repository public class MyBatisAdmissionRegistrationProjection implements AdmissionRegistrationProjectionPort{private final AdmissionRegistrationProjectionMapper mapper;public MyBatisAdmissionRegistrationProjection(AdmissionRegistrationProjectionMapper mapper){this.mapper=mapper;}public void registered(long admissionId,long supplierId,String supplierCode,String eventCode,long sourceVersion,OffsetDateTime occurredAt){mapper.registered(admissionId,supplierId,supplierCode,eventCode,sourceVersion,occurredAt);}}

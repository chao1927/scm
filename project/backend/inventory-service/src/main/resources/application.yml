spring:
  application:
    name: inventory-service
  datasource:
    url: jdbc:mysql://localhost:3306/scm_inventory?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: root
  flyway:
    enabled: true
mybatis:
  configuration:
    map-underscore-to-camel-case: true

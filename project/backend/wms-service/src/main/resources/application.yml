spring:
  application:
    name: wms-service
  datasource:
    url: jdbc:mysql://localhost:3306/scm_wms?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: root
  flyway:
    enabled: true

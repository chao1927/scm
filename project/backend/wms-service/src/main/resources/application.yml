spring:
  application:
    name: wms-service
  profiles:
    active: @spring.profiles.active@
  cloud:
    nacos:
      username: ${NACOS_USERNAME:@nacos.username@}
      password: ${NACOS_PASSWORD:@nacos.password@}
      config:
        server-addr: ${NACOS_SERVER_ADDR:@nacos.server-addr@}
        namespace: ${NACOS_NAMESPACE:@nacos.namespace@}
        group: ${NACOS_GROUP:@nacos.group@}
  flyway:
    enabled: true

server:
  port: ${SERVER_PORT:8103}

scm:
  security:
    hmac-secret: ${IAM_JWT_SECRET:}
    permission-namespace: wms
  rocketmq:
    enabled: ${SCM_ROCKETMQ_ENABLED:true}
    endpoints: ${ROCKETMQ_ENDPOINTS:127.0.0.1:8081}
    wms-topic: ${ROCKETMQ_WMS_TOPIC:wms-domain-event}
    wms-consumer:
      topics: ${ROCKETMQ_WMS_CONSUMER_TOPICS:purchase-domain-event,oms-domain-event,inventory-domain-event,supplier-domain-event}
      group: ${ROCKETMQ_WMS_CONSUMER_GROUP:wms-business-event-consumer}

# 供应商系统产品功能设计

> 本文是 `docs/04-子系统功能设计/供应商系统` 的唯一产品功能设计文档，从产品视角描述菜单、页面、按钮、操作逻辑、角色权限、数据权限、查询、列表、详情、新增、修改、登录/登出和审计。领域规则参考 `docs/03-核心业务模型/01-供应商领域模型`，表字段参考 `docs/05-子系统数据库设计/01-供应商系统数据库设计.md`。

## 1. 产品定位与设计边界

| 项 | 设计 |
| --- | --- |
| 系统 | 供应商系统 |
| 设计对象 | 后台管理页面、业务操作页面、配置页面、日志页面和权限控制 |
| 不承载内容 | 数据库 DDL、领域聚合细节、跨系统接口实现；这些分别在数据库设计、核心业务模型、接口事件文档维护 |
| 产品原则 | 先通过权限系统完成登录和授权，再按菜单进入功能；所有写操作必须校验按钮权限、数据权限、状态机和幂等键 |

## 2. 登录、登出与全局交互

| 页面/动作 | 使用角色 | 展示字段 | 操作逻辑 |
| --- | --- | --- | --- |
| 登录页 | 未登录用户 | 账号、密码、验证码/MFA、租户/组织可选 | 用户输入凭证后调用权限系统认证；成功写入 access_token/refresh_token，拉取菜单、按钮权限和数据范围；失败记录登录日志。 |
| 退出登录 | 已登录用户 | 当前用户、当前系统、会话过期时间 | 用户点击退出后吊销当前 Token，清理前端缓存、菜单缓存和权限缓存，并跳转登录页。 |
| 个人中心 | 已登录用户 | 姓名、账号、手机号、邮箱、角色、所属组织 | 读取当前用户资料和角色权限；修改密码或个人资料时记录操作日志。 |

全局页面规则：

- 所有列表页默认包含查询、重置、分页、排序、导出入口；导出必须受数据权限约束。
- 所有详情页默认包含基础信息、明细信息、状态流转、领域事件、操作日志。
- 所有新增/编辑页默认校验必填、唯一、枚举、引用关系、乐观锁和状态机。
- 所有写操作默认记录操作日志；会改变业务事实的操作写入领域事件发布表。

## 3. 角色、菜单权限与数据权限

| 角色 | 可用功能 | 典型动作 | 数据范围 |
| --- | --- | --- | --- |
| 供应商业务员 | 订单确认、交期反馈、ASN 创建 | 确认采购订单、拆分送货、上传发货信息 | 仅本人绑定供应商数据 |
| 供应商财务 | 对账、发票、结算信息 | 查看对账单、上传发票、确认差异 | 仅本人绑定供应商数据 |
| 供应商质量 | 质检反馈、资质资料 | 上传资质、查看质量异常 | 仅本人绑定供应商数据 |
| 采购员 | 供应商协同监控、评分查看 | 催确认、处理差异、维护供货关系、参考评分选择供应商 | 本人、本部门或被分配业务范围 |
| 质量人员 | 供应商质量管理、质量评分确认 | 查看不良、发起整改、确认质量扣分 | 本人、本部门或被分配业务范围 |
| 采购经理 | 供应商绩效评审 | 查看评分排名、调整评分权重、冻结低分供应商 | 本组织及下级组织，或审批授权范围 |
| 系统管理员 | 账号和权限 | 开通供应商账号、绑定供应商主体 | 按授权组织和系统配置范围 |

数据权限维度：

| 维度 | 控制方式 | 页面效果 |
| --- | --- | --- |
| 组织 | 按当前组织、下级组织或自定义组织集合过滤 | 列表、详情、导出只返回可见组织数据 |
| 仓库 | WMS、库存、OMS、BMS 按仓库授权过滤 | 未授权仓库不可查询、不可操作 |
| 货主 | 多货主场景按货主授权过滤 | 只能查看和操作授权货主的单据和库存 |
| 供应商/客户 | 外部协同用户绑定供应商或客户 | 只展示自身协同单据，不展示其他主体数据 |
| 本人/部门 | 普通业务用户可限制为本人或本部门 | 请购、待办、审批等页面优先使用该范围 |

## 4. 左侧菜单与页面总览

| 菜单 | 路由 | 页面目的 | 主要按钮 |
| --- | --- | --- | --- |
| 供应商工作台 | `/supplier/workbench` | 展示待办、预警、订单、ASN、退供、对账 | 查看待办、进入处理 |
| 供应商档案页 | `/supplier/profile` | 查看主数据同步来的供应商资料快照 | 查看、申请资料变更 |
| 供应商账号绑定页 | `/supplier/user-bindings` | 管理供应商用户与供应商主体关系 | 绑定、解绑、启用、停用 |
| 供应商商品页 | `/supplier/skus` | 查看可供 SKU、MOQ、交期、供应商 SKU | 查看、维护供应商 SKU、申请变更 |
| 采购订单协同页 | `/supplier/po-confirms` | 供应商确认采购订单或反馈差异 | 确认、拒绝、反馈差异 |
| ASN 管理页 | `/supplier/asns` | 供应商创建送货预告和预约 | 新增、编辑、提交、取消、打印 |
| 退供协同页 | `/supplier/returns` | 供应商确认退供、收货或反馈差异 | 确认、拒绝、签收、反馈差异 |
| 对账协同页 | `/supplier/reconciliations` | 供应商查看对账并上传发票 | 确认、反馈差异、上传发票 |
| 质量协同页 | `/supplier/quality-issues` | 查看质检异常和整改任务 | 查看、提交整改、上传附件 |
| 供应商评分页 | `/supplier/scores` | 查看综合评分、维度评分和趋势 | 查看明细、导出、发起重算 |
| 评分规则页 | `/supplier/score-rules` | 配置评分维度、权重、阈值 | 新增、编辑、启停、发布 |
| 整改管理页 | `/supplier/rectifications` | 管理低分或质量异常触发的整改 | 发起、提交、审核、关闭 |
| 操作日志页 | `/supplier/operation-logs` | 查询供应商协同关键操作 | 查询、导出 |
| 枚举配置页 | `/supplier/enums` | 维护供应商系统枚举 | 新增、编辑、排序、停用 |

## 5. 页面详细设计

### 供应商工作台

| 项 | 设计 |
| --- | --- |
| 菜单路由 | `/supplier/workbench` |
| 主要用户 | 业务操作员、业务主管、系统管理员；外部协同页面按供应商/客户/货主数据范围过滤 |
| 业务对象 | 质量问题 |
| 关联表/读模型 | `sup_quality` |
| 页面目的 | 展示待办、预警、订单、ASN、退供、对账 |
| 菜单权限 | `supplier:workbench:read` |

| 区域 | 字段/按钮 | 规则 |
| --- | --- | --- |
| 查询条件 | 问题单号、质检单、退供单、客诉、抽检、来源单号、外观、规格、包装、性能、证照、待处理、整改中、待审核、已关闭 | 支持组合查询；未选条件默认按用户数据权限过滤 |
| 排序 | 创建时间、更新时间 | 默认按更新时间倒序；支持分页，每页 10/20/50 条 |
| 列表展示 | 待确认订单数、待发货 ASN、待处理退供、待确认对账、质量问题、质量问题ID、问题单号、质检单、退供单、客诉、抽检、来源单号、外观、规格、包装、性能、证照 | 列表展示业务识别字段、状态、关键金额/数量和更新时间 |
| 顶部按钮 | 查询、重置 | 根据按钮权限和数据范围显示 |
| 行内操作 | 查看详情、查看待办、进入处理 | 终态数据只允许查看、导出或复制，不允许直接编辑 |
| 详情展示 | 质量问题ID、问题单号、质检单、退供单、客诉、抽检、来源单号、外观、规格、包装、性能、证照、轻微、一般、严重、致命、待处理、整改中、待审核、已关闭、整改截止时间、关闭时间 | 详情页展示基础信息、明细、状态流转、事件记录和操作日志 |
| 新增页面 | 质量问题ID、问题单号、质检单、退供单、客诉、抽检、来源单号、外观、规格、包装、性能、证照、轻微、一般、严重、致命、整改截止时间、关闭时间 | 新增时校验必填、唯一、枚举、引用关系和数据权限 |
| 修改页面 | 质量问题ID、问题单号、质检单、退供单、客诉、抽检、来源单号、外观、规格、包装、性能、证照、轻微、一般、严重、致命、整改截止时间、关闭时间 | 已生效/终态数据只允许走变更、关闭、停用或补偿流程 |

| 操作 | 权限点 | 操作逻辑 | 结果/审计 |
| --- | --- | --- | --- |
| 查询 | `supplier:supplierworkbench:read` | 读取读模型或列表投影，不修改业务状态 | 不记录业务事件，可记录访问日志 |
| 查看待办 | `supplier:supplierworkbench:page` | 执行业务动作，后端校验状态、权限、幂等键和参数 | 记录操作日志，按需要发布领域事件 |
| 进入处理 | `supplier:supplierworkbench:page` | 执行业务动作，后端校验状态、权限、幂等键和参数 | 记录操作日志，按需要发布领域事件 |

### 供应商档案页

| 项 | 设计 |
| --- | --- |
| 菜单路由 | `/supplier/profile` |
| 主要用户 | 业务操作员、业务主管、系统管理员；外部协同页面按供应商/客户/货主数据范围过滤 |
| 业务对象 | 供应商用户绑定 |
| 关联表/读模型 | `sup_supplier_user` |
| 页面目的 | 查看主数据同步来的供应商资料快照 |
| 菜单权限 | `supplier:profile:read` |

| 区域 | 字段/按钮 | 规则 |
| --- | --- | --- |
| 查询条件 | 供应商编码、名称、状态、启用、停用 | 支持组合查询；未选条件默认按用户数据权限过滤 |
| 排序 | 状态 | 默认按更新时间倒序；支持分页，每页 10/20/50 条 |
| 列表展示 | 供应商编码、名称、等级、状态、联系人、结算方式、供应商用户绑定ID、业务、财务、质量、管理员、是否主账号、启用、停用 | 列表展示业务识别字段、状态、关键金额/数量和更新时间 |
| 顶部按钮 | 查询、重置 | 根据按钮权限和数据范围显示 |
| 行内操作 | 查看、申请资料变更 | 终态数据只允许查看、导出或复制，不允许直接编辑 |
| 详情展示 | 供应商用户绑定ID、业务、财务、质量、管理员、是否主账号、启用、停用、绑定人、绑定时间、解绑时间 | 详情页展示基础信息、明细、状态流转、事件记录和操作日志 |
| 新增页面 | 供应商用户绑定ID、业务、财务、质量、管理员、是否主账号、绑定人、绑定时间、解绑时间 | 新增时校验必填、唯一、枚举、引用关系和数据权限 |
| 修改页面 | 供应商用户绑定ID、业务、财务、质量、管理员、是否主账号、绑定人、绑定时间、解绑时间 | 已生效/终态数据只允许走变更、关闭、停用或补偿流程 |

| 操作 | 权限点 | 操作逻辑 | 结果/审计 |
| --- | --- | --- | --- |
| 查询 | `supplier:supplier_profile:read` | 读取读模型或列表投影，不修改业务状态 | 不记录业务事件，可记录访问日志 |
| 查看 | `supplier:supplier_profile:read` | 读取读模型或列表投影，不修改业务状态 | 不记录业务事件，可记录访问日志 |
| 申请资料变更 | `supplier:supplier_profile:page` | 执行业务动作，后端校验状态、权限、幂等键和参数 | 记录操作日志，按需要发布领域事件 |

### 供应商账号绑定页

| 项 | 设计 |
| --- | --- |
| 菜单路由 | `/supplier/user-bindings` |
| 主要用户 | 业务操作员、业务主管、系统管理员；外部协同页面按供应商/客户/货主数据范围过滤 |
| 业务对象 | 供应商用户绑定 |
| 关联表/读模型 | `sup_supplier_user` |
| 页面目的 | 管理供应商用户与供应商主体关系 |
| 菜单权限 | `supplier:user_binding:read` |

| 区域 | 字段/按钮 | 规则 |
| --- | --- | --- |
| 查询条件 | 绑定供应商、状态、启用、停用 | 支持组合查询；未选条件默认按用户数据权限过滤 |
| 排序 | 状态 | 默认按更新时间倒序；支持分页，每页 10/20/50 条 |
| 列表展示 | 用户、手机号、邮箱、绑定供应商、状态、供应商用户绑定ID、业务、财务、质量、管理员、是否主账号、启用、停用、绑定人 | 列表展示业务识别字段、状态、关键金额/数量和更新时间 |
| 顶部按钮 | 查询、重置 | 根据按钮权限和数据范围显示 |
| 行内操作 | 查看详情、绑定、解绑、启用、停用 | 终态数据只允许查看、导出或复制，不允许直接编辑 |
| 详情展示 | 供应商用户绑定ID、业务、财务、质量、管理员、是否主账号、启用、停用、绑定人、绑定时间、解绑时间 | 详情页展示基础信息、明细、状态流转、事件记录和操作日志 |
| 新增页面 | 供应商用户绑定ID、业务、财务、质量、管理员、是否主账号、绑定人、绑定时间、解绑时间 | 新增时校验必填、唯一、枚举、引用关系和数据权限 |
| 修改页面 | 供应商用户绑定ID、业务、财务、质量、管理员、是否主账号、绑定人、绑定时间、解绑时间 | 已生效/终态数据只允许走变更、关闭、停用或补偿流程 |

| 操作 | 权限点 | 操作逻辑 | 结果/审计 |
| --- | --- | --- | --- |
| 查询 | `supplier:supplier:read` | 读取读模型或列表投影，不修改业务状态 | 不记录业务事件，可记录访问日志 |
| 绑定 | `supplier:supplier:page` | 执行业务动作，后端校验状态、权限、幂等键和参数 | 记录操作日志，按需要发布领域事件 |
| 解绑 | `supplier:supplier:page` | 执行业务动作，后端校验状态、权限、幂等键和参数 | 记录操作日志，按需要发布领域事件 |
| 启用 | `supplier:supplier:enable` | 执行状态推进命令；状态流转必须符合领域模型状态机 | 写操作日志，写领域事件表，刷新列表读模型 |
| 停用 | `supplier:supplier:disable` | 执行状态推进命令；状态流转必须符合领域模型状态机 | 写操作日志，写领域事件表，刷新列表读模型 |

### 供应商商品页

| 项 | 设计 |
| --- | --- |
| 菜单路由 | `/supplier/skus` |
| 主要用户 | 业务操作员、业务主管、系统管理员；外部协同页面按供应商/客户/货主数据范围过滤 |
| 业务对象 | 供应商商品 |
| 关联表/读模型 | `sup_supplier_item` |
| 页面目的 | 查看可供 SKU、MOQ、交期、供应商 SKU |
| 菜单权限 | `supplier:sku:read` |

| 区域 | 字段/按钮 | 规则 |
| --- | --- | --- |
| 查询条件 | SKU、供应商 SKU、报价状态、内部 SKU 编码、供应商商品编码、可供、暂停、停供 | 支持组合查询；未选条件默认按用户数据权限过滤 |
| 排序 | 状态 | 默认按更新时间倒序；支持分页，每页 10/20/50 条 |
| 列表展示 | SKU、供应商 SKU、MOQ、供货周期、报价状态、供应商商品ID、内部 SKU 编码、供应商商品编码、最小起订量、最小包装量 | 列表展示业务识别字段、状态、关键金额/数量和更新时间 |
| 顶部按钮 | 查询、重置 | 根据按钮权限和数据范围显示 |
| 行内操作 | 查看、维护供应商 SKU、申请变更 | 终态数据只允许查看、导出或复制，不允许直接编辑 |
| 详情展示 | 供应商商品ID、内部 SKU 编码、供应商商品编码、最小起订量、最小包装量、供货周期、可供、暂停、停供、生效日期、失效日期 | 详情页展示基础信息、明细、状态流转、事件记录和操作日志 |
| 新增页面 | 供应商商品ID、内部 SKU 编码、供应商商品编码、最小起订量、最小包装量、供货周期、生效日期、失效日期 | 新增时校验必填、唯一、枚举、引用关系和数据权限 |
| 修改页面 | 供应商商品ID、内部 SKU 编码、供应商商品编码、最小起订量、最小包装量、供货周期、生效日期、失效日期 | 已生效/终态数据只允许走变更、关闭、停用或补偿流程 |

| 操作 | 权限点 | 操作逻辑 | 结果/审计 |
| --- | --- | --- | --- |
| 查询 | `supplier:supplierproduct:read` | 读取读模型或列表投影，不修改业务状态 | 不记录业务事件，可记录访问日志 |
| 查看 | `supplier:supplierproduct:read` | 读取读模型或列表投影，不修改业务状态 | 不记录业务事件，可记录访问日志 |
| 维护供应商 SKU | `supplier:supplierproduct:supplier_sku` | 执行业务动作，后端校验状态、权限、幂等键和参数 | 记录操作日志，按需要发布领域事件 |
| 申请变更 | `supplier:supplierproduct:page` | 执行业务动作，后端校验状态、权限、幂等键和参数 | 记录操作日志，按需要发布领域事件 |

### 采购订单协同页

| 项 | 设计 |
| --- | --- |
| 菜单路由 | `/supplier/po-confirms` |
| 主要用户 | 业务操作员、业务主管、系统管理员；外部协同页面按供应商/客户/货主数据范围过滤 |
| 业务对象 | 采购订单协同行 |
| 关联表/读模型 | `sup_order_line` |
| 页面目的 | 供应商确认采购订单或反馈差异 |
| 菜单权限 | `supplier:po_confirm:read` |

| 区域 | 字段/按钮 | 规则 |
| --- | --- | --- |
| 查询条件 | PO 单号、SKU、状态、SKU 编码、待确认、已确认、有差异、已拒绝 | 支持组合查询；未选条件默认按用户数据权限过滤 |
| 排序 | 状态、数量 | 默认按更新时间倒序；支持分页，每页 10/20/50 条 |
| 列表展示 | PO 单号、SKU、采购数量、确认数量、交期、状态、采购订单协同行ID、SKU 编码、要求交期、确认交期 | 列表展示业务识别字段、状态、关键金额/数量和更新时间 |
| 顶部按钮 | 查询、重置 | 根据按钮权限和数据范围显示 |
| 行内操作 | 查看详情、确认、拒绝、反馈差异 | 终态数据只允许查看、导出或复制，不允许直接编辑 |
| 详情展示 | 采购订单协同行ID、SKU 编码、采购数量、确认数量、要求交期、确认交期、待确认、已确认、有差异、已拒绝、行差异说明 | 详情页展示基础信息、明细、状态流转、事件记录和操作日志 |
| 新增页面 | 采购订单协同行ID、SKU 编码、采购数量、确认数量、要求交期、确认交期、行差异说明 | 新增时校验必填、唯一、枚举、引用关系和数据权限 |
| 修改页面 | 采购订单协同行ID、SKU 编码、采购数量、确认数量、要求交期、确认交期、行差异说明 | 已生效/终态数据只允许走变更、关闭、停用或补偿流程 |

| 操作 | 权限点 | 操作逻辑 | 结果/审计 |
| --- | --- | --- | --- |
| 查询 | `supplier:purchase_order:read` | 读取读模型或列表投影，不修改业务状态 | 不记录业务事件，可记录访问日志 |
| 确认 | `supplier:purchase_order:page` | 执行业务动作，后端校验状态、权限、幂等键和参数 | 记录操作日志，按需要发布领域事件 |
| 拒绝 | `supplier:purchase_order:page` | 执行业务动作，后端校验状态、权限、幂等键和参数 | 记录操作日志，按需要发布领域事件 |
| 反馈差异 | `supplier:purchase_order:page` | 执行业务动作，后端校验状态、权限、幂等键和参数 | 记录操作日志，按需要发布领域事件 |

### ASN 管理页

| 项 | 设计 |
| --- | --- |
| 菜单路由 | `/supplier/asns` |
| 主要用户 | 业务操作员、业务主管、系统管理员；外部协同页面按供应商/客户/货主数据范围过滤 |
| 业务对象 | ASN 主表 |
| 关联表/读模型 | `sup_asn` |
| 页面目的 | 供应商创建送货预告和预约 |
| 菜单权限 | `supplier:asn:read` |

| 区域 | 字段/按钮 | 规则 |
| --- | --- | --- |
| 查询条件 | ASN 单号、PO 单号、状态、承运商、运单号、草稿、已提交、已预约、已发货、已到仓、已收货、已取消、已关闭 | 支持组合查询；未选条件默认按用户数据权限过滤 |
| 排序 | 状态、数量 | 默认按更新时间倒序；支持分页，每页 10/20/50 条 |
| 列表展示 | ASN 单号、PO 单号、预计到仓、发货数量、状态、ASN 主表ID、预计到仓时间、发货时间、承运商、运单号 | 列表展示业务识别字段、状态、关键金额/数量和更新时间 |
| 顶部按钮 | 查询、重置、新增 | 根据按钮权限和数据范围显示 |
| 行内操作 | 查看详情、编辑、提交、取消、打印 | 终态数据只允许查看、导出或复制，不允许直接编辑 |
| 详情展示 | ASN 主表ID、ASN 单号、预计到仓时间、发货时间、承运商、运单号、草稿、已提交、已预约、已发货、已到仓、已收货、已取消、已关闭、取消原因 | 详情页展示基础信息、明细、状态流转、事件记录和操作日志 |
| 新增页面 | ASN 主表ID、ASN 单号、预计到仓时间、发货时间、承运商、运单号、取消原因 | 新增时校验必填、唯一、枚举、引用关系和数据权限 |
| 修改页面 | ASN 主表ID、ASN 单号、预计到仓时间、发货时间、承运商、运单号、取消原因 | 已生效/终态数据只允许走变更、关闭、停用或补偿流程 |

| 操作 | 权限点 | 操作逻辑 | 结果/审计 |
| --- | --- | --- | --- |
| 查询 | `supplier:asn:read` | 读取读模型或列表投影，不修改业务状态 | 不记录业务事件，可记录访问日志 |
| 新增 | `supplier:asn:create` | 打开新增页，提交后由后端应用服务校验并创建聚合 | 记录操作日志，必要时发布已创建事件 |
| 编辑 | `supplier:asn:update` | 加载当前版本，校验乐观锁、状态机和数据权限后保存 | 记录前后快照，必要时发布已变更事件 |
| 提交 | `supplier:asn:submit` | 执行状态推进命令；状态流转必须符合领域模型状态机 | 写操作日志，写领域事件表，刷新列表读模型 |
| 取消 | `supplier:asn:cancel` | 执行状态推进命令；状态流转必须符合领域模型状态机 | 写操作日志，写领域事件表，刷新列表读模型 |
| 打印 | `supplier:asn:page` | 执行业务动作，后端校验状态、权限、幂等键和参数 | 记录操作日志，按需要发布领域事件 |

### 退供协同页

| 项 | 设计 |
| --- | --- |
| 菜单路由 | `/supplier/returns` |
| 主要用户 | 业务操作员、业务主管、系统管理员；外部协同页面按供应商/客户/货主数据范围过滤 |
| 业务对象 | 退供协同单 |
| 关联表/读模型 | `sup_supplier_return` |
| 页面目的 | 供应商确认退供、收货或反馈差异 |
| 菜单权限 | `supplier:return:read` |

| 区域 | 字段/按钮 | 规则 |
| --- | --- | --- |
| 查询条件 | 退供单号、SKU、状态、退供协同单号、待确认、已确认、已拒绝、已签收、差异待处理、已关闭 | 支持组合查询；未选条件默认按用户数据权限过滤 |
| 排序 | 状态、数量 | 默认按更新时间倒序；支持分页，每页 10/20/50 条 |
| 列表展示 | 退供单号、SKU、退供数量、原因、状态、退供协同单ID、退供协同单号、待确认、已确认、已拒绝、已签收、差异待处理、已关闭、退货地址、确认接收数量 | 列表展示业务识别字段、状态、关键金额/数量和更新时间 |
| 顶部按钮 | 查询、重置 | 根据按钮权限和数据范围显示 |
| 行内操作 | 查看详情、确认、拒绝、签收、反馈差异 | 终态数据只允许查看、导出或复制，不允许直接编辑 |
| 详情展示 | 退供协同单ID、退供协同单号、待确认、已确认、已拒绝、已签收、差异待处理、已关闭、退货地址、确认接收数量、签收数量、差异原因、确认时间、签收时间 | 详情页展示基础信息、明细、状态流转、事件记录和操作日志 |
| 新增页面 | 退供协同单ID、退供协同单号、退货地址、确认接收数量、签收数量、差异原因、确认时间、签收时间 | 新增时校验必填、唯一、枚举、引用关系和数据权限 |
| 修改页面 | 退供协同单ID、退供协同单号、退货地址、确认接收数量、签收数量、差异原因、确认时间、签收时间 | 已生效/终态数据只允许走变更、关闭、停用或补偿流程 |

| 操作 | 权限点 | 操作逻辑 | 结果/审计 |
| --- | --- | --- | --- |
| 查询 | `supplier:supplier_return:read` | 读取读模型或列表投影，不修改业务状态 | 不记录业务事件，可记录访问日志 |
| 确认 | `supplier:supplier_return:page` | 执行业务动作，后端校验状态、权限、幂等键和参数 | 记录操作日志，按需要发布领域事件 |
| 拒绝 | `supplier:supplier_return:page` | 执行业务动作，后端校验状态、权限、幂等键和参数 | 记录操作日志，按需要发布领域事件 |
| 签收 | `supplier:supplier_return:page` | 执行业务动作，后端校验状态、权限、幂等键和参数 | 记录操作日志，按需要发布领域事件 |
| 反馈差异 | `supplier:supplier_return:page` | 执行业务动作，后端校验状态、权限、幂等键和参数 | 记录操作日志，按需要发布领域事件 |

### 对账协同页

| 项 | 设计 |
| --- | --- |
| 菜单路由 | `/supplier/reconciliations` |
| 主要用户 | 业务操作员、业务主管、系统管理员；外部协同页面按供应商/客户/货主数据范围过滤 |
| 业务对象 | 对账协同单 |
| 关联表/读模型 | `sup_reconcile` |
| 页面目的 | 供应商查看对账并上传发票 |
| 菜单权限 | `supplier:recon:read` |

| 区域 | 字段/按钮 | 规则 |
| --- | --- | --- |
| 查询条件 | 对账单号、状态、待确认、已确认、差异待处理、已开票、已关闭、发票号 | 支持组合查询；未选条件默认按用户数据权限过滤 |
| 排序 | 状态、金额 | 默认按更新时间倒序；支持分页，每页 10/20/50 条 |
| 列表展示 | 对账单号、金额、税额、差异金额、状态、对账协同单ID、应付金额、待确认、已确认、差异待处理、已开票、已关闭、发票号、发票附件 | 列表展示业务识别字段、状态、关键金额/数量和更新时间 |
| 顶部按钮 | 查询、重置 | 根据按钮权限和数据范围显示 |
| 行内操作 | 查看详情、确认、反馈差异、上传发票 | 终态数据只允许查看、导出或复制，不允许直接编辑 |
| 详情展示 | 对账协同单ID、对账单号、应付金额、税额、差异金额、待确认、已确认、差异待处理、已开票、已关闭、发票号、发票附件、确认时间 | 详情页展示基础信息、明细、状态流转、事件记录和操作日志 |
| 新增页面 | 对账协同单ID、对账单号、应付金额、税额、差异金额、发票号、发票附件、确认时间 | 新增时校验必填、唯一、枚举、引用关系和数据权限 |
| 修改页面 | 对账协同单ID、对账单号、应付金额、税额、差异金额、发票号、发票附件、确认时间 | 已生效/终态数据只允许走变更、关闭、停用或补偿流程 |

| 操作 | 权限点 | 操作逻辑 | 结果/审计 |
| --- | --- | --- | --- |
| 查询 | `supplier:reconcile:read` | 读取读模型或列表投影，不修改业务状态 | 不记录业务事件，可记录访问日志 |
| 确认 | `supplier:reconcile:page` | 执行业务动作，后端校验状态、权限、幂等键和参数 | 记录操作日志，按需要发布领域事件 |
| 反馈差异 | `supplier:reconcile:page` | 执行业务动作，后端校验状态、权限、幂等键和参数 | 记录操作日志，按需要发布领域事件 |
| 上传发票 | `supplier:reconcile:invoice` | 执行业务动作，后端校验状态、权限、幂等键和参数 | 记录操作日志，按需要发布领域事件 |

### 质量协同页

| 项 | 设计 |
| --- | --- |
| 菜单路由 | `/supplier/quality-issues` |
| 主要用户 | 业务操作员、业务主管、系统管理员；外部协同页面按供应商/客户/货主数据范围过滤 |
| 业务对象 | 供应商商品 |
| 关联表/读模型 | `sup_supplier_item` |
| 页面目的 | 查看质检异常和整改任务 |
| 菜单权限 | `supplier:quality:read` |

| 区域 | 字段/按钮 | 规则 |
| --- | --- | --- |
| 查询条件 | 问题单号、SKU、整改状态、内部 SKU 编码、供应商商品编码、可供、暂停、停供 | 支持组合查询；未选条件默认按用户数据权限过滤 |
| 排序 | 状态 | 默认按更新时间倒序；支持分页，每页 10/20/50 条 |
| 列表展示 | 问题单号、SKU、问题类型、责任判定、整改状态、供应商商品ID、内部 SKU 编码、供应商商品编码、最小起订量、最小包装量 | 列表展示业务识别字段、状态、关键金额/数量和更新时间 |
| 顶部按钮 | 查询、重置 | 根据按钮权限和数据范围显示 |
| 行内操作 | 查看、提交整改、上传附件 | 终态数据只允许查看、导出或复制，不允许直接编辑 |
| 详情展示 | 供应商商品ID、内部 SKU 编码、供应商商品编码、最小起订量、最小包装量、供货周期、可供、暂停、停供、生效日期、失效日期 | 详情页展示基础信息、明细、状态流转、事件记录和操作日志 |
| 新增页面 | 供应商商品ID、内部 SKU 编码、供应商商品编码、最小起订量、最小包装量、供货周期、生效日期、失效日期 | 新增时校验必填、唯一、枚举、引用关系和数据权限 |
| 修改页面 | 供应商商品ID、内部 SKU 编码、供应商商品编码、最小起订量、最小包装量、供货周期、生效日期、失效日期 | 已生效/终态数据只允许走变更、关闭、停用或补偿流程 |

| 操作 | 权限点 | 操作逻辑 | 结果/审计 |
| --- | --- | --- | --- |
| 查询 | `supplier:page:read` | 读取读模型或列表投影，不修改业务状态 | 不记录业务事件，可记录访问日志 |
| 查看 | `supplier:page:read` | 读取读模型或列表投影，不修改业务状态 | 不记录业务事件，可记录访问日志 |
| 提交整改 | `supplier:page:page` | 执行业务动作，后端校验状态、权限、幂等键和参数 | 记录操作日志，按需要发布领域事件 |
| 上传附件 | `supplier:page:page` | 执行业务动作，后端校验状态、权限、幂等键和参数 | 记录操作日志，按需要发布领域事件 |

### 供应商评分页

| 项 | 设计 |
| --- | --- |
| 菜单路由 | `/supplier/scores` |
| 主要用户 | 业务操作员、业务主管、系统管理员；外部协同页面按供应商/客户/货主数据范围过滤 |
| 业务对象 | 供应商评分 |
| 关联表/读模型 | `sup_supplier_score` |
| 页面目的 | 查看综合评分、维度评分和趋势 |
| 菜单权限 | `supplier:score:read` |

| 区域 | 字段/按钮 | 规则 |
| --- | --- | --- |
| 查询条件 | 月度、季度、半年、年度、待计算、已计算、正常、预警、整改中、冻结建议、已冻结 | 支持组合查询；未选条件默认按用户数据权限过滤 |
| 排序 | 创建时间、更新时间 | 默认按更新时间倒序；支持分页，每页 10/20/50 条 |
| 列表展示 | 周期、综合分、等级、质量分、价格分、交付分、供应商评分ID、评分周期、月度、季度、半年、年度、综合评分 | 列表展示业务识别字段、状态、关键金额/数量和更新时间 |
| 顶部按钮 | 查询、重置、导出 | 根据按钮权限和数据范围显示 |
| 行内操作 | 查看详情、查看明细、发起重算 | 终态数据只允许查看、导出或复制，不允许直接编辑 |
| 详情展示 | 供应商评分ID、评分周期、月度、季度、半年、年度、综合评分、A/B/C/D/E、待计算、已计算、正常、预警、整改中、冻结建议、已冻结、预警原因、计算时间、是否人工修正、修正原因 | 详情页展示基础信息、明细、状态流转、事件记录和操作日志 |
| 新增页面 | 供应商评分ID、评分周期、月度、季度、半年、年度、综合评分、A/B/C/D/E、预警原因、计算时间、是否人工修正、修正原因 | 新增时校验必填、唯一、枚举、引用关系和数据权限 |
| 修改页面 | 供应商评分ID、评分周期、月度、季度、半年、年度、综合评分、A/B/C/D/E、预警原因、计算时间、是否人工修正、修正原因 | 已生效/终态数据只允许走变更、关闭、停用或补偿流程 |

| 操作 | 权限点 | 操作逻辑 | 结果/审计 |
| --- | --- | --- | --- |
| 查询 | `supplier:supplier_score:read` | 读取读模型或列表投影，不修改业务状态 | 不记录业务事件，可记录访问日志 |
| 查看明细 | `supplier:supplier_score:page` | 执行业务动作，后端校验状态、权限、幂等键和参数 | 记录操作日志，按需要发布领域事件 |
| 导出 | `supplier:supplier_score:export` | 导出使用当前查询条件和数据权限；导入先生成任务并逐行校验 | 记录导入/导出日志，失败明细可下载 |
| 发起重算 | `supplier:supplier_score:page` | 执行业务动作，后端校验状态、权限、幂等键和参数 | 记录操作日志，按需要发布领域事件 |

### 评分规则页

| 项 | 设计 |
| --- | --- |
| 菜单路由 | `/supplier/score-rules` |
| 主要用户 | 业务操作员、业务主管、系统管理员；外部协同页面按供应商/客户/货主数据范围过滤 |
| 业务对象 | 评分规则 |
| 关联表/读模型 | `sup_score_rule` |
| 页面目的 | 配置评分维度、权重、阈值 |
| 菜单权限 | `supplier:score_rule:read` |

| 区域 | 字段/按钮 | 规则 |
| --- | --- | --- |
| 查询条件 | 生效时间、规则编码、阈值、比例、扣分、人工、启用、停用 | 支持组合查询；未选条件默认按用户数据权限过滤 |
| 排序 | 创建时间、更新时间 | 默认按更新时间倒序；支持分页，每页 10/20/50 条 |
| 列表展示 | 维度、权重、公式、预警阈值、生效时间、评分规则ID、规则编码、评分维度、维度权重、阈值、比例、扣分、人工 | 列表展示业务识别字段、状态、关键金额/数量和更新时间 |
| 顶部按钮 | 查询、重置、新增 | 根据按钮权限和数据范围显示 |
| 行内操作 | 查看详情、编辑、启停、发布 | 终态数据只允许查看、导出或复制，不允许直接编辑 |
| 详情展示 | 评分规则ID、规则编码、评分维度、维度权重、阈值、比例、扣分、人工、公式配置、预警阈值、生效日期、失效日期、启用、停用 | 详情页展示基础信息、明细、状态流转、事件记录和操作日志 |
| 新增页面 | 评分规则ID、规则编码、评分维度、维度权重、阈值、比例、扣分、人工、公式配置、预警阈值、生效日期、失效日期 | 新增时校验必填、唯一、枚举、引用关系和数据权限 |
| 修改页面 | 评分规则ID、规则编码、评分维度、维度权重、阈值、比例、扣分、人工、公式配置、预警阈值、生效日期、失效日期 | 已生效/终态数据只允许走变更、关闭、停用或补偿流程 |

| 操作 | 权限点 | 操作逻辑 | 结果/审计 |
| --- | --- | --- | --- |
| 查询 | `supplier:page:read` | 读取读模型或列表投影，不修改业务状态 | 不记录业务事件，可记录访问日志 |
| 新增 | `supplier:page:create` | 打开新增页，提交后由后端应用服务校验并创建聚合 | 记录操作日志，必要时发布已创建事件 |
| 编辑 | `supplier:page:update` | 加载当前版本，校验乐观锁、状态机和数据权限后保存 | 记录前后快照，必要时发布已变更事件 |
| 启停 | `supplier:page:page` | 执行业务动作，后端校验状态、权限、幂等键和参数 | 记录操作日志，按需要发布领域事件 |
| 发布 | `supplier:page:release` | 执行状态推进命令；状态流转必须符合领域模型状态机 | 写操作日志，写领域事件表，刷新列表读模型 |

### 整改管理页

| 项 | 设计 |
| --- | --- |
| 菜单路由 | `/supplier/rectifications` |
| 主要用户 | 业务操作员、业务主管、系统管理员；外部协同页面按供应商/客户/货主数据范围过滤 |
| 业务对象 | 整改记录 |
| 关联表/读模型 | `sup_record` |
| 页面目的 | 管理低分或质量异常触发的整改 |
| 菜单权限 | `supplier:rectification:read` |

| 区域 | 字段/按钮 | 规则 |
| --- | --- | --- |
| 查询条件 | 供应商、截止时间、状态、整改单号、评分预警、质量问题、人工发起、待提交、已提交、审核通过、驳回、已关闭 | 支持组合查询；未选条件默认按用户数据权限过滤 |
| 排序 | 状态 | 默认按更新时间倒序；支持分页，每页 10/20/50 条 |
| 列表展示 | 供应商、问题类型、截止时间、状态、处理人、整改记录ID、整改单号、评分预警、质量问题、人工发起、问题描述、整改截止时间 | 列表展示业务识别字段、状态、关键金额/数量和更新时间 |
| 顶部按钮 | 查询、重置 | 根据按钮权限和数据范围显示 |
| 行内操作 | 查看详情、发起、提交、审核、关闭 | 终态数据只允许查看、导出或复制，不允许直接编辑 |
| 详情展示 | 整改记录ID、整改单号、评分预警、质量问题、人工发起、问题描述、整改截止时间、待提交、已提交、审核通过、驳回、已关闭、提交时间、审核人、审核时间 | 详情页展示基础信息、明细、状态流转、事件记录和操作日志 |
| 新增页面 | 整改记录ID、整改单号、评分预警、质量问题、人工发起、问题描述、整改截止时间、提交时间、审核人、审核时间 | 新增时校验必填、唯一、枚举、引用关系和数据权限 |
| 修改页面 | 整改记录ID、整改单号、评分预警、质量问题、人工发起、问题描述、整改截止时间、提交时间、审核人、审核时间 | 已生效/终态数据只允许走变更、关闭、停用或补偿流程 |

| 操作 | 权限点 | 操作逻辑 | 结果/审计 |
| --- | --- | --- | --- |
| 查询 | `supplier:page:read` | 读取读模型或列表投影，不修改业务状态 | 不记录业务事件，可记录访问日志 |
| 发起 | `supplier:page:page` | 执行业务动作，后端校验状态、权限、幂等键和参数 | 记录操作日志，按需要发布领域事件 |
| 提交 | `supplier:page:submit` | 执行状态推进命令；状态流转必须符合领域模型状态机 | 写操作日志，写领域事件表，刷新列表读模型 |
| 审核 | `supplier:page:approval` | 执行业务动作，后端校验状态、权限、幂等键和参数 | 记录操作日志，按需要发布领域事件 |
| 关闭 | `supplier:page:close` | 执行状态推进命令；状态流转必须符合领域模型状态机 | 写操作日志，写领域事件表，刷新列表读模型 |

### 操作日志页

| 项 | 设计 |
| --- | --- |
| 菜单路由 | `/supplier/operation-logs` |
| 主要用户 | 业务操作员、业务主管、系统管理员；外部协同页面按供应商/客户/货主数据范围过滤 |
| 业务对象 | 供应商用户绑定 |
| 关联表/读模型 | `sup_supplier_user` |
| 页面目的 | 查询供应商协同关键操作 |
| 菜单权限 | `supplier:operation_log:read` |

| 区域 | 字段/按钮 | 规则 |
| --- | --- | --- |
| 查询条件 | 供应商、时间、启用、停用 | 支持组合查询；未选条件默认按用户数据权限过滤 |
| 排序 | 创建时间、更新时间 | 默认按更新时间倒序；支持分页，每页 10/20/50 条 |
| 列表展示 | 操作人、供应商、对象、动作、时间、结果、供应商用户绑定ID、业务、财务、质量、管理员、是否主账号、启用、停用 | 列表展示业务识别字段、状态、关键金额/数量和更新时间 |
| 顶部按钮 | 重置、查询、导出 | 根据按钮权限和数据范围显示 |
| 行内操作 | 查看详情 | 终态数据只允许查看、导出或复制，不允许直接编辑 |
| 详情展示 | 供应商用户绑定ID、业务、财务、质量、管理员、是否主账号、启用、停用、绑定人、绑定时间、解绑时间 | 详情页展示基础信息、明细、状态流转、事件记录和操作日志 |
| 新增页面 | 供应商用户绑定ID、业务、财务、质量、管理员、是否主账号、绑定人、绑定时间、解绑时间 | 新增时校验必填、唯一、枚举、引用关系和数据权限 |
| 修改页面 | 供应商用户绑定ID、业务、财务、质量、管理员、是否主账号、绑定人、绑定时间、解绑时间 | 已生效/终态数据只允许走变更、关闭、停用或补偿流程 |

| 操作 | 权限点 | 操作逻辑 | 结果/审计 |
| --- | --- | --- | --- |
| 查询 | `supplier:operation_log:read` | 读取读模型或列表投影，不修改业务状态 | 不记录业务事件，可记录访问日志 |
| 导出 | `supplier:operation_log:export` | 导出使用当前查询条件和数据权限；导入先生成任务并逐行校验 | 记录导入/导出日志，失败明细可下载 |

### 枚举配置页

| 项 | 设计 |
| --- | --- |
| 菜单路由 | `/supplier/enums` |
| 主要用户 | 业务操作员、业务主管、系统管理员；外部协同页面按供应商/客户/货主数据范围过滤 |
| 业务对象 | 供应商用户绑定 |
| 关联表/读模型 | `sup_supplier_user` |
| 页面目的 | 维护供应商系统枚举 |
| 菜单权限 | `supplier:enum:read` |

| 区域 | 字段/按钮 | 规则 |
| --- | --- | --- |
| 查询条件 | 状态、启用、停用 | 支持组合查询；未选条件默认按用户数据权限过滤 |
| 排序 | 状态 | 默认按更新时间倒序；支持分页，每页 10/20/50 条 |
| 列表展示 | 枚举类型、枚举值、标签、状态、供应商用户绑定ID、业务、财务、质量、管理员、是否主账号、启用、停用、绑定人、绑定时间 | 列表展示业务识别字段、状态、关键金额/数量和更新时间 |
| 顶部按钮 | 查询、重置、新增 | 根据按钮权限和数据范围显示 |
| 行内操作 | 查看详情、编辑、排序、停用 | 终态数据只允许查看、导出或复制，不允许直接编辑 |
| 详情展示 | 供应商用户绑定ID、业务、财务、质量、管理员、是否主账号、启用、停用、绑定人、绑定时间、解绑时间 | 详情页展示基础信息、明细、状态流转、事件记录和操作日志 |
| 新增页面 | 供应商用户绑定ID、业务、财务、质量、管理员、是否主账号、绑定人、绑定时间、解绑时间 | 新增时校验必填、唯一、枚举、引用关系和数据权限 |
| 修改页面 | 供应商用户绑定ID、业务、财务、质量、管理员、是否主账号、绑定人、绑定时间、解绑时间 | 已生效/终态数据只允许走变更、关闭、停用或补偿流程 |

| 操作 | 权限点 | 操作逻辑 | 结果/审计 |
| --- | --- | --- | --- |
| 查询 | `supplier:enumsettings:read` | 读取读模型或列表投影，不修改业务状态 | 不记录业务事件，可记录访问日志 |
| 新增 | `supplier:enumsettings:create` | 打开新增页，提交后由后端应用服务校验并创建聚合 | 记录操作日志，必要时发布已创建事件 |
| 编辑 | `supplier:enumsettings:update` | 加载当前版本，校验乐观锁、状态机和数据权限后保存 | 记录前后快照，必要时发布已变更事件 |
| 排序 | `supplier:enumsettings:sort` | 执行业务动作，后端校验状态、权限、幂等键和参数 | 记录操作日志，按需要发布领域事件 |
| 停用 | `supplier:enumsettings:disable` | 执行状态推进命令；状态流转必须符合领域模型状态机 | 写操作日志，写领域事件表，刷新列表读模型 |

## 6. 权限点总表

| 页面 | 查询权限 | 按钮权限 | 数据权限 |
| --- | --- | --- | --- |
| 供应商工作台 | `supplier:workbench:read` | `supplier:supplierworkbench:page`、`supplier:supplierworkbench:page` | 组织、仓库、货主、供应商/客户、本人/部门 |
| 供应商档案页 | `supplier:profile:read` | `supplier:supplier_profile:read`、`supplier:supplier_profile:page` | 组织、仓库、货主、供应商/客户、本人/部门 |
| 供应商账号绑定页 | `supplier:user_binding:read` | `supplier:supplier:page`、`supplier:supplier:page`、`supplier:supplier:enable`、`supplier:supplier:disable` | 组织、仓库、货主、供应商/客户、本人/部门 |
| 供应商商品页 | `supplier:sku:read` | `supplier:supplierproduct:read`、`supplier:supplierproduct:supplier_sku`、`supplier:supplierproduct:page` | 组织、仓库、货主、供应商/客户、本人/部门 |
| 采购订单协同页 | `supplier:po_confirm:read` | `supplier:purchase_order:page`、`supplier:purchase_order:page`、`supplier:purchase_order:page` | 组织、仓库、货主、供应商/客户、本人/部门 |
| ASN 管理页 | `supplier:asn:read` | `supplier:asn:create`、`supplier:asn:update`、`supplier:asn:submit`、`supplier:asn:cancel`、`supplier:asn:page` | 组织、仓库、货主、供应商/客户、本人/部门 |
| 退供协同页 | `supplier:return:read` | `supplier:supplier_return:page`、`supplier:supplier_return:page`、`supplier:supplier_return:page`、`supplier:supplier_return:page` | 组织、仓库、货主、供应商/客户、本人/部门 |
| 对账协同页 | `supplier:recon:read` | `supplier:reconcile:page`、`supplier:reconcile:page`、`supplier:reconcile:invoice` | 组织、仓库、货主、供应商/客户、本人/部门 |
| 质量协同页 | `supplier:quality:read` | `supplier:page:read`、`supplier:page:page`、`supplier:page:page` | 组织、仓库、货主、供应商/客户、本人/部门 |
| 供应商评分页 | `supplier:score:read` | `supplier:supplier_score:page`、`supplier:supplier_score:export`、`supplier:supplier_score:page` | 组织、仓库、货主、供应商/客户、本人/部门 |
| 评分规则页 | `supplier:score_rule:read` | `supplier:page:create`、`supplier:page:update`、`supplier:page:page`、`supplier:page:release` | 组织、仓库、货主、供应商/客户、本人/部门 |
| 整改管理页 | `supplier:rectification:read` | `supplier:page:page`、`supplier:page:submit`、`supplier:page:approval`、`supplier:page:close` | 组织、仓库、货主、供应商/客户、本人/部门 |
| 操作日志页 | `supplier:operation_log:read` | `supplier:operation_log:export` | 组织、仓库、货主、供应商/客户、本人/部门 |
| 枚举配置页 | `supplier:enum:read` | `supplier:enumsettings:create`、`supplier:enumsettings:update`、`supplier:enumsettings:sort`、`supplier:enumsettings:disable` | 组织、仓库、货主、供应商/客户、本人/部门 |

## 7. 关键操作逻辑

| 操作类型 | 前置校验 | 状态/数据变化 | 事件与审计 |
| --- | --- | --- | --- |
| 新增 | 菜单权限、按钮权限、必填、唯一、引用主数据有效 | 创建草稿或初始状态数据 | 记录操作日志；必要时发布已创建事件 |
| 修改 | 按钮权限、数据权限、乐观锁、非终态、字段可编辑 | 更新业务字段或生成变更记录 | 记录前后快照；关键变更发布事件 |
| 删除 | 通常只允许草稿或未引用数据 | 逻辑删除，历史单据不物理删除 | 记录删除原因和操作者 |
| 提交/发布/审批 | 状态机允许、审批权限、幂等键未重复 | 推进状态，锁定或发布业务事实 | 写领域事件表，刷新读模型 |
| 取消/关闭/停用 | 无未完成依赖或允许补偿路径 | 进入终态或禁用状态 | 记录原因；通知下游或生成待办 |
| 查询/导出 | 查询权限、数据范围、脱敏策略 | 不改变业务状态 | 导出记录审计日志 |

## 8. 事件、日志与审计

| 事件/日志 | 用途 | 页面影响 |
| --- | --- | --- |
| 潜在供应商已创建 | 供应商列表读模型、审计 | 刷新列表、详情时间线、待办或状态展示 |
| 供应商准入已提交 | 待办中心、审批流 | 刷新列表、详情时间线、待办或状态展示 |
| 供应商已启用 | 采购系统、供应商商品、合同、权限/账号开通 | 刷新列表、详情时间线、待办或状态展示 |
| 供应商资料变更已生效 | 采购、BMS、主数据缓存 | 刷新列表、详情时间线、待办或状态展示 |
| 供应商已冻结 | 采购下单控制、报价、合同、供应商商品 | 刷新列表、详情时间线、待办或状态展示 |
| 供应商已停用 | 采购、合同、BMS、读模型 | 刷新列表、详情时间线、待办或状态展示 |
| 供应商黑名单已命中 | 来源上下文+事件编号+supplierId | 刷新列表、详情时间线、待办或状态展示 |
| 供应商评分预警已产生 | 来源上下文+事件编号+supplierId+period | 刷新列表、详情时间线、待办或状态展示 |
| 供应商未完业务已清理 | 来源上下文+事件编号+supplierId | 刷新列表、详情时间线、待办或状态展示 |
| 供应商商品已启用 | 采购系统可采购清单、报价、合同 | 刷新列表、详情时间线、待办或状态展示 |
| 供应商商品供货条件已变更 | 采购下单校验、供应商评分 | 刷新列表、详情时间线、待办或状态展示 |
| 供应商商品已暂停 | 采购系统、报价、合同 | 刷新列表、详情时间线、待办或状态展示 |
| 供应商商品已恢复 | 采购系统可采购清单 | 刷新列表、详情时间线、待办或状态展示 |
| 供应商商品已停供 | 采购系统、报价、合同 | 刷新列表、详情时间线、待办或状态展示 |
| SKU已启用 | 来源上下文+事件编号+skuId | 刷新列表、详情时间线、待办或状态展示 |
| SKU已停用 | 来源上下文+事件编号+skuId | 刷新列表、详情时间线、待办或状态展示 |
| 供应商报价已采纳 | 来源上下文+事件编号+quoteId | 刷新列表、详情时间线、待办或状态展示 |
| 供应商报价已创建 | 报价读模型 | 刷新列表、详情时间线、待办或状态展示 |
| 供应商报价已提交 | 采购员待办、比价看板 | 刷新列表、详情时间线、待办或状态展示 |
| 供应商报价已确认 | 价格审批或采纳待办 | 刷新列表、详情时间线、待办或状态展示 |
| 供应商报价已拒绝 | 供应商协同门户 | 刷新列表、详情时间线、待办或状态展示 |
| 供应商报价已过期 | 采购可用价格读模型 | 刷新列表、详情时间线、待办或状态展示 |
| 询价单已发布 | 来源上下文+事件编号+rfqId+supplierId | 刷新列表、详情时间线、待办或状态展示 |
| 询价已截标 | 来源上下文+事件编号+rfqId | 刷新列表、详情时间线、待办或状态展示 |
| 操作日志 | 记录用户写操作、敏感查询、导入导出和授权变化 | 详情页时间线、操作日志页可查询 |
| 事件消费日志 | 记录外部事件幂等消费和失败重试 | 异常页或系统运维页可定位问题 |

## 9. 与领域模型和数据库设计的对应关系

| 来源 | 使用方式 |
| --- | --- |
| `docs/03-核心业务模型/01-供应商领域模型` | 决定页面动作是否能推进状态、是否要发布事件、是否需要补偿 |
| `docs/05-子系统数据库设计/01-供应商系统数据库设计.md` | 决定列表字段、详情字段、新增/编辑字段、枚举值和日志表字段 |
| 权限系统 | 决定登录认证、菜单展示、按钮展示、API 校验和数据范围 |

当前结论：本文是产品功能设计入口；后续页面原型、接口定义和后端实现都应以本文的菜单、按钮、权限和操作逻辑为准。

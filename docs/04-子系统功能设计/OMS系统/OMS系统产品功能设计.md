# OMS系统产品功能设计

> 本文是 `docs/04-子系统功能设计/OMS系统` 的唯一产品功能设计文档，从产品视角描述菜单、页面、按钮、操作逻辑、角色权限、数据权限、查询、列表、详情、新增、修改、登录/登出和审计。领域规则参考 `docs/03-核心业务模型/05-OMS领域模型`，表字段参考 `docs/05-子系统数据库设计/05-OMS系统数据库设计.md`。

## 1. 产品定位与设计边界

| 项 | 设计 |
| --- | --- |
| 系统 | OMS系统 |
| 设计对象 | 后台管理页面、业务操作页面、配置页面、日志页面和权限控制 |
| 不承载内容 | 数据库 DDL、领域聚合细节、跨系统接口实现；这些分别在数据库设计、核心业务模型、接口事件文档维护 |
| 产品原则 | 先通过权限系统完成登录和授权，再按菜单进入功能；所有写操作必须校验按钮权限、数据权限、状态机和幂等键 |

## 2. 登录、登出与全局交互

| 页面/动作 | 使用角色 | 展示字段 | 操作逻辑 |
| --- | --- | --- | --- |
| 登录页 | 未登录用户 | 账号、密码、验证码/MFA、租户/组织可选 | 用户输入凭证后调用权限系统认证；成功写入 access_token/refresh_token，拉取菜单、按钮权限和数据范围；失败记录登录日志。 |
| 退出登录 | 已登录用户 | 当前用户、当前系统、会话过期时间 | 用户点击退出后吊销当前 Token，清理前端缓存、菜单缓存和权限缓存，并跳转登录页。 |
| 个人中心 | 已登录用户 | 姓名、账号、手机号、邮箱、角色、所属组织 | 读取当前用户资料和角色权限；修改密码或个人资料时记录操作日志。 |

全局页面规则：

- 所有列表页默认包含查询、重置、分页、排序、导出入口；导出必须受数据权限约束。
- 所有详情页默认包含基础信息、明细信息、状态流转、领域事件、操作日志。
- 所有新增/编辑页默认校验必填、唯一、枚举、引用关系、乐观锁和状态机。
- 所有写操作默认记录操作日志；会改变业务事实的操作写入领域事件发布表。

## 3. 角色、菜单权限与数据权限

| 角色 | 可用功能 | 典型动作 | 数据范围 |
| --- | --- | --- | --- |
| 客服 | 订单查询、取消、售后 | 修改地址、创建售后、处理异常 | 本人、本部门或被分配业务范围 |
| 订单运营 | 审单、拆单、分仓、重推 | 处理缺货、拦截、异常订单 | 本人、本部门或被分配业务范围 |
| 渠道运营 | 渠道订单接入 | 查看平台订单同步状态 | 本人、本部门或被分配业务范围 |
| 仓配运营 | 履约监控 | 跟踪出库、物流、签收 | 按仓库、货主、库区授权 |
| 财务/风控 | 订单风控查看 | 查看金额、信用、异常 | 按账套、组织、客户/供应商、货主授权 |
| 管理员 | 规则配置 | 分仓规则、审单规则、取消规则 | 按授权组织和系统配置范围 |

数据权限维度：

| 维度 | 控制方式 | 页面效果 |
| --- | --- | --- |
| 组织 | 按当前组织、下级组织或自定义组织集合过滤 | 列表、详情、导出只返回可见组织数据 |
| 仓库 | WMS、库存、OMS、BMS 按仓库授权过滤 | 未授权仓库不可查询、不可操作 |
| 货主 | 多货主场景按货主授权过滤 | 只能查看和操作授权货主的单据和库存 |
| 供应商/客户 | 外部协同用户绑定供应商或客户 | 只展示自身协同单据，不展示其他主体数据 |
| 本人/部门 | 普通业务用户可限制为本人或本部门 | 请购、待办、审批等页面优先使用该范围 |

## 4. 左侧菜单与页面总览

| 菜单 | 路由 | 页面目的 | 主要按钮 |
| --- | --- | --- | --- |
| OMS 工作台 | `/oms/workbench` | 展示待审、缺货、待下发、出库异常、售后待办 | 查看待办、进入处理 |
| 渠道订单接入页 | `/oms/channel-orders` | 查看渠道订单同步和幂等结果 | 重试、忽略、手工创建 |
| 销售订单页 | `/oms/sales-orders` | 管理内部销售订单主数据 | 新增、编辑、审核、取消、重审 |
| 订单审单页 | `/oms/audit-results` | 处理商品、价格、地址、风控等校验异常 | 通过、驳回、修正、重审 |
| 分仓履约页 | `/oms/fulfillments` | 查看和调整分仓、拆单、承诺时效 | 分仓、换仓、拆单、合单 |
| 库存预占页 | `/oms/reservations` | 查看预占请求和结果 | 重试预占、释放预占 |
| 出库单页 | `/oms/outbounds` | 管理下发 WMS 的出库指令 | 创建、下发、取消、重推 |
| 取消管理页 | `/oms/cancel-requests` | 处理用户、客服或渠道取消请求 | 审核、同意、拒绝、转售后 |
| 售后管理页 | `/oms/after-sales` | 处理仅退款、退货退款、换货补发 | 创建、审核、驳回、关闭 |
| 异常处理页 | `/oms/exceptions` | 集中处理缺货、地址不可达、风控、仓库拒单 | 分派、处理、关闭、重试 |
| 规则配置页 | `/oms/rules` | 配置审单、分仓、取消、售后规则 | 新增、编辑、启用、停用、发布 |
| 操作日志页 | `/oms/operation-logs` | 查询 OMS 关键写操作 | 查询、导出 |
| 枚举配置页 | `/oms/enums` | 维护 OMS 页面枚举项 | 新增、编辑、排序、停用 |

## 5. 页面详细设计

### OMS 工作台

| 项 | 设计 |
| --- | --- |
| 菜单路由 | `/oms/workbench` |
| 主要用户 | 业务操作员、业务主管、系统管理员；外部协同页面按供应商/客户/货主数据范围过滤 |
| 业务对象 | OMS 规则配置 |
| 关联表/读模型 | `oms_oms_rule` |
| 页面目的 | 展示待审、缺货、待下发、出库异常、售后待办 |
| 菜单权限 | `oms:workbench:read` |

| 区域 | 字段/按钮 | 规则 |
| --- | --- | --- |
| 查询条件 | 规则编码、规则名称、审单、分仓、取消、售后、承运商、启用、停用 | 支持组合查询；未选条件默认按用户数据权限过滤 |
| 排序 | 优先级 | 默认按更新时间倒序；支持分页，每页 10/20/50 条 |
| 列表展示 | 待审核、缺货、待发仓、出库异常、售后待审、OMS 规则配置ID、规则编码、规则名称、审单、分仓、取消、售后、承运商、优先级 | 列表展示业务识别字段、状态、关键金额/数量和更新时间 |
| 顶部按钮 | 查询、重置 | 根据按钮权限和数据范围显示 |
| 行内操作 | 查看详情、查看待办、进入处理 | 终态数据只允许查看、导出或复制，不允许直接编辑 |
| 详情展示 | OMS 规则配置ID、规则编码、规则名称、审单、分仓、取消、售后、承运商、优先级、条件配置、动作配置、启用、停用、生效时间、失效时间 | 详情页展示基础信息、明细、状态流转、事件记录和操作日志 |
| 新增页面 | OMS 规则配置ID、规则编码、规则名称、审单、分仓、取消、售后、承运商、优先级、条件配置、动作配置、生效时间、失效时间 | 新增时校验必填、唯一、枚举、引用关系和数据权限 |
| 修改页面 | OMS 规则配置ID、规则编码、规则名称、审单、分仓、取消、售后、承运商、优先级、条件配置、动作配置、生效时间、失效时间 | 已生效/终态数据只允许走变更、关闭、停用或补偿流程 |

| 操作 | 权限点 | 操作逻辑 | 结果/审计 |
| --- | --- | --- | --- |
| 查询 | `oms:oms_workbench:read` | 读取读模型或列表投影，不修改业务状态 | 不记录业务事件，可记录访问日志 |
| 查看待办 | `oms:oms_workbench:page` | 执行业务动作，后端校验状态、权限、幂等键和参数 | 记录操作日志，按需要发布领域事件 |
| 进入处理 | `oms:oms_workbench:page` | 执行业务动作，后端校验状态、权限、幂等键和参数 | 记录操作日志，按需要发布领域事件 |

### 渠道订单接入页

| 项 | 设计 |
| --- | --- |
| 菜单路由 | `/oms/channel-orders` |
| 主要用户 | 业务操作员、业务主管、系统管理员；外部协同页面按供应商/客户/货主数据范围过滤 |
| 业务对象 | 库存预占引用 |
| 关联表/读模型 | `oms_stock_reservation` |
| 页面目的 | 查看渠道订单同步和幂等结果 |
| 菜单权限 | `oms:channel_order:read` |

| 区域 | 字段/按钮 | 规则 |
| --- | --- | --- |
| 查询条件 | 外部单号、接入状态、时间、中央库存预占号、待预占、预占成功、预占失败、已释放、已扣减 | 支持组合查询；未选条件默认按用户数据权限过滤 |
| 排序 | 状态、数量 | 默认按更新时间倒序；支持分页，每页 10/20/50 条 |
| 列表展示 | 渠道、外部单号、接入状态、失败原因、时间、库存预占引用ID、中央库存预占号、请求预占数量、实际预占数量、待预占、预占成功、预占失败、已释放、已扣减 | 列表展示业务识别字段、状态、关键金额/数量和更新时间 |
| 顶部按钮 | 查询、重置 | 根据按钮权限和数据范围显示 |
| 行内操作 | 查看详情、重试、忽略、手工创建 | 终态数据只允许查看、导出或复制，不允许直接编辑 |
| 详情展示 | 库存预占引用ID、中央库存预占号、请求预占数量、实际预占数量、待预占、预占成功、预占失败、已释放、已扣减、失败原因 | 详情页展示基础信息、明细、状态流转、事件记录和操作日志 |
| 新增页面 | 库存预占引用ID、中央库存预占号、请求预占数量、实际预占数量、失败原因 | 新增时校验必填、唯一、枚举、引用关系和数据权限 |
| 修改页面 | 库存预占引用ID、中央库存预占号、请求预占数量、实际预占数量、失败原因 | 已生效/终态数据只允许走变更、关闭、停用或补偿流程 |

| 操作 | 权限点 | 操作逻辑 | 结果/审计 |
| --- | --- | --- | --- |
| 查询 | `oms:order:read` | 读取读模型或列表投影，不修改业务状态 | 不记录业务事件，可记录访问日志 |
| 重试 | `oms:order:page` | 执行业务动作，后端校验状态、权限、幂等键和参数 | 记录操作日志，按需要发布领域事件 |
| 忽略 | `oms:order:page` | 执行业务动作，后端校验状态、权限、幂等键和参数 | 记录操作日志，按需要发布领域事件 |
| 手工创建 | `oms:order:page` | 执行业务动作，后端校验状态、权限、幂等键和参数 | 记录操作日志，按需要发布领域事件 |

### 销售订单页

| 项 | 设计 |
| --- | --- |
| 菜单路由 | `/oms/sales-orders` |
| 主要用户 | 业务操作员、业务主管、系统管理员；外部协同页面按供应商/客户/货主数据范围过滤 |
| 业务对象 | 销售订单 |
| 关联表/读模型 | `oms_sales_order` |
| 页面目的 | 管理内部销售订单主数据 |
| 菜单权限 | `oms:sales_order:read` |

| 区域 | 字段/按钮 | 规则 |
| --- | --- | --- |
| 查询条件 | 订单号、客户、状态、履约状态、内部销售订单号、外部订单号、客户名称、普通、预售、换货补发、手工 | 支持组合查询；未选条件默认按用户数据权限过滤 |
| 排序 | 状态、金额 | 默认按更新时间倒序；支持分页，每页 10/20/50 条 |
| 列表展示 | 订单号、客户、渠道、金额、状态、履约状态、销售订单ID、内部销售订单号、外部订单号、客户名称 | 列表展示业务识别字段、状态、关键金额/数量和更新时间 |
| 顶部按钮 | 查询、重置、新增 | 根据按钮权限和数据范围显示 |
| 行内操作 | 查看详情、编辑、审核、取消、重审 | 终态数据只允许查看、导出或复制，不允许直接编辑 |
| 详情展示 | 销售订单ID、内部销售订单号、外部订单号、客户名称、普通、预售、换货补发、手工、未支付、已支付、部分退款、已退款、待审核、通过、异常、驳回、已创建、待审核、异常待处理、待预占、缺货待处理、已预占、已下发仓库、出库中、已发货、已签收、已完成、已取消、未履约、部分履约、履约中、已履约、履约失败、订单总额、优惠金额、实付金额、收货人、收货电话、收货地址、支付时间 | 详情页展示基础信息、明细、状态流转、事件记录和操作日志 |
| 新增页面 | 销售订单ID、内部销售订单号、外部订单号、客户名称、普通、预售、换货补发、手工、订单总额、优惠金额、实付金额、收货人、收货电话、收货地址、支付时间 | 新增时校验必填、唯一、枚举、引用关系和数据权限 |
| 修改页面 | 销售订单ID、内部销售订单号、外部订单号、客户名称、普通、预售、换货补发、手工、订单总额、优惠金额、实付金额、收货人、收货电话、收货地址、支付时间 | 已生效/终态数据只允许走变更、关闭、停用或补偿流程 |

| 操作 | 权限点 | 操作逻辑 | 结果/审计 |
| --- | --- | --- | --- |
| 查询 | `oms:sales_order:read` | 读取读模型或列表投影，不修改业务状态 | 不记录业务事件，可记录访问日志 |
| 新增 | `oms:sales_order:create` | 打开新增页，提交后由后端应用服务校验并创建聚合 | 记录操作日志，必要时发布已创建事件 |
| 编辑 | `oms:sales_order:update` | 加载当前版本，校验乐观锁、状态机和数据权限后保存 | 记录前后快照，必要时发布已变更事件 |
| 审核 | `oms:sales_order:approval` | 执行业务动作，后端校验状态、权限、幂等键和参数 | 记录操作日志，按需要发布领域事件 |
| 取消 | `oms:sales_order:cancel` | 执行状态推进命令；状态流转必须符合领域模型状态机 | 写操作日志，写领域事件表，刷新列表读模型 |
| 重审 | `oms:sales_order:page` | 执行业务动作，后端校验状态、权限、幂等键和参数 | 记录操作日志，按需要发布领域事件 |

### 订单审单页

| 项 | 设计 |
| --- | --- |
| 菜单路由 | `/oms/audit-results` |
| 主要用户 | 业务操作员、业务主管、系统管理员；外部协同页面按供应商/客户/货主数据范围过滤 |
| 业务对象 | 订单审单结果 |
| 关联表/读模型 | `oms_order_result` |
| 页面目的 | 处理商品、价格、地址、风控等校验异常 |
| 菜单权限 | `oms:audit:read` |

| 区域 | 字段/按钮 | 规则 |
| --- | --- | --- |
| 查询条件 | 订单号、处理状态、商品、客户、地址、价格、风控、信用、待处理、已放行、已驳回、已修正 | 支持组合查询；未选条件默认按用户数据权限过滤 |
| 排序 | 状态 | 默认按更新时间倒序；支持分页，每页 10/20/50 条 |
| 列表展示 | 订单号、异常类型、命中规则、处理状态、订单审单结果ID、商品、客户、地址、价格、风控、信用、通过、拦截、警告、异常原因、待处理、已放行、已驳回、已修正、处理人 | 列表展示业务识别字段、状态、关键金额/数量和更新时间 |
| 顶部按钮 | 查询、重置 | 根据按钮权限和数据范围显示 |
| 行内操作 | 查看详情、通过、驳回、修正、重审 | 终态数据只允许查看、导出或复制，不允许直接编辑 |
| 详情展示 | 订单审单结果ID、商品、客户、地址、价格、风控、信用、通过、拦截、警告、异常原因、待处理、已放行、已驳回、已修正、处理人、处理时间 | 详情页展示基础信息、明细、状态流转、事件记录和操作日志 |
| 新增页面 | 订单审单结果ID、商品、客户、地址、价格、风控、信用、通过、拦截、警告、异常原因、处理人、处理时间 | 新增时校验必填、唯一、枚举、引用关系和数据权限 |
| 修改页面 | 订单审单结果ID、商品、客户、地址、价格、风控、信用、通过、拦截、警告、异常原因、处理人、处理时间 | 已生效/终态数据只允许走变更、关闭、停用或补偿流程 |

| 操作 | 权限点 | 操作逻辑 | 结果/审计 |
| --- | --- | --- | --- |
| 查询 | `oms:order:read` | 读取读模型或列表投影，不修改业务状态 | 不记录业务事件，可记录访问日志 |
| 通过 | `oms:order:approve` | 执行状态推进命令；状态流转必须符合领域模型状态机 | 写操作日志，写领域事件表，刷新列表读模型 |
| 驳回 | `oms:order:reject` | 执行状态推进命令；状态流转必须符合领域模型状态机 | 写操作日志，写领域事件表，刷新列表读模型 |
| 修正 | `oms:order:page` | 执行业务动作，后端校验状态、权限、幂等键和参数 | 记录操作日志，按需要发布领域事件 |
| 重审 | `oms:order:page` | 执行业务动作，后端校验状态、权限、幂等键和参数 | 记录操作日志，按需要发布领域事件 |

### 分仓履约页

| 项 | 设计 |
| --- | --- |
| 菜单路由 | `/oms/fulfillments` |
| 主要用户 | 业务操作员、业务主管、系统管理员；外部协同页面按供应商/客户/货主数据范围过滤 |
| 业务对象 | 销售订单 |
| 关联表/读模型 | `oms_sales_order` |
| 页面目的 | 查看和调整分仓、拆单、承诺时效 |
| 菜单权限 | `oms:fulfillment:read` |

| 区域 | 字段/按钮 | 规则 |
| --- | --- | --- |
| 查询条件 | 订单号、仓库、承诺发货时间、状态、内部销售订单号、外部订单号、客户名称、普通、预售、换货补发、手工 | 支持组合查询；未选条件默认按用户数据权限过滤 |
| 排序 | 状态、金额 | 默认按更新时间倒序；支持分页，每页 10/20/50 条 |
| 列表展示 | 订单号、仓库、履约单、承诺发货时间、状态、销售订单ID、内部销售订单号、外部订单号、客户名称、普通、预售、换货补发、手工 | 列表展示业务识别字段、状态、关键金额/数量和更新时间 |
| 顶部按钮 | 查询、重置 | 根据按钮权限和数据范围显示 |
| 行内操作 | 查看详情、分仓、换仓、拆单、合单 | 终态数据只允许查看、导出或复制，不允许直接编辑 |
| 详情展示 | 销售订单ID、内部销售订单号、外部订单号、客户名称、普通、预售、换货补发、手工、未支付、已支付、部分退款、已退款、待审核、通过、异常、驳回、已创建、待审核、异常待处理、待预占、缺货待处理、已预占、已下发仓库、出库中、已发货、已签收、已完成、已取消、未履约、部分履约、履约中、已履约、履约失败、订单总额、优惠金额、实付金额、收货人、收货电话、收货地址、支付时间 | 详情页展示基础信息、明细、状态流转、事件记录和操作日志 |
| 新增页面 | 销售订单ID、内部销售订单号、外部订单号、客户名称、普通、预售、换货补发、手工、订单总额、优惠金额、实付金额、收货人、收货电话、收货地址、支付时间 | 新增时校验必填、唯一、枚举、引用关系和数据权限 |
| 修改页面 | 销售订单ID、内部销售订单号、外部订单号、客户名称、普通、预售、换货补发、手工、订单总额、优惠金额、实付金额、收货人、收货电话、收货地址、支付时间 | 已生效/终态数据只允许走变更、关闭、停用或补偿流程 |

| 操作 | 权限点 | 操作逻辑 | 结果/审计 |
| --- | --- | --- | --- |
| 查询 | `oms:fulfillment:read` | 读取读模型或列表投影，不修改业务状态 | 不记录业务事件，可记录访问日志 |
| 分仓 | `oms:fulfillment:page` | 执行业务动作，后端校验状态、权限、幂等键和参数 | 记录操作日志，按需要发布领域事件 |
| 换仓 | `oms:fulfillment:page` | 执行业务动作，后端校验状态、权限、幂等键和参数 | 记录操作日志，按需要发布领域事件 |
| 拆单 | `oms:fulfillment:page` | 执行业务动作，后端校验状态、权限、幂等键和参数 | 记录操作日志，按需要发布领域事件 |
| 合单 | `oms:fulfillment:page` | 执行业务动作，后端校验状态、权限、幂等键和参数 | 记录操作日志，按需要发布领域事件 |

### 库存预占页

| 项 | 设计 |
| --- | --- |
| 菜单路由 | `/oms/reservations` |
| 主要用户 | 业务操作员、业务主管、系统管理员；外部协同页面按供应商/客户/货主数据范围过滤 |
| 业务对象 | 库存预占引用 |
| 关联表/读模型 | `oms_stock_reservation` |
| 页面目的 | 查看预占请求和结果 |
| 菜单权限 | `oms:reservation:read` |

| 区域 | 字段/按钮 | 规则 |
| --- | --- | --- |
| 查询条件 | 订单号、SKU、仓库、状态、中央库存预占号、待预占、预占成功、预占失败、已释放、已扣减 | 支持组合查询；未选条件默认按用户数据权限过滤 |
| 排序 | 状态、数量 | 默认按更新时间倒序；支持分页，每页 10/20/50 条 |
| 列表展示 | 订单号、SKU、仓库、预占数量、预占号、状态、库存预占引用ID、中央库存预占号、请求预占数量、实际预占数量 | 列表展示业务识别字段、状态、关键金额/数量和更新时间 |
| 顶部按钮 | 查询、重置 | 根据按钮权限和数据范围显示 |
| 行内操作 | 查看详情、重试预占、释放预占 | 终态数据只允许查看、导出或复制，不允许直接编辑 |
| 详情展示 | 库存预占引用ID、中央库存预占号、请求预占数量、实际预占数量、待预占、预占成功、预占失败、已释放、已扣减、失败原因 | 详情页展示基础信息、明细、状态流转、事件记录和操作日志 |
| 新增页面 | 库存预占引用ID、中央库存预占号、请求预占数量、实际预占数量、失败原因 | 新增时校验必填、唯一、枚举、引用关系和数据权限 |
| 修改页面 | 库存预占引用ID、中央库存预占号、请求预占数量、实际预占数量、失败原因 | 已生效/终态数据只允许走变更、关闭、停用或补偿流程 |

| 操作 | 权限点 | 操作逻辑 | 结果/审计 |
| --- | --- | --- | --- |
| 查询 | `oms:stockreservation:read` | 读取读模型或列表投影，不修改业务状态 | 不记录业务事件，可记录访问日志 |
| 重试预占 | `oms:stockreservation:reservation` | 执行业务动作，后端校验状态、权限、幂等键和参数 | 记录操作日志，按需要发布领域事件 |
| 释放预占 | `oms:stockreservation:releasereservation` | 执行业务动作，后端校验状态、权限、幂等键和参数 | 记录操作日志，按需要发布领域事件 |

### 出库单页

| 项 | 设计 |
| --- | --- |
| 菜单路由 | `/oms/outbounds` |
| 主要用户 | 业务操作员、业务主管、系统管理员；外部协同页面按供应商/客户/货主数据范围过滤 |
| 业务对象 | 出库单 |
| 关联表/读模型 | `oms_outbound` |
| 页面目的 | 管理下发 WMS 的出库指令 |
| 菜单权限 | `oms:outbound:read` |

| 区域 | 字段/按钮 | 规则 |
| --- | --- | --- |
| 查询条件 | 出库单号、仓库、订单号、状态、OMS 出库单号、销售出库、换货补发、手工补发、WMS 出库单号、草稿、已下发、WMS已接单、拣货中、已发货、已取消、异常 | 支持组合查询；未选条件默认按用户数据权限过滤 |
| 排序 | 状态、数量 | 默认按更新时间倒序；支持分页，每页 10/20/50 条 |
| 列表展示 | 出库单号、仓库、订单号、状态、发货数量、出库单ID、OMS 出库单号、销售出库、换货补发、手工补发、WMS 出库单号、草稿、已下发、WMS已接单、拣货中、已发货、已取消、异常 | 列表展示业务识别字段、状态、关键金额/数量和更新时间 |
| 顶部按钮 | 查询、重置 | 根据按钮权限和数据范围显示 |
| 行内操作 | 查看详情、创建、下发、取消、重推 | 终态数据只允许查看、导出或复制，不允许直接编辑 |
| 详情展示 | 出库单ID、OMS 出库单号、销售出库、换货补发、手工补发、WMS 出库单号、草稿、已下发、WMS已接单、拣货中、已发货、已取消、异常、下发时间、发货时间、取消原因 | 详情页展示基础信息、明细、状态流转、事件记录和操作日志 |
| 新增页面 | 出库单ID、OMS 出库单号、销售出库、换货补发、手工补发、WMS 出库单号、下发时间、发货时间、取消原因 | 新增时校验必填、唯一、枚举、引用关系和数据权限 |
| 修改页面 | 出库单ID、OMS 出库单号、销售出库、换货补发、手工补发、WMS 出库单号、下发时间、发货时间、取消原因 | 已生效/终态数据只允许走变更、关闭、停用或补偿流程 |

| 操作 | 权限点 | 操作逻辑 | 结果/审计 |
| --- | --- | --- | --- |
| 查询 | `oms:outbound:read` | 读取读模型或列表投影，不修改业务状态 | 不记录业务事件，可记录访问日志 |
| 创建 | `oms:outbound:page` | 执行业务动作，后端校验状态、权限、幂等键和参数 | 记录操作日志，按需要发布领域事件 |
| 下发 | `oms:outbound:page` | 执行业务动作，后端校验状态、权限、幂等键和参数 | 记录操作日志，按需要发布领域事件 |
| 取消 | `oms:outbound:cancel` | 执行状态推进命令；状态流转必须符合领域模型状态机 | 写操作日志，写领域事件表，刷新列表读模型 |
| 重推 | `oms:outbound:page` | 执行业务动作，后端校验状态、权限、幂等键和参数 | 记录操作日志，按需要发布领域事件 |

### 取消管理页

| 项 | 设计 |
| --- | --- |
| 菜单路由 | `/oms/cancel-requests` |
| 主要用户 | 业务操作员、业务主管、系统管理员；外部协同页面按供应商/客户/货主数据范围过滤 |
| 业务对象 | 取消申请 |
| 关联表/读模型 | `oms_cancel` |
| 页面目的 | 处理用户、客服或渠道取消请求 |
| 菜单权限 | `oms:cancel:read` |

| 区域 | 字段/按钮 | 规则 |
| --- | --- | --- |
| 查询条件 | 取消单号、订单号、状态、取消申请号、待审核、已同意、已拒绝、取消中、已完成、转售后、未请求、请求中、成功、失败、未释放、释放中、已释放、释放失败 | 支持组合查询；未选条件默认按用户数据权限过滤 |
| 排序 | 状态 | 默认按更新时间倒序；支持分页，每页 10/20/50 条 |
| 列表展示 | 取消单号、订单号、原因、可取消结果、状态、取消申请ID、取消申请号、客户、客服、渠道、系统、不想要、地址错误、缺货、风控、其他、待审核、已同意、已拒绝、取消中、已完成、转售后 | 列表展示业务识别字段、状态、关键金额/数量和更新时间 |
| 顶部按钮 | 查询、重置 | 根据按钮权限和数据范围显示 |
| 行内操作 | 查看详情、审核、同意、拒绝、转售后 | 终态数据只允许查看、导出或复制，不允许直接编辑 |
| 详情展示 | 取消申请ID、取消申请号、客户、客服、渠道、系统、不想要、地址错误、缺货、风控、其他、待审核、已同意、已拒绝、取消中、已完成、转售后、未请求、请求中、成功、失败、未释放、释放中、已释放、释放失败、处理时间 | 详情页展示基础信息、明细、状态流转、事件记录和操作日志 |
| 新增页面 | 取消申请ID、取消申请号、客户、客服、渠道、系统、不想要、地址错误、缺货、风控、其他、处理时间 | 新增时校验必填、唯一、枚举、引用关系和数据权限 |
| 修改页面 | 取消申请ID、取消申请号、客户、客服、渠道、系统、不想要、地址错误、缺货、风控、其他、处理时间 | 已生效/终态数据只允许走变更、关闭、停用或补偿流程 |

| 操作 | 权限点 | 操作逻辑 | 结果/审计 |
| --- | --- | --- | --- |
| 查询 | `oms:page:read` | 读取读模型或列表投影，不修改业务状态 | 不记录业务事件，可记录访问日志 |
| 审核 | `oms:page:approval` | 执行业务动作，后端校验状态、权限、幂等键和参数 | 记录操作日志，按需要发布领域事件 |
| 同意 | `oms:page:page` | 执行业务动作，后端校验状态、权限、幂等键和参数 | 记录操作日志，按需要发布领域事件 |
| 拒绝 | `oms:page:page` | 执行业务动作，后端校验状态、权限、幂等键和参数 | 记录操作日志，按需要发布领域事件 |
| 转售后 | `oms:page:after_sale` | 执行业务动作，后端校验状态、权限、幂等键和参数 | 记录操作日志，按需要发布领域事件 |

### 售后管理页

| 项 | 设计 |
| --- | --- |
| 菜单路由 | `/oms/after-sales` |
| 主要用户 | 业务操作员、业务主管、系统管理员；外部协同页面按供应商/客户/货主数据范围过滤 |
| 业务对象 | 售后单 |
| 关联表/读模型 | `oms_after_sale` |
| 页面目的 | 处理仅退款、退货退款、换货补发 |
| 菜单权限 | `oms:after_sale:read` |

| 区域 | 字段/按钮 | 规则 |
| --- | --- | --- |
| 查询条件 | 售后单号、订单号、状态、仅退款、退货退款、换货补发、已创建、待审核、审核驳回、待退货、待验收、待退款、待补发、异常待处理、已完成、已关闭 | 支持组合查询；未选条件默认按用户数据权限过滤 |
| 排序 | 状态、金额 | 默认按更新时间倒序；支持分页，每页 10/20/50 条 |
| 列表展示 | 售后单号、订单号、类型、退款金额、状态、售后单ID、仅退款、退货退款、换货补发、质量问题、错发、少发、不想要、其他、已创建、待审核、审核驳回、待退货、待验收、待退款、待补发、异常待处理、已完成、已关闭、审核时间 | 列表展示业务识别字段、状态、关键金额/数量和更新时间 |
| 顶部按钮 | 查询、重置 | 根据按钮权限和数据范围显示 |
| 行内操作 | 查看详情、创建、审核、驳回、关闭 | 终态数据只允许查看、导出或复制，不允许直接编辑 |
| 详情展示 | 售后单ID、售后单号、仅退款、退货退款、换货补发、质量问题、错发、少发、不想要、其他、退款金额、已创建、待审核、审核驳回、待退货、待验收、待退款、待补发、异常待处理、已完成、已关闭、审核时间、完成时间 | 详情页展示基础信息、明细、状态流转、事件记录和操作日志 |
| 新增页面 | 售后单ID、售后单号、仅退款、退货退款、换货补发、质量问题、错发、少发、不想要、其他、退款金额、审核时间、完成时间 | 新增时校验必填、唯一、枚举、引用关系和数据权限 |
| 修改页面 | 售后单ID、售后单号、仅退款、退货退款、换货补发、质量问题、错发、少发、不想要、其他、退款金额、审核时间、完成时间 | 已生效/终态数据只允许走变更、关闭、停用或补偿流程 |

| 操作 | 权限点 | 操作逻辑 | 结果/审计 |
| --- | --- | --- | --- |
| 查询 | `oms:after_sale:read` | 读取读模型或列表投影，不修改业务状态 | 不记录业务事件，可记录访问日志 |
| 创建 | `oms:after_sale:page` | 执行业务动作，后端校验状态、权限、幂等键和参数 | 记录操作日志，按需要发布领域事件 |
| 审核 | `oms:after_sale:approval` | 执行业务动作，后端校验状态、权限、幂等键和参数 | 记录操作日志，按需要发布领域事件 |
| 驳回 | `oms:after_sale:reject` | 执行状态推进命令；状态流转必须符合领域模型状态机 | 写操作日志，写领域事件表，刷新列表读模型 |
| 关闭 | `oms:after_sale:close` | 执行状态推进命令；状态流转必须符合领域模型状态机 | 写操作日志，写领域事件表，刷新列表读模型 |

### 异常处理页

| 项 | 设计 |
| --- | --- |
| 菜单路由 | `/oms/exceptions` |
| 主要用户 | 业务操作员、业务主管、系统管理员；外部协同页面按供应商/客户/货主数据范围过滤 |
| 业务对象 | 销售订单 |
| 关联表/读模型 | `oms_sales_order` |
| 页面目的 | 集中处理缺货、地址不可达、风控、仓库拒单 |
| 菜单权限 | `oms:exception:read` |

| 区域 | 字段/按钮 | 规则 |
| --- | --- | --- |
| 查询条件 | 异常单号、订单号、状态、内部销售订单号、外部订单号、客户名称、普通、预售、换货补发、手工、未支付、已支付、部分退款、已退款 | 支持组合查询；未选条件默认按用户数据权限过滤 |
| 排序 | 状态、金额 | 默认按更新时间倒序；支持分页，每页 10/20/50 条 |
| 列表展示 | 异常单号、订单号、类型、责任方、状态、销售订单ID、内部销售订单号、外部订单号、客户名称、普通、预售、换货补发、手工 | 列表展示业务识别字段、状态、关键金额/数量和更新时间 |
| 顶部按钮 | 查询、重置 | 根据按钮权限和数据范围显示 |
| 行内操作 | 查看详情、分派、处理、关闭、重试 | 终态数据只允许查看、导出或复制，不允许直接编辑 |
| 详情展示 | 销售订单ID、内部销售订单号、外部订单号、客户名称、普通、预售、换货补发、手工、未支付、已支付、部分退款、已退款、待审核、通过、异常、驳回、已创建、待审核、异常待处理、待预占、缺货待处理、已预占、已下发仓库、出库中、已发货、已签收、已完成、已取消、未履约、部分履约、履约中、已履约、履约失败、订单总额、优惠金额、实付金额、收货人、收货电话、收货地址、支付时间 | 详情页展示基础信息、明细、状态流转、事件记录和操作日志 |
| 新增页面 | 销售订单ID、内部销售订单号、外部订单号、客户名称、普通、预售、换货补发、手工、订单总额、优惠金额、实付金额、收货人、收货电话、收货地址、支付时间 | 新增时校验必填、唯一、枚举、引用关系和数据权限 |
| 修改页面 | 销售订单ID、内部销售订单号、外部订单号、客户名称、普通、预售、换货补发、手工、订单总额、优惠金额、实付金额、收货人、收货电话、收货地址、支付时间 | 已生效/终态数据只允许走变更、关闭、停用或补偿流程 |

| 操作 | 权限点 | 操作逻辑 | 结果/审计 |
| --- | --- | --- | --- |
| 查询 | `oms:exception:read` | 读取读模型或列表投影，不修改业务状态 | 不记录业务事件，可记录访问日志 |
| 分派 | `oms:exception:page` | 执行业务动作，后端校验状态、权限、幂等键和参数 | 记录操作日志，按需要发布领域事件 |
| 处理 | `oms:exception:page` | 执行业务动作，后端校验状态、权限、幂等键和参数 | 记录操作日志，按需要发布领域事件 |
| 关闭 | `oms:exception:close` | 执行状态推进命令；状态流转必须符合领域模型状态机 | 写操作日志，写领域事件表，刷新列表读模型 |
| 重试 | `oms:exception:page` | 执行业务动作，后端校验状态、权限、幂等键和参数 | 记录操作日志，按需要发布领域事件 |

### 规则配置页

| 项 | 设计 |
| --- | --- |
| 菜单路由 | `/oms/rules` |
| 主要用户 | 业务操作员、业务主管、系统管理员；外部协同页面按供应商/客户/货主数据范围过滤 |
| 业务对象 | OMS 规则配置 |
| 关联表/读模型 | `oms_oms_rule` |
| 页面目的 | 配置审单、分仓、取消、售后规则 |
| 菜单权限 | `oms:rule:read` |

| 区域 | 字段/按钮 | 规则 |
| --- | --- | --- |
| 查询条件 | 规则编码、状态、规则名称、审单、分仓、取消、售后、承运商、启用、停用 | 支持组合查询；未选条件默认按用户数据权限过滤 |
| 排序 | 状态、优先级 | 默认按更新时间倒序；支持分页，每页 10/20/50 条 |
| 列表展示 | 规则编码、规则类型、优先级、状态、OMS 规则配置ID、规则名称、审单、分仓、取消、售后、承运商、条件配置、动作配置、启用、停用 | 列表展示业务识别字段、状态、关键金额/数量和更新时间 |
| 顶部按钮 | 查询、重置、新增 | 根据按钮权限和数据范围显示 |
| 行内操作 | 查看详情、编辑、启用、停用、发布 | 终态数据只允许查看、导出或复制，不允许直接编辑 |
| 详情展示 | OMS 规则配置ID、规则编码、规则名称、审单、分仓、取消、售后、承运商、优先级、条件配置、动作配置、启用、停用、生效时间、失效时间 | 详情页展示基础信息、明细、状态流转、事件记录和操作日志 |
| 新增页面 | OMS 规则配置ID、规则编码、规则名称、审单、分仓、取消、售后、承运商、优先级、条件配置、动作配置、生效时间、失效时间 | 新增时校验必填、唯一、枚举、引用关系和数据权限 |
| 修改页面 | OMS 规则配置ID、规则编码、规则名称、审单、分仓、取消、售后、承运商、优先级、条件配置、动作配置、生效时间、失效时间 | 已生效/终态数据只允许走变更、关闭、停用或补偿流程 |

| 操作 | 权限点 | 操作逻辑 | 结果/审计 |
| --- | --- | --- | --- |
| 查询 | `oms:settings:read` | 读取读模型或列表投影，不修改业务状态 | 不记录业务事件，可记录访问日志 |
| 新增 | `oms:settings:create` | 打开新增页，提交后由后端应用服务校验并创建聚合 | 记录操作日志，必要时发布已创建事件 |
| 编辑 | `oms:settings:update` | 加载当前版本，校验乐观锁、状态机和数据权限后保存 | 记录前后快照，必要时发布已变更事件 |
| 启用 | `oms:settings:enable` | 执行状态推进命令；状态流转必须符合领域模型状态机 | 写操作日志，写领域事件表，刷新列表读模型 |
| 停用 | `oms:settings:disable` | 执行状态推进命令；状态流转必须符合领域模型状态机 | 写操作日志，写领域事件表，刷新列表读模型 |
| 发布 | `oms:settings:release` | 执行状态推进命令；状态流转必须符合领域模型状态机 | 写操作日志，写领域事件表，刷新列表读模型 |

### 操作日志页

| 项 | 设计 |
| --- | --- |
| 菜单路由 | `/oms/operation-logs` |
| 主要用户 | 业务操作员、业务主管、系统管理员；外部协同页面按供应商/客户/货主数据范围过滤 |
| 业务对象 | 订单审单结果 |
| 关联表/读模型 | `oms_order_result` |
| 页面目的 | 查询 OMS 关键写操作 |
| 菜单权限 | `oms:operation_log:read` |

| 区域 | 字段/按钮 | 规则 |
| --- | --- | --- |
| 查询条件 | 时间、商品、客户、地址、价格、风控、信用、待处理、已放行、已驳回、已修正 | 支持组合查询；未选条件默认按用户数据权限过滤 |
| 排序 | 创建时间、更新时间 | 默认按更新时间倒序；支持分页，每页 10/20/50 条 |
| 列表展示 | 操作人、对象、动作、时间、结果、订单审单结果ID、商品、客户、地址、价格、风控、信用、通过、拦截、警告、异常原因、待处理、已放行、已驳回、已修正 | 列表展示业务识别字段、状态、关键金额/数量和更新时间 |
| 顶部按钮 | 重置、查询、导出 | 根据按钮权限和数据范围显示 |
| 行内操作 | 查看详情 | 终态数据只允许查看、导出或复制，不允许直接编辑 |
| 详情展示 | 订单审单结果ID、商品、客户、地址、价格、风控、信用、通过、拦截、警告、异常原因、待处理、已放行、已驳回、已修正、处理人、处理时间 | 详情页展示基础信息、明细、状态流转、事件记录和操作日志 |
| 新增页面 | 订单审单结果ID、商品、客户、地址、价格、风控、信用、通过、拦截、警告、异常原因、处理人、处理时间 | 新增时校验必填、唯一、枚举、引用关系和数据权限 |
| 修改页面 | 订单审单结果ID、商品、客户、地址、价格、风控、信用、通过、拦截、警告、异常原因、处理人、处理时间 | 已生效/终态数据只允许走变更、关闭、停用或补偿流程 |

| 操作 | 权限点 | 操作逻辑 | 结果/审计 |
| --- | --- | --- | --- |
| 查询 | `oms:operation_log:read` | 读取读模型或列表投影，不修改业务状态 | 不记录业务事件，可记录访问日志 |
| 导出 | `oms:operation_log:export` | 导出使用当前查询条件和数据权限；导入先生成任务并逐行校验 | 记录导入/导出日志，失败明细可下载 |

### 枚举配置页

| 项 | 设计 |
| --- | --- |
| 菜单路由 | `/oms/enums` |
| 主要用户 | 业务操作员、业务主管、系统管理员；外部协同页面按供应商/客户/货主数据范围过滤 |
| 业务对象 | 销售订单 |
| 关联表/读模型 | `oms_sales_order` |
| 页面目的 | 维护 OMS 页面枚举项 |
| 菜单权限 | `oms:enum:read` |

| 区域 | 字段/按钮 | 规则 |
| --- | --- | --- |
| 查询条件 | 状态、内部销售订单号、外部订单号、客户名称、普通、预售、换货补发、手工、未支付、已支付、部分退款、已退款、待审核、通过、异常、驳回、已创建、待审核、异常待处理、待预占、缺货待处理、已预占、已下发仓库、出库中、已发货、已签收、已完成、已取消 | 支持组合查询；未选条件默认按用户数据权限过滤 |
| 排序 | 状态、金额 | 默认按更新时间倒序；支持分页，每页 10/20/50 条 |
| 列表展示 | 枚举类型、枚举值、标签、状态、销售订单ID、内部销售订单号、外部订单号、客户名称、普通、预售、换货补发、手工、未支付、已支付、部分退款、已退款 | 列表展示业务识别字段、状态、关键金额/数量和更新时间 |
| 顶部按钮 | 查询、重置、新增 | 根据按钮权限和数据范围显示 |
| 行内操作 | 查看详情、编辑、排序、停用 | 终态数据只允许查看、导出或复制，不允许直接编辑 |
| 详情展示 | 销售订单ID、内部销售订单号、外部订单号、客户名称、普通、预售、换货补发、手工、未支付、已支付、部分退款、已退款、待审核、通过、异常、驳回、已创建、待审核、异常待处理、待预占、缺货待处理、已预占、已下发仓库、出库中、已发货、已签收、已完成、已取消、未履约、部分履约、履约中、已履约、履约失败、订单总额、优惠金额、实付金额、收货人、收货电话、收货地址、支付时间 | 详情页展示基础信息、明细、状态流转、事件记录和操作日志 |
| 新增页面 | 销售订单ID、内部销售订单号、外部订单号、客户名称、普通、预售、换货补发、手工、订单总额、优惠金额、实付金额、收货人、收货电话、收货地址、支付时间 | 新增时校验必填、唯一、枚举、引用关系和数据权限 |
| 修改页面 | 销售订单ID、内部销售订单号、外部订单号、客户名称、普通、预售、换货补发、手工、订单总额、优惠金额、实付金额、收货人、收货电话、收货地址、支付时间 | 已生效/终态数据只允许走变更、关闭、停用或补偿流程 |

| 操作 | 权限点 | 操作逻辑 | 结果/审计 |
| --- | --- | --- | --- |
| 查询 | `oms:enumsettings:read` | 读取读模型或列表投影，不修改业务状态 | 不记录业务事件，可记录访问日志 |
| 新增 | `oms:enumsettings:create` | 打开新增页，提交后由后端应用服务校验并创建聚合 | 记录操作日志，必要时发布已创建事件 |
| 编辑 | `oms:enumsettings:update` | 加载当前版本，校验乐观锁、状态机和数据权限后保存 | 记录前后快照，必要时发布已变更事件 |
| 排序 | `oms:enumsettings:sort` | 执行业务动作，后端校验状态、权限、幂等键和参数 | 记录操作日志，按需要发布领域事件 |
| 停用 | `oms:enumsettings:disable` | 执行状态推进命令；状态流转必须符合领域模型状态机 | 写操作日志，写领域事件表，刷新列表读模型 |

## 6. 权限点总表

| 页面 | 查询权限 | 按钮权限 | 数据权限 |
| --- | --- | --- | --- |
| OMS 工作台 | `oms:workbench:read` | `oms:oms_workbench:page`、`oms:oms_workbench:page` | 组织、仓库、货主、供应商/客户、本人/部门 |
| 渠道订单接入页 | `oms:channel_order:read` | `oms:order:page`、`oms:order:page`、`oms:order:page` | 组织、仓库、货主、供应商/客户、本人/部门 |
| 销售订单页 | `oms:sales_order:read` | `oms:sales_order:create`、`oms:sales_order:update`、`oms:sales_order:approval`、`oms:sales_order:cancel`、`oms:sales_order:page` | 组织、仓库、货主、供应商/客户、本人/部门 |
| 订单审单页 | `oms:audit:read` | `oms:order:approve`、`oms:order:reject`、`oms:order:page`、`oms:order:page` | 组织、仓库、货主、供应商/客户、本人/部门 |
| 分仓履约页 | `oms:fulfillment:read` | `oms:fulfillment:page`、`oms:fulfillment:page`、`oms:fulfillment:page`、`oms:fulfillment:page` | 组织、仓库、货主、供应商/客户、本人/部门 |
| 库存预占页 | `oms:reservation:read` | `oms:stockreservation:reservation`、`oms:stockreservation:releasereservation` | 组织、仓库、货主、供应商/客户、本人/部门 |
| 出库单页 | `oms:outbound:read` | `oms:outbound:page`、`oms:outbound:page`、`oms:outbound:cancel`、`oms:outbound:page` | 组织、仓库、货主、供应商/客户、本人/部门 |
| 取消管理页 | `oms:cancel:read` | `oms:page:approval`、`oms:page:page`、`oms:page:page`、`oms:page:after_sale` | 组织、仓库、货主、供应商/客户、本人/部门 |
| 售后管理页 | `oms:after_sale:read` | `oms:after_sale:page`、`oms:after_sale:approval`、`oms:after_sale:reject`、`oms:after_sale:close` | 组织、仓库、货主、供应商/客户、本人/部门 |
| 异常处理页 | `oms:exception:read` | `oms:exception:page`、`oms:exception:page`、`oms:exception:close`、`oms:exception:page` | 组织、仓库、货主、供应商/客户、本人/部门 |
| 规则配置页 | `oms:rule:read` | `oms:settings:create`、`oms:settings:update`、`oms:settings:enable`、`oms:settings:disable`、`oms:settings:release` | 组织、仓库、货主、供应商/客户、本人/部门 |
| 操作日志页 | `oms:operation_log:read` | `oms:operation_log:export` | 组织、仓库、货主、供应商/客户、本人/部门 |
| 枚举配置页 | `oms:enum:read` | `oms:enumsettings:create`、`oms:enumsettings:update`、`oms:enumsettings:sort`、`oms:enumsettings:disable` | 组织、仓库、货主、供应商/客户、本人/部门 |

## 7. 关键操作逻辑

| 操作类型 | 前置校验 | 状态/数据变化 | 事件与审计 |
| --- | --- | --- | --- |
| 新增 | 菜单权限、按钮权限、必填、唯一、引用主数据有效 | 创建草稿或初始状态数据 | 记录操作日志；必要时发布已创建事件 |
| 修改 | 按钮权限、数据权限、乐观锁、非终态、字段可编辑 | 更新业务字段或生成变更记录 | 记录前后快照；关键变更发布事件 |
| 删除 | 通常只允许草稿或未引用数据 | 逻辑删除，历史单据不物理删除 | 记录删除原因和操作者 |
| 提交/发布/审批 | 状态机允许、审批权限、幂等键未重复 | 推进状态，锁定或发布业务事实 | 写领域事件表，刷新读模型 |
| 取消/关闭/停用 | 无未完成依赖或允许补偿路径 | 进入终态或禁用状态 | 记录原因；通知下游或生成待办 |
| 查询/导出 | 查询权限、数据范围、脱敏策略 | 不改变业务状态 | 导出记录审计日志 |

## 8. 事件、日志与审计

| 事件/日志 | 用途 | 页面影响 |
| --- | --- | --- |
| 销售订单已创建 | 中央库存、WMS、BMS、读模型、审计日志、报表 | 刷新列表、详情时间线、待办或状态展示 |
| 订单已审核 | 中央库存、WMS、BMS、读模型、审计日志、报表 | 刷新列表、详情时间线、待办或状态展示 |
| 订单异常已标记 | 中央库存、WMS、BMS、读模型、审计日志、报表 | 刷新列表、详情时间线、待办或状态展示 |
| 订单已发货 | 中央库存、WMS、BMS、读模型、审计日志、报表 | 刷新列表、详情时间线、待办或状态展示 |
| 订单已签收 | 中央库存、WMS、BMS、读模型、审计日志、报表 | 刷新列表、详情时间线、待办或状态展示 |
| 订单已完成 | 中央库存、WMS、BMS、读模型、审计日志、报表 | 刷新列表、详情时间线、待办或状态展示 |
| 订单已取消 | 中央库存、WMS、BMS、读模型、审计日志、报表 | 刷新列表、详情时间线、待办或状态展示 |
| WMS已发货 | 来源上下文+事件编号+业务主键 | 刷新列表、详情时间线、待办或状态展示 |
| 库存已预占 | 库存上下文+事件编号+reservationId | 刷新列表、详情时间线、待办或状态展示 |
| 退款已完成 | BMS上下文+事件编号+refundId | 刷新列表、详情时间线、待办或状态展示 |
| 履约单已创建 | 中央库存、WMS、BMS、读模型、审计日志、报表 | 刷新列表、详情时间线、待办或状态展示 |
| 库存预占已请求 | 中央库存、WMS、BMS、读模型、审计日志、报表 | 刷新列表、详情时间线、待办或状态展示 |
| 履约库存已预占 | 中央库存、WMS、BMS、读模型、审计日志、报表 | 刷新列表、详情时间线、待办或状态展示 |
| 履约出库已请求 | 中央库存、WMS、BMS、读模型、审计日志、报表 | 刷新列表、详情时间线、待办或状态展示 |
| 履约单已关闭 | 中央库存、WMS、BMS、读模型、审计日志、报表 | 刷新列表、详情时间线、待办或状态展示 |
| 出库单已创建 | 中央库存、WMS、BMS、读模型、审计日志、报表 | 刷新列表、详情时间线、待办或状态展示 |
| 出库单已下发WMS | 中央库存、WMS、BMS、读模型、审计日志、报表 | 刷新列表、详情时间线、待办或状态展示 |
| WMS已接单 | 中央库存、WMS、BMS、读模型、审计日志、报表 | 刷新列表、详情时间线、待办或状态展示 |
| 出库单已取消 | 中央库存、WMS、BMS、读模型、审计日志、报表 | 刷新列表、详情时间线、待办或状态展示 |
| 出库单已关闭 | 中央库存、WMS、BMS、读模型、审计日志、报表 | 刷新列表、详情时间线、待办或状态展示 |
| 取消申请已创建 | 中央库存、WMS、BMS、读模型、审计日志、报表 | 刷新列表、详情时间线、待办或状态展示 |
| 取消申请已审核 | 中央库存、WMS、BMS、读模型、审计日志、报表 | 刷新列表、详情时间线、待办或状态展示 |
| WMS取消已请求 | 中央库存、WMS、BMS、读模型、审计日志、报表 | 刷新列表、详情时间线、待办或状态展示 |
| 取消库存释放已请求 | 中央库存、WMS、BMS、读模型、审计日志、报表 | 刷新列表、详情时间线、待办或状态展示 |
| 操作日志 | 记录用户写操作、敏感查询、导入导出和授权变化 | 详情页时间线、操作日志页可查询 |
| 事件消费日志 | 记录外部事件幂等消费和失败重试 | 异常页或系统运维页可定位问题 |

## 9. 与领域模型和数据库设计的对应关系

| 来源 | 使用方式 |
| --- | --- |
| `docs/03-核心业务模型/05-OMS领域模型` | 决定页面动作是否能推进状态、是否要发布事件、是否需要补偿 |
| `docs/05-子系统数据库设计/05-OMS系统数据库设计.md` | 决定列表字段、详情字段、新增/编辑字段、枚举值和日志表字段 |
| 权限系统 | 决定登录认证、菜单展示、按钮展示、API 校验和数据范围 |

当前结论：本文是产品功能设计入口；后续页面原型、接口定义和后端实现都应以本文的菜单、按钮、权限和操作逻辑为准。

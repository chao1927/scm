# 权限系统数据库设计

> 本文根据 `docs/03-核心业务模型` 的聚合、不变量、命令事件，以及 `docs/04-子系统功能设计` 的页面、状态、枚举和操作场景整理。本文是数据库表字段设计入口；领域模型和功能设计文档不再保留字段迁移链接。

## 1. 设计口径

| 项目 | 口径 |
| --- | --- |
| 所属系统 | 权限系统 |
| 表名前缀 | `iam_` |
| 主键策略 | 业务表使用 bigint 主键，建议雪花 ID；业务单号另设唯一索引 |
| 时间类型 | 业务时间使用 `datetime`，日期使用 `date`，金额使用 `decimal(18,2)` 或更高精度 |
| 数量类型 | 库存、采购、履约数量统一使用 `decimal(18,4)` |
| 状态字段 | 状态机字段只能由聚合命令推进，页面只配置展示名、排序和颜色 |
| 枚举字段 | 业务可配置枚举进入枚举配置；核心状态枚举可以配置标签但不能随意增删 |

## 2. 通用字段

| 字段 | 类型 | 是否必填 | 约束 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | bigint | 是 | 主键，自增或雪花ID | 技术主键 |
| `tenant_id` | bigint | 否 | 多租户时启用 | 租户ID |
| `org_id` | bigint | 否 | 按组织隔离时启用 | 组织ID |
| `created_by` | bigint | 是 | 写入时填充 | 创建人 |
| `created_at` | datetime | 是 | 默认当前时间 | 创建时间 |
| `updated_by` | bigint | 否 | 更新时填充 | 更新人 |
| `updated_at` | datetime | 是 | 默认当前时间并自动更新 | 更新时间 |
| `version` | int | 是 | 默认 0，乐观锁 | 数据版本 |
| `deleted` | tinyint | 是 | 0 否，1 是 | 逻辑删除标记 |

## 3. 表清单

| 表名 | 业务对象 | 来源聚合 | 说明 |
| --- | --- | --- | --- |
| `iam_app` | 应用系统 | `02-应用聚合CQRS设计.md` | 承载聚合当前状态、业务快照和列表查询核心字段 |
| `iam_sso` | SSO 客户端 | `02-应用聚合CQRS设计.md` | 承载聚合当前状态、业务快照和列表查询核心字段 |
| `iam_user` | 用户 | `03-用户聚合CQRS设计.md` | 承载聚合当前状态、业务快照和列表查询核心字段 |
| `iam_user_role` | 用户角色关系 | `03-用户聚合CQRS设计.md` | 承载聚合当前状态、业务快照和列表查询核心字段 |
| `iam_role` | 角色 | `04-角色聚合CQRS设计.md` | 承载聚合当前状态、业务快照和列表查询核心字段 |
| `iam_role_permission` | 角色权限关系 | `04-角色聚合CQRS设计.md` | 承载聚合当前状态、业务快照和列表查询核心字段 |
| `iam_menu_page` | 菜单/页面 | `05-权限点聚合CQRS设计.md` | 承载聚合当前状态、业务快照和列表查询核心字段 |
| `iam_permission` | 权限点 | `05-权限点聚合CQRS设计.md` | 承载聚合当前状态、业务快照和列表查询核心字段 |
| `iam_data_scope` | 数据权限范围 | `06-数据权限聚合CQRS设计.md` | 承载聚合当前状态、业务快照和列表查询核心字段 |
| `iam_user_token` | 用户会话/Token | `07-会话Token聚合CQRS设计.md` | 承载聚合当前状态、业务快照和列表查询核心字段 |
| `iam_login_log` | 登录日志 | `07-会话Token聚合CQRS设计.md` | 承载聚合当前状态、业务快照和列表查询核心字段 |
| `iam_operation_log` | 操作日志 | `09-操作日志聚合CQRS设计.md` | 承载聚合当前状态、业务快照和列表查询核心字段 |

## 4. 表字段设计

### 4.1 `iam_app`

业务对象：应用系统

| 字段 | 类型 | 是否必填 | 约束/枚举 | 领域归类 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `app_id` | bigint | 是 | 主键，雪花ID或数据库自增 | 技术主键 | 应用系统主键 |
| `app_code` | varchar(64) | 是 | 唯一 | 标识/引用 | 应用编码，如 `OMS`、`WMS` |
| `app_name` | varchar(128) | 是 |   | 业务属性 | 应用名称 |
| `app_type` | varchar(32) | 是 | `APP_TYPE` | 状态/分类 | Web、OpenAPI、移动端、后台服务 |
| `base_url` | varchar(512) | 否 | URL | 业务属性 | 应用首页地址 |
| `status` | varchar(32) | 是 | `COMMON_STATUS` | 状态/分类 | 启用、停用 |
| `sort_no` | int | 否 |   | 标识/引用 | 排序 |
| `remark` | varchar(512) | 否 |   | 业务属性 | 备注 |
| `created_by` | bigint | 是 | 写入时填充 | 审计 | 创建人 |
| `created_at` | datetime | 是 | 默认当前时间 | 审计 | 创建时间 |
| `updated_by` | bigint | 否 | 更新时填充 | 审计 | 更新人 |
| `updated_at` | datetime | 是 | 默认当前时间并自动更新 | 审计 | 更新时间 |
| `version` | int | 是 | 默认 0，乐观锁 | 并发控制 | 数据版本 |
| `deleted` | tinyint | 是 | 0 否，1 是 | 数据治理 | 逻辑删除标记 |

建议索引：

| 索引 | 字段 | 说明 |
| --- | --- | --- |
| `uk_iam_app_biz_no` | 业务单号或编码字段 | 如果该表存在编码/单号字段，必须唯一 |
| `idx_iam_app_status_time` | 状态字段、`updated_at` | 支撑列表筛选和待办查询 |
| `idx_iam_app_source` | 来源单号、外部单号或引用编码 | 支撑跨系统追踪 |

### 4.2 `iam_sso`

业务对象：SSO 客户端

| 字段 | 类型 | 是否必填 | 约束/枚举 | 领域归类 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `sso_id` | bigint | 是 | 主键，雪花ID或数据库自增 | 技术主键 | SSO 客户端主键 |
| `client_code` | varchar(64) | 是 | 唯一 | 标识/引用 | OAuth/OIDC client id |
| `client_secret_hash` | varchar(256) | 是 | 加密存储 | 业务属性 | 客户端密钥哈希 |
| `redirect_uris` | text | 是 | JSON 数组 | 业务属性 | 允许回调地址 |
| `grant_types` | varchar(256) | 是 | `GRANT_TYPE` 多选 | 业务属性 | 授权码、密码、刷新 Token 等 |
| `token_ttl_seconds` | int | 是 | > 0 | 业务属性 | access token 有效期 |
| `refresh_ttl_seconds` | int | 是 | > 0 | 业务属性 | refresh token 有效期 |
| `status` | varchar(32) | 是 | `COMMON_STATUS` | 状态/分类 | 启用、停用 |
| `created_by` | bigint | 是 | 写入时填充 | 审计 | 创建人 |
| `created_at` | datetime | 是 | 默认当前时间 | 审计 | 创建时间 |
| `updated_by` | bigint | 否 | 更新时填充 | 审计 | 更新人 |
| `updated_at` | datetime | 是 | 默认当前时间并自动更新 | 审计 | 更新时间 |
| `version` | int | 是 | 默认 0，乐观锁 | 并发控制 | 数据版本 |
| `deleted` | tinyint | 是 | 0 否，1 是 | 数据治理 | 逻辑删除标记 |

建议索引：

| 索引 | 字段 | 说明 |
| --- | --- | --- |
| `uk_iam_sso_biz_no` | 业务单号或编码字段 | 如果该表存在编码/单号字段，必须唯一 |
| `idx_iam_sso_status_time` | 状态字段、`updated_at` | 支撑列表筛选和待办查询 |
| `idx_iam_sso_source` | 来源单号、外部单号或引用编码 | 支撑跨系统追踪 |

### 4.3 `iam_user`

业务对象：用户

| 字段 | 类型 | 是否必填 | 约束/枚举 | 领域归类 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `user_id` | bigint | 是 | 主键，雪花ID或数据库自增 | 技术主键 | 用户主键 |
| `username` | varchar(64) | 是 | 唯一 | 业务属性 | 登录账号 |
| `password_hash` | varchar(256) | 否 | 加密存储 | 业务属性 | 本地账号密码哈希；外部 SSO 可为空 |
| `real_name` | varchar(128) | 是 |   | 业务属性 | 姓名 |
| `nickname` | varchar(128) | 否 |   | 业务属性 | 昵称 |
| `mobile` | varchar(32) | 否 | 唯一可选 | 业务属性 | 手机号 |
| `email` | varchar(128) | 否 | 唯一可选 | 业务属性 | 邮箱 |
| `user_type` | varchar(32) | 是 | `USER_TYPE` | 状态/分类 | 内部员工、供应商用户、客户用户、系统账号 |
| `employee_no` | varchar(64) | 否 |   | 标识/引用 | 工号 |
| `mfa_enabled` | boolean | 是 | true/false | 业务属性 | 是否启用 MFA |
| `last_login_at` | datetime | 否 |   | 业务属性 | 最近登录时间 |
| `login_fail_count` | int | 是 | 默认 0 | 业务属性 | 连续失败次数 |
| `locked_until` | datetime | 否 |   | 业务属性 | 锁定到期时间 |
| `status` | varchar(32) | 是 | `USER_STATUS` | 状态/分类 | 待激活、已启用、已锁定、已停用、已注销 |
| `created_by` | bigint | 是 | 写入时填充 | 审计 | 创建人 |
| `created_at` | datetime | 是 | 默认当前时间 | 审计 | 创建时间 |
| `updated_by` | bigint | 否 | 更新时填充 | 审计 | 更新人 |
| `updated_at` | datetime | 是 | 默认当前时间并自动更新 | 审计 | 更新时间 |
| `version` | int | 是 | 默认 0，乐观锁 | 并发控制 | 数据版本 |
| `deleted` | tinyint | 是 | 0 否，1 是 | 数据治理 | 逻辑删除标记 |

建议索引：

| 索引 | 字段 | 说明 |
| --- | --- | --- |
| `uk_iam_user_biz_no` | 业务单号或编码字段 | 如果该表存在编码/单号字段，必须唯一 |
| `idx_iam_user_status_time` | 状态字段、`updated_at` | 支撑列表筛选和待办查询 |
| `idx_iam_user_source` | 来源单号、外部单号或引用编码 | 支撑跨系统追踪 |

### 4.4 `iam_user_role`

业务对象：用户角色关系

| 字段 | 类型 | 是否必填 | 约束/枚举 | 领域归类 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `user_role_id` | bigint | 是 | 主键，雪花ID或数据库自增 | 技术主键 | 用户角色关系主键 |
| `grant_type` | varchar(32) | 是 | `GRANT_TYPE_USER_ROLE` | 状态/分类 | 手工、组织继承、岗位继承、系统默认 |
| `effective_from` | datetime | 否 |   | 业务属性 | 生效时间 |
| `effective_to` | datetime | 否 |   | 业务属性 | 失效时间 |
| `status` | varchar(32) | 是 | `COMMON_STATUS` | 状态/分类 | 启用、停用 |
| `granted_by` | bigint | 是 |   | 业务属性 | 授权人 |
| `granted_at` | datetime | 是 |   | 业务属性 | 授权时间 |
| `created_by` | bigint | 是 | 写入时填充 | 审计 | 创建人 |
| `created_at` | datetime | 是 | 默认当前时间 | 审计 | 创建时间 |
| `updated_by` | bigint | 否 | 更新时填充 | 审计 | 更新人 |
| `updated_at` | datetime | 是 | 默认当前时间并自动更新 | 审计 | 更新时间 |
| `version` | int | 是 | 默认 0，乐观锁 | 并发控制 | 数据版本 |
| `deleted` | tinyint | 是 | 0 否，1 是 | 数据治理 | 逻辑删除标记 |

建议索引：

| 索引 | 字段 | 说明 |
| --- | --- | --- |
| `uk_iam_user_role_biz_no` | 业务单号或编码字段 | 如果该表存在编码/单号字段，必须唯一 |
| `idx_iam_user_role_status_time` | 状态字段、`updated_at` | 支撑列表筛选和待办查询 |
| `idx_iam_user_role_source` | 来源单号、外部单号或引用编码 | 支撑跨系统追踪 |

### 4.5 `iam_role`

业务对象：角色

| 字段 | 类型 | 是否必填 | 约束/枚举 | 领域归类 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `role_id` | bigint | 是 | 主键，雪花ID或数据库自增 | 技术主键 | 角色主键 |
| `role_code` | varchar(64) | 是 | 唯一 | 标识/引用 | 角色编码 |
| `role_name` | varchar(128) | 是 |   | 业务属性 | 角色名称 |
| `role_type` | varchar(32) | 是 | `ROLE_TYPE` | 状态/分类 | 系统角色、业务角色、外部角色 |
| `app_scope` | varchar(64) | 否 | 应用编码或 `ALL` | 业务属性 | 角色适用应用 |
| `data_scope_type` | varchar(32) | 是 | `DATA_SCOPE_TYPE` | 状态/分类 | 默认数据范围 |
| `description` | varchar(512) | 否 |   | 业务属性 | 角色说明 |
| `status` | varchar(32) | 是 | `COMMON_STATUS` | 状态/分类 | 启用、停用 |
| `created_by` | bigint | 是 | 写入时填充 | 审计 | 创建人 |
| `created_at` | datetime | 是 | 默认当前时间 | 审计 | 创建时间 |
| `updated_by` | bigint | 否 | 更新时填充 | 审计 | 更新人 |
| `updated_at` | datetime | 是 | 默认当前时间并自动更新 | 审计 | 更新时间 |
| `version` | int | 是 | 默认 0，乐观锁 | 并发控制 | 数据版本 |
| `deleted` | tinyint | 是 | 0 否，1 是 | 数据治理 | 逻辑删除标记 |

建议索引：

| 索引 | 字段 | 说明 |
| --- | --- | --- |
| `uk_iam_role_biz_no` | 业务单号或编码字段 | 如果该表存在编码/单号字段，必须唯一 |
| `idx_iam_role_status_time` | 状态字段、`updated_at` | 支撑列表筛选和待办查询 |
| `idx_iam_role_source` | 来源单号、外部单号或引用编码 | 支撑跨系统追踪 |

### 4.6 `iam_role_permission`

业务对象：角色权限关系

| 字段 | 类型 | 是否必填 | 约束/枚举 | 领域归类 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `role_id` | bigint | 是 | 主键，雪花ID或数据库自增 | 技术主键 | 角色权限关系主键 |
| `grant_status` | varchar(32) | 是 | `GRANT_STATUS` | 状态/分类 | 已授权、已取消 |
| `granted_by` | bigint | 是 |   | 业务属性 | 授权人 |
| `granted_at` | datetime | 是 |   | 业务属性 | 授权时间 |
| `revoked_by` | bigint | 否 |   | 业务属性 | 取消授权人 |
| `revoked_at` | datetime | 否 |   | 业务属性 | 取消授权时间 |
| `created_by` | bigint | 是 | 写入时填充 | 审计 | 创建人 |
| `created_at` | datetime | 是 | 默认当前时间 | 审计 | 创建时间 |
| `updated_by` | bigint | 否 | 更新时填充 | 审计 | 更新人 |
| `updated_at` | datetime | 是 | 默认当前时间并自动更新 | 审计 | 更新时间 |
| `version` | int | 是 | 默认 0，乐观锁 | 并发控制 | 数据版本 |
| `deleted` | tinyint | 是 | 0 否，1 是 | 数据治理 | 逻辑删除标记 |

建议索引：

| 索引 | 字段 | 说明 |
| --- | --- | --- |
| `uk_iam_role_permission_biz_no` | 业务单号或编码字段 | 如果该表存在编码/单号字段，必须唯一 |
| `idx_iam_role_permission_status_time` | 状态字段、`updated_at` | 支撑列表筛选和待办查询 |
| `idx_iam_role_permission_source` | 来源单号、外部单号或引用编码 | 支撑跨系统追踪 |

### 4.7 `iam_menu_page`

业务对象：菜单/页面

| 字段 | 类型 | 是否必填 | 约束/枚举 | 领域归类 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `business_object_id` | bigint | 是 | 主键，雪花ID或数据库自增 | 技术主键 | 菜单/页面主键 |
| `menu_code` | varchar(128) | 是 | 应用内唯一 | 标识/引用 | 菜单编码 |
| `menu_name` | varchar(128) | 是 |   | 业务属性 | 菜单名称 |
| `menu_type` | varchar(32) | 是 | `MENU_TYPE` | 状态/分类 | 目录、页面、外链 |
| `route_path` | varchar(256) | 否 |   | 业务属性 | 前端路由 |
| `component_path` | varchar(256) | 否 |   | 业务属性 | 前端组件路径 |
| `icon` | varchar(64) | 否 |   | 业务属性 | 图标 |
| `sort_no` | int | 是 | 默认 0 | 标识/引用 | 排序 |
| `visible` | boolean | 是 | true/false | 业务属性 | 是否在菜单展示 |
| `status` | varchar(32) | 是 | `COMMON_STATUS` | 状态/分类 | 启用、停用 |
| `created_by` | bigint | 是 | 写入时填充 | 审计 | 创建人 |
| `created_at` | datetime | 是 | 默认当前时间 | 审计 | 创建时间 |
| `updated_by` | bigint | 否 | 更新时填充 | 审计 | 更新人 |
| `updated_at` | datetime | 是 | 默认当前时间并自动更新 | 审计 | 更新时间 |
| `version` | int | 是 | 默认 0，乐观锁 | 并发控制 | 数据版本 |
| `deleted` | tinyint | 是 | 0 否，1 是 | 数据治理 | 逻辑删除标记 |

建议索引：

| 索引 | 字段 | 说明 |
| --- | --- | --- |
| `uk_iam_menu_page_biz_no` | 业务单号或编码字段 | 如果该表存在编码/单号字段，必须唯一 |
| `idx_iam_menu_page_status_time` | 状态字段、`updated_at` | 支撑列表筛选和待办查询 |
| `idx_iam_menu_page_source` | 来源单号、外部单号或引用编码 | 支撑跨系统追踪 |

### 4.8 `iam_permission`

业务对象：权限点

| 字段 | 类型 | 是否必填 | 约束/枚举 | 领域归类 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `permission_id` | bigint | 是 | 主键，雪花ID或数据库自增 | 技术主键 | 权限点主键 |
| `permission_code` | varchar(128) | 是 | 全局唯一 | 标识/引用 | 权限编码，如 `wms:outbound:create` |
| `permission_name` | varchar(128) | 是 |   | 业务属性 | 权限名称 |
| `permission_type` | varchar(32) | 是 | `PERMISSION_TYPE` | 状态/分类 | 菜单、按钮、API、字段 |
| `resource_type` | varchar(32) | 是 | `RESOURCE_TYPE` | 状态/分类 | 页面、按钮、接口、字段 |
| `action_type` | varchar(32) | 是 | `ACTION_TYPE` | 状态/分类 | CREATE、READ、UPDATE、DELETE、EXPORT、IMPORT、APPROVE 等 |
| `api_method` | varchar(16) | 否 | `HTTP_METHOD` | 业务属性 | GET、POST、PUT、DELETE |
| `api_path` | varchar(256) | 否 |   | 业务属性 | API 路径 |
| `field_code` | varchar(128) | 否 |   | 标识/引用 | 字段权限适用 |
| `status` | varchar(32) | 是 | `COMMON_STATUS` | 状态/分类 | 启用、停用 |
| `created_by` | bigint | 是 | 写入时填充 | 审计 | 创建人 |
| `created_at` | datetime | 是 | 默认当前时间 | 审计 | 创建时间 |
| `updated_by` | bigint | 否 | 更新时填充 | 审计 | 更新人 |
| `updated_at` | datetime | 是 | 默认当前时间并自动更新 | 审计 | 更新时间 |
| `version` | int | 是 | 默认 0，乐观锁 | 并发控制 | 数据版本 |
| `deleted` | tinyint | 是 | 0 否，1 是 | 数据治理 | 逻辑删除标记 |

建议索引：

| 索引 | 字段 | 说明 |
| --- | --- | --- |
| `uk_iam_permission_biz_no` | 业务单号或编码字段 | 如果该表存在编码/单号字段，必须唯一 |
| `idx_iam_permission_status_time` | 状态字段、`updated_at` | 支撑列表筛选和待办查询 |
| `idx_iam_permission_source` | 来源单号、外部单号或引用编码 | 支撑跨系统追踪 |

### 4.9 `iam_data_scope`

业务对象：数据权限范围

| 字段 | 类型 | 是否必填 | 约束/枚举 | 领域归类 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `data_scope_id` | bigint | 是 | 主键，雪花ID或数据库自增 | 技术主键 | 数据权限范围主键 |
| `subject_type` | varchar(32) | 是 | `SUBJECT_TYPE` | 状态/分类 | 用户、角色 |
| `scope_type` | varchar(32) | 是 | `DATA_SCOPE_TYPE` | 状态/分类 | 全部、本组织、本组织及下级、自定义、本人 |
| `resource_type` | varchar(32) | 是 | `DATA_RESOURCE_TYPE` | 状态/分类 | 组织、仓库、供应商、客户、货主 |
| `resource_ids` | text | 否 | JSON 数组 | 业务属性 | 自定义资源 ID |
| `status` | varchar(32) | 是 | `COMMON_STATUS` | 状态/分类 | 启用、停用 |
| `created_by` | bigint | 是 | 写入时填充 | 审计 | 创建人 |
| `created_at` | datetime | 是 | 默认当前时间 | 审计 | 创建时间 |
| `updated_by` | bigint | 否 | 更新时填充 | 审计 | 更新人 |
| `updated_at` | datetime | 是 | 默认当前时间并自动更新 | 审计 | 更新时间 |
| `version` | int | 是 | 默认 0，乐观锁 | 并发控制 | 数据版本 |
| `deleted` | tinyint | 是 | 0 否，1 是 | 数据治理 | 逻辑删除标记 |

建议索引：

| 索引 | 字段 | 说明 |
| --- | --- | --- |
| `uk_iam_data_scope_biz_no` | 业务单号或编码字段 | 如果该表存在编码/单号字段，必须唯一 |
| `idx_iam_data_scope_status_time` | 状态字段、`updated_at` | 支撑列表筛选和待办查询 |
| `idx_iam_data_scope_source` | 来源单号、外部单号或引用编码 | 支撑跨系统追踪 |

### 4.10 `iam_user_token`

业务对象：用户会话/Token

| 字段 | 类型 | 是否必填 | 约束/枚举 | 领域归类 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `user_token_id` | bigint | 是 | 主键，雪花ID或数据库自增 | 技术主键 | 用户会话/Token主键 |
| `access_token_jti` | varchar(128) | 是 | 唯一 | 业务属性 | access token 唯一 ID |
| `refresh_token_jti` | varchar(128) | 否 | 唯一 | 业务属性 | refresh token 唯一 ID |
| `token_status` | varchar(32) | 是 | `TOKEN_STATUS` | 状态/分类 | 有效、已刷新、已撤销、已过期 |
| `login_ip` | varchar(64) | 否 |   | 业务属性 | 登录 IP |
| `user_agent` | varchar(512) | 否 |   | 业务属性 | 浏览器/客户端信息 |
| `login_at` | datetime | 是 |   | 业务属性 | 登录时间 |
| `expires_at` | datetime | 是 |   | 业务属性 | access token 过期时间 |
| `refresh_expires_at` | datetime | 否 |   | 业务属性 | refresh token 过期时间 |
| `logout_at` | datetime | 否 |   | 业务属性 | 登出时间 |
| `created_by` | bigint | 是 | 写入时填充 | 审计 | 创建人 |
| `created_at` | datetime | 是 | 默认当前时间 | 审计 | 创建时间 |
| `updated_by` | bigint | 否 | 更新时填充 | 审计 | 更新人 |
| `updated_at` | datetime | 是 | 默认当前时间并自动更新 | 审计 | 更新时间 |
| `version` | int | 是 | 默认 0，乐观锁 | 并发控制 | 数据版本 |
| `deleted` | tinyint | 是 | 0 否，1 是 | 数据治理 | 逻辑删除标记 |

建议索引：

| 索引 | 字段 | 说明 |
| --- | --- | --- |
| `uk_iam_user_token_biz_no` | 业务单号或编码字段 | 如果该表存在编码/单号字段，必须唯一 |
| `idx_iam_user_token_status_time` | 状态字段、`updated_at` | 支撑列表筛选和待办查询 |
| `idx_iam_user_token_source` | 来源单号、外部单号或引用编码 | 支撑跨系统追踪 |

### 4.11 `iam_login_log`

业务对象：登录日志

| 字段 | 类型 | 是否必填 | 约束/枚举 | 领域归类 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `business_object_id` | bigint | 是 | 主键，雪花ID或数据库自增 | 技术主键 | 登录日志主键 |
| `username` | varchar(64) | 是 |   | 业务属性 | 登录账号 |
| `login_result` | varchar(32) | 是 | `LOGIN_RESULT` | 业务属性 | 成功、失败 |
| `failure_reason` | varchar(128) | 否 | `LOGIN_FAILURE_REASON` | 业务属性 | 密码错误、用户停用、Token 过期等 |
| `login_ip` | varchar(64) | 否 |   | 业务属性 | IP |
| `user_agent` | varchar(512) | 否 |   | 业务属性 | 客户端信息 |
| `login_at` | datetime | 是 |   | 业务属性 | 登录时间 |
| `created_by` | bigint | 是 | 写入时填充 | 审计 | 创建人 |
| `created_at` | datetime | 是 | 默认当前时间 | 审计 | 创建时间 |
| `updated_by` | bigint | 否 | 更新时填充 | 审计 | 更新人 |
| `updated_at` | datetime | 是 | 默认当前时间并自动更新 | 审计 | 更新时间 |
| `version` | int | 是 | 默认 0，乐观锁 | 并发控制 | 数据版本 |
| `deleted` | tinyint | 是 | 0 否，1 是 | 数据治理 | 逻辑删除标记 |

建议索引：

| 索引 | 字段 | 说明 |
| --- | --- | --- |
| `uk_iam_login_log_biz_no` | 业务单号或编码字段 | 如果该表存在编码/单号字段，必须唯一 |
| `idx_iam_login_log_status_time` | 状态字段、`updated_at` | 支撑列表筛选和待办查询 |
| `idx_iam_login_log_source` | 来源单号、外部单号或引用编码 | 支撑跨系统追踪 |

### 4.12 `iam_operation_log`

业务对象：操作日志

| 字段 | 类型 | 是否必填 | 约束/枚举 | 领域归类 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `operation_log_id` | bigint | 是 | 主键，雪花ID或数据库自增 | 技术主键 | 操作日志主键 |
| `operator_name` | varchar(128) | 是 | 快照 | 业务属性 | 操作人姓名 |
| `app_code` | varchar(64) | 是 |   | 标识/引用 | 所属应用 |
| `module_code` | varchar(64) | 是 |   | 标识/引用 | 模块编码 |
| `operation_type` | varchar(32) | 是 | `OPERATION_TYPE` | 状态/分类 | CREATE、UPDATE、DELETE、APPROVE、LOGIN 等 |
| `permission_code` | varchar(128) | 否 |   | 标识/引用 | 对应权限点 |
| `biz_type` | varchar(64) | 否 |   | 状态/分类 | 业务对象类型，如 USER、ROLE |
| `biz_no` | varchar(128) | 否 |   | 标识/引用 | 业务单号 |
| `operation_desc` | varchar(512) | 否 |   | 业务属性 | 简要说明 |
| `before_snapshot` | text | 否 | JSON | 业务属性 | 修改前摘要 |
| `after_snapshot` | text | 否 | JSON | 业务属性 | 修改后摘要 |
| `operation_result` | varchar(32) | 是 | `OPERATION_RESULT` | 业务属性 | 成功、失败 |
| `failure_reason` | varchar(512) | 否 |   | 业务属性 | 失败原因 |
| `ip` | varchar(64) | 否 |   | 业务属性 | IP |
| `user_agent` | varchar(512) | 否 |   | 业务属性 | 客户端 |
| `created_by` | bigint | 是 | 写入时填充 | 审计 | 创建人 |
| `created_at` | datetime | 是 | 默认当前时间 | 审计 | 创建时间 |
| `updated_by` | bigint | 否 | 更新时填充 | 审计 | 更新人 |
| `updated_at` | datetime | 是 | 默认当前时间并自动更新 | 审计 | 更新时间 |
| `version` | int | 是 | 默认 0，乐观锁 | 并发控制 | 数据版本 |
| `deleted` | tinyint | 是 | 0 否，1 是 | 数据治理 | 逻辑删除标记 |

建议索引：

| 索引                                  | 字段                | 说明                 |
| ----------------------------------- | ----------------- | ------------------ |
| `uk_iam_operation_log_biz_no`       | 业务单号或编码字段         | 如果该表存在编码/单号字段，必须唯一 |
| `idx_iam_operation_log_status_time` | 状态字段、`updated_at` | 支撑列表筛选和待办查询        |
| `idx_iam_operation_log_source`      | 来源单号、外部单号或引用编码    | 支撑跨系统追踪            |

## 5. 枚举值

枚举字段在 DDL 中统一使用 `smallint` 存储，枚举项从 `1` 开始递增；页面展示名、排序和启停状态通过枚举配置表维护。

| 枚举类型 | 数值 | 枚举项 | 说明 |
| --- | --- | --- | --- |
| `ACTION_TYPE` | `1` | 待定义1 | ACTION_TYPE 的第 1 个枚举项 |
| `ACTION_TYPE` | `2` | 待定义2 | ACTION_TYPE 的第 2 个枚举项 |
| `ACTION_TYPE` | `3` | 待定义3 | ACTION_TYPE 的第 3 个枚举项 |
| `ALL` | `1` | 角色适用应用 | ALL 的第 1 个枚举项 |
| `APPROVAL_STATUS` | `1` | 草稿 | APPROVAL_STATUS 的第 1 个枚举项 |
| `APPROVAL_STATUS` | `2` | 待审批 | APPROVAL_STATUS 的第 2 个枚举项 |
| `APPROVAL_STATUS` | `3` | 已批准 | APPROVAL_STATUS 的第 3 个枚举项 |
| `APPROVAL_STATUS` | `4` | 已驳回 | APPROVAL_STATUS 的第 4 个枚举项 |
| `APP_TYPE` | `1` | 移动端 | APP_TYPE 的第 1 个枚举项 |
| `APP_TYPE` | `2` | 后台服务 | APP_TYPE 的第 2 个枚举项 |
| `COMMON_STATUS` | `1` | 启用 | COMMON_STATUS 的第 1 个枚举项 |
| `COMMON_STATUS` | `2` | 停用 | COMMON_STATUS 的第 2 个枚举项 |
| `CONSUME_STATUS` | `1` | 待消费 | CONSUME_STATUS 的第 1 个枚举项 |
| `CONSUME_STATUS` | `2` | 处理中 | CONSUME_STATUS 的第 2 个枚举项 |
| `CONSUME_STATUS` | `3` | 消费成功 | CONSUME_STATUS 的第 3 个枚举项 |
| `CONSUME_STATUS` | `4` | 消费失败 | CONSUME_STATUS 的第 4 个枚举项 |
| `CONSUME_STATUS` | `5` | 已忽略 | CONSUME_STATUS 的第 5 个枚举项 |
| `CURRENCY` | `1` | 人民币 | CURRENCY 的第 1 个枚举项 |
| `CURRENCY` | `2` | 美元 | CURRENCY 的第 2 个枚举项 |
| `CURRENCY` | `3` | 欧元 | CURRENCY 的第 3 个枚举项 |
| `CURRENCY` | `4` | 港币 | CURRENCY 的第 4 个枚举项 |
| `CURRENCY` | `5` | 日元 | CURRENCY 的第 5 个枚举项 |
| `DATA_RESOURCE_TYPE` | `1` | 组织 | DATA_RESOURCE_TYPE 的第 1 个枚举项 |
| `DATA_RESOURCE_TYPE` | `2` | 仓库 | DATA_RESOURCE_TYPE 的第 2 个枚举项 |
| `DATA_RESOURCE_TYPE` | `3` | 供应商 | DATA_RESOURCE_TYPE 的第 3 个枚举项 |
| `DATA_RESOURCE_TYPE` | `4` | 客户 | DATA_RESOURCE_TYPE 的第 4 个枚举项 |
| `DATA_RESOURCE_TYPE` | `5` | 货主 | DATA_RESOURCE_TYPE 的第 5 个枚举项 |
| `DATA_SCOPE_TYPE` | `1` | 默认数据范围 | DATA_SCOPE_TYPE 的第 1 个枚举项 |
| `DATA_SCOPE_TYPE` | `2` | 全部 | DATA_SCOPE_TYPE 的第 2 个枚举项 |
| `DATA_SCOPE_TYPE` | `3` | 本组织 | DATA_SCOPE_TYPE 的第 3 个枚举项 |
| `DATA_SCOPE_TYPE` | `4` | 本组织及下级 | DATA_SCOPE_TYPE 的第 4 个枚举项 |
| `DATA_SCOPE_TYPE` | `5` | 自定义 | DATA_SCOPE_TYPE 的第 5 个枚举项 |
| `DATA_SCOPE_TYPE` | `6` | 本人 | DATA_SCOPE_TYPE 的第 6 个枚举项 |
| `EVENT_STATUS` | `1` | 待发布 | EVENT_STATUS 的第 1 个枚举项 |
| `EVENT_STATUS` | `2` | 发布中 | EVENT_STATUS 的第 2 个枚举项 |
| `EVENT_STATUS` | `3` | 已发布 | EVENT_STATUS 的第 3 个枚举项 |
| `EVENT_STATUS` | `4` | 发布失败 | EVENT_STATUS 的第 4 个枚举项 |
| `EVENT_STATUS` | `5` | 已取消 | EVENT_STATUS 的第 5 个枚举项 |
| `GRANT_STATUS` | `1` | 已授权 | GRANT_STATUS 的第 1 个枚举项 |
| `GRANT_STATUS` | `2` | 已取消 | GRANT_STATUS 的第 2 个枚举项 |
| `GRANT_TYPE` | `1` | 授权码 | GRANT_TYPE 的第 1 个枚举项 |
| `GRANT_TYPE` | `2` | 密码 | GRANT_TYPE 的第 2 个枚举项 |
| `GRANT_TYPE` | `3` | 刷新 | GRANT_TYPE 的第 3 个枚举项 |
| `GRANT_TYPE_USER_ROLE` | `1` | 手工 | GRANT_TYPE_USER_ROLE 的第 1 个枚举项 |
| `GRANT_TYPE_USER_ROLE` | `2` | 组织继承 | GRANT_TYPE_USER_ROLE 的第 2 个枚举项 |
| `GRANT_TYPE_USER_ROLE` | `3` | 岗位继承 | GRANT_TYPE_USER_ROLE 的第 3 个枚举项 |
| `GRANT_TYPE_USER_ROLE` | `4` | 系统默认 | GRANT_TYPE_USER_ROLE 的第 4 个枚举项 |
| `HTTP_METHOD` | `1` | 待定义1 | HTTP_METHOD 的第 1 个枚举项 |
| `HTTP_METHOD` | `2` | 待定义2 | HTTP_METHOD 的第 2 个枚举项 |
| `HTTP_METHOD` | `3` | 待定义3 | HTTP_METHOD 的第 3 个枚举项 |
| `LOGIN_FAILURE_REASON` | `1` | 密码错误 | LOGIN_FAILURE_REASON 的第 1 个枚举项 |
| `LOGIN_FAILURE_REASON` | `2` | 用户停用 | LOGIN_FAILURE_REASON 的第 2 个枚举项 |
| `LOGIN_FAILURE_REASON` | `3` | 过期 | LOGIN_FAILURE_REASON 的第 3 个枚举项 |
| `LOGIN_RESULT` | `1` | 成功 | LOGIN_RESULT 的第 1 个枚举项 |
| `LOGIN_RESULT` | `2` | 失败 | LOGIN_RESULT 的第 2 个枚举项 |
| `MENU_TYPE` | `1` | 目录 | MENU_TYPE 的第 1 个枚举项 |
| `MENU_TYPE` | `2` | 页面 | MENU_TYPE 的第 2 个枚举项 |
| `MENU_TYPE` | `3` | 外链 | MENU_TYPE 的第 3 个枚举项 |
| `OPERATION_RESULT` | `1` | 成功 | OPERATION_RESULT 的第 1 个枚举项 |
| `OPERATION_RESULT` | `2` | 失败 | OPERATION_RESULT 的第 2 个枚举项 |
| `OPERATION_TYPE` | `1` | 待定义1 | OPERATION_TYPE 的第 1 个枚举项 |
| `OPERATION_TYPE` | `2` | 待定义2 | OPERATION_TYPE 的第 2 个枚举项 |
| `OPERATION_TYPE` | `3` | 待定义3 | OPERATION_TYPE 的第 3 个枚举项 |
| `PERMISSION_TYPE` | `1` | 菜单 | PERMISSION_TYPE 的第 1 个枚举项 |
| `PERMISSION_TYPE` | `2` | 按钮 | PERMISSION_TYPE 的第 2 个枚举项 |
| `PERMISSION_TYPE` | `3` | 字段 | PERMISSION_TYPE 的第 3 个枚举项 |
| `RESOURCE_TYPE` | `1` | 页面 | RESOURCE_TYPE 的第 1 个枚举项 |
| `RESOURCE_TYPE` | `2` | 按钮 | RESOURCE_TYPE 的第 2 个枚举项 |
| `RESOURCE_TYPE` | `3` | 接口 | RESOURCE_TYPE 的第 3 个枚举项 |
| `RESOURCE_TYPE` | `4` | 字段 | RESOURCE_TYPE 的第 4 个枚举项 |
| `ROLE_TYPE` | `1` | 系统角色 | ROLE_TYPE 的第 1 个枚举项 |
| `ROLE_TYPE` | `2` | 业务角色 | ROLE_TYPE 的第 2 个枚举项 |
| `ROLE_TYPE` | `3` | 外部角色 | ROLE_TYPE 的第 3 个枚举项 |
| `SUBJECT_TYPE` | `1` | 用户 | SUBJECT_TYPE 的第 1 个枚举项 |
| `SUBJECT_TYPE` | `2` | 角色 | SUBJECT_TYPE 的第 2 个枚举项 |
| `TOKEN_STATUS` | `1` | 有效 | TOKEN_STATUS 的第 1 个枚举项 |
| `TOKEN_STATUS` | `2` | 已刷新 | TOKEN_STATUS 的第 2 个枚举项 |
| `TOKEN_STATUS` | `3` | 已撤销 | TOKEN_STATUS 的第 3 个枚举项 |
| `TOKEN_STATUS` | `4` | 已过期 | TOKEN_STATUS 的第 4 个枚举项 |
| `USER_STATUS` | `1` | 待激活 | USER_STATUS 的第 1 个枚举项 |
| `USER_STATUS` | `2` | 已启用 | USER_STATUS 的第 2 个枚举项 |
| `USER_STATUS` | `3` | 已锁定 | USER_STATUS 的第 3 个枚举项 |
| `USER_STATUS` | `4` | 已停用 | USER_STATUS 的第 4 个枚举项 |
| `USER_STATUS` | `5` | 已注销 | USER_STATUS 的第 5 个枚举项 |
| `USER_TYPE` | `1` | 内部员工 | USER_TYPE 的第 1 个枚举项 |
| `USER_TYPE` | `2` | 供应商用户 | USER_TYPE 的第 2 个枚举项 |
| `USER_TYPE` | `3` | 客户用户 | USER_TYPE 的第 3 个枚举项 |
| `USER_TYPE` | `4` | 系统账号 | USER_TYPE 的第 4 个枚举项 |

## 6. 事件、事件表与审计表字段

事件采用本地消息表模式：应用服务在同一事务内保存业务表和领域事件表，异步发布后更新事件状态；消费外部事件时先写消费日志，使用幂等键避免重复处理。

### 6.1 本系统领域事件

| 事件 | 来源聚合 | 触发动作 | 主要载荷 | 订阅/用途 |
| --- | --- | --- | --- | --- |
| 应用已创建 | 02-应用聚合CQRS设计 | 创建应用 | 聚合ID、应用、用户、角色、权限点、数据范围、权限版本、状态`草稿`、操作者、原因、幂等键 | 业务子系统、权限缓存、审计日志、安全风控、待办中心、读模型 |
| 应用已启用 | 02-应用聚合CQRS设计 | 启用应用 | 聚合ID、应用、用户、角色、权限点、数据范围、权限版本、状态`已启用`、操作者、原因、幂等键 | 业务子系统、权限缓存、审计日志、安全风控、待办中心、读模型 |
| SSO客户端已配置 | 02-应用聚合CQRS设计 | 配置SSO客户端 | 聚合ID、应用、用户、角色、权限点、数据范围、权限版本、状态`已启用`、操作者、原因、幂等键 | 业务子系统、权限缓存、审计日志、安全风控、待办中心、读模型 |
| 应用已停用 | 02-应用聚合CQRS设计 | 停用应用 | 聚合ID、应用、用户、角色、权限点、数据范围、权限版本、状态`已停用`、操作者、原因、幂等键 | 业务子系统、权限缓存、审计日志、安全风控、待办中心、读模型 |
| 子系统接入申请已提交 | 02-应用聚合CQRS设计 | 应用接入应用服务 | 创建应用接入待办 | 来源上下文+事件编号+业务主键 |
| 安全风险已识别 | 02-应用聚合CQRS设计 | 应用接入应用服务 | 冻结高风险客户端或要求重置密钥 | 来源上下文+事件编号+业务主键 |
| 主数据已变更 | 02-应用聚合CQRS设计 | 权限对象同步服务 | 更新组织、仓库、货主、供应商、客户等数据范围对象快照 | 主数据上下文+事件编号+对象编码 |
| 用户已创建 | 03-用户聚合CQRS设计 | 创建用户 | 聚合ID、应用、用户、角色、权限点、数据范围、权限版本、状态`待激活`、操作者、原因、幂等键 | 业务子系统、权限缓存、审计日志、安全风控、待办中心、读模型 |
| 用户已激活 | 03-用户聚合CQRS设计 | 激活用户 | 聚合ID、应用、用户、角色、权限点、数据范围、权限版本、状态`已启用`、操作者、原因、幂等键 | 业务子系统、权限缓存、审计日志、安全风控、待办中心、读模型 |
| 用户已锁定 | 03-用户聚合CQRS设计 | 锁定用户 | 聚合ID、应用、用户、角色、权限点、数据范围、权限版本、状态`已锁定`、操作者、原因、幂等键 | 业务子系统、权限缓存、审计日志、安全风控、待办中心、读模型 |
| 用户已停用 | 03-用户聚合CQRS设计 | 停用用户 | 聚合ID、应用、用户、角色、权限点、数据范围、权限版本、状态`已停用`、操作者、原因、幂等键 | 业务子系统、权限缓存、审计日志、安全风控、待办中心、读模型 |
| 用户角色已分配 | 03-用户聚合CQRS设计 | 分配角色 | 聚合ID、应用、用户、角色、权限点、数据范围、权限版本、状态`已启用`、操作者、原因、幂等键 | 业务子系统、权限缓存、审计日志、安全风控、待办中心、读模型 |
| 用户角色已取消 | 03-用户聚合CQRS设计 | 取消角色 | 聚合ID、应用、用户、角色、权限点、数据范围、权限版本、状态`已启用`、操作者、原因、幂等键 | 业务子系统、权限缓存、审计日志、安全风控、待办中心、读模型 |
| 员工已入职 | 03-用户聚合CQRS设计 | 用户应用服务 | 创建内部用户草稿或待激活账号 | 来源上下文+事件编号+业务主键 |
| 员工已离职 | 03-用户聚合CQRS设计 | 用户应用服务 | 停用用户并触发会话失效 | 来源上下文+事件编号+业务主键 |
| 角色已创建 | 04-角色聚合CQRS设计 | 创建角色 | 聚合ID、应用、用户、角色、权限点、数据范围、权限版本、状态`草稿`、操作者、原因、幂等键 | 业务子系统、权限缓存、审计日志、安全风控、待办中心、读模型 |
| 角色已启用 | 04-角色聚合CQRS设计 | 启用角色 | 聚合ID、应用、用户、角色、权限点、数据范围、权限版本、状态`已启用`、操作者、原因、幂等键 | 业务子系统、权限缓存、审计日志、安全风控、待办中心、读模型 |
| 角色权限已分配 | 04-角色聚合CQRS设计 | 分配权限 | 聚合ID、应用、用户、角色、权限点、数据范围、权限版本、状态`已启用`、操作者、原因、幂等键 | 业务子系统、权限缓存、审计日志、安全风控、待办中心、读模型 |
| 角色权限已取消 | 04-角色聚合CQRS设计 | 取消权限 | 聚合ID、应用、用户、角色、权限点、数据范围、权限版本、状态`已启用`、操作者、原因、幂等键 | 业务子系统、权限缓存、审计日志、安全风控、待办中心、读模型 |
| 角色已停用 | 04-角色聚合CQRS设计 | 停用角色 | 聚合ID、应用、用户、角色、权限点、数据范围、权限版本、状态`已停用`、操作者、原因、幂等键 | 业务子系统、权限缓存、审计日志、安全风控、待办中心、读模型 |
| 权限点已注册 | 04-角色聚合CQRS设计 | 角色应用服务 | 允许角色引用新增权限点 | 来源上下文+事件编号+业务主键 |
| 权限点已变更 | 05-权限点聚合CQRS设计 | 变更权限点 | 聚合ID、应用、用户、角色、权限点、数据范围、权限版本、状态`已启用`、操作者、原因、幂等键 | 业务子系统、权限缓存、审计日志、安全风控、待办中心、读模型 |
| 权限点API已绑定 | 05-权限点聚合CQRS设计 | 绑定API资源 | 聚合ID、应用、用户、角色、权限点、数据范围、权限版本、状态`已启用`、操作者、原因、幂等键 | 业务子系统、权限缓存、审计日志、安全风控、待办中心、读模型 |
| 权限点已停用 | 05-权限点聚合CQRS设计 | 停用权限点 | 聚合ID、应用、用户、角色、权限点、数据范围、权限版本、状态`已停用`、操作者、原因、幂等键 | 业务子系统、权限缓存、审计日志、安全风控、待办中心、读模型 |
| API资源已扫描 | 05-权限点聚合CQRS设计 | 权限点应用服务 | 生成或更新权限点建议 | 来源上下文+事件编号+业务主键 |
| 数据权限已创建 | 06-数据权限聚合CQRS设计 | 创建数据权限 | 聚合ID、应用、用户、角色、权限点、数据范围、权限版本、状态`已启用`、操作者、原因、幂等键 | 业务子系统、权限缓存、审计日志、安全风控、待办中心、读模型 |
| 数据范围已分配 | 06-数据权限聚合CQRS设计 | 分配数据范围 | 聚合ID、应用、用户、角色、权限点、数据范围、权限版本、状态`已启用`、操作者、原因、幂等键 | 业务子系统、权限缓存、审计日志、安全风控、待办中心、读模型 |
| 数据范围已变更 | 06-数据权限聚合CQRS设计 | 变更数据范围 | 聚合ID、应用、用户、角色、权限点、数据范围、权限版本、状态`已启用`、操作者、原因、幂等键 | 业务子系统、权限缓存、审计日志、安全风控、待办中心、读模型 |
| 数据范围已取消 | 06-数据权限聚合CQRS设计 | 取消数据范围 | 聚合ID、应用、用户、角色、权限点、数据范围、权限版本、状态`已启用`、操作者、原因、幂等键 | 业务子系统、权限缓存、审计日志、安全风控、待办中心、读模型 |
| 数据权限已停用 | 06-数据权限聚合CQRS设计 | 停用数据权限 | 聚合ID、应用、用户、角色、权限点、数据范围、权限版本、状态`已停用`、操作者、原因、幂等键 | 业务子系统、权限缓存、审计日志、安全风控、待办中心、读模型 |
| 用户已登录 | 07-会话Token聚合CQRS设计 | 签发Token | 聚合ID、应用、用户、角色、权限点、数据范围、权限版本、状态`有效`、操作者、原因、幂等键 | 业务子系统、权限缓存、审计日志、安全风控、待办中心、读模型 |
| Token已刷新 | 07-会话Token聚合CQRS设计 | 刷新Token | 聚合ID、应用、用户、角色、权限点、数据范围、权限版本、状态`有效`、操作者、原因、幂等键 | 业务子系统、权限缓存、审计日志、安全风控、待办中心、读模型 |
| Token校验已通过 | 07-会话Token聚合CQRS设计 | 校验Token | 聚合ID、应用、用户、角色、权限点、数据范围、权限版本、状态`有效`、操作者、原因、幂等键 | 业务子系统、权限缓存、审计日志、安全风控、待办中心、读模型 |
| Token已失效 | 07-会话Token聚合CQRS设计 | 失效Token | 聚合ID、应用、用户、角色、权限点、数据范围、权限版本、状态`已失效`、操作者、原因、幂等键 | 业务子系统、权限缓存、审计日志、安全风控、待办中心、读模型 |
| 会话已踢出 | 07-会话Token聚合CQRS设计 | 踢出会话 | 聚合ID、应用、用户、角色、权限点、数据范围、权限版本、状态`已踢出`、操作者、原因、幂等键 | 业务子系统、权限缓存、审计日志、安全风控、待办中心、读模型 |
| 角色权限已变更 | 07-会话Token聚合CQRS设计 | 登录应用服务 | 标记相关会话权限版本过期 | 来源上下文+事件编号+业务主键 |
| 审批已开始 | 08-审批实例聚合CQRS设计 | 发起审批 | 聚合ID、应用、用户、角色、权限点、数据范围、权限版本、状态`审批中`、操作者、原因、幂等键 | 业务子系统、权限缓存、审计日志、安全风控、待办中心、读模型 |
| 审批节点已通过 | 08-审批实例聚合CQRS设计 | 审批通过 | 聚合ID、应用、用户、角色、权限点、数据范围、权限版本、状态`审批中`、操作者、原因、幂等键 | 业务子系统、权限缓存、审计日志、安全风控、待办中心、读模型 |
| 审批已驳回 | 08-审批实例聚合CQRS设计 | 审批驳回 | 聚合ID、应用、用户、角色、权限点、数据范围、权限版本、状态`已驳回`、操作者、原因、幂等键 | 业务子系统、权限缓存、审计日志、安全风控、待办中心、读模型 |
| 审批已撤回 | 08-审批实例聚合CQRS设计 | 撤回审批 | 聚合ID、应用、用户、角色、权限点、数据范围、权限版本、状态`已撤回`、操作者、原因、幂等键 | 业务子系统、权限缓存、审计日志、安全风控、待办中心、读模型 |
| 审批已取消 | 08-审批实例聚合CQRS设计 | 取消审批 | 聚合ID、应用、用户、角色、权限点、数据范围、权限版本、状态`已取消`、操作者、原因、幂等键 | 业务子系统、权限缓存、审计日志、安全风控、待办中心、读模型 |
| 审批已完成 | 08-审批实例聚合CQRS设计 | 完成审批 | 聚合ID、应用、用户、角色、权限点、数据范围、权限版本、状态`已通过`、操作者、原因、幂等键 | 业务子系统、权限缓存、审计日志、安全风控、待办中心、读模型 |
| 审批请求已提交 | 08-审批实例聚合CQRS设计 | 审批应用服务 | 创建审批实例和首个待办任务 | 来源上下文+事件编号+业务主键 |
| 审批请求已取消 | 08-审批实例聚合CQRS设计 | 审批应用服务 | 取消未完成审批实例 | 来源上下文+事件编号+业务主键 |
| 审计日志已创建 | 09-操作日志聚合CQRS设计 | 写入操作日志 | 聚合ID、应用、用户、角色、权限点、数据范围、权限版本、状态`已记录`、操作者、原因、幂等键 | 业务子系统、权限缓存、审计日志、安全风控、待办中心、读模型 |
| 敏感日志已标记 | 09-操作日志聚合CQRS设计 | 标记敏感日志 | 聚合ID、应用、用户、角色、权限点、数据范围、权限版本、状态`已记录`、操作者、原因、幂等键 | 业务子系统、权限缓存、审计日志、安全风控、待办中心、读模型 |
| 审计日志已归档 | 09-操作日志聚合CQRS设计 | 归档操作日志 | 聚合ID、应用、用户、角色、权限点、数据范围、权限版本、状态`已归档`、操作者、原因、幂等键 | 业务子系统、权限缓存、审计日志、安全风控、待办中心、读模型 |
| 审计日志已查询 | 09-操作日志聚合CQRS设计 | 查询审计日志 | 聚合ID、应用、用户、角色、权限点、数据范围、权限版本、状态`已记录`、操作者、原因、幂等键 | 业务子系统、权限缓存、审计日志、安全风控、待办中心、读模型 |
| 敏感操作已发生 | 09-操作日志聚合CQRS设计 | 审计应用服务 | 写入敏感操作审计日志 | 来源上下文+事件编号+业务主键 |
| 权限校验已拒绝 | 09-操作日志聚合CQRS设计 | 审计应用服务 | 写入拒绝访问日志 | 来源上下文+事件编号+业务主键 |
| 安全策略已创建 | 10-安全策略聚合CQRS设计 | 创建安全策略 | 聚合ID、应用、用户、角色、权限点、数据范围、权限版本、状态`草稿`、操作者、原因、幂等键 | 业务子系统、权限缓存、审计日志、安全风控、待办中心、读模型 |
| 安全策略已发布 | 10-安全策略聚合CQRS设计 | 发布安全策略 | 聚合ID、应用、用户、角色、权限点、数据范围、权限版本、状态`已发布`、操作者、原因、幂等键 | 业务子系统、权限缓存、审计日志、安全风控、待办中心、读模型 |
| 安全风险已处置 | 10-安全策略聚合CQRS设计 | 执行风险处置 | 聚合ID、应用、用户、角色、权限点、数据范围、权限版本、状态`已处置`、操作者、原因、幂等键 | 业务子系统、权限缓存、审计日志、安全风控、待办中心、读模型 |
| 安全策略已停用 | 10-安全策略聚合CQRS设计 | 停用安全策略 | 聚合ID、应用、用户、角色、权限点、数据范围、权限版本、状态`已停用`、操作者、原因、幂等键 | 业务子系统、权限缓存、审计日志、安全风控、待办中心、读模型 |
| 登录失败已记录 | 10-安全策略聚合CQRS设计 | 安全策略应用服务 | 累计失败次数并判断是否锁定 | 来源上下文+事件编号+业务主键 |

### 6.2 `iam_domain_event`

| 字段 | 类型 | 是否必填 | 说明 |
| --- | --- | --- | --- |
| `event_id` | bigint | 是 | 事件主键 |
| `event_code` | varchar(128) | 是 | 全局唯一事件编码 |
| `event_name` | varchar(128) | 是 | 中文事件名 |
| `event_type` | varchar(128) | 是 | 稳定事件类型编码 |
| `aggregate_type` | varchar(128) | 是 | 聚合类型 |
| `aggregate_id` | bigint | 是 | 聚合 ID |
| `aggregate_no` | varchar(128) | 否 | 业务单号或编码 |
| `source_system` | varchar(64) | 是 | 来源系统 |
| `payload_json` | json | 是 | 事件载荷 |
| `event_status` | smallint | 是 | `EVENT_STATUS`：1 待发布，2 发布中，3 已发布，4 发布失败，5 已取消 |
| `retry_count` | int | 是 | 重试次数 |
| `fail_reason` | varchar(1024) | 否 | 失败原因 |
| `occurred_at` | datetime | 是 | 业务发生时间 |
| `published_at` | datetime | 否 | 发布时间 |
| `created_at` | datetime | 是 | 创建时间 |
| `updated_at` | datetime | 是 | 更新时间 |

### 6.3 `iam_event_consume_log`

| 字段 | 类型 | 是否必填 | 说明 |
| --- | --- | --- | --- |
| `consume_log_id` | bigint | 是 | 消费日志主键 |
| `event_code` | varchar(128) | 是 | 来源事件编码 |
| `source_system` | varchar(64) | 是 | 来源系统 |
| `consumer_name` | varchar(128) | 是 | 消费者名称 |
| `idempotent_key` | varchar(256) | 是 | 幂等键 |
| `consume_status` | smallint | 是 | `CONSUME_STATUS`：1 待消费，2 处理中，3 消费成功，4 消费失败，5 已忽略 |
| `retry_count` | int | 是 | 重试次数 |
| `fail_reason` | varchar(1024) | 否 | 失败原因 |
| `consumed_at` | datetime | 否 | 消费完成时间 |
| `created_at` | datetime | 是 | 创建时间 |
| `updated_at` | datetime | 是 | 更新时间 |

### 6.4 `iam_operation_audit_log`

| 字段 | 类型 | 是否必填 | 说明 |
| --- | --- | --- | --- |
| `operation_log_id` | bigint | 是 | 操作日志主键 |
| `operator_id` | bigint | 是 | 操作人 ID |
| `operator_name` | varchar(128) | 否 | 操作人名称 |
| `operation_type` | varchar(64) | 是 | 操作类型 |
| `target_type` | varchar(128) | 是 | 操作对象类型 |
| `target_id` | bigint | 否 | 操作对象 ID |
| `target_no` | varchar(128) | 否 | 操作对象单号或编码 |
| `before_snapshot` | json | 否 | 操作前快照 |
| `after_snapshot` | json | 否 | 操作后快照 |
| `result` | smallint | 是 | `OPERATION_RESULT`：1 成功，2 失败 |
| `fail_reason` | varchar(1024) | 否 | 失败原因 |
| `request_id` | varchar(128) | 否 | 请求 ID |
| `operation_at` | datetime | 是 | 操作时间 |
| `created_at` | datetime | 是 | 创建时间 |

## 7. DDL 文件

完整 MySQL 8.0 DDL 已生成到：

- [`ddl/08-权限系统.sql`](ddl/08-权限系统.sql)

DDL 包含业务表、枚举类型表、枚举项初始化数据、领域事件发布表、事件消费日志表和操作日志表。

当前结论：数据库表围绕聚合当前状态、内部实体明细、事件日志和读模型支撑设计；枚举字段使用数值编码落库，通过枚举配置表维护显示名和启停状态。

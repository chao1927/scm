# 08-主数据系统数据库设计

> 本文根据 `docs/03-核心业务模型` 的聚合、不变量、命令事件，以及 `docs/04-子系统功能设计` 的页面、状态、枚举和操作场景整理。本文是数据库表字段设计入口；领域模型和功能设计文档不再保留字段迁移链接。

## 1. 设计口径

| 项目 | 口径 |
| --- | --- |
| 所属系统 | 主数据系统 |
| 表名前缀 | `mdm_` |
| 主键策略 | 业务表使用 bigint 主键，建议雪花 ID；业务单号另设唯一索引 |
| 时间类型 | 业务时间使用 `datetime`，日期使用 `date`，金额使用 `decimal(18,2)` 或更高精度 |
| 数量类型 | 库存、采购、履约数量统一使用 `decimal(18,4)` |
| 状态字段 | 状态机字段只能由聚合命令推进，页面只配置展示名、排序和颜色 |
| 枚举字段 | 业务可配置枚举进入枚举配置；核心状态枚举可以配置标签但不能随意增删 |

## 2. 通用字段

| 字段 | 类型 | 是否必填 | 约束 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | bigint | 是 | 主键，自增或雪花ID | 技术主键 |
| `tenant_id` | bigint | 否 | 多租户时启用 | 租户ID |
| `org_id` | bigint | 否 | 按组织隔离时启用 | 组织ID |
| `created_by` | bigint | 是 | 写入时填充 | 创建人 |
| `created_at` | datetime | 是 | 默认当前时间 | 创建时间 |
| `updated_by` | bigint | 否 | 更新时填充 | 更新人 |
| `updated_at` | datetime | 是 | 默认当前时间并自动更新 | 更新时间 |
| `version` | int | 是 | 默认 0，乐观锁 | 数据版本 |
| `deleted` | tinyint | 是 | 0 否，1 是 | 逻辑删除标记 |

## 3. 表清单

| 表名 | 业务对象 | 来源聚合 | 说明 |
| --- | --- | --- | --- |
| `mdm_master_data_type` | 主数据类型 | `02-主数据类型聚合CQRS设计.md` | 承载聚合当前状态、业务快照和列表查询核心字段 |
| `mdm_field_definition` | 字段定义 | `03-字段模板聚合CQRS设计.md` | 承载聚合当前状态、业务快照和列表查询核心字段 |
| `mdm_code_rule` | 编码规则 | `04-编码规则聚合CQRS设计.md` | 承载聚合当前状态、业务快照和列表查询核心字段 |
| `mdm_master_data_record` | 主数据记录 | `05-主数据记录聚合CQRS设计.md` | 承载聚合当前状态、业务快照和列表查询核心字段 |
| `mdm_master_data_change_log` | 主数据变更日志 | `05-主数据记录聚合CQRS设计.md` | 承载聚合当前状态、业务快照和列表查询核心字段 |
| `mdm_master_data_version` | 主数据版本 | `06-主数据版本聚合CQRS设计.md` | 承载聚合当前状态、业务快照和列表查询核心字段 |
| `mdm_publish_subscribe` | 发布订阅配置 | `07-发布订阅聚合CQRS设计.md` | 承载聚合当前状态、业务快照和列表查询核心字段 |
| `mdm_publish_log` | 发布日志 | `07-发布订阅聚合CQRS设计.md` | 承载聚合当前状态、业务快照和列表查询核心字段 |
| `mdm_import_task` | 导入任务 | `08-导入任务聚合CQRS设计.md` | 承载聚合当前状态、业务快照和列表查询核心字段 |

## 4. 表字段设计

### 4.1 `mdm_master_data_type`

业务对象：主数据类型

| 字段 | 类型 | 是否必填 | 约束/枚举 | 领域归类 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `master_data_type_id` | bigint | 是 | 主键，雪花ID或数据库自增 | 技术主键 | 主数据类型主键 |
| `type_code` | varchar(64) | 是 | 唯一 | 标识/引用 | 类型编码，如 `SKU`、`SUPPLIER` |
| `type_name` | varchar(128) | 是 |   | 业务属性 | 类型名称 |
| `domain` | varchar(32) | 是 | `MASTER_DATA_DOMAIN` | 业务属性 | 商品、伙伴、仓储、物流、组织、财务 |
| `parent_type_code` | varchar(64) | 否 |   | 标识/引用 | 上级类型，如 SKU 的上级是 SPU |
| `approval_required` | boolean | 是 | true/false | 业务属性 | 是否需要审核 |
| `version_enabled` | boolean | 是 | true/false | 业务属性 | 是否启用版本 |
| `publish_enabled` | boolean | 是 | true/false | 业务属性 | 是否发布给子系统 |
| `status` | varchar(32) | 是 | `COMMON_STATUS` | 状态/分类 | 启用、停用 |
| `sort_no` | int | 否 |   | 标识/引用 | 排序 |
| `remark` | varchar(512) | 否 |   | 业务属性 | 说明 |
| `created_by` | bigint | 是 | 写入时填充 | 审计 | 创建人 |
| `created_at` | datetime | 是 | 默认当前时间 | 审计 | 创建时间 |
| `updated_by` | bigint | 否 | 更新时填充 | 审计 | 更新人 |
| `updated_at` | datetime | 是 | 默认当前时间并自动更新 | 审计 | 更新时间 |
| `version` | int | 是 | 默认 0，乐观锁 | 并发控制 | 数据版本 |
| `deleted` | tinyint | 是 | 0 否，1 是 | 数据治理 | 逻辑删除标记 |

建议索引：

| 索引 | 字段 | 说明 |
| --- | --- | --- |
| `uk_mdm_master_data_type_biz_no` | 业务单号或编码字段 | 如果该表存在编码/单号字段，必须唯一 |
| `idx_mdm_master_data_type_status_time` | 状态字段、`updated_at` | 支撑列表筛选和待办查询 |
| `idx_mdm_master_data_type_source` | 来源单号、外部单号或引用编码 | 支撑跨系统追踪 |

### 4.2 `mdm_field_definition`

业务对象：字段定义

| 字段 | 类型 | 是否必填 | 约束/枚举 | 领域归类 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `business_object_id` | bigint | 是 | 主键，雪花ID或数据库自增 | 技术主键 | 字段定义主键 |
| `type_code` | varchar(64) | 是 | 外键 | 标识/引用 | 主数据类型 |
| `field_code` | varchar(64) | 是 | 类型内唯一 | 标识/引用 | 字段编码 |
| `field_name` | varchar(128) | 是 |   | 业务属性 | 字段名称 |
| `field_type` | varchar(32) | 是 | `FIELD_TYPE` | 状态/分类 | 字符串、数字、日期、枚举、布尔、引用 |
| `data_length` | int | 否 |   | 业务属性 | 字符串长度 |
| `decimal_scale` | int | 否 |   | 业务属性 | 小数位 |
| `required` | boolean | 是 | true/false | 业务属性 | 是否必填 |
| `unique_flag` | boolean | 是 | true/false | 业务属性 | 是否唯一 |
| `enum_type_code` | varchar(64) | 否 | 外键 | 标识/引用 | 枚举类型 |
| `reference_type_code` | varchar(64) | 否 | 外键 | 标识/引用 | 引用的主数据类型 |
| `default_value` | varchar(256) | 否 |   | 业务属性 | 默认值 |
| `editable_after_enabled` | boolean | 是 | true/false | 业务属性 | 启用后是否可改 |
| `critical_flag` | boolean | 是 | true/false | 业务属性 | 是否关键字段，关键变更需审批 |
| `visible_in_list` | boolean | 是 | true/false | 业务属性 | 列表是否展示 |
| `visible_in_form` | boolean | 是 | true/false | 业务属性 | 表单是否展示 |
| `sort_no` | int | 是 |   | 标识/引用 | 表单排序 |
| `status` | varchar(32) | 是 | `COMMON_STATUS` | 状态/分类 | 启用、停用 |
| `created_by` | bigint | 是 | 写入时填充 | 审计 | 创建人 |
| `created_at` | datetime | 是 | 默认当前时间 | 审计 | 创建时间 |
| `updated_by` | bigint | 否 | 更新时填充 | 审计 | 更新人 |
| `updated_at` | datetime | 是 | 默认当前时间并自动更新 | 审计 | 更新时间 |
| `version` | int | 是 | 默认 0，乐观锁 | 并发控制 | 数据版本 |
| `deleted` | tinyint | 是 | 0 否，1 是 | 数据治理 | 逻辑删除标记 |

建议索引：

| 索引 | 字段 | 说明 |
| --- | --- | --- |
| `uk_mdm_field_definition_biz_no` | 业务单号或编码字段 | 如果该表存在编码/单号字段，必须唯一 |
| `idx_mdm_field_definition_status_time` | 状态字段、`updated_at` | 支撑列表筛选和待办查询 |
| `idx_mdm_field_definition_source` | 来源单号、外部单号或引用编码 | 支撑跨系统追踪 |

### 4.3 `mdm_code_rule`

业务对象：编码规则

| 字段 | 类型 | 是否必填 | 约束/枚举 | 领域归类 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `code_rule_id` | bigint | 是 | 主键，雪花ID或数据库自增 | 技术主键 | 编码规则主键 |
| `rule_code` | varchar(64) | 是 | 唯一 | 标识/引用 | 规则编码 |
| `rule_name` | varchar(128) | 是 |   | 业务属性 | 规则名称 |
| `prefix` | varchar(32) | 否 |   | 业务属性 | 前缀 |
| `date_pattern` | varchar(32) | 否 | `yyyyMMdd` 等 | 时间 | 日期格式 |
| `sequence_length` | int | 是 | > 0 | 业务属性 | 流水号长度 |
| `current_sequence` | bigint | 是 | 默认 0 | 业务属性 | 当前流水 |
| `reset_cycle` | varchar(32) | 是 | `RESET_CYCLE` | 业务属性 | 不重置、每日、每月、每年 |
| `status` | varchar(32) | 是 | `COMMON_STATUS` | 状态/分类 | 启用、停用 |
| `created_by` | bigint | 是 | 写入时填充 | 审计 | 创建人 |
| `created_at` | datetime | 是 | 默认当前时间 | 审计 | 创建时间 |
| `updated_by` | bigint | 否 | 更新时填充 | 审计 | 更新人 |
| `updated_at` | datetime | 是 | 默认当前时间并自动更新 | 审计 | 更新时间 |
| `version` | int | 是 | 默认 0，乐观锁 | 并发控制 | 数据版本 |
| `deleted` | tinyint | 是 | 0 否，1 是 | 数据治理 | 逻辑删除标记 |

建议索引：

| 索引 | 字段 | 说明 |
| --- | --- | --- |
| `uk_mdm_code_rule_biz_no` | 业务单号或编码字段 | 如果该表存在编码/单号字段，必须唯一 |
| `idx_mdm_code_rule_status_time` | 状态字段、`updated_at` | 支撑列表筛选和待办查询 |
| `idx_mdm_code_rule_source` | 来源单号、外部单号或引用编码 | 支撑跨系统追踪 |

### 4.4 `mdm_master_data_record`

业务对象：主数据记录

| 字段 | 类型 | 是否必填 | 约束/枚举 | 领域归类 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `master_data_record_id` | bigint | 是 | 主键，雪花ID或数据库自增 | 技术主键 | 主数据记录主键 |
| `type_code` | varchar(64) | 是 | 外键 | 标识/引用 | 主数据类型 |
| `data_code` | varchar(128) | 是 | 类型内唯一 | 标识/引用 | 主数据编码 |
| `data_name` | varchar(256) | 是 |   | 业务属性 | 主数据名称 |
| `data_payload` | text | 是 | JSON | 业务属性 | 业务字段值 |
| `data_status` | varchar(32) | 是 | `MASTER_DATA_STATUS` | 状态/分类 | 草稿、待审核、已启用等 |
| `approval_status` | varchar(32) | 是 | `APPROVAL_STATUS` | 状态/分类 | 未提交、审批中、通过、驳回 |
| `current_version` | int | 是 | 默认 1 | 业务属性 | 当前版本 |
| `effective_from` | datetime | 否 |   | 业务属性 | 生效时间 |
| `effective_to` | datetime | 否 |   | 业务属性 | 失效时间 |
| `created_by` | bigint | 是 | 写入时填充 | 审计 | 创建人 |
| `created_at` | datetime | 是 | 默认当前时间 | 审计 | 创建时间 |
| `updated_by` | bigint | 否 | 更新时填充 | 审计 | 更新人 |
| `updated_at` | datetime | 是 | 默认当前时间并自动更新 | 审计 | 更新时间 |
| `version` | int | 是 | 默认 0，乐观锁 | 并发控制 | 数据版本 |
| `deleted` | tinyint | 是 | 0 否，1 是 | 数据治理 | 逻辑删除标记 |

建议索引：

| 索引 | 字段 | 说明 |
| --- | --- | --- |
| `uk_mdm_master_data_record_biz_no` | 业务单号或编码字段 | 如果该表存在编码/单号字段，必须唯一 |
| `idx_mdm_master_data_record_status_time` | 状态字段、`updated_at` | 支撑列表筛选和待办查询 |
| `idx_mdm_master_data_record_source` | 来源单号、外部单号或引用编码 | 支撑跨系统追踪 |

### 4.5 `mdm_master_data_change_log`

业务对象：主数据变更日志

| 字段 | 类型 | 是否必填 | 约束/枚举 | 领域归类 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `master_data_id` | bigint | 是 | 主键，雪花ID或数据库自增 | 技术主键 | 主数据变更日志主键 |
| `type_code` | varchar(64) | 是 |   | 标识/引用 | 主数据类型 |
| `field_code` | varchar(64) | 否 |   | 标识/引用 | 变更字段 |
| `old_value` | text | 否 |   | 业务属性 | 旧值 |
| `new_value` | text | 否 |   | 业务属性 | 新值 |
| `critical_flag` | boolean | 是 | true/false | 业务属性 | 是否关键字段 |
| `change_type` | varchar(32) | 是 | `MASTER_CHANGE_TYPE` | 状态/分类 | 变更类型 |
| `operated_at` | datetime | 是 |   | 数量/金额 | 操作时间 |
| `created_by` | bigint | 是 | 写入时填充 | 审计 | 创建人 |
| `created_at` | datetime | 是 | 默认当前时间 | 审计 | 创建时间 |
| `updated_by` | bigint | 否 | 更新时填充 | 审计 | 更新人 |
| `updated_at` | datetime | 是 | 默认当前时间并自动更新 | 审计 | 更新时间 |
| `version` | int | 是 | 默认 0，乐观锁 | 并发控制 | 数据版本 |
| `deleted` | tinyint | 是 | 0 否，1 是 | 数据治理 | 逻辑删除标记 |

建议索引：

| 索引 | 字段 | 说明 |
| --- | --- | --- |
| `uk_mdm_master_data_change_log_biz_no` | 业务单号或编码字段 | 如果该表存在编码/单号字段，必须唯一 |
| `idx_mdm_master_data_change_log_status_time` | 状态字段、`updated_at` | 支撑列表筛选和待办查询 |
| `idx_mdm_master_data_change_log_source` | 来源单号、外部单号或引用编码 | 支撑跨系统追踪 |

### 4.6 `mdm_master_data_version`

业务对象：主数据版本

| 字段 | 类型 | 是否必填 | 约束/枚举 | 领域归类 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `master_data_version_id` | bigint | 是 | 主键，雪花ID或数据库自增 | 技术主键 | 主数据版本主键 |
| `version_no` | int | 是 | 递增 | 标识/引用 | 版本号 |
| `snapshot_payload` | text | 是 | JSON | 业务属性 | 版本快照 |
| `change_type` | varchar(32) | 是 | `MASTER_CHANGE_TYPE` | 状态/分类 | 新增、修改、启用、停用、淘汰 |
| `change_summary` | varchar(1024) | 否 |   | 业务属性 | 变更摘要 |
| `effective_from` | datetime | 否 |   | 业务属性 | 生效时间 |
| `created_by` | bigint | 是 | 写入时填充 | 审计 | 创建人 |
| `created_at` | datetime | 是 | 默认当前时间 | 审计 | 创建时间 |
| `updated_by` | bigint | 否 | 更新时填充 | 审计 | 更新人 |
| `updated_at` | datetime | 是 | 默认当前时间并自动更新 | 审计 | 更新时间 |
| `version` | int | 是 | 默认 0，乐观锁 | 并发控制 | 数据版本 |
| `deleted` | tinyint | 是 | 0 否，1 是 | 数据治理 | 逻辑删除标记 |

建议索引：

| 索引 | 字段 | 说明 |
| --- | --- | --- |
| `uk_mdm_master_data_version_biz_no` | 业务单号或编码字段 | 如果该表存在编码/单号字段，必须唯一 |
| `idx_mdm_master_data_version_status_time` | 状态字段、`updated_at` | 支撑列表筛选和待办查询 |
| `idx_mdm_master_data_version_source` | 来源单号、外部单号或引用编码 | 支撑跨系统追踪 |

### 4.7 `mdm_publish_subscribe`

业务对象：发布订阅配置

| 字段 | 类型 | 是否必填 | 约束/枚举 | 领域归类 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `publish_subscribe_id` | bigint | 是 | 主键，雪花ID或数据库自增 | 技术主键 | 发布订阅配置主键 |
| `type_code` | varchar(64) | 是 | 外键 | 标识/引用 | 主数据类型 |
| `target_system` | varchar(64) | 是 | `TARGET_SYSTEM` | 业务属性 | OMS、WMS、库存、BMS 等 |
| `event_topic` | varchar(128) | 是 |   | 业务属性 | 事件主题 |
| `publish_mode` | varchar(32) | 是 | `PUBLISH_MODE` | 状态/分类 | 事件、API、批量 |
| `filter_rule` | text | 否 | JSON | 业务属性 | 发布过滤规则 |
| `status` | varchar(32) | 是 | `COMMON_STATUS` | 状态/分类 | 启用、停用 |
| `created_by` | bigint | 是 | 写入时填充 | 审计 | 创建人 |
| `created_at` | datetime | 是 | 默认当前时间 | 审计 | 创建时间 |
| `updated_by` | bigint | 否 | 更新时填充 | 审计 | 更新人 |
| `updated_at` | datetime | 是 | 默认当前时间并自动更新 | 审计 | 更新时间 |
| `version` | int | 是 | 默认 0，乐观锁 | 并发控制 | 数据版本 |
| `deleted` | tinyint | 是 | 0 否，1 是 | 数据治理 | 逻辑删除标记 |

建议索引：

| 索引 | 字段 | 说明 |
| --- | --- | --- |
| `uk_mdm_publish_subscribe_biz_no` | 业务单号或编码字段 | 如果该表存在编码/单号字段，必须唯一 |
| `idx_mdm_publish_subscribe_status_time` | 状态字段、`updated_at` | 支撑列表筛选和待办查询 |
| `idx_mdm_publish_subscribe_source` | 来源单号、外部单号或引用编码 | 支撑跨系统追踪 |

### 4.8 `mdm_publish_log`

业务对象：发布日志

| 字段 | 类型 | 是否必填 | 约束/枚举 | 领域归类 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `business_object_id` | bigint | 是 | 主键，雪花ID或数据库自增 | 技术主键 | 发布日志主键 |
| `type_code` | varchar(64) | 是 |   | 标识/引用 | 主数据类型 |
| `version_no` | int | 是 |   | 标识/引用 | 发布版本 |
| `target_system` | varchar(64) | 是 | `TARGET_SYSTEM` | 业务属性 | 目标系统 |
| `event_name` | varchar(128) | 是 |   | 业务属性 | 事件名 |
| `publish_status` | varchar(32) | 是 | `PUBLISH_STATUS` | 状态/分类 | 待发布、发布中、成功、失败 |
| `retry_count` | int | 是 | 默认 0 | 业务属性 | 重试次数 |
| `failure_reason` | varchar(1024) | 否 |   | 业务属性 | 失败原因 |
| `published_at` | datetime | 否 |   | 业务属性 | 发布时间 |
| `created_by` | bigint | 是 | 写入时填充 | 审计 | 创建人 |
| `created_at` | datetime | 是 | 默认当前时间 | 审计 | 创建时间 |
| `updated_by` | bigint | 否 | 更新时填充 | 审计 | 更新人 |
| `updated_at` | datetime | 是 | 默认当前时间并自动更新 | 审计 | 更新时间 |
| `version` | int | 是 | 默认 0，乐观锁 | 并发控制 | 数据版本 |
| `deleted` | tinyint | 是 | 0 否，1 是 | 数据治理 | 逻辑删除标记 |

建议索引：

| 索引 | 字段 | 说明 |
| --- | --- | --- |
| `uk_mdm_publish_log_biz_no` | 业务单号或编码字段 | 如果该表存在编码/单号字段，必须唯一 |
| `idx_mdm_publish_log_status_time` | 状态字段、`updated_at` | 支撑列表筛选和待办查询 |
| `idx_mdm_publish_log_source` | 来源单号、外部单号或引用编码 | 支撑跨系统追踪 |

### 4.9 `mdm_import_task`

业务对象：导入任务

| 字段 | 类型 | 是否必填 | 约束/枚举 | 领域归类 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `import_task_id` | bigint | 是 | 主键，雪花ID或数据库自增 | 技术主键 | 导入任务主键 |
| `type_code` | varchar(64) | 是 | 外键 | 标识/引用 | 主数据类型 |
| `file_name` | varchar(256) | 是 |   | 业务属性 | 文件名 |
| `import_mode` | varchar(32) | 是 | `IMPORT_MODE` | 状态/分类 | 新增、更新、覆盖 |
| `task_status` | varchar(32) | 是 | `IMPORT_STATUS` | 状态/分类 | 待处理、处理中、成功、部分成功、失败 |
| `total_count` | int | 是 | 默认 0 | 业务属性 | 总行数 |
| `success_count` | int | 是 | 默认 0 | 业务属性 | 成功数 |
| `failed_count` | int | 是 | 默认 0 | 业务属性 | 失败数 |
| `error_file_url` | varchar(512) | 否 |   | 业务属性 | 错误文件 |
| `created_by` | bigint | 是 | 写入时填充 | 审计 | 创建人 |
| `created_at` | datetime | 是 | 默认当前时间 | 审计 | 创建时间 |
| `updated_by` | bigint | 否 | 更新时填充 | 审计 | 更新人 |
| `updated_at` | datetime | 是 | 默认当前时间并自动更新 | 审计 | 更新时间 |
| `version` | int | 是 | 默认 0，乐观锁 | 并发控制 | 数据版本 |
| `deleted` | tinyint | 是 | 0 否，1 是 | 数据治理 | 逻辑删除标记 |

建议索引：

| 索引 | 字段 | 说明 |
| --- | --- | --- |
| `uk_mdm_import_task_biz_no` | 业务单号或编码字段 | 如果该表存在编码/单号字段，必须唯一 |
| `idx_mdm_import_task_status_time` | 状态字段、`updated_at` | 支撑列表筛选和待办查询 |
| `idx_mdm_import_task_source` | 来源单号、外部单号或引用编码 | 支撑跨系统追踪 |

## 5. 枚举值

枚举字段在 DDL 中统一使用 `smallint` 存储，枚举项从 `1` 开始递增；页面展示名、排序和启停状态通过枚举配置表维护。

| 枚举类型 | 数值 | 枚举项 | 说明 |
| --- | --- | --- | --- |
| `APPROVAL_STATUS` | `1` | 草稿 | APPROVAL_STATUS 的第 1 个枚举项 |
| `APPROVAL_STATUS` | `2` | 待审批 | APPROVAL_STATUS 的第 2 个枚举项 |
| `APPROVAL_STATUS` | `3` | 已批准 | APPROVAL_STATUS 的第 3 个枚举项 |
| `APPROVAL_STATUS` | `4` | 已驳回 | APPROVAL_STATUS 的第 4 个枚举项 |
| `COMMON_STATUS` | `1` | 启用 | COMMON_STATUS 的第 1 个枚举项 |
| `COMMON_STATUS` | `2` | 停用 | COMMON_STATUS 的第 2 个枚举项 |
| `CONSUME_STATUS` | `1` | 待消费 | CONSUME_STATUS 的第 1 个枚举项 |
| `CONSUME_STATUS` | `2` | 处理中 | CONSUME_STATUS 的第 2 个枚举项 |
| `CONSUME_STATUS` | `3` | 消费成功 | CONSUME_STATUS 的第 3 个枚举项 |
| `CONSUME_STATUS` | `4` | 消费失败 | CONSUME_STATUS 的第 4 个枚举项 |
| `CONSUME_STATUS` | `5` | 已忽略 | CONSUME_STATUS 的第 5 个枚举项 |
| `CURRENCY` | `1` | 人民币 | CURRENCY 的第 1 个枚举项 |
| `CURRENCY` | `2` | 美元 | CURRENCY 的第 2 个枚举项 |
| `CURRENCY` | `3` | 欧元 | CURRENCY 的第 3 个枚举项 |
| `CURRENCY` | `4` | 港币 | CURRENCY 的第 4 个枚举项 |
| `CURRENCY` | `5` | 日元 | CURRENCY 的第 5 个枚举项 |
| `EVENT_STATUS` | `1` | 待发布 | EVENT_STATUS 的第 1 个枚举项 |
| `EVENT_STATUS` | `2` | 发布中 | EVENT_STATUS 的第 2 个枚举项 |
| `EVENT_STATUS` | `3` | 已发布 | EVENT_STATUS 的第 3 个枚举项 |
| `EVENT_STATUS` | `4` | 发布失败 | EVENT_STATUS 的第 4 个枚举项 |
| `EVENT_STATUS` | `5` | 已取消 | EVENT_STATUS 的第 5 个枚举项 |
| `FIELD_TYPE` | `1` | 字符串 | FIELD_TYPE 的第 1 个枚举项 |
| `FIELD_TYPE` | `2` | 数字 | FIELD_TYPE 的第 2 个枚举项 |
| `FIELD_TYPE` | `3` | 日期 | FIELD_TYPE 的第 3 个枚举项 |
| `FIELD_TYPE` | `4` | 枚举 | FIELD_TYPE 的第 4 个枚举项 |
| `FIELD_TYPE` | `5` | 布尔 | FIELD_TYPE 的第 5 个枚举项 |
| `FIELD_TYPE` | `6` | 引用 | FIELD_TYPE 的第 6 个枚举项 |
| `IMPORT_MODE` | `1` | 新增 | IMPORT_MODE 的第 1 个枚举项 |
| `IMPORT_MODE` | `2` | 更新 | IMPORT_MODE 的第 2 个枚举项 |
| `IMPORT_MODE` | `3` | 覆盖 | IMPORT_MODE 的第 3 个枚举项 |
| `IMPORT_STATUS` | `1` | 待处理 | IMPORT_STATUS 的第 1 个枚举项 |
| `IMPORT_STATUS` | `2` | 处理中 | IMPORT_STATUS 的第 2 个枚举项 |
| `IMPORT_STATUS` | `3` | 成功 | IMPORT_STATUS 的第 3 个枚举项 |
| `IMPORT_STATUS` | `4` | 部分成功 | IMPORT_STATUS 的第 4 个枚举项 |
| `IMPORT_STATUS` | `5` | 失败 | IMPORT_STATUS 的第 5 个枚举项 |
| `MASTER_CHANGE_TYPE` | `1` | 变更类型 | MASTER_CHANGE_TYPE 的第 1 个枚举项 |
| `MASTER_CHANGE_TYPE` | `2` | 新增 | MASTER_CHANGE_TYPE 的第 2 个枚举项 |
| `MASTER_CHANGE_TYPE` | `3` | 修改 | MASTER_CHANGE_TYPE 的第 3 个枚举项 |
| `MASTER_CHANGE_TYPE` | `4` | 启用 | MASTER_CHANGE_TYPE 的第 4 个枚举项 |
| `MASTER_CHANGE_TYPE` | `5` | 停用 | MASTER_CHANGE_TYPE 的第 5 个枚举项 |
| `MASTER_CHANGE_TYPE` | `6` | 淘汰 | MASTER_CHANGE_TYPE 的第 6 个枚举项 |
| `MASTER_DATA_DOMAIN` | `1` | 商品 | MASTER_DATA_DOMAIN 的第 1 个枚举项 |
| `MASTER_DATA_DOMAIN` | `2` | 伙伴 | MASTER_DATA_DOMAIN 的第 2 个枚举项 |
| `MASTER_DATA_DOMAIN` | `3` | 仓储 | MASTER_DATA_DOMAIN 的第 3 个枚举项 |
| `MASTER_DATA_DOMAIN` | `4` | 物流 | MASTER_DATA_DOMAIN 的第 4 个枚举项 |
| `MASTER_DATA_DOMAIN` | `5` | 组织 | MASTER_DATA_DOMAIN 的第 5 个枚举项 |
| `MASTER_DATA_DOMAIN` | `6` | 财务 | MASTER_DATA_DOMAIN 的第 6 个枚举项 |
| `MASTER_DATA_STATUS` | `1` | 草稿 | MASTER_DATA_STATUS 的第 1 个枚举项 |
| `MASTER_DATA_STATUS` | `2` | 待审核 | MASTER_DATA_STATUS 的第 2 个枚举项 |
| `MASTER_DATA_STATUS` | `3` | 已启用 | MASTER_DATA_STATUS 的第 3 个枚举项 |
| `OPERATION_RESULT` | `1` | 成功 | OPERATION_RESULT 的第 1 个枚举项 |
| `OPERATION_RESULT` | `2` | 失败 | OPERATION_RESULT 的第 2 个枚举项 |
| `PUBLISH_MODE` | `1` | 事件 | PUBLISH_MODE 的第 1 个枚举项 |
| `PUBLISH_MODE` | `2` | 批量 | PUBLISH_MODE 的第 2 个枚举项 |
| `PUBLISH_STATUS` | `1` | 待发布 | PUBLISH_STATUS 的第 1 个枚举项 |
| `PUBLISH_STATUS` | `2` | 发布中 | PUBLISH_STATUS 的第 2 个枚举项 |
| `PUBLISH_STATUS` | `3` | 成功 | PUBLISH_STATUS 的第 3 个枚举项 |
| `PUBLISH_STATUS` | `4` | 失败 | PUBLISH_STATUS 的第 4 个枚举项 |
| `RESET_CYCLE` | `1` | 不重置 | RESET_CYCLE 的第 1 个枚举项 |
| `RESET_CYCLE` | `2` | 每日 | RESET_CYCLE 的第 2 个枚举项 |
| `RESET_CYCLE` | `3` | 每月 | RESET_CYCLE 的第 3 个枚举项 |
| `RESET_CYCLE` | `4` | 每年 | RESET_CYCLE 的第 4 个枚举项 |
| `TARGET_SYSTEM` | `1` | 库存 | TARGET_SYSTEM 的第 1 个枚举项 |
| `TARGET_SYSTEM` | `2` | 目标系统 | TARGET_SYSTEM 的第 2 个枚举项 |

## 6. 事件、事件表与审计表字段

事件采用本地消息表模式：应用服务在同一事务内保存业务表和领域事件表，异步发布后更新事件状态；消费外部事件时先写消费日志，使用幂等键避免重复处理。

### 6.1 本系统领域事件

| 事件 | 来源聚合 | 触发动作 | 主要载荷 | 订阅/用途 |
| --- | --- | --- | --- | --- |
| 主数据类型已创建 | 02-主数据类型聚合CQRS设计 | 创建类型 | 聚合ID、类型编码、主数据编码、版本号、状态`草稿`、变更摘要、目标系统、操作者、幂等键 | 采购、OMS、WMS、中央库存、BMS、供应商系统、读模型、审计日志 |
| 主数据类型已启用 | 02-主数据类型聚合CQRS设计 | 启用类型 | 聚合ID、类型编码、主数据编码、版本号、状态`已启用`、变更摘要、目标系统、操作者、幂等键 | 采购、OMS、WMS、中央库存、BMS、供应商系统、读模型、审计日志 |
| 主数据类型已变更 | 02-主数据类型聚合CQRS设计 | 变更类型 | 聚合ID、类型编码、主数据编码、版本号、状态`已启用`、变更摘要、目标系统、操作者、幂等键 | 采购、OMS、WMS、中央库存、BMS、供应商系统、读模型、审计日志 |
| 主数据类型已停用 | 02-主数据类型聚合CQRS设计 | 停用类型 | 聚合ID、类型编码、主数据编码、版本号、状态`已停用`、变更摘要、目标系统、操作者、幂等键 | 采购、OMS、WMS、中央库存、BMS、供应商系统、读模型、审计日志 |
| 审批已完成 | 02-主数据类型聚合CQRS设计 | 主数据类型应用服务 | 根据审批结果启用或驳回类型配置 | 来源上下文+事件编号+业务主键 |
| 下游回执已返回 | 02-主数据类型聚合CQRS设计 | 主数据类型应用服务 | 更新类型发布确认状态 | 来源上下文+事件编号+业务主键 |
| 字段模板已创建 | 03-字段模板聚合CQRS设计 | 创建字段模板 | 聚合ID、类型编码、主数据编码、版本号、状态`草稿`、变更摘要、目标系统、操作者、幂等键 | 采购、OMS、WMS、中央库存、BMS、供应商系统、读模型、审计日志 |
| 字段模板已发布 | 03-字段模板聚合CQRS设计 | 发布字段模板 | 聚合ID、类型编码、主数据编码、版本号、状态`已发布`、变更摘要、目标系统、操作者、幂等键 | 采购、OMS、WMS、中央库存、BMS、供应商系统、读模型、审计日志 |
| 字段模板已变更 | 03-字段模板聚合CQRS设计 | 变更字段定义 | 聚合ID、类型编码、主数据编码、版本号、状态`已发布`、变更摘要、目标系统、操作者、幂等键 | 采购、OMS、WMS、中央库存、BMS、供应商系统、读模型、审计日志 |
| 字段模板已停用 | 03-字段模板聚合CQRS设计 | 停用字段模板 | 聚合ID、类型编码、主数据编码、版本号、状态`已停用`、变更摘要、目标系统、操作者、幂等键 | 采购、OMS、WMS、中央库存、BMS、供应商系统、读模型、审计日志 |
| 枚举值已变更 | 03-字段模板聚合CQRS设计 | 字段模板应用服务 | 标记引用枚举字段需要复核 | 来源上下文+事件编号+业务主键 |
| 编码规则已创建 | 04-编码规则聚合CQRS设计 | 创建编码规则 | 聚合ID、类型编码、主数据编码、版本号、状态`草稿`、变更摘要、目标系统、操作者、幂等键 | 采购、OMS、WMS、中央库存、BMS、供应商系统、读模型、审计日志 |
| 编码规则已启用 | 04-编码规则聚合CQRS设计 | 启用编码规则 | 聚合ID、类型编码、主数据编码、版本号、状态`已启用`、变更摘要、目标系统、操作者、幂等键 | 采购、OMS、WMS、中央库存、BMS、供应商系统、读模型、审计日志 |
| 主数据编码已生成 | 04-编码规则聚合CQRS设计 | 生成主数据编码 | 聚合ID、类型编码、主数据编码、版本号、状态`已使用`、变更摘要、目标系统、操作者、幂等键 | 采购、OMS、WMS、中央库存、BMS、供应商系统、读模型、审计日志 |
| 编码规则已停用 | 04-编码规则聚合CQRS设计 | 停用编码规则 | 聚合ID、类型编码、主数据编码、版本号、状态`已停用`、变更摘要、目标系统、操作者、幂等键 | 采购、OMS、WMS、中央库存、BMS、供应商系统、读模型、审计日志 |
| 导入任务已创建 | 04-编码规则聚合CQRS设计 | 编码规则应用服务 | 为缺失编码的导入行分配编码 | 来源上下文+事件编号+业务主键 |
| 主数据草稿已创建 | 05-主数据记录聚合CQRS设计 | 创建记录 | 聚合ID、类型编码、主数据编码、版本号、状态`草稿`、变更摘要、目标系统、操作者、幂等键 | 采购、OMS、WMS、中央库存、BMS、供应商系统、读模型、审计日志 |
| 主数据已提交审核 | 05-主数据记录聚合CQRS设计 | 提交审核 | 聚合ID、类型编码、主数据编码、版本号、状态`待审核`、变更摘要、目标系统、操作者、幂等键 | 采购、OMS、WMS、中央库存、BMS、供应商系统、读模型、审计日志 |
| 主数据已启用 | 05-主数据记录聚合CQRS设计 | 审核通过 | 聚合ID、类型编码、主数据编码、版本号、状态`已启用`、变更摘要、目标系统、操作者、幂等键 | 采购、OMS、WMS、中央库存、BMS、供应商系统、读模型、审计日志 |
| 主数据已驳回 | 05-主数据记录聚合CQRS设计 | 驳回记录 | 聚合ID、类型编码、主数据编码、版本号、状态`已驳回`、变更摘要、目标系统、操作者、幂等键 | 采购、OMS、WMS、中央库存、BMS、供应商系统、读模型、审计日志 |
| 主数据变更已提交 | 05-主数据记录聚合CQRS设计 | 变更记录 | 聚合ID、类型编码、主数据编码、版本号、状态`变更待审核`、变更摘要、目标系统、操作者、幂等键 | 采购、OMS、WMS、中央库存、BMS、供应商系统、读模型、审计日志 |
| 主数据已冻结 | 05-主数据记录聚合CQRS设计 | 冻结记录 | 聚合ID、类型编码、主数据编码、版本号、状态`已冻结`、变更摘要、目标系统、操作者、幂等键 | 采购、OMS、WMS、中央库存、BMS、供应商系统、读模型、审计日志 |
| 主数据已停用 | 05-主数据记录聚合CQRS设计 | 停用记录 | 聚合ID、类型编码、主数据编码、版本号、状态`已停用`、变更摘要、目标系统、操作者、幂等键 | 采购、OMS、WMS、中央库存、BMS、供应商系统、读模型、审计日志 |
| 数据质量问题已发现 | 05-主数据记录聚合CQRS设计 | 主数据记录应用服务 | 标记风险字段或生成治理待办 | 来源上下文+事件编号+业务主键 |
| 主数据版本已生成 | 06-主数据版本聚合CQRS设计 | 生成版本 | 聚合ID、类型编码、主数据编码、版本号、状态`待发布`、变更摘要、目标系统、操作者、幂等键 | 采购、OMS、WMS、中央库存、BMS、供应商系统、读模型、审计日志 |
| 主数据版本已发布 | 06-主数据版本聚合CQRS设计 | 发布版本 | 聚合ID、类型编码、主数据编码、版本号、状态`已发布`、变更摘要、目标系统、操作者、幂等键 | 采购、OMS、WMS、中央库存、BMS、供应商系统、读模型、审计日志 |
| 主数据版本已重新发布 | 06-主数据版本聚合CQRS设计 | 重试发布版本 | 聚合ID、类型编码、主数据编码、版本号、状态`已发布`、变更摘要、目标系统、操作者、幂等键 | 采购、OMS、WMS、中央库存、BMS、供应商系统、读模型、审计日志 |
| 主数据变更已生效 | 06-主数据版本聚合CQRS设计 | 主数据版本应用服务 | 生成变更版本快照 | 来源上下文+事件编号+业务主键 |
| 订阅已创建 | 07-发布订阅聚合CQRS设计 | 创建订阅 | 聚合ID、类型编码、主数据编码、版本号、状态`已启用`、变更摘要、目标系统、操作者、幂等键 | 采购、OMS、WMS、中央库存、BMS、供应商系统、读模型、审计日志 |
| 主数据已发布 | 07-发布订阅聚合CQRS设计 | 发布主数据 | 聚合ID、类型编码、主数据编码、版本号、状态`发布中`、变更摘要、目标系统、操作者、幂等键 | 采购、OMS、WMS、中央库存、BMS、供应商系统、读模型、审计日志 |
| 主数据发布已确认 | 07-发布订阅聚合CQRS设计 | 确认发布成功 | 聚合ID、类型编码、主数据编码、版本号、状态`已确认`、变更摘要、目标系统、操作者、幂等键 | 采购、OMS、WMS、中央库存、BMS、供应商系统、读模型、审计日志 |
| 主数据已重新发布 | 07-发布订阅聚合CQRS设计 | 重试发布 | 聚合ID、类型编码、主数据编码、版本号、状态`发布中`、变更摘要、目标系统、操作者、幂等键 | 采购、OMS、WMS、中央库存、BMS、供应商系统、读模型、审计日志 |
| 订阅已停用 | 07-发布订阅聚合CQRS设计 | 停用订阅 | 聚合ID、类型编码、主数据编码、版本号、状态`已停用`、变更摘要、目标系统、操作者、幂等键 | 采购、OMS、WMS、中央库存、BMS、供应商系统、读模型、审计日志 |
| 导入文件已校验 | 08-导入任务聚合CQRS设计 | 校验导入文件 | 聚合ID、类型编码、主数据编码、版本号、状态`待执行`、变更摘要、目标系统、操作者、幂等键 | 采购、OMS、WMS、中央库存、BMS、供应商系统、读模型、审计日志 |
| 导入任务已执行 | 08-导入任务聚合CQRS设计 | 执行导入 | 聚合ID、类型编码、主数据编码、版本号、状态`执行中`、变更摘要、目标系统、操作者、幂等键 | 采购、OMS、WMS、中央库存、BMS、供应商系统、读模型、审计日志 |
| 导入任务已完成 | 08-导入任务聚合CQRS设计 | 完成导入 | 聚合ID、类型编码、主数据编码、版本号、状态`已完成`、变更摘要、目标系统、操作者、幂等键 | 采购、OMS、WMS、中央库存、BMS、供应商系统、读模型、审计日志 |
| 导入任务已取消 | 08-导入任务聚合CQRS设计 | 取消导入 | 聚合ID、类型编码、主数据编码、版本号、状态`已取消`、变更摘要、目标系统、操作者、幂等键 | 采购、OMS、WMS、中央库存、BMS、供应商系统、读模型、审计日志 |
| 数据质量问题已分派 | 09-数据质量问题聚合CQRS设计 | 分派质量问题 | 聚合ID、类型编码、主数据编码、版本号、状态`处理中`、变更摘要、目标系统、操作者、幂等键 | 采购、OMS、WMS、中央库存、BMS、供应商系统、读模型、审计日志 |
| 数据质量问题已修复 | 09-数据质量问题聚合CQRS设计 | 修复质量问题 | 聚合ID、类型编码、主数据编码、版本号、状态`待验证`、变更摘要、目标系统、操作者、幂等键 | 采购、OMS、WMS、中央库存、BMS、供应商系统、读模型、审计日志 |
| 数据质量问题已关闭 | 09-数据质量问题聚合CQRS设计 | 确认治理结果 | 聚合ID、类型编码、主数据编码、版本号、状态`已关闭`、变更摘要、目标系统、操作者、幂等键 | 采购、OMS、WMS、中央库存、BMS、供应商系统、读模型、审计日志 |
| 数据质量问题已重新打开 | 09-数据质量问题聚合CQRS设计 | 驳回治理结果 | 聚合ID、类型编码、主数据编码、版本号、状态`处理中`、变更摘要、目标系统、操作者、幂等键 | 采购、OMS、WMS、中央库存、BMS、供应商系统、读模型、审计日志 |
| 主数据已变更 | 09-数据质量问题聚合CQRS设计 | 数据质量应用服务 | 重新检测该记录是否仍存在质量风险 | 来源上下文+事件编号+业务主键 |

### 6.2 `mdm_domain_event`

| 字段 | 类型 | 是否必填 | 说明 |
| --- | --- | --- | --- |
| `event_id` | bigint | 是 | 事件主键 |
| `event_code` | varchar(128) | 是 | 全局唯一事件编码 |
| `event_name` | varchar(128) | 是 | 中文事件名 |
| `event_type` | varchar(128) | 是 | 稳定事件类型编码 |
| `aggregate_type` | varchar(128) | 是 | 聚合类型 |
| `aggregate_id` | bigint | 是 | 聚合 ID |
| `aggregate_no` | varchar(128) | 否 | 业务单号或编码 |
| `source_system` | varchar(64) | 是 | 来源系统 |
| `payload_json` | json | 是 | 事件载荷 |
| `event_status` | smallint | 是 | `EVENT_STATUS`：1 待发布，2 发布中，3 已发布，4 发布失败，5 已取消 |
| `retry_count` | int | 是 | 重试次数 |
| `fail_reason` | varchar(1024) | 否 | 失败原因 |
| `occurred_at` | datetime | 是 | 业务发生时间 |
| `published_at` | datetime | 否 | 发布时间 |
| `created_at` | datetime | 是 | 创建时间 |
| `updated_at` | datetime | 是 | 更新时间 |

### 6.3 `mdm_event_consume_log`

| 字段 | 类型 | 是否必填 | 说明 |
| --- | --- | --- | --- |
| `consume_log_id` | bigint | 是 | 消费日志主键 |
| `event_code` | varchar(128) | 是 | 来源事件编码 |
| `source_system` | varchar(64) | 是 | 来源系统 |
| `consumer_name` | varchar(128) | 是 | 消费者名称 |
| `idempotent_key` | varchar(256) | 是 | 幂等键 |
| `consume_status` | smallint | 是 | `CONSUME_STATUS`：1 待消费，2 处理中，3 消费成功，4 消费失败，5 已忽略 |
| `retry_count` | int | 是 | 重试次数 |
| `fail_reason` | varchar(1024) | 否 | 失败原因 |
| `consumed_at` | datetime | 否 | 消费完成时间 |
| `created_at` | datetime | 是 | 创建时间 |
| `updated_at` | datetime | 是 | 更新时间 |

### 6.4 `mdm_operation_audit_log`

| 字段 | 类型 | 是否必填 | 说明 |
| --- | --- | --- | --- |
| `operation_log_id` | bigint | 是 | 操作日志主键 |
| `operator_id` | bigint | 是 | 操作人 ID |
| `operator_name` | varchar(128) | 否 | 操作人名称 |
| `operation_type` | varchar(64) | 是 | 操作类型 |
| `target_type` | varchar(128) | 是 | 操作对象类型 |
| `target_id` | bigint | 否 | 操作对象 ID |
| `target_no` | varchar(128) | 否 | 操作对象单号或编码 |
| `before_snapshot` | json | 否 | 操作前快照 |
| `after_snapshot` | json | 否 | 操作后快照 |
| `result` | smallint | 是 | `OPERATION_RESULT`：1 成功，2 失败 |
| `fail_reason` | varchar(1024) | 否 | 失败原因 |
| `request_id` | varchar(128) | 否 | 请求 ID |
| `operation_at` | datetime | 是 | 操作时间 |
| `created_at` | datetime | 是 | 创建时间 |

## 7. DDL 文件

完整 MySQL 8.0 DDL 已生成到：

- [`ddl/08-主数据系统.sql`](ddl/08-主数据系统.sql)

DDL 包含业务表、枚举类型表、枚举项初始化数据、领域事件发布表、事件消费日志表和操作日志表。

当前结论：数据库表围绕聚合当前状态、内部实体明细、事件日志和读模型支撑设计；枚举字段使用数值编码落库，通过枚举配置表维护显示名和启停状态。

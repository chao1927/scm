# 45 主数据系统前端 UI 设计

> 本文记录主数据系统本地前端原型页面，承接 [36-主数据系统功能设计](./36-主数据系统功能设计.md) 和 [39-主数据系统详细设计](./39-主数据系统详细设计.md)。

## 1. 本地静态原型

原型入口：[主数据系统原型](../../../prototype/master-data-system/index.html)

原型文件目录：`prototype/master-data-system`

说明：本地原型不依赖后端和 Figma，直接打开 `index.html` 即可查看。每个业务菜单包含列表页、详情页、新增页、修改页，列表支持条件查询、重置、分页和行内操作。

## 2. 页面清单

| 页面 | 主要内容 |
| --- | --- |
| 主数据工作台 | 待审核、发布失败、导入失败、数据质量问题、近期变更 |
| 主数据类型 | 类型编码、类型名称、所属领域、审核、版本、发布、启停状态 |
| 字段模板 | 字段编码、字段名称、字段类型、必填、唯一、关键字段、列表展示 |
| 编码规则 | 规则编码、前缀、日期格式、流水号长度、重置周期、适用类型 |
| 商品主数据 | SPU/SKU、类目、品牌、条码、库存单位、状态、版本 |
| 合作伙伴主数据 | 供应商、客户、货主、物流商、结算对象、评分、资质状态 |
| 仓储主数据 | 仓库、库区、库位、温区、容量、货主范围、状态 |
| 物流主数据 | 物流商、物流产品、服务类型、服务区域、面单模板、联调状态 |
| 主数据审核 | 类型、编码、名称、变更摘要、申请人、审批状态 |
| 主数据发布 | 类型、编码、版本、目标系统、事件名、发布状态、失败原因 |
| 导入导出 | 导入任务、主数据类型、文件名、总数、成功数、失败数、状态 |
| 变更日志 | 类型、编码、字段、旧值、新值、关键字段、操作人 |
| 操作日志 | 操作人、对象类型、对象编码、动作、结果、失败原因、时间 |
| 枚举配置 | 枚举类型、枚举值、显示名称、排序、启停状态 |

## 3. 左侧菜单

```text
主数据工作台
主数据类型
字段模板
编码规则
商品主数据
合作伙伴主数据
仓储主数据
物流主数据
主数据审核
主数据发布
导入导出
变更日志
操作日志
枚举配置
```

## DDD 对齐说明

本文属于 **主数据上下文**。设计时应把页面、字段和流程统一回到该上下文的模型边界，避免跨上下文直接修改数据。

| DDD 项 | 对齐口径 |
| --- | --- |
| 限界上下文 | 主数据上下文 |
| 核心聚合 | SKU、Supplier、WarehouseLocation、Customer、Owner、Carrier |
| 数据主权 | 基础资料权威来源和发布语言 |
| 生产事件 | 只发布本上下文已经发生的业务事实 |
| 消费事件 | 消费外部事实时必须记录 event_id、幂等键、处理状态和失败原因 |
| 查询模型 | 列表、看板、导出可使用读模型，不强行加载聚合 |

## 4. 后续上下文

当前结论：主数据系统已经形成第一版本地可运行原型，覆盖类型、字段模板、编码、商品、伙伴、仓储、物流、审核、发布、导入导出、变更日志、操作日志和枚举配置。

下一步建议：如果继续细化主数据系统，可以优先补充“字段模板设计器”“主数据版本对比页”“发布失败重试详情”“批量导入错误明细页”。

# 45 OMS 系统前端 UI 设计

> 本文记录 OMS 系统本地前端原型页面，承接 [32-OMS系统功能设计](./32-OMS系统功能设计.md) 和 [42-OMS系统详细设计](./42-OMS系统详细设计.md)。

## 1. 本地静态原型

原型入口：[OMS系统原型](../../../prototype/oms-system/index.html)

原型文件目录：`prototype/oms-system`

说明：本地原型不依赖后端和 Figma，直接打开 `index.html` 即可查看。每个业务菜单包含列表页、详情页、新增页、修改页，列表支持条件查询、重置、分页和行内操作。

## 2. 页面清单

| 页面 | 主要内容 |
| --- | --- |
| OMS工作台 | 待审核、缺货待处理、待发仓、出库异常、售后待审 |
| 渠道订单接入 | 渠道、外部单号、客户、接入状态、失败原因、处理时间 |
| 销售订单 | 订单号、渠道、客户、金额、支付状态、审单状态、订单状态、履约状态 |
| 订单审单 | 订单号、审单类型、命中规则、审单结果、异常原因、处理状态 |
| 分仓履约 | 履约单、订单号、发货仓、物流商、承诺发货/送达、履约状态 |
| 库存预占 | 订单号、履约单、仓库、SKU、请求数量、预占数量、预占号、状态 |
| 出库单 | 出库单、销售订单、履约单、仓库、WMS 单号、出库状态 |
| 取消管理 | 取消单、订单号、来源、原因、取消状态、WMS 取消、库存释放 |
| 售后管理 | 售后单、订单号、售后类型、原因、退款金额、退货仓、状态 |
| 异常处理 | 异常单、订单号、异常类型、责任方、状态、说明 |
| 规则配置 | 规则编码、规则名称、规则类型、优先级、状态、生效时间 |
| 操作日志 | 操作人、对象类型、对象单号、动作、结果、失败原因、时间 |
| 枚举配置 | 枚举类型、枚举值、显示名称、排序、启停状态 |

## 3. 左侧菜单

```text
OMS工作台
渠道订单接入
销售订单
订单审单
分仓履约
库存预占
出库单
取消管理
售后管理
异常处理
规则配置
操作日志
枚举配置
```

## DDD 对齐说明

本文属于 **OMS 上下文**。设计时应把页面、字段和流程统一回到该上下文的模型边界，避免跨上下文直接修改数据。

| DDD 项 | 对齐口径 |
| --- | --- |
| 限界上下文 | OMS 上下文 |
| 核心聚合 | SalesOrder、FulfillmentOrder、AfterSaleOrder |
| 数据主权 | 销售订单、履约编排、售后入口 |
| 生产事件 | 只发布本上下文已经发生的业务事实 |
| 消费事件 | 消费外部事实时必须记录 event_id、幂等键、处理状态和失败原因 |
| 查询模型 | 列表、看板、导出可使用读模型，不强行加载聚合 |

## 4. 后续上下文

当前结论：OMS 系统已经形成第一版本地可运行原型，覆盖接单、审单、分仓履约、库存预占、出库下发、取消、售后、异常处理、规则配置、日志和枚举配置。

下一步建议：如果继续细化 OMS，可以优先补充“订单详情完整履约轨迹”“审单异常处理弹窗”“分仓换仓向导”“售后审核与退款/补发表单”。

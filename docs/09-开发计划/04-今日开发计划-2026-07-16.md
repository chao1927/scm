# 2026-07-16 供应链系统开发计划

> 历史计划：本文保留 2026-07-16 当日判断。独立报表服务和统一集成中心方案已由 `ARCH-REQ-001` 撤销/迁移；当前状态见 [05-九子系统模块状态矩阵.md](05-九子系统模块状态矩阵.md)，当前多 Agent 任务见 [06-多Agent并行执行计划.md](06-多Agent并行执行计划.md)。

## 1. 今日目标

承接 2026-07-13 计划，完成仓库内仍可开发的前端联调、计划治理与经营报表；外部中间件和上线环境任务保持阻塞，不使用本地模拟冒充验收。

状态只使用：`待开发`、`进行中`、`已完成`、`阻塞`。每完成一项，必须同步更新本表、`tasks/todo.md` 和 `docs/09-开发计划/02-开发日志.md`。

## 2. 新增技能/工具状态

| 项目 | 状态 | 验收 |
| --- | --- | --- |
| `ponytail` 插件技能 | 已安装 | `ponytail:ponytail`、审计/评审/债务等技能可用 |
| `mattpocock/skills` 工程技能集 | 已安装 | 规划、实现、TDD、安全、评审等项目技能可用 |
| `addyosmani/agent-skills` 能力 | 已安装/已纳入本地技能目录 | 前端、测试、性能、安全等相关技能可调用 |
| 供应链系统项目技能 | 已安装 | `supply-chain-system-designer`、`system-design-ddd-refactor` 可用 |

## 3. 今日开发计划表

| 顺序  | 编号                | 优先级 | 功能切片                       | 状态  | 核心验收                                   |
| --- | ----------------- | --- | -------------------------- | --- | -------------------------------------- |
| 1   | PLAN-20260716-001 | P0  | 同步已完成项并建立今日状态表             | 已完成 | 今日计划、任务看板和 QA 开发日志已同步                  |
| 2   | FE-REQ-009        | P1  | 完整业务菜单、登录态和 IAM 动态权限联调     | 已完成 | 供应商订单/质量菜单启用；登录、权限快照与动态菜单来自真实接口；前端测试通过 |
| 3   | DOC-REQ-001       | P1  | 总计划纳入集成、前端、QA、报表和运维索引      | 已完成 | 各类工作具备需求单、状态、依赖和验收入口；历史计划与当前计划可追踪      |
| 4   | RPT-REQ-001       | P3  | 统一经营分析与异步报表                | 已迁移 | `ARCH-REQ-001` 后按事实主权拆入 OMS、库存、BMS、供应商系统 |
| 5   | INT-REQ-005B      | P1  | RocketMQ、Dubbo 与外部财税支付真实联调 | 阻塞  | 需要中间件、注册中心、ERP/税务/支付沙箱凭证               |
| 6   | OPS-REQ-001       | P2  | 监控、告警、备份恢复与上线演练            | 阻塞  | 需要可运行集成/生产预演环境、测试数据和告警通道               |
| 7   | RPT-20260716-001  | P3  | 生成本轮开发日志报表                 | 待开发 | 汇总完成项、接口、DDL、事件、测试、阻塞和剩余风险             |

## 4. 已完成基线

| 检查点 | 状态 | 结果 |
| --- | --- | --- |
| 安全与可靠性 | 已完成 | 统一 JWT、权限/数据范围、采购持久化幂等和集成重试完成 |
| 调拨闭环 | 已完成 | 中央库存、WMS、TMS 调出、在途、调入、差异确认完成 |
| 售后逆向 | 已完成 | OMS、WMS、库存、BMS 退货质检、处置、退款可靠性完成 |
| 真实数据库 QA | 已完成 | MySQL 8/Flyway V1-V5、MyBatis、唯一约束、乐观锁通过 |

## 5. 执行顺序

当日原顺序为 `FE-REQ-009 -> DOC-REQ-001 -> RPT-REQ-001 -> 开发日志报表`；该顺序已由 `ARCH-REQ-001` 和当前多 Agent 计划替代。阻塞项仅在外部条件满足后转为进行中。

## 继续上下文

当前结论：本文是历史快照；其中前端权限和计划索引已经完成，独立报表方案已经迁移。
关键假设：真实中间件、财税支付沙箱和上线演练环境仍未提供。
待决问题：阻塞项所需地址、凭证、Topic、注册中心和测试数据。
下一步：按 `06-多Agent并行执行计划.md` 领取当前任务。

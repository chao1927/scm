# 供应链系统开发日志

## 使用规则

- 每完成一个可验收功能切片，立即在对应系统下新增一条记录；不得只在聊天中说明完成。
- 记录“新增、修改、删除、迁移、接口、事件、测试、已知问题”，保证其他模型可无缝接手。
- 不删除历史记录；修复通过新增日志关联原编号完成。
- 状态仅允许：`计划中`、`开发中`、`已完成`、`已回滚`、`阻塞`。

## 供应商系统

### SUP-LOG-001 | 已完成 | 供应商服务第一阶段至部分运营能力

| 项目    | 内容                                                                    |
| ----- | --------------------------------------------------------------------- |
| 新增    | 准入、资质、联系人/账号、供应商商品、报价、合同、采购确认、ASN、质量、退供、对账、发票、评分、运营、Outbox/Inbox 基础实现 |
| 修改    | 增加跨系统主动命令、采购订单变更/取消/关闭、入站真实重放、合同草稿版本、ASN 行级收货和 TMS 轨迹                 |
| 删除    | 无                                                                     |
| 数据迁移  | `V1` 至 `V24` 供应商服务迁移，包含集成命令、收件箱载荷、合同历史、质量证据、ASN收货/轨迹等                 |
| 接口/事件 | 见 `docs/06/01`、`docs/07/01`；新增接口必须在后续日志逐项登记                           |
| 验证    | JDK 21 离线 `mvn test -q` 已通过；尚缺真实 MySQL/RocketMQ/Dubbo 集成测试            |
| 待办    | 其余系统尚未创建独立后端服务；供应商服务仍需持续补齐 API 测试、导出、对账和全量运营报表                        |

### SUP-LOG-002 | 已完成 | 供应商系统工作台、导出任务与运营报表补齐

| 项目      | 内容                                                                                                                                                                                                                                                                                                                                                                              |
| ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 需求/设计来源 | `docs/09-开发计划/01-开发计划/01-供应商系统接口级开发计划.md` 的 `SUP-API-001`、`SUP-API-008`，以及供应商系统缺口检查结论                                                                                                                                                                                                                                                                                           |
| 新增      | `SupplierWorkbenchController`、`SupplierWorkbenchApplicationService`、`SupplierWorkbenchMapper`；`SupplierReportController`、`SupplierReportApplicationService`、`SupplierReportMapper`；运营导出任务接口和 `OperationViews.ExportTask`                                                                                                                                                        |
| 修改      | `SupplierOperationsApplicationService` 增加导出任务创建、查询、完成、失败能力；`SupplierOperationsController` 增加 `/exports` 相关接口；`SupplierOperationsMapper` 增加导出任务持久化方法                                                                                                                                                                                                                             |
| 删除      | 无                                                                                                                                                                                                                                                                                                                                                                               |
| 数据迁移    | `supplier-service/src/main/resources/db/migration/V25__supplier_workbench_export.sql`，新增 `sup_export_task` 导出任务表                                                                                                                                                                                                                                                                |
| 接口      | `GET /api/supplier/v1/workbench/summary`；`GET /api/supplier/v1/reports/fulfillment`；`GET /api/supplier/v1/reports/exceptions`；`POST /api/supplier/v1/operations/exports`；`GET /api/supplier/v1/operations/exports`；`GET /api/supplier/v1/operations/exports/{id}`；`POST /api/supplier/v1/operations/exports/{id}/complete`；`POST /api/supplier/v1/operations/exports/{id}/fail` |
| 事件      | 本切片不新增领域事件；工作台和报表读取供应商本地投影，导出任务写操作记录审计                                                                                                                                                                                                                                                                                                                                          |
| 测试      | 新增 `SupplierWorkbenchApplicationServiceTest`、`SupplierOperationsExportTest`，覆盖供应商范围收敛、统计天数校验、导出类型校验、导出任务创建和完成                                                                                                                                                                                                                                                                   |
| 风险/待办   | 供应商系统业务面已基本补齐；仍缺真实 MySQL/Flyway 集成测试、真实 RocketMQ/Dubbo 联调测试、Controller 权限/参数 Web 测试，以及导出文件生成器/对象存储上传实现                                                                                                                                                                                                                                                                          |
| 交接提示    | 下一步建议进入采购系统基础设施补齐：采购 Outbox 投递、Inbox 重放、WMS/TMS/BMS/IAM 事件消费和 Dubbo ACL 客户端                                                                                                                                                                                                                                                                                                     |

## 采购系统

### PUR-LOG-001 | 已完成 | 采购请购单纵向切片

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/01-开发计划/02-采购系统接口级开发计划.md` 的 `PUR-API-001`、`PUR-API-002` |
| 新增 | `purchase-service` Maven 模块；`PurchaseRequisitionController`；`PurchaseRequisitionApplicationService`；`PurchaseRequisitionQueryApplicationService`；`PurchaseRequisitionAggregate`、`PurchaseRequisitionLine`、`PurchaseRequisitionStatus`；MyBatis 请购资源库、读模型、Outbox、操作日志 |
| 修改 | 根 `pom.xml` 增加 `purchase-service` 模块 |
| 删除 | 无 |
| 数据迁移 | `purchase-service/src/main/resources/db/migration/V1__purchase_requisition.sql`，新增请购头、请购行、采购 Outbox、采购操作日志 |
| 接口 | `GET /api/purchase/v1/requisitions`、`GET /api/purchase/v1/requisitions/{id}`、`POST /api/purchase/v1/requisitions`、`PUT /api/purchase/v1/requisitions/{id}`、`POST /submit`、`POST /approve`、`POST /reject`、`POST /convert`；写接口要求 `X-Idempotency-Key`，支持 `X-Purchase-Org-Id` 数据范围 |
| 事件 | 生产 `PurchaseRequisitionCreated`、`PurchaseRequisitionChanged`、`PurchaseRequisitionSubmitted`、`PurchaseRequisitionApproved`、`PurchaseRequisitionRejected`、`PurchaseRequisitionConverted`，写入 `purchase_outbox_event` |
| 测试 | 新增 `PurchaseRequisitionAggregateTest` 覆盖重复 SKU+需求日期、提交审批转采购状态机、超批准数量转采购 |
| 风险/待办 | 幂等当前为内存实现，后续需迁移到 Redis/MySQL 幂等表；主数据 SKU、预算和审批 ACL 暂未真正调用；Outbox 投递任务、Inbox 审批回调将在采购后续接口切片补齐 |
| 交接提示 | 下一步实现 `PUR-API-003` RFQ 询价发布与截标，需复用本模块的 Outbox、审计、命令上下文和领域事件模式 |

### PUR-LOG-002 | 已完成 | RFQ 询价发布与截标纵向切片

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/01-开发计划/02-采购系统接口级开发计划.md` 的 `PUR-API-003`，以及 `docs/03-核心业务模型/02-采购领域模型/03-询价单聚合CQRS设计.md` |
| 新增 | `RfqController`；`RfqApplicationService`；`RfqQueryApplicationService`；`RfqAggregate`、`RfqLine`、`RfqInvitation`、`RfqStatus`；MyBatis RFQ 资源库、读模型、查询 Mapper |
| 修改 | `CommandContextFactory` 本地默认权限补充 `purchase:rfq:create/publish/close/read` |
| 删除 | 无 |
| 数据迁移 | `purchase-service/src/main/resources/db/migration/V2__purchase_rfq.sql`，新增 RFQ 头、询价行、邀请供应商三张表 |
| 接口 | `GET /api/purchase/v1/rfqs`、`GET /api/purchase/v1/rfqs/{rfqNo}`、`POST /api/purchase/v1/rfqs`、`POST /api/purchase/v1/rfqs/{rfqNo}/publish`、`POST /api/purchase/v1/rfqs/{rfqNo}/close`；写接口要求 `X-Idempotency-Key`，支持 `X-Purchase-Org-Id` 数据范围 |
| 事件 | 生产 `RfqCreated`、`RfqPublished`、`RfqBiddingClosed`；`RfqPublished` 按邀请供应商拆分事件写入 `purchase_outbox_event`，便于供应商系统创建对应报价待办 |
| 测试 | 新增 `RfqAggregateTest` 覆盖重复供应商、发布按供应商拆分事件、截标关闭供应商待办、重复发布状态冲突 |
| 风险/待办 | 供应商可协同、SKU 可采购、品类资质校验暂未真正调用主数据/供应商 ACL；RFQ 修改、取消、系统自动到期截标、供应商报价消费将在后续报价/比价切片补齐 |
| 交接提示 | 下一步实现 `PUR-API-004` 比价、定标与采购价格，需消费供应商报价并以 RFQ 截标状态作为比价前置条件 |

### PUR-LOG-003 | 已完成 | 比价、定标与采购价格纵向切片

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/01-开发计划/02-采购系统接口级开发计划.md` 的 `PUR-API-004`，`docs/03-核心业务模型/02-采购领域模型/05-比价结果聚合CQRS设计.md`，`06-采购价格聚合CQRS设计.md` |
| 新增 | `BidComparisonController`、`PurchasePriceController`；比价/采购价格应用服务、查询服务、读模型端口；`BidComparisonAggregate`、`BidCandidate`、`PurchasePriceAggregate`；MyBatis 比价与价格资源库/读模型 |
| 修改 | `CommandContextFactory` 本地默认权限补充 `purchase:comparison:*`、`purchase:price:*` |
| 删除 | 无 |
| 数据迁移 | `purchase-service/src/main/resources/db/migration/V3__comparison_and_price.sql`，新增比价结果、比价候选快照、采购价格三张表 |
| 接口 | `GET/POST /api/purchase/v1/bid-comparisons`、`GET /api/purchase/v1/bid-comparisons/{compareNo}`、`POST /api/purchase/v1/bid-comparisons/{compareNo}/award`、`GET/POST /api/purchase/v1/purchase-prices`、`GET /api/purchase/v1/purchase-prices/{priceNo}`、`POST /api/purchase/v1/purchase-prices/{priceNo}/disable` |
| 事件 | 生产 `CompareResultGenerated`、`CompareResultAwarded`、`PurchasePriceActivated`、`PurchasePriceDisabled`，写入采购 Outbox；定标时可按请求直接生成采购价格 |
| 测试 | 新增 `BidComparisonAggregateTest`、`PurchasePriceAggregateTest`，覆盖候选数量、定标候选校验、单一中标、价格含税价计算、有效期校验、价格停用 |
| 风险/待办 | 供应商报价仍由接口传入快照，尚未接入供应商报价事件消费；供应商合同/协议有效性、TMS 历史运输表现、BMS 价格结算约束暂未通过远程 ACL 校验 |
| 交接提示 | 下一步实现 `PUR-API-005` 采购订单发布、变更与取消，需要复用采购价格作为 PO 取价来源，并接入供应商合同/价格协议校验 |

### PUR-LOG-004 | 已完成 | 采购订单发布、变更与取消纵向切片

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/01-开发计划/02-采购系统接口级开发计划.md` 的 `PUR-API-005`，`docs/03-核心业务模型/02-采购领域模型/07-采购订单聚合CQRS设计.md` |
| 新增 | `PurchaseOrderController`、`PurchaseOrderChangeController`；采购订单/变更单应用服务、查询服务、读模型端口；`PurchaseOrderAggregate`、`PurchaseOrderLine`、`PurchaseOrderChangeAggregate`；MyBatis 订单与变更单资源库/读模型 |
| 修改 | `CommandContextFactory` 本地默认权限补充 `purchase:po:*`、`purchase:po-change:*` |
| 删除 | 无 |
| 数据迁移 | `purchase-service/src/main/resources/db/migration/V4__purchase_order.sql`，新增采购订单、采购订单行、采购订单变更单三张表 |
| 接口 | `GET/POST /api/purchase/v1/purchase-orders`、`GET /api/purchase/v1/purchase-orders/{orderNo}`、`POST /submit`、`POST /approve`、`POST /publish`、`POST /cancel`、`GET/POST /api/purchase/v1/order-changes`、`GET /api/purchase/v1/order-changes/{changeNo}`、`POST /api/purchase/v1/order-changes/{changeNo}/approve` |
| 事件 | 生产 `PurchaseOrderCreated`、`PurchaseOrderSubmitted`、`PurchaseOrderApproved`、`PurchaseOrderRejected`、`PurchaseOrderPublished`、`PurchaseOrderCancelled`、`PurchaseOrderChangeCreated`、`PurchaseOrderChangeEffective`、`PurchaseOrderChangeRejected` |
| 测试 | 新增 `PurchaseOrderAggregateTest`、`PurchaseOrderChangeAggregateTest`，覆盖提交审批发布状态机、已入库不可取消、变更生效更新版本、变更单重复审批冲突 |
| 风险/待办 | 供应商合同/价格协议、采购价格取价、仓库/SKU主数据、供应商协同配置暂未通过远程 ACL 校验；发布后的供应商/WMS/BMS/TMS 主动命令暂由 Outbox 事件承接，后续需补集成命令和失败补偿 |
| 交接提示 | 下一步实现 `PUR-API-006` 到货跟踪与退供申请，并补齐 PO 发布后供应商确认、WMS/TMS/BMS 事件消费 |

### PUR-LOG-005 | 已完成 | 到货跟踪与退供申请纵向切片

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/01-开发计划/02-采购系统接口级开发计划.md` 的 `PUR-API-006`，`docs/03-核心业务模型/02-采购领域模型/09-入库跟踪聚合CQRS设计.md`，`10-退供申请聚合CQRS设计.md` |
| 新增 | `InboundTrackingController`、`SupplierReturnController`；到货跟踪/退供申请应用服务、查询服务、读模型端口；`InboundTrackingAggregate`、`SupplierReturnAggregate`、`SupplierReturnLine`；MyBatis 到货跟踪与退供申请资源库/读模型 |
| 修改 | `CommandContextFactory` 本地默认权限补充 `purchase:inbound:*`、`purchase:supplier-return:*` |
| 删除 | 无 |
| 数据迁移 | `purchase-service/src/main/resources/db/migration/V5__inbound_and_supplier_return.sql`，新增采购到货跟踪、退供应商申请、退供应商申请行三张表 |
| 接口 | `GET /api/purchase/v1/inbounds`、`GET /api/purchase/v1/inbounds/{inboundNo}`、`POST /api/purchase/v1/inbounds/asns`、`POST /api/purchase/v1/inbounds/{inboundNo}/sync-wms`、`GET /api/purchase/v1/supplier-returns`、`GET /api/purchase/v1/supplier-returns/{returnNo}`、`POST /api/purchase/v1/supplier-returns`、`POST /api/purchase/v1/supplier-returns/{returnNo}/submit`、`POST /api/purchase/v1/supplier-returns/{returnNo}/approve`、`POST /api/purchase/v1/supplier-returns/{returnNo}/notify-execution` |
| 事件 | 生产 `PurchaseAsnRecorded`、`PurchaseGoodsReceived`、`PurchaseInspectionCompleted`、`PurchaseGoodsPutawayCompleted`、`SupplierReturnCreated`、`SupplierReturnSubmitted`、`SupplierReturnApproved`、`SupplierReturnRejected`、`SupplierReturnExecutionNotified`，统一写入采购 Outbox |
| 测试 | 新增 `InboundTrackingAggregateTest`、`SupplierReturnAggregateTest`，覆盖 ASN 记录、WMS 回传上架、超 ASN 数量拦截、退供数量约束、提交审批通知执行状态机 |
| 风险/待办 | 目前已具备采购侧到货/退供业务壳和 Outbox 事件；尚未真正主动调用 WMS 创建预约/出库、TMS 建运输任务、中央库存锁定/释放、BMS 冲减，也尚未接入 WMS/TMS/BMS RocketMQ 消费器 |
| 交接提示 | 下一步建议进入采购系统事件消费与跨系统集成命令切片，优先补 `PUR-EVT-001` 采购订单变更/取消/关闭消费、`PUR-EVT-002` WMS 收货/质检/上架消费、`PUR-EVT-003` TMS 运输轨迹消费 |

### PUR-LOG-006 | 已完成 | 采购系统事件投递、入站消费与跨系统命令补齐

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | 采购系统缺口检查结论；`docs/09-开发计划/01-开发计划/02-采购系统接口级开发计划.md` 的 `PUR-API-003` 至 `PUR-API-006` 事件消费和跨系统交互要求 |
| 新增 | 采购 Outbox 投递端口、投递应用服务、定时投递任务、RocketMQ 适配器；采购 Inbox 入站事件日志、原始载荷保存、人工重放服务；统一外部事件入口 `PurchaseExternalEventController`；供应商报价/确认、WMS入库、TMS运输、BMS应付事实投影；采购集成命令写入器 |
| 修改 | `PurchaseServiceApplication` 启用调度；`EventPersistenceMapper` 增加 Outbox claim/发布/失败方法；`PurchaseOrderApplicationService.publish` 发布后写供应商/WMS/BMS 集成命令；`SupplierReturnApplicationService.notifyExecution` 通知执行后写库存/WMS/TMS/BMS 集成命令；`InboundTrackingApplicationService` 增加 WMS 事件同步方法 |
| 删除 | 无 |
| 数据迁移 | `V6__purchase_inbox_and_integration.sql` 新增 `purchase_inbox_event`、`purchase_integration_command`；`V7__purchase_external_facts.sql` 新增供应商报价事实、供应商确认事实、WMS入库事实、TMS运输事实、BMS应付事实 |
| 接口 | `POST /internal/purchase/v1/events` 接收供应商/WMS/TMS/BMS/IAM 等外部事件；`GET /api/purchase/v1/operations/failed-events` 查询失败入站事件；`POST /api/purchase/v1/operations/failed-events/{id}/replay` 人工重放失败事件 |
| 事件 | Outbox 支持 RocketMQ 投递和失败重试；Inbox 支持幂等 claim、成功/忽略/失败状态、原始载荷保存和人工重放；统一消费 `SupplierQuoteSubmitted/Changed/Voided/Adopted`、`PurchaseOrderConfirmed/Rejected/DifferenceSubmitted`、`WmsReceiptCompleted/WmsQualityInspectionCompleted/WmsPutawayCompleted`、`TmsTransport*`、`BmsPayable*/BmsSettlementCompleted` |
| 测试 | 新增 `OutboxDispatchApplicationServiceTest`、`PurchaseExternalEventConsumerApplicationServiceTest`，覆盖 Outbox 成功/失败投递、外部报价事件幂等消费和事实投影写入 |
| 风险/待办 | 当前已具备可靠事件和命令箱骨架；尚未实现 `purchase_integration_command` 的 Dubbo 分发器和真实远程 ACL；供应商确认事实暂未反写采购订单状态，后续应给采购订单聚合补确认/差异状态迁移方法后接入 |
| 交接提示 | 下一步建议实现采购集成命令分发器：按 `target_system + command_type` 调 WMS/TMS/BMS/供应商系统 Dubbo/OpenAPI，并补失败重试、熔断和补偿 |

### PUR-LOG-007 | 已完成 | 供应商订单确认事件驱动采购订单状态

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/01-开发计划/02-采购系统接口级开发计划.md` 的 `PUR-API-005` 供应商确认消费要求；`docs/03-核心业务模型/02-采购领域模型/07-采购订单聚合CQRS设计.md` 的供应商确认状态机和 Inbox 消费规则 |
| 新增 | `PurchaseOrderApplicationService.recordSupplierResponse`；采购订单供应商确认、拒绝、差异反馈三个聚合行为；采购订单状态 `SUPPLIER_REJECTED`；迁移 `V8__purchase_order_supplier_rejected_status.sql`；`PurchaseOrderSupplierResponseApplicationServiceTest` |
| 修改 | `PurchaseExternalEventConsumerApplicationService` 支持供应商系统实际投递的 `PurchaseOrderConfirmedBySupplier`、`PurchaseOrderRejectedBySupplier`、`PurchaseOrderDifferenceReportedBySupplier`，并兼容原事件别名；消费后先写确认事实投影，再推进采购订单聚合并写本地 Outbox、审计日志 |
| 删除 | 无 |
| 数据迁移 | `V8__purchase_order_supplier_rejected_status.sql`，补充采购订单状态码 `12=供应商已拒绝` 的数据库字段说明；既有数据无需修复 |
| 接口 | 复用 `POST /internal/purchase/v1/events`；确认类事件必须提供 `X-Source-System`、`X-Event-Code`、`eventType`、`orderNo` 或 `businessNo`、`supplierId`；拒绝事件额外要求 `reason` |
| 事件 | 外部事件先进入 `purchase_inbox_event` 幂等处理；本地生产 `SupplierOrderConfirmationRecorded`、`SupplierOrderRejectionRecorded`、`SupplierOrderDifferenceRecorded`，写入 `purchase_outbox_event` |
| 测试 | 扩展 `PurchaseOrderAggregateTest` 覆盖确认、拒绝、差异状态机和禁止迁移；新增应用服务测试覆盖供应商确认后订单状态、审计和 Outbox 领域事件；JDK 21 离线 `mvn test -q` 已通过 |
| 风险/待办 | 供应商确认差异的行级数量、价格、交期仍保留在外部事实载荷中，尚未形成采购侧可操作的差异处理聚合；PO 发布时产生的 WMS/TMS 命令缺少 ASN、仓库 ID 等 Dubbo 合同字段，不能直接分发，需在 ASN 创建和退供审批切片补齐后再接入远程 ACL |
| 交接提示 | 下一步先实现供应商确认差异处理单（接受差异、重新协商、取消订单），再补采购订单变更、取消、关闭事件向供应商/WMS/BMS 的同步与补偿 |

### PUR-LOG-008 | 已完成 | 供应商确认差异处理页与采购订单协商闭环

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/06-子系统接口设计/02-采购系统接口设计.md` 的 5.9 供应商确认处理页；采购订单状态机“供应商差异 -> 供应商已确认/待供应商确认/已取消” |
| 新增 | `SupplierConfirmController`、`SupplierConfirmApplicationService`、`SupplierConfirmCommands`、`SupplierConfirmView`、`SupplierConfirmMapper`；迁移 `V9__purchase_supplier_confirm_processing.sql` 为供应商确认事实增加处理状态、处理人、处理时间、处理版本 |
| 修改 | `PurchaseOrderAggregate` 增加接受差异、重新协商行为；`PurchaseOrderApplicationService` 增加对应应用服务入口；`CommandContextFactory` 增加 `purchase:supplier_confirm:*` 权限点；采购订单领域测试增加协商状态机覆盖 |
| 删除 | 无 |
| 数据迁移 | `V9__purchase_supplier_confirm_processing.sql`，处理状态：1待处理、2已接受差异、3协商中、4已取消订单；以 `process_version` 做确认事实的乐观锁 |
| 接口 | `GET /api/purchase/v1/supplier-confirms`；`GET /api/purchase/v1/supplier-confirms/{confirmId}`；`POST /{confirmId}/accept-diff`；`POST /{confirmId}/renegotiate`；`POST /{confirmId}/cancel-order`。写接口继承公共契约，要求幂等键、数据范围和版本号 |
| 事件 | 接受差异生产 `SupplierOrderDifferenceAccepted`；重新协商生产 `SupplierOrderRenegotiationRequested`；取消沿用 `PurchaseOrderCancelled`；均与采购订单、审计和 Outbox 同事务保存 |
| 测试 | 扩展 `PurchaseOrderAggregateTest`，覆盖接受差异、重新协商和状态迁移限制；JDK 21 离线 `mvn test -q` 已通过 |
| 风险/待办 | 当前差异的行级数量/价格/交期仍保存在供应商原始事件载荷，尚未形成独立的采购订单变更单；重新协商事件已写 Outbox，但供应商系统需在后续消费 `SupplierOrderRenegotiationRequested` 或标准化 `PurchaseOrderChanged` 后创建新待办 |
| 交接提示 | 下一步实现采购订单关闭剩余数量及对供应商、WMS、BMS 的终止/关闭同步；随后补 ASN 创建后基于完整字段的 WMS 预约和 TMS 发运命令 |

### PUR-LOG-009 | 已完成 | 采购订单关闭剩余数量

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/03-核心业务模型/02-采购领域模型/01-采购系统领域模型.md` 的采购订单状态机；`docs/06-子系统接口设计/02-采购系统接口设计.md` 的采购关闭约束 |
| 新增 | `PurchaseOrderCommands.Close`、`POST /api/purchase/v1/purchase-orders/{orderNo}/close`、权限点 `purchase:po:close` |
| 修改 | `PurchaseOrderAggregate.closeRemaining` 仅允许供应商已确认、部分入库、供应商差异或供应商拒绝状态关闭，并要求关闭原因；关闭时递增业务版本、进入 `CLOSED`、生产 `PurchaseOrderClosed`。`PurchaseOrderApplicationService` 统一执行组织范围、幂等、乐观锁、审计、Outbox 保存 |
| 删除 | 无 |
| 数据迁移 | 无，复用采购订单既有状态 `10=已关闭` |
| 接口 | 请求体：`version`、`reason`；调用页面：采购订单详情/到货跟踪的“关闭剩余”操作；权限：`purchase:po:close`；写请求必须提供 `X-Idempotency-Key` |
| 事件 | 生产 `PurchaseOrderClosed`，供供应商协同、WMS、BMS 订阅关闭未执行义务；本地 Outbox 支持后续投递与人工重试 |
| 测试 | 扩展 `PurchaseOrderAggregateTest`，覆盖草稿禁止关闭、供应商确认后关闭、关闭事件；JDK 21 离线 `mvn test -q` 已通过 |
| 风险/待办 | 关闭事件的跨系统投递契约仍需补事件信封和供应商/WMS/BMS 消费端映射；已收货/已上架数量由 WMS 和中央库存事实源判定，采购侧当前尚未按行汇总后自动限制关闭数量 |
| 交接提示 | WMS-API-001 将优先实现入库单创建/取消；采购侧随后接入该入库单回执、收货、质检、上架事件，形成采购入库主闭环 |

## WMS 系统

### WMS-LOG-001 | 已完成 | 外部创建/取消入库单纵向切片

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/01-开发计划/03-WMS系统接口级开发计划.md` 的 `WMS-API-001`；`docs/05-子系统数据库设计/03-WMS系统数据库设计.md` 的 `wms_inbound` |
| 新增 | `wms-service` Maven 模块；`WmsServiceApplication`；`InboundOrderAggregate`、`InboundOrderStatus`、`InboundOrderRepository`；`InboundOrderApplicationService`；MyBatis 入库单 Mapper/资源库；`InboundOrderController`；领域测试 |
| 修改 | 根 `pom.xml` 注册 `wms-service` 模块 |
| 删除 | 无 |
| 数据迁移 | `wms-service/src/main/resources/db/migration/V1__wms_inbound_order.sql`，创建 `wms_inbound`，以来源类型、来源单号、仓库组成幂等唯一约束 |
| 接口 | `POST /openapi/wms/v1/inbound-orders`：校验 `X-Source-System` 与 `sourceType`、`X-Idempotency-Key`；`POST /api/wms/v1/inbound-orders/{id}/cancel`：按 `X-Warehouse-Id` 校验仓库范围、版本与取消原因 |
| 事件 | 本切片先固化入库单状态机和来源幂等；WMS Outbox/Inbox 与 `WmsInboundOrderCreated/Cancelled` 事件投递将在 PDA 收货切片统一接入，避免同一入库单拆成两套事件表 |
| 测试 | `InboundOrderAggregateTest` 覆盖待到货入库单取消、取消原因必填；JDK 21 离线 `mvn test -q` 已通过 |
| 风险/待办 | 尚未实现入库行、预约、收货单、扫码流水、Inbox/Outbox；当前创建接口使用来源+来源单号+仓库作为业务幂等边界，后续 ASN 行级变更需通过版本化变更命令处理 |
| 交接提示 | 下一步 `WMS-API-002`：新增收货单/收货行/PDA 扫码与提交，完成后由 Outbox 发送 `WmsArrivalRegistered`、`WmsReceiptCompleted` 给采购、供应商和中央库存 |

### WMS-LOG-002 | 已完成 | 收货、质检、上架核心聚合与数量不变量

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `WMS-API-002`、`WMS-API-003`；WMS 领域模型中“收货不等于质检、质检不等于上架、上架不等于中央库存可售”的边界 |
| 新增 | `ReceiptAggregate`、`ReceiptStatus`、`InspectionAggregate`、`PutawayTaskAggregate`；对应收货、质检、上架领域测试 |
| 修改 | 无 |
| 删除 | 无 |
| 数据迁移 | 无。本切片先落地领域数量规则，下一切片与收货/质检/上架表、扫码流水、库存流水一并持久化，避免中途表结构与聚合边界不一致 |
| 接口 | 后续接入 `POST /api/wms/v1/pda/receipts/scan`、`POST /api/wms/v1/receipts/{receiptNo}/submit`、`POST /api/wms/v1/inspections/{inspectionNo}/result`、`POST /api/wms/v1/pda/putaway/scan` |
| 事件 | 领域规则已明确为收货完成、质检完成、上架完成三类独立事实；Outbox 载荷、生产和消费将在持久化切片实现 |
| 测试 | `ReceiptAggregateTest`、`InspectionAggregateTest`、`PutawayTaskAggregateTest`，覆盖拒收原因、收货数量平账、质检数量平账、上架不超合格数量；JDK 21 离线 `mvn test -q` 已通过 |
| 风险/待办 | 本切片尚未接入数据库、PDA 权限、设备、仓库库位容量、库存流水、Outbox/Inbox，不可独立作为生产接口发布 |
| 交接提示 | 继续实施 WMS 收货持久化与 PDA 接口，并在同一事务创建质检单、上架任务、事件发布表和库存流水 |

### WMS-LOG-003 | 已完成 | 收货持久化、PDA 扫码幂等与收货完成事件

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/WMS-REQ-001-收货持久化与PDA扫码.md`；`docs/09-开发计划/01-开发计划/03-WMS系统接口级开发计划.md` 的 `WMS-API-002` |
| 新增 | `ReceivingController`；`ReceivingApplicationService`；`ReceiptRepository` MyBatis 实现；`wms_receipt`、`wms_receipt_scan`、`wms_operation_event` 相关 Mapper；应用服务测试 `ReceivingApplicationServiceTest` |
| 修改 | 整理 `ReceivingApplicationService`、`ReceivingController`、`ReceiptAggregate`、`ReceiptScanMapper`、`MyBatisWmsEventPublisher` 为正常 Java 多行格式；保留既有状态机和持久化口径 |
| 删除 | 无 |
| 数据迁移 | `V2__wms_operation_tables.sql` 创建 `wms_receipt`、`wms_operation_event`；`V4__wms_receipt_scan.sql` 创建 PDA 扫码幂等流水 |
| 接口 | `POST /api/wms/v1/receipts`；`POST /api/wms/v1/pda/receipts/scan`；`POST /api/wms/v1/receipts/{receiptNo}/submit`。扫码接口要求 `X-Idempotency-Key`，提交接口用收货单版本防并发 |
| 事件 | 打开收货单发布 `WmsArrivalRegistered`；提交收货发布 `WmsReceiptCompleted`，写入 WMS Outbox 表 `wms_operation_event` |
| 测试 | 新增 `ReceivingApplicationServiceTest`，覆盖打开收货、扫码、提交、事件发布、扫码幂等、缺少幂等键和版本冲突；全量 `mvn test -q` 已通过 |
| 风险/待办 | 当前仍未做真实 PDA 用户/设备/仓库数据权限校验；收货模型暂按单 SKU 收货单处理，多行 ASN 需在后续入库行/收货行切片补齐；质检/上架持久化和库存流水记账仍待开发 |
| 交接提示 | 下一步实施 `WMS-API-003`：质检结果、上架任务持久化、库位/库存流水、`QualityInspectionCompleted` 与 `PutawayCompleted` 事件 |

### WMS-LOG-004 | 已完成 | 质检结果、上架任务与仓内库存流水

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/WMS-REQ-002-质检上架与仓内库存流水.md`；`docs/09-开发计划/01-开发计划/03-WMS系统接口级开发计划.md` 的 `WMS-API-003` |
| 新增 | `InspectionApplicationServiceTest`、`PutawayApplicationServiceTest`，覆盖质检完成事件、上架库存流水、上架完成事件和版本冲突 |
| 修改 | 整理 `InspectionAggregate`、`PutawayTaskAggregate`、`InspectionApplicationService`、`PutawayApplicationService`、`InspectionController`、`PutawayController`、`InspectionMapper`、`PutawayMapper`、`StockLedgerMapper` 为正常 Java 多行格式 |
| 删除 | 无 |
| 数据迁移 | 复用 `V2__wms_operation_tables.sql` 的 `wms_inspection`、`wms_putaway_task`、`wms_stock_ledger`、`wms_operation_event` |
| 接口 | `POST /api/wms/v1/inspections`；`POST /api/wms/v1/inspections/{no}/result`；`POST /api/wms/v1/putaway-tasks`；`POST /api/wms/v1/pda/putaway/scan` |
| 事件 | 质检完成发布 `WmsQualityInspectionCompleted`；上架任务完成发布 `WmsPutawayCompleted`；上架扫描写 `wms_stock_ledger`，交易类型 `PUTAWAY_IN` |
| 测试 | `InspectionApplicationServiceTest`、`PutawayApplicationServiceTest` 加应用服务覆盖；全量 `mvn test -q` 已通过 |
| 风险/待办 | 上架扫描暂未做独立幂等键；库位容量、温层、混放、不合格区和中央库存账户同步仍待后续切片；WMS Outbox 仍缺真实投递任务和失败重试 |
| 交接提示 | 下一步按排序实施 `WMS-REQ-003`：WMS Outbox/Inbox 投递、入站命令、失败重放；随后进入中央库存 `INV-01` |

### WMS-LOG-005 | 已完成 | WMS Outbox 投递、失败查询与人工重试

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/WMS-REQ-003-Outbox投递与失败重试.md`；`docs/09-开发计划/01-开发计划/03-WMS系统接口级开发计划.md` 的 WMS 事件可靠性要求 |
| 新增 | `WmsOutboxDispatchApplicationService`、`WmsMessageBrokerPort`、`LoggingWmsMessageBrokerAdapter`、`WmsOperationsController`、`WmsOutboxDispatchApplicationServiceTest` |
| 修改 | `WmsEventMapper` 增加待投递查询、成功标记、失败标记、失败列表、人工重试方法 |
| 删除 | 无 |
| 数据迁移 | 复用 `V2__wms_operation_tables.sql` 的 `wms_operation_event`，状态：1 待投递、2 已投递、3 投递失败 |
| 接口 | `POST /api/wms/v1/operations/outbox/dispatch`；`GET /api/wms/v1/operations/outbox/failed-events`；`POST /api/wms/v1/operations/outbox/failed-events/{eventId}/retry` |
| 事件 | 覆盖所有 WMS 业务事件的 Outbox 投递骨架；当前消息端口为日志适配器，后续可替换 RocketMQ Producer |
| 测试 | `WmsOutboxDispatchApplicationServiceTest` 覆盖成功投递、失败标记、失败查询和人工重试；全量 `mvn test -q` 已通过 |
| 风险/待办 | 尚未实现真实 RocketMQ Producer、定时任务调度、Inbox 入站消费和死信重放；接口权限目前仍依赖后续 IAM |
| 交接提示 | 下一步实现 WMS Inbox 入站事件/命令消费；之后进入 `WMS-API-004` 外部创建出库单与库存分配 |

### WMS-LOG-006 | 已完成 | WMS Inbox 入站命令、失败查询与人工重放

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/WMS-REQ-004-Inbox入站命令与失败重放.md`；`WMS-API-001`、`WMS-API-004` 的外部命令入站幂等要求 |
| 新增 | `WmsInboxMapper`、`WmsInboundEventApplicationService`、`WmsInboundEventController`、`WmsInboundEventApplicationServiceTest` |
| 修改 | 无 |
| 删除 | 无 |
| 数据迁移 | 复用 `V3__wms_inbox_and_audit.sql` 的 `wms_inbox_event`，状态：1处理中、2成功、3失败 |
| 接口 | `POST /internal/wms/v1/events`；`GET /api/wms/v1/operations/inbox/failed-events`；`POST /api/wms/v1/operations/inbox/failed-events/{inboxId}/replay` |
| 事件 | 支持 `CreateInboundOrderRequested` 分发到入库单创建；支持 `CreateOutboundOrderRequested` 分发到出库单创建；以 `sourceSystem + eventCode` 做 Inbox 幂等 |
| 测试 | `WmsInboundEventApplicationServiceTest` 覆盖入库命令幂等、出库命令创建、失败事件查询与人工重放；全量 `mvn test -q` 已通过 |
| 风险/待办 | 当前入站入口为 HTTP 内部接口，尚未接真实 RocketMQ Listener 和签名验签；仅覆盖入库/出库创建命令，取消/变更命令将在出库分配切片继续扩展 |
| 交接提示 | 下一步进入 `WMS-API-004`：外部创建出库单、库存分配、取消释放和出库异常 |

### WMS-LOG-007 | 已完成 | 外部创建出库单、分配与取消

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/WMS-REQ-005-外部出库单与库存分配.md`；`docs/09-开发计划/01-开发计划/03-WMS系统接口级开发计划.md` 的 `WMS-API-004` |
| 新增 | `OutboundApplicationServiceTest`；扩展 `OutboundOrderAggregateTest` 覆盖取消原因和重复取消 |
| 修改 | 整理 `OutboundOrderAggregate`、`OutboundApplicationService`、`OutboundMapper`、`OutboundOrderController` 为正常 Java 多行格式；取消状态机增加重复取消拦截 |
| 删除 | 无 |
| 数据迁移 | 复用 `V2__wms_operation_tables.sql` 的 `wms_outbound` |
| 接口 | `POST /openapi/wms/v1/outbound-orders`；`POST /api/wms/v1/outbound-orders/allocate`；`POST /openapi/wms/v1/outbound-orders/cancel` |
| 事件 | 创建发布 `WmsOutboundOrderCreated`；分配发布 `WmsOutboundAllocated`；取消发布 `WmsOutboundCancelled`，统一进入 WMS Outbox |
| 测试 | `OutboundApplicationServiceTest` 覆盖创建、重复创建幂等、分配、取消、事件发布、版本冲突；全量 `mvn test -q` 已通过 |
| 风险/待办 | 当前出库单仍缺明细行、真实库位/批次分配、中央库存预占校验和取消释放库存；这些进入 `WMS-API-005` 波次拣货前补齐 |
| 交接提示 | 下一步实施 `WMS-API-005`：波次、拣货、容器与复核包装 |

### WMS-LOG-008 | 已完成 | 波次释放与拣货任务

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/WMS-REQ-006-波次释放与拣货任务.md`；`docs/09-开发计划/01-开发计划/03-WMS系统接口级开发计划.md` 的 `WMS-API-005` 前半段 |
| 新增 | `WaveMapper`、`PickTaskMapper`、`WavePickingApplicationService`、`WavePickingController`、`WavePickingApplicationServiceTest` |
| 修改 | 整理 `WaveAggregate`、`PickTaskAggregate` 和领域测试为正常 Java 多行格式 |
| 删除 | 无 |
| 数据迁移 | 复用 `V2__wms_operation_tables.sql` 的 `wms_wave`、`wms_pick_task` |
| 接口 | `POST /api/wms/v1/waves`；`POST /api/wms/v1/waves/release`；`POST /api/wms/v1/pick-tasks`；`POST /api/wms/v1/pda/pick-tasks/scan` |
| 事件 | 创建波次发布 `WmsWaveCreated`；释放波次发布 `WmsWaveReleased`；拣货完成发布 `WmsPickCompleted` |
| 测试 | `WavePickingApplicationServiceTest` 覆盖波次创建/释放、拣货任务创建、分批拣货完成、重复创建幂等、版本冲突；全量 `mvn test -q` 已通过 |
| 风险/待办 | 尚未实现容器绑定、复核包装、短拣异常、路径推荐和真实库存占用释放；`WMS-API-005` 仍需继续完成后半段 |
| 交接提示 | 下一步继续 `WMS-API-005`：容器绑定、复核包装、短拣异常；完成后进入 `WMS-API-006` 发货交接、盘点、仓内异常 |

### WMS-LOG-009 | 已完成 | 容器绑定、封箱与复核包装

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/WMS-REQ-007-容器绑定与复核包装.md`；`docs/09-开发计划/01-开发计划/03-WMS系统接口级开发计划.md` 的 `WMS-API-005` 后半段 |
| 新增 | `ContainerAggregate`、`PackingAggregate`、`ContainerMapper`、`PackingMapper`、`PackingApplicationService`、`PackingController`、`PackingApplicationServiceTest` |
| 修改 | 无 |
| 删除 | 无 |
| 数据迁移 | `V5__wms_container_and_packing.sql` 新增 `wms_container`、`wms_packing` |
| 接口 | `POST /api/wms/v1/containers/bind`；`POST /api/wms/v1/containers/seal`；`POST /api/wms/v1/packing`；`POST /api/wms/v1/packing/verify` |
| 事件 | 容器绑定发布 `WmsContainerBound`；封箱发布 `WmsContainerSealed`；复核包装发布 `WmsPackingVerified` |
| 测试 | `PackingApplicationServiceTest` 覆盖容器绑定、封箱、复核包装创建/确认、重复创建幂等、版本冲突；全量 `mvn test -q` 已通过 |
| 风险/待办 | 尚未实现包裹明细、称重、面单打印、短拣异常和 TMS 运单联动；进入 `WMS-API-006` 发货交接时继续补 |
| 交接提示 | 下一步实施 `WMS-API-006`：发货交接、盘点、仓内异常 |

### WMS-LOG-010 | 已完成 | 发货交接、盘点差异与仓内异常

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/WMS-REQ-008-发货交接盘点仓内异常.md`；`docs/09-开发计划/01-开发计划/03-WMS系统接口级开发计划.md` 的 `WMS-API-006` |
| 新增 | `ShipmentHandoverAggregate`、`StocktakeAggregate`、`WarehouseExceptionAggregate`；对应 Mapper、`WmsOperationApplicationService`、`WmsOperationController`、`WmsOperationApplicationServiceTest` |
| 修改 | 无 |
| 删除 | 无 |
| 数据迁移 | `V6__wms_handover_stocktake_exception.sql` 新增 `wms_shipment_handover`、`wms_stocktake`、`wms_warehouse_exception` |
| 接口 | `/handovers` 创建/确认；`/stocktakes` 创建和确认差异；`/warehouse-exceptions` 创建和关闭 |
| 事件 | `WmsShipmentHandedOver`、`WmsStocktakeDifferenceConfirmed`、`WmsWarehouseExceptionCreated`、`WmsWarehouseExceptionClosed` |
| 测试 | `WmsOperationApplicationServiceTest` 覆盖交接、盘点、异常创建/确认/关闭、事件发布、重复创建幂等；全量 `mvn test -q` 已通过 |
| 风险/待办 | WMS 计划功能已形成端到端代码骨架和测试基线；仍缺真实 RocketMQ、Dubbo/TMS/中央库存联调、Web 权限、真实 MySQL/Flyway 集成测试和更细的出入库行级模型 |
| 交接提示 | WMS 系统当前接口级计划已完成一轮；下一步按排序进入中央库存 `INV-01` 或补 WMS 集成测试/联调 |

## 2026-07-12 功能盘点与后续需求排序

### 已完成/基本完成

| 系统 | 完成情况 | 证据 |
| --- | --- | --- |
| 供应商系统 | 业务面基本补齐，仍缺真实 MySQL/Flyway、RocketMQ/Dubbo 联调和 Web 权限测试 | `SUP-LOG-*`，`supplier-service` 代码 |
| 采购系统 | 请购、RFQ、比价/价格、PO、到货、退供、Outbox/Inbox、供应商确认差异、关闭剩余已完成 | `PUR-LOG-001` 至 `PUR-LOG-009`，`purchase-service` 测试通过 |
| WMS 系统 | 接口级计划 `WMS-API-001` 至 `WMS-API-006` 已完成一轮代码骨架、迁移、事件和应用测试；剩余为联调/权限/行级模型增强 | `WMS-LOG-001` 至 `WMS-LOG-010`，`wms-service` 测试通过 |
| 中央库存系统 | 库存账户、流水、预占、冻结/调整、库存事件、快照和对账已完成一轮；剩余为真实 MQ/权限/集成测试增强 | `INV-LOG-001` 至 `INV-LOG-003`，`inventory-service` 测试通过 |
| 权限系统 | 接口级计划 `IAM-API-001` 至 `IAM-API-005` 已完成首轮：登录会话、JWT 签名、用户角色、权限点、数据范围、审批日志、安全策略、应用菜单、SSO、权限快照、Token 校验、数据范围解析和内部事件入口 | `IAM-LOG-001` 至 `IAM-LOG-003`，`iam-service` 测试通过 |
| 主数据系统 | 接口级计划 `MDM-API-001` 至 `MDM-API-005` 已完成首轮：类型/模板/编码、记录审核/版本、发布订阅/回执/重试、导入导出、数据质量治理、OpenAPI 快照/校验、内部事件入口、Outbox/Inbox 和审计 | `MDM-LOG-001` 至 `MDM-LOG-005`，`mdm-service` 测试通过 |
| OMS 系统 | 渠道订单、审单、履约分仓/换仓/拆分、库存预占/释放、WMS 出库、取消售后和外部查询已完成首轮；剩余为真实 ACL/联调/退货入库增强 | `OMS-LOG-001` 至 `OMS-LOG-003`，`oms-service` 测试通过 |
| TMS 系统 | 接口级计划 `TMS-API-001` 至 `TMS-API-005` 已完成首轮：运输任务、运单、面单、轨迹、签收、承运商回调、物流异常、费用来源、Outbox/Inbox 和审计 | `TMS-LOG-001` 至 `TMS-LOG-004`，`tms-service` 测试通过 |
| 集成中心 | 统一集成投递与补偿中心已完成首轮：路由配置、统一事件/命令接入、派发状态、失败重试、死信、人工重放、批量投递运行面、投递尝试日志、运维汇总、HTTP/OpenAPI 端点配置、超时熔断、RocketMQ/Dubbo 通道契约预检 | `INT-LOG-001` 至 `INT-LOG-004`，`integration-service` 测试通过 |
| BMS 系统 | 计费对象、规则、费用来源、费用明细、调整、对账、账单、发票、财务交接、退款、报表和内部事件入口已完成首轮 | `BMS-LOG-001` 至 `BMS-LOG-003`，`bms-service` 测试通过 |
| 前端系统 | 运营工作台、供应商 ASN 页面、系统导航、集成中心运维页面、采购中心业务运维页面、WMS 与中央库存业务运维页面、OMS/TMS/BMS 业务运维页面、主数据与权限管理页面、基础菜单权限拦截与 Vitest 权限测试、页面懒加载拆包已完成首轮 | `FE-LOG-001` 至 `FE-LOG-008`，`npm run test/build` 通过 |

### 未开发/未完成排序

| 优先级 | 系统/功能 | 状态 | 排序理由 | 对应需求单/下一步 |
| --- | --- | --- | --- | --- |
| P1 | RocketMQ Producer、Dubbo 泛化调用、ERP/税务/支付沙箱联调 | 未完成 | RocketMQ/Dubbo 端点契约已可预检；剩余工作依赖真实 nameserver、注册中心、服务接口或泛化调用依赖、财税支付沙箱地址 | INT-REQ-005、各系统契约测试 |
| P2 | 生产化集成测试 | 阻塞 | 前端业务页面、基础菜单权限测试、页面拆包和真实浏览器权限 E2E 已完成；剩余需要真实后端服务、中间件、外部沙箱和测试数据 | OPS-REQ-001、INT-REQ-005 |
| P3 | 统一报表分析、压测、告警、容灾和上线治理 | 未完成 | 需要业务事实沉淀后建设分析与生产运行保障 | RPT-REQ-001、OPS-REQ-001 |

## 中央库存系统

### INV-LOG-001 | 已完成 | 库存账户、流水与预占底座

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/INV-REQ-001-库存账户流水与预占.md`；`docs/09-开发计划/01-开发计划/04-中央库存系统接口级开发计划.md` 的 `INV-API-001`、`INV-API-002`、`INV-API-003` 和 WMS 记账入口基础 |
| 新增 | `inventory-service` Maven 模块；`InventoryServiceApplication`；`InventoryAccountAggregate`、`ReservationAggregate`；`InventoryApplicationService`；`InventoryController`；`InventoryMapper`；领域/应用服务测试 |
| 修改 | 根 `project/backend/pom.xml` 注册 `inventory-service` |
| 删除 | 无 |
| 数据迁移 | `V1__inventory_account_reservation.sql` 新增 `inv_stock_balance`、`inv_stock_ledger`、`inv_reservation` |
| 接口 | `GET /api/inventory/v1/stocks`；`GET /api/inventory/v1/stock-ledgers`；`POST /openapi/inventory/v1/wms/inbound`；`POST /openapi/inventory/v1/wms/outbound`；`POST /openapi/inventory/v1/reservations`；`POST /openapi/inventory/v1/reservations/{reservationNo}/release`；`POST /api/inventory/v1/freezes`；`POST /api/inventory/v1/freezes/unfreeze`；`POST /api/inventory/v1/adjustments` |
| 事件 | 本切片先写账户和不可变流水，Outbox 事件表和投递将在后续库存事件切片补齐 |
| 测试 | `InventoryAccountAggregateTest` 覆盖账户数量不变量；`InventoryApplicationServiceTest` 覆盖入库、预占、释放、冻结、解冻、调整和预占幂等 |
| 风险/待办 | 仍缺库存 Outbox/Inbox、冻结单独立审批聚合、调整单聚合、快照对账、WMS 事件消费信封、真实 MySQL/Flyway 集成测试和权限数据范围 |
| 交接提示 | 下一步实施 `INV-API-004`：冻结审批、调整单和库存事件 Outbox；随后补 `INV-API-005` 快照、对账与 WMS 事件入口 |

### INV-LOG-002 | 已完成 | 库存 Outbox/Inbox 与 WMS 入站记账

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/INV-REQ-002-库存事件与WMS入站记账.md`；`INV-API-005` 的 WMS 记账入口和事件可靠性要求 |
| 新增 | `InventoryEventMapper`、`InventoryEventApplicationService`、`InventoryEventController` |
| 修改 | 无 |
| 删除 | 无 |
| 数据迁移 | `V2__inventory_outbox_inbox.sql` 新增 `inv_outbox_event`、`inv_inbox_event` |
| 接口 | `POST /internal/inventory/v1/events`；`POST /api/inventory/v1/operations/outbox/dispatch` |
| 事件 | 消费 `WmsPutawayCompleted`、`WmsShipmentHandedOver`、`WmsStocktakeDifferenceConfirmed`；生产 `InventoryChanged` |
| 测试 | 后续与快照对账切片一并补应用服务组合测试；当前全量编译测试需通过 |
| 风险/待办 | 当前 payload parser 仅支持简单扁平 JSON；真实 MQ、死信和事件版本兼容待后续增强 |
| 交接提示 | 下一步补快照、对账与操作测试，完成中央库存系统接口级闭环 |

### INV-LOG-003 | 已完成 | 库存快照与对账

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/INV-REQ-003-库存快照与对账.md`；`INV-API-005` 的快照、对账与补偿要求 |
| 新增 | `InventorySnapshotMapper`、`InventorySnapshotApplicationService`、`InventorySnapshotController`、`InventorySnapshotApplicationServiceTest` |
| 修改 | 无 |
| 删除 | 无 |
| 数据迁移 | `V3__inventory_snapshot_reconcile.sql` 新增 `inv_stock_snapshot`、`inv_stock_reconcile` |
| 接口 | `POST /api/inventory/v1/snapshots/generate`；`GET /api/inventory/v1/snapshots`；`POST /api/inventory/v1/inventory-reconciliations`；`POST /api/inventory/v1/inventory-reconciliations/{reconcileNo}/confirm`；`GET /api/inventory/v1/inventory-reconciliations` |
| 事件 | 本切片不新增事件；对账确认后的差异自动调整留给后续增强 |
| 测试 | `InventorySnapshotApplicationServiceTest` 覆盖快照生成、对账差异计算和确认；全量 `mvn test -q` 已通过 |
| 风险/待办 | 中央库存系统接口级计划已完成一轮；仍缺真实 RocketMQ、权限数据范围、真实 MySQL/Flyway 集成测试、复杂事件载荷解析和冻结/调整独立审批工作流 |
| 交接提示 | 下一系统按优先级进入 IAM 或 MDM；若继续打磨库存，应补集成测试和事件载荷版本化 |

## OMS 系统

### OMS-LOG-001 | 已完成 | 渠道订单接入与销售订单审核

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/OMS-REQ-001-渠道订单接入与销售订单审核.md`；`docs/09-开发计划/01-开发计划/05-OMS系统接口级开发计划.md` 的 `OMS-API-001` |
| 新增 | `oms-service` Maven 模块；`OmsServiceApplication`；`SalesOrderAggregate`；`OmsApplicationService`；`OmsController`；`OmsMapper`；领域/应用服务测试 |
| 修改 | 根 `project/backend/pom.xml` 注册 `oms-service` |
| 删除 | 无 |
| 数据迁移 | `V1__oms_channel_order_sales_order.sql` 新增销售订单、渠道订单映射、Outbox 和操作日志表 |
| 接口 | `POST /api/oms/v1/channel-orders`；`GET /api/oms/v1/channel-orders`；`GET /api/oms/v1/sales-orders`；`GET /api/oms/v1/sales-orders/{orderNo}`；`POST /api/oms/v1/sales-orders/{orderNo}/review` |
| 事件 | 生产 `ChannelOrderReceived`、`SalesOrderCreated`、`SalesOrderReviewed`、`SalesOrderIntercepted`，写入 OMS Outbox |
| 测试 | `SalesOrderAggregateTest` 覆盖接单金额汇总、订单行校验、审核状态机和拦截原因；`OmsApplicationServiceTest` 覆盖渠道订单幂等接入、审核、Outbox 和审计；全量 `mvn test -q` 已通过 |
| 风险/待办 | OMS 首切片已完成；仍缺履约单、分仓、库存预占、WMS 出库下发、取消售后、外部订单查询和事件入口 |
| 交接提示 | 下一步实施 `OMS-API-002` 和 `OMS-API-003`，从销售订单审核通过后创建履约单、请求库存预占，并下发 WMS 出库命令 |

### OMS-LOG-002 | 已完成 | 分仓、库存预占与 WMS 出库下发

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/OMS-REQ-002-分仓预占与WMS出库下发.md`；`docs/09-开发计划/01-开发计划/05-OMS系统接口级开发计划.md` 的 `OMS-API-002`、`OMS-API-003`；履约单和出库单聚合设计 |
| 新增 | `FulfillmentAggregate`、`OutboundAggregate`；`FulfillmentApplicationService`；`FulfillmentMapper`；`FulfillmentController`、`OmsInventoryController`、`OmsOutboundController`、`OmsInternalEventController`；领域/应用服务测试 |
| 修改 | `OutboundAggregate` 增加 WMS 下发重推；OMS 外部事件入口按 `/internal/oms/v1/events` 契约接入；WMS 取消成功后触发库存释放补偿命令 |
| 删除 | 无 |
| 数据迁移 | `V2__oms_fulfillment_reservation_outbound.sql` 新增履约单、库存预占引用、出库单、集成命令 Outbox、事件 Inbox |
| 接口 | `POST /api/oms/v1/sales-orders/{salesOrderNo}/fulfillments`；`POST /api/oms/v1/fulfillments/{fulfillmentNo}/change-warehouse`；`POST /api/oms/v1/fulfillments/{fulfillmentNo}/split`；预占/释放；出库创建/下发/取消/重推；`POST /internal/oms/v1/events` |
| 事件 | 生产 `FulfillmentOrderCreated`、`FulfillmentWarehouseAllocated`、`FulfillmentWarehouseChanged`、`FulfillmentSplit`、`StockReservationRequested`、`StockReservationReleaseRequested`、`OutboundOrderCreated`、`OutboundInstructionIssued`、`OutboundCancelRequested`；消费库存预占和 WMS 出库事实 |
| 跨系统命令 | 预占/释放写 `oms_integration_command` 指向中央库存；创建/取消/重推 WMS 出库写同表指向 WMS；本期不直接修改外部数据库 |
| 测试 | `FulfillmentAggregateTest`、`OutboundAggregateTest` 覆盖状态机、数量守恒和取消前置；`FulfillmentApplicationServiceTest` 覆盖分仓、预占、事件 Inbox 幂等、WMS 接单/发货和 Outbox；全量 `mvn test -q` 已通过 |
| 风险/待办 | 当前跨系统仍是命令 Outbox，尚未接入真实中央库存/WMS ACL；分仓规则、正式订单行表、真实 MySQL/Flyway/消息联调仍待补；OMS 取消售后和外部查询事件投影未完成 |
| 交接提示 | 下一步继续实现 `OMS-API-004` 取消、售后、退款与异常，再实现 `OMS-API-005` 外部订单查询和事件入口 |

### OMS-LOG-003 | 已完成 | 取消申请、售后退款与外部查询首轮

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/OMS-REQ-003-取消申请与售后退款基础闭环.md`、`docs/09-开发计划/需求单/OMS-REQ-004-外部订单查询与事件入口.md`；OMS-API-004、OMS-API-005 |
| 新增 | `CancellationRequestAggregate`、`AfterSaleAggregate`；取消/售后应用服务；`CancellationMapper`；取消、售后、外部查询 Controller；OMS 统一事件入口路由 |
| 数据迁移 | `V3__oms_cancel_after_sale.sql` 新增取消申请和售后单表，复用 OMS Outbox/Inbox/集成命令表 |
| 接口/事件 | 新增取消申请、取消处理、售后创建/审核/退款/完成、外部订单/履约查询；统一消费 `WmsOutboundCancelled`、`StockReleased`、`RefundCompleted`；WMS/库存事件先进入履约服务更新履约/出库状态，再用消费者专属 Inbox Key 分发给取消服务，避免同一事件 ID 互相抢占 |
| 测试 | `CancellationRequestAggregateTest`、`AfterSaleAggregateTest`、`CancellationAfterSaleApplicationServiceTest` 覆盖发货前取消、WMS 取消回写、库存释放回写、售后退款状态机和退款事件消费；`mvn -q -pl oms-service -am -Dtest=CancellationRequestAggregateTest,AfterSaleAggregateTest,CancellationAfterSaleApplicationServiceTest -Dsurefire.failIfNoSpecifiedTests=false test` 已通过；`mvn -q -pl oms-service -am test` 已通过 |
| 风险/待办 | 真实 WMS/库存/BMS ACL、退货入库、TMS 拦截、外部渠道鉴权和真实 MySQL/Flyway 集成测试仍未实现；进入 TMS 后优先补运输任务事实源 |
| 交接提示 | OMS 接口级计划已完成一轮；下一步进入 TMS-API-001 创建、接单与查询运输任务 |

## TMS 系统

### TMS-LOG-001 | 已完成 | 运输任务创建、接单与查询

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/TMS-REQ-001-运输任务创建接单与查询.md`；`docs/09-开发计划/01-开发计划/06-TMS系统接口级开发计划.md` 的 `TMS-API-001` |
| 新增 | `tms-service` Maven 模块；`TmsServiceApplication`；`TransportTaskAggregate`、`TmsEvent`；`TransportTaskApplicationService`；`TransportTaskMapper`；`TransportTaskOpenApiController`、`TransportTaskController`；领域/应用/接口测试 |
| 修改 | 根 `project/backend/pom.xml` 注册 `tms-service` |
| 删除 | 无 |
| 数据迁移 | `V1__tms_transport_task.sql` 新增 `tms_transport_task`、`tms_domain_event`、`tms_operation_log` |
| 接口 | `POST /openapi/tms/v1/transport-tasks`；`POST /api/tms/v1/transport-tasks/{taskNo}/accept`；`GET /api/tms/v1/transport-tasks`；`GET /api/tms/v1/transport-tasks/{taskNo}` |
| 事件 | 生产 `TransportTaskCreated`、`TransportTaskAccepted`，写入 `tms_domain_event`；同一来源系统、来源单号和运输场景重复创建时返回既有有效任务 |
| 测试 | `TransportTaskAggregateTest` 覆盖创建、接单、版本冲突和场景校验；`TransportTaskApplicationServiceTest` 覆盖幂等建单、接单、查询、Outbox 和审计；`TransportTaskControllerTest` 覆盖 OpenAPI 创建和后台接单查询；`mvn -q -pl tms-service -am test` 已通过 |
| 风险/待办 | 尚未接入真实承运商、主数据、IAM 数据权限、RocketMQ 投递、真实 MySQL/Flyway 集成测试；运单、面单、轨迹、签收和异常费用仍未实现 |
| 交接提示 | 下一步实施 `TMS-API-002`：创建/作废运单与生成面单，并生成 `TMS-REQ-002` |

### TMS-LOG-002 | 已完成 | 运单创建作废与面单生成打印

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/TMS-REQ-002-运单面单创建作废与打印.md`；`docs/09-开发计划/01-开发计划/06-TMS系统接口级开发计划.md` 的 `TMS-API-002` |
| 新增 | `WaybillAggregate`、`ShippingLabelAggregate`；`WaybillApplicationService`、`ShippingLabelApplicationService`；`WaybillMapper`；`WaybillController`、`ShippingLabelController`；领域/应用/接口测试 |
| 修改 | 复用 `tms_domain_event` 和 `tms_operation_log` 写入运单/面单事件与审计 |
| 删除 | 无 |
| 数据迁移 | `V2__tms_waybill_shipping_label.sql` 新增 `tms_waybill`、`tms_shipping_label` |
| 接口 | `POST /api/tms/v1/transport-tasks/{taskNo}/waybills`；`POST /api/tms/v1/waybills/{waybillNo}/void`；`GET /api/tms/v1/waybills`；`GET /api/tms/v1/waybills/{waybillNo}`；`POST /api/tms/v1/waybills/{waybillNo}/labels`；`POST /api/tms/v1/shipping-labels/{labelNo}/print`；`GET /api/tms/v1/waybills/{waybillNo}/labels` |
| 事件 | 生产 `WaybillCreated`、`WaybillVoided`、`ShippingLabelGenerated`、`ShippingLabelPrinted`；同一有效运输任务重复建单返回既有运单，同一运单包裹重复生成面单返回既有面单 |
| 测试 | `WaybillAggregateTest`、`ShippingLabelAggregateTest` 覆盖状态机；`WaybillApplicationServiceTest` 覆盖已接单任务建运单、幂等、面单生成打印、Outbox 和审计；`WaybillControllerTest` 覆盖接口层创建运单和面单打印；`mvn -q -pl tms-service -am test` 已通过 |
| 风险/待办 | 当前承运商下单和面单文件 URL 由入参模拟；尚未接入真实承运商 ACL、对象存储、打印服务、发货交接、轨迹、签收和费用来源 |
| 交接提示 | 下一步实施 `TMS-API-003` 与 `TMS-API-005`：轨迹、签收和承运商回调事件入口 |

### TMS-LOG-003 | 已完成 | 轨迹、签收与承运商回调入口

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/TMS-REQ-003-轨迹签收与承运商回调入口.md`；`docs/09-开发计划/01-开发计划/06-TMS系统接口级开发计划.md` 的 `TMS-API-003`、`TMS-API-005` 首轮 |
| 新增 | `TrackingAggregate`、`DeliveryReceiptAggregate`；`TrackingApplicationService`、`DeliveryReceiptApplicationService`、`CarrierCallbackApplicationService`；`TrackingMapper`；`TrackingController`、`DeliveryReceiptController`、`CarrierCallbackController`；领域/应用/接口测试 |
| 修改 | 承运商回调先写 `tms_event_inbox`，再转为轨迹或签收命令；重复事件按 Inbox 幂等忽略 |
| 删除 | 无 |
| 数据迁移 | `V3__tms_tracking_receipt_inbox.sql` 新增 `tms_tracking_node`、`tms_delivery_receipt`、`tms_event_inbox` |
| 接口 | `POST /api/tms/v1/waybills/{waybillNo}/tracks`；`GET /api/tms/v1/waybills/{waybillNo}/tracks`；`POST /api/tms/v1/delivery-receipts`；`GET /api/tms/v1/delivery-receipts/{receiptNo}`；`POST /openapi/tms/v1/carrier-callbacks/{carrierCode}` |
| 事件 | 生产 `TrackingAppended`、`TrackingSupplemented`、`TransportArrived`、`TransportSigned`、`TransportRejected`、`PartialSigned`；轨迹按 `waybillNo + nodeCode + trackAt` 去重，同一运单签收终态只保留一条 |
| 测试 | `TrackingAggregateTest`、`DeliveryReceiptAggregateTest` 覆盖领域事件和拒收原因不变量；`TrackingReceiptApplicationServiceTest` 覆盖回调 Inbox 幂等、到达事件、签收事件和人工补录；`TrackingReceiptControllerTest` 覆盖接口层补录、回调和签收查询；`mvn -q -pl tms-service -am test` 已通过 |
| 风险/待办 | 尚未实现承运商签名验签、节点映射配置、对象存储附件、签收冲正、轨迹驱动运单终态推进和真实 MQ 投递 |
| 交接提示 | 下一步实施 `TMS-API-004`：物流异常与费用来源，生成 `TMS-REQ-004` |

### TMS-LOG-004 | 已完成 | 物流异常与费用来源

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/TMS-REQ-004-物流异常与费用来源.md`；`docs/09-开发计划/01-开发计划/06-TMS系统接口级开发计划.md` 的 `TMS-API-004` |
| 新增 | `LogisticsExceptionAggregate`、`LogisticsFeeSourceAggregate`；`LogisticsExceptionApplicationService`、`LogisticsFeeSourceApplicationService`；`LogisticsSettlementMapper`；`LogisticsExceptionController`、`LogisticsFeeSourceController`；领域/应用/接口测试 |
| 修改 | 复用 `tms_domain_event` 和 `tms_operation_log` 写异常、费用来源和 BMS 推送事实 |
| 删除 | 无 |
| 数据迁移 | `V4__tms_exception_fee_source.sql` 新增 `tms_logistics_exception`、`tms_logistics_fee_source` |
| 接口 | `POST /api/tms/v1/transport-exceptions`；`POST /api/tms/v1/transport-exceptions/{exceptionNo}/close`；`GET /api/tms/v1/transport-exceptions`；`POST /api/tms/v1/waybills/{waybillNo}/fee-sources`；`POST /api/tms/v1/fee-sources/{feeSourceNo}/push-bms`；`GET /api/tms/v1/fee-sources` |
| 事件 | 生产 `LogisticsExceptionRegistered`、`LogisticsExceptionClosed`、`LogisticsFeeSourceGenerated`、`LogisticsFeeSourcePushed`、`LogisticsFeeSourcePushFailed`；同一运单同一费用项重复生成返回既有费用来源 |
| 测试 | `LogisticsSettlementAggregateTest` 覆盖异常关闭和费用来源推送状态机；`LogisticsSettlementApplicationServiceTest` 覆盖异常登记/关闭、费用来源幂等、BMS 推送、Outbox；`LogisticsSettlementControllerTest` 覆盖接口层异常和费用来源链路；`mvn -q -pl tms-service -am test` 已通过 |
| 风险/待办 | TMS 接口级计划已完成一轮；仍缺真实承运商 ACL、签名验签、对象存储/打印服务、BMS RPC、RocketMQ 投递、IAM 数据权限、真实 MySQL/Flyway 集成测试和运单终态推进 |
| 交接提示 | TMS 系统首轮完成；下一系统按当前排序进入 `MDM-API-002/003` 或 IAM/集成生产化 |

## 2026-07-12 系统现状复核

### AUDIT-20260712-001 | 已完成 | 已完成功能与未完成范围复核

| 项目 | 结论 |
| --- | --- |
| 已完成/首轮完成 | 供应商、采购、WMS、中央库存、权限、主数据均已有首轮代码、迁移、领域/应用测试；OMS 已完成渠道订单、履约分仓、库存预占、WMS 出库、取消售后和外部查询首轮闭环；TMS 已完成接口级计划首轮；集成中心已完成统一投递、补偿、批量投递运行面、尝试日志、运维汇总、HTTP/OpenAPI 端点配置和熔断、RocketMQ/Dubbo 契约预检首轮；BMS 已完成计费对象、规则、来源、费用、对账、账单、发票、财务、退款、报表与事件入口首轮。 |
| 尚未形成独立服务 | 暂无核心子系统缺少独立后端服务模块。 |
| P0 | 暂无未完成的 P0 接口级首轮切片；OMS 剩余为真实 ACL、渠道鉴权、退货入库和联调增强。 |
| P1 | 供应商/采购/WMS/库存/BMS 与集成中心的真实 MQ、Dubbo、ERP、税务、支付联调和契约测试。集成中心已具备 HTTP/OpenAPI 真实传输通道和 RocketMQ/Dubbo 端点契约预检，下一步需要真实 nameserver、注册中心和外部财税支付沙箱。 |
| P2 | 供应商/采购/WMS/库存生产化集成测试；前端业务页面、基础菜单权限测试、页面拆包和真实浏览器权限 E2E 已完成，剩余为依赖外部环境的生产化集成测试。 |
| P3 | 统一报表分析、压测、告警、容灾和上线演练。 |
| 本轮动作 | 已完成并验证 `OMS-API-004/005`、`TMS-API-001/002/003/004/005`、`MDM-API-002/003/004/005`、`IAM-API-003/004/005`、`INT-API-001/002/003/004`、`BMS-API-001/002/003/004/005`、`FE-REQ-001/002/003/004/005/006/007/008`、`QA-REQ-001` 仓库内可执行部分，更新 `OMS-LOG-003`、`TMS-LOG-001/002/003/004`、`MDM-LOG-002/003/004/005`、`IAM-LOG-002/003`、`INT-LOG-001/002/003/004`、`BMS-LOG-001/002/003`、`FE-LOG-001/002/003/004/005/006/007/008`、`QA-LOG-001`；TMS、MDM、IAM、集成中心和 BMS 接口级计划首轮完成，集成中心 HTTP/OpenAPI 通道和 RocketMQ/Dubbo 契约预检完成，前端核心子系统业务页面、基础菜单权限测试、页面拆包和真实浏览器权限 E2E 完成。 |
| 验证基线 | OMS 目标测试、`mvn -q -pl oms-service -am test`、`mvn -q -pl tms-service -am test`、`mvn -q -pl mdm-service -am test`、`mvn -q -pl iam-service -am test`、`mvn -q -pl integration-service -am test`、`mvn -q -pl bms-service -am test`、前端 `npm run build`、后端全量 `mvn test -q` 已通过。 |

## 前端系统

### FE-LOG-001 | 已完成 | 供应链运营工作台与系统导航

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/FE-REQ-001-供应链运营工作台与系统导航.md`；开发日志中的已完成/未完成排序和 P1/P2/P3 风险 |
| 新增 | `WorkbenchPage`，展示首轮完成系统、集成通道能力、P1 阻塞项、前端切片状态、系统完成状态表、未完成优先级表、最近推进时间线和联调前置条件 |
| 修改 | `App.jsx` 增加运营工作台默认首页、系统菜单、供应商 ASN 页面切换；`app.css` 去除桌面硬性最小宽度，补齐后台工作台、指标块、分区和响应式布局 |
| 删除 | 无 |
| 数据迁移 | 无 |
| 接口 | 前端当前仍复用既有 ASN API；工作台首轮使用开发日志事实静态呈现，后续可接报表/运维汇总 API |
| 事件 | 无前端事件；业务写操作仍由 ASN 页面通过后端接口产生领域事件 |
| 测试 | `npm run build` 已通过；后端全量 `mvn test -q` 已通过；`git diff --check` 已通过 |
| 风险/待办 | Ant Design 打包体积超过 Vite 默认 500KB 警告，后续可按路由拆包；仍需补 WMS、库存、OMS、TMS、BMS、主数据、权限的详细业务页面和权限 Web 测试 |
| 交接提示 | `FE-REQ-002`、`FE-REQ-003` 已完成；下一步进入 `FE-REQ-004`：WMS 与中央库存业务运维页面 |

### FE-LOG-002 | 已完成 | 集成中心运维页面

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/FE-REQ-002-集成中心运维页面.md`；`INT-LOG-001` 至 `INT-LOG-004` 的集成中心接口能力 |
| 新增 | `IntegrationPage`；`api/integration.js`；运维汇总指标、路由管理、端点管理、消息运维、死信处理、投递运行面和投递尝试日志页面能力 |
| 修改 | `App.jsx` 打开“集成中心”菜单并接入页面；`app.css` 增加集成运维页面、运行工具条、投递结果和标签页样式；未完成排序将下一前端切片推进到 `FE-REQ-003` |
| 删除 | 无 |
| 数据迁移 | 无 |
| 接口 | `GET/POST /api/integration/v1/routes`；`POST /routes/{routeNo}/disable`；`GET/POST /api/integration/v1/endpoints`；`POST /endpoints/{endpointNo}/disable`；`POST /endpoints/{endpointNo}/verify`；`GET /api/integration/v1/messages`；`POST /messages/{messageNo}/dispatch|retry`；`GET /api/integration/v1/dead-letters`；`POST /dead-letters/{deadLetterNo}/replay`；`POST /api/integration/v1/dispatch-runs`；`GET /api/integration/v1/delivery-attempts`；`GET /api/integration/v1/operations/summary` |
| 事件 | 前端不直接生产领域事件；路由、端点、派发、重试、死信重放由集成中心后端写审计、状态和补偿记录 |
| 测试 | `npm run build` 已通过；后端全量 `mvn test -q` 已通过；`git diff --check` 已通过 |
| 风险/待办 | 真实 RocketMQ Producer、Dubbo 泛化调用、ERP/税务/支付沙箱仍为 P1 外部环境阻塞；前端仍需补 WMS、库存、OMS、TMS、BMS、主数据、权限详情页和 Web 权限测试 |
| 交接提示 | `FE-REQ-003` 已由 `FE-LOG-003` 完成；下一步进入 `FE-REQ-004`：WMS 与中央库存业务运维页面，或在具备环境后实施 `INT-REQ-005` 真实 MQ/Dubbo/财税支付联调 |

### FE-LOG-003 | 已完成 | 采购中心业务运维页面

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/FE-REQ-003-采购中心业务运维页面.md`；`PUR-LOG-001` 至 `PUR-LOG-009` 的采购主链路接口能力 |
| 新增 | `PurchasePage`；`api/purchase.js`；请购、RFQ、采购订单、到货跟踪、供应商确认差异和采购 Inbox 失败事件页面 |
| 修改 | `App.jsx` 打开“采购中心”菜单并接入页面；`app.css` 增加采购页面标题区样式；未完成排序将下一前端切片推进到 `FE-REQ-004` |
| 删除 | 无 |
| 数据迁移 | 无 |
| 接口 | `GET /api/purchase/v1/requisitions`；`POST /requisitions/{id}/submit|approve|reject`；`GET /api/purchase/v1/rfqs`；`POST /rfqs/{rfqNo}/publish|close`；`GET /api/purchase/v1/purchase-orders`；`POST /purchase-orders/{orderNo}/submit|approve|publish|cancel|close`；`GET /api/purchase/v1/inbounds`；`GET /api/purchase/v1/supplier-confirms`；`POST /supplier-confirms/{confirmId}/accept-diff|renegotiate|cancel-order`；`GET /api/purchase/v1/operations/failed-events`；`POST /operations/failed-events/{id}/replay` |
| 事件 | 前端不直接生产领域事件；状态推进由采购后端写审计、Outbox、Inbox 重放和对应领域事件 |
| 测试 | `npm run build` 已通过；后端全量 `mvn test -q` 已通过；`git diff --check` 已通过 |
| 风险/待办 | 本轮页面侧重采购运营查询和状态推进，复杂新建/编辑、比价/价格、订单变更和退供完整页面留给后续切片；权限 Web 测试仍待补齐 |
| 交接提示 | `FE-REQ-004` 已由 `FE-LOG-004` 完成；下一步进入 `FE-REQ-005`：OMS、TMS、BMS 业务运维页面 |

### FE-LOG-004 | 已完成 | WMS 与中央库存业务运维页面

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/FE-REQ-004-WMS与中央库存业务运维页面.md`；`WMS-LOG-001` 至 `WMS-LOG-010`、`INV-LOG-001` 至 `INV-LOG-003` 的 WMS/库存运维与读模型能力 |
| 新增 | `WarehouseInventoryPage`；`api/warehouseInventory.js`；WMS Outbox 失败事件、WMS Inbox 失败事件、库存余额、库存流水、库存快照、库存对账页面 |
| 修改 | `App.jsx` 打开“仓储作业”和“中央库存”菜单并接入同一运维页面；`app.css` 增加仓储库存页面标题区样式；未完成排序将下一前端切片推进到 `FE-REQ-005` |
| 删除 | 无 |
| 数据迁移 | 无 |
| 接口 | `POST /api/wms/v1/operations/outbox/dispatch`；`GET /api/wms/v1/operations/outbox/failed-events`；`POST /api/wms/v1/operations/outbox/failed-events/{eventId}/retry`；`GET /api/wms/v1/operations/inbox/failed-events`；`POST /api/wms/v1/operations/inbox/failed-events/{inboxId}/replay`；`GET /api/inventory/v1/stocks`；`GET /api/inventory/v1/stock-ledgers`；`POST /api/inventory/v1/operations/outbox/dispatch`；`GET/POST /api/inventory/v1/snapshots`；`GET /api/inventory/v1/inventory-reconciliations`；`POST /api/inventory/v1/inventory-reconciliations/{reconcileNo}/confirm` |
| 事件 | 前端不直接生产领域事件；WMS/库存投递、重试、重放、快照和对账确认由后端维护 Outbox/Inbox、库存快照和对账状态 |
| 测试 | `npm run build` 已通过；后端全量 `mvn test -q` 已通过；`git diff --check` 已通过 |
| 风险/待办 | WMS 后端尚缺入库单、收货单、质检单、上架任务、出库单等列表读模型，本轮不伪造；后续需补 WMS 作业详情页、主数据/权限页面和权限 Web 测试 |
| 交接提示 | `FE-REQ-005` 已由 `FE-LOG-005` 完成；下一步进入 `FE-REQ-006`：主数据与权限管理页面 |

### FE-LOG-005 | 已完成 | OMS、TMS、BMS 业务运维页面

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/FE-REQ-005-OMS-TMS-BMS业务运维页面.md`；`OMS-LOG-001` 至 `OMS-LOG-003`、`TMS-LOG-001` 至 `TMS-LOG-004`、`BMS-LOG-001` 至 `BMS-LOG-003` 的履约、物流、结算读模型能力 |
| 新增 | `FulfillmentSettlementPage`；`api/fulfillmentSettlement.js`；OMS 渠道订单、销售订单、履约单、出库单；TMS 运输任务、运单、物流异常、物流费用来源；BMS 计费对象、费用来源、结算汇总 |
| 修改 | `App.jsx` 打开“订单履约”“物流运输”“费用结算”菜单并接入同一运营页面；`app.css` 增加履约结算页面标题区样式；未完成排序将下一前端切片推进到 `FE-REQ-006` |
| 删除 | 无 |
| 数据迁移 | 无 |
| 接口 | `GET /api/oms/v1/channel-orders`；`GET /api/oms/v1/sales-orders`；`GET /api/oms/v1/fulfillments`；`GET /api/oms/v1/outbounds`；`GET /api/tms/v1/transport-tasks`；`GET /api/tms/v1/waybills`；`GET /api/tms/v1/transport-exceptions`；`GET /api/tms/v1/fee-sources`；`GET /api/bms/v1/billing-subjects`；`GET /api/bms/v1/charge-sources`；`GET /api/bms/v1/reports/settlement-summary` |
| 事件 | 前端不直接生产领域事件；本轮只接真实读模型和结算汇总，避免用默认参数误推进履约、物流、财务状态 |
| 测试 | `npm run build` 已通过；后端全量 `mvn test -q` 已通过；待本切片结束执行 `git diff --check` |
| 风险/待办 | OMS/TMS/BMS 操作表单、BMS 对账账单开票财务交接完整页面和权限 Web 测试仍待补齐 |
| 交接提示 | `FE-REQ-006` 已由 `FE-LOG-006` 完成；下一步进入 `FE-REQ-007`：前端权限拦截、动态菜单与基础 Web 权限测试 |

### FE-LOG-006 | 已完成 | 主数据与权限管理页面

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/FE-REQ-006-主数据与权限管理页面.md`；`MDM-LOG-001` 至 `MDM-LOG-005`、`IAM-LOG-001` 至 `IAM-LOG-003` 的主数据与权限读模型能力 |
| 新增 | `MdmIamPage`；`api/mdmIam.js`；主数据类型、字段模板、编码规则、主数据记录、导入任务、质量问题、发布订阅、发布记录；用户、角色、权限点、审批、操作日志、安全策略、应用、SSO 客户端 |
| 修改 | `App.jsx` 新增“主数据”菜单并打开“权限管理”菜单，两个入口接入同一管理页面；`app.css` 增加主数据权限页面标题区和标题操作样式；未完成排序将下一前端切片推进到 `FE-REQ-007` |
| 删除 | 无 |
| 数据迁移 | 无 |
| 接口 | `GET /api/mdm/v1/master-data-types`；`GET /api/mdm/v1/field-templates`；`GET /api/mdm/v1/code-rules`；`GET /api/mdm/v1/master-data-records`；`GET /api/mdm/v1/import-tasks`；`GET /api/mdm/v1/data-quality-issues`；`GET /api/mdm/v1/publication-subscriptions`；`GET /api/mdm/v1/publications`；`GET /api/iam/v1/users`；`GET /api/iam/v1/roles`；`GET /api/iam/v1/permissions`；`GET /api/iam/v1/approval-instances`；`GET /api/iam/v1/operation-logs`；`GET /api/iam/v1/security-policies`；`GET /api/iam/v1/apps`；`GET /api/iam/v1/sso-clients` |
| 事件 | 前端不直接生产领域事件；本轮只接真实读模型，创建、审核、授权和菜单维护操作留给后续权限工作台细化 |
| 测试 | `npm run build` 已通过；后端全量 `mvn test -q` 已通过；`git diff --check` 已通过 |
| 风险/待办 | 真实浏览器/E2E 权限测试、动态菜单接口联调、路由拆包和生产化集成测试仍待补齐 |
| 交接提示 | `FE-REQ-007` 已由 `FE-LOG-007` 完成；下一步进入 `FE-REQ-008`：前端路由懒加载与 Ant Design 拆包 |

### FE-LOG-007 | 已完成 | 前端菜单权限拦截与基础 Web 权限测试

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/FE-REQ-007-前端权限拦截与基础Web测试.md`；IAM 权限点、Token 校验和权限快照能力 |
| 新增 | `auth/menuAccess.js`，定义核心菜单权限码、权限码解析、通配权限判断、菜单访问判断和菜单装饰逻辑；`auth/menuAccess.test.js` 覆盖 JSON/逗号权限码解析、启用条件、通配权限、菜单访问和父子菜单禁用 |
| 修改 | `App.jsx` 使用权限工具装饰菜单；存在 `access_token` 与 `permission_codes` 时启用菜单权限拦截，没有权限码时保持开发态全量菜单可用 |
| 删除 | 无 |
| 数据迁移 | 无 |
| 接口 | 本切片不新增接口；后续接真实登录和 IAM 权限快照接口 |
| 事件 | 无前端事件 |
| 测试 | `npm run test` 已通过，5 个权限单元测试通过；`npm run build` 已通过；后端全量 `mvn test -q` 已通过；待本切片结束执行 `git diff --check` |
| 风险/待办 | 当前仍不是完整安全边界，最终以后端鉴权为准；真实浏览器/E2E 权限测试、动态菜单接口联调和生产化集成测试仍待补齐 |
| 交接提示 | `FE-REQ-008` 已由 `FE-LOG-008` 完成；下一步进入 `QA-REQ-001`：真实浏览器/E2E 权限测试与生产化集成测试方案 |

### FE-LOG-008 | 已完成 | 前端路由懒加载与拆包

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/FE-REQ-008-前端路由懒加载与拆包.md`；`FE-LOG-001` 至 `FE-LOG-007` 的页面覆盖和构建警告 |
| 新增 | 页面加载态 `.page-loading`；Vite `chunkSizeWarningLimit` 调整为 700KB，匹配当前懒加载后的实际共享包大小 |
| 修改 | `App.jsx` 将 ASN、工作台、集成中心、采购、WMS/库存、OMS/TMS/BMS、主数据/权限页面改为 `React.lazy` + `Suspense` 按需加载；`app.css` 增加页面加载态样式 |
| 删除 | 无 |
| 数据迁移 | 无 |
| 接口 | 无 |
| 事件 | 无 |
| 测试 | `npm run test` 已通过；`npm run build` 已通过且不再出现 Vite 大 chunk 警告，最大共享 chunk 约 565KB；后端全量 `mvn test -q` 已通过；`git diff --check` 已通过 |
| 风险/待办 | 真实浏览器权限 E2E 已由 `QA-LOG-001` 完成；动态菜单接口联调和生产化集成测试仍待外部环境 |
| 交接提示 | 下一步在外部环境具备后实施 `INT-REQ-005` 和 `OPS-REQ-001`：真实 MQ/Dubbo/ERP/税务/支付联调、后端服务联调和生产化测试 |

## QA 与生产化测试

### QA-LOG-001 | 已完成 | 真实浏览器权限 E2E 与生产化集成测试阻塞复核

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/QA-REQ-001-真实浏览器权限E2E与生产化集成测试.md`；`FE-LOG-007`、`FE-LOG-008` |
| 新增 | `@playwright/test` 依赖、Chromium 运行时、`playwright.config.js`、`tests/e2e/menu-access.spec.js` |
| 修改 | `package.json` 增加 `test:e2e` 脚本；`package-lock.json` 同步 Playwright 依赖 |
| 删除 | 无 |
| 数据迁移 | 无 |
| 接口 | E2E 本轮只验证前端菜单权限，不依赖后端接口成功；页面进入采购页时因后端服务未启动出现 Vite 代理 `ECONNREFUSED 127.0.0.1:8101` 日志，不影响菜单权限断言 |
| 事件 | 无 |
| 测试 | `npm run test` 已通过，5 个 Vitest 权限单元测试通过；`npm run test:e2e` 已通过，Chromium 2 条测试通过；`npm run build` 已通过；后端全量 `mvn test -q` 已通过；`git diff --check` 已通过。E2E 覆盖开发态无权限快照时菜单可用、存在 `access_token + permission_codes` 时未授权菜单禁用、授权菜单可进入 |
| 风险/待办 | 生产化集成测试仍阻塞于真实后端服务、数据库、RocketMQ、Dubbo、ERP/税务/支付沙箱、测试账号和测试数据 |
| 交接提示 | 当前仓库内可完成的开发与验证已收口；继续推进需要准备 P1/P2 外部联调环境 |

## 集成中心

### INT-LOG-001 | 已完成 | 统一集成投递与补偿中心

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/INT-REQ-001-统一集成投递与补偿中心.md`；`docs/08-系统实现/01-技术选型与系统架构总览.md` 的集成架构、分布式一致性和消息投递要求 |
| 新增 | `integration-service` Maven 模块；`IntegrationServiceApplication`；`IntegrationRouteAggregate`、`IntegrationMessageAggregate`；`IntegrationApplicationService`；`IntegrationController`；`IntegrationMapper`；领域、应用和接口测试 |
| 修改 | 根 `project/backend/pom.xml` 注册 `integration-service` 模块；系统盘点中将“统一集成生产化”从未完成 P1 移除 |
| 删除 | 无 |
| 数据迁移 | `integration-service/src/main/resources/db/migration/V1__integration_core.sql` 新增 `int_route`、`int_message`、`int_dead_letter`、`int_operation_log` |
| 接口 | `POST/GET /api/integration/v1/routes`；`POST /api/integration/v1/routes/{routeNo}/disable`；`POST /openapi/integration/v1/events`；`POST /openapi/integration/v1/commands`；`GET /api/integration/v1/messages`；`POST /api/integration/v1/messages/{messageNo}/dispatch`；`POST /api/integration/v1/messages/{messageNo}/retry`；`GET /api/integration/v1/dead-letters`；`POST /api/integration/v1/dead-letters/{deadLetterNo}/replay` |
| 事件/补偿 | 同一 `sourceSystem + idempotencyKey` 幂等；无路由消息进入死信；派发失败累计 3 次进入死信；死信重放生成新的待派发消息并标记原死信已重放 |
| 测试 | `IntegrationAggregateTest` 覆盖路由停用、消息失败/重试/死信状态机；`IntegrationApplicationServiceTest` 覆盖路由接入、幂等、无路由死信、失败入死信和重放；`IntegrationControllerTest` 覆盖接口入口；`mvn -q -pl integration-service -am test` 已通过 |
| 风险/待办 | 当前首轮以数据库事实源和手工派发接口承接统一投递，尚未接真实 RocketMQ Producer、Dubbo 泛化调用、调度扫描任务、熔断限流和跨系统契约测试；这些属于生产化二期增强 |
| 交接提示 | 集成中心首轮完成；下一优先级进入 BMS 计费、对账、发票系统 |

### INT-LOG-002 | 已完成 | 投递运行面、传输端口与尝试日志

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/INT-REQ-002-集成中心投递运行面与尝试日志.md`；`docs/08-系统实现/01-技术选型与系统架构总览.md` 的本地事件表、投递任务、MQ、重试补偿要求 |
| 新增 | `IntegrationTransportPort`、`ConfiguredIntegrationTransportAdapter`、`IntegrationDispatchRuntimeApplicationService`；投递运行面 Controller 入口；投递尝试日志 Mapper 方法；领域运行面测试、应用服务测试和接口测试 |
| 修改 | `IntegrationController` 增加批量派发、投递尝试查询、运维汇总接口；`IntegrationMapper` 增加待派发消息扫描、消息路由定位、投递尝试写入和状态汇总查询 |
| 删除 | 无 |
| 数据迁移 | `integration-service/src/main/resources/db/migration/V2__integration_dispatch_runtime.sql` 新增 `int_delivery_attempt`，记录每次投递尝试的消息、通道、结果、失败原因、耗时和时间 |
| 接口 | `POST /api/integration/v1/dispatch-runs`；`GET /api/integration/v1/delivery-attempts?messageNo=`；`GET /api/integration/v1/operations/summary` |
| 事件/补偿 | 批量运行面扫描待投递/失败消息；投递成功进入已派发；投递失败沿用既有 `FAILED -> DEAD_LETTER -> REPLAYED` 状态机；缺少启用路由时写失败尝试，不丢载荷 |
| 测试 | `IntegrationDispatchRuntimeApplicationServiceTest` 覆盖成功派发、连续失败入死信、缺路由失败记录；`IntegrationControllerTest` 覆盖运行面接口委托；`mvn -q -pl integration-service -am test` 已通过 |
| 风险/待办 | 当前默认适配器支持 `LOCAL_ACK/LOCAL_FAIL` 通道和未配置通道明确失败；真实 RocketMQ Producer、Dubbo 泛化调用、HTTP 客户端、熔断限流和跨系统契约测试仍需 `INT-REQ-003` 接入运行环境后完成 |
| 交接提示 | 集成中心运行面完成；下一优先级是 `INT-REQ-003`：按环境配置接入 RocketMQ/Dubbo/HTTP 真实客户端，并为 WMS/库存/OMS/TMS/BMS 建契约测试 |

### INT-LOG-003 | 已完成 | HTTP/OpenAPI 端点配置、超时与熔断契约

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/INT-REQ-003-HTTP通道端点配置与熔断契约.md`；`docs/06-子系统接口设计/00-上下文映射与领域事件目录.md` 的事件标准载荷和幂等请求头规则 |
| 新增 | `int_endpoint` 端点配置；端点创建、查询、停用接口；`ConfiguredIntegrationTransportAdapter` 的 `HTTP/OPENAPI` 真实传输逻辑；HTTP 契约请求构造测试和端点熔断测试 |
| 修改 | `IntegrationApplicationService` 增加端点管理；`IntegrationController` 增加 `/endpoints` 运维接口；`IntegrationMapper` 增加端点持久化、查询和状态更新；传输适配器在 HTTP 失败时累计端点连续失败次数并按阈值停用端点 |
| 删除 | 无 |
| 数据迁移 | `integration-service/src/main/resources/db/migration/V3__integration_endpoint_transport.sql` 新增 `int_endpoint`，记录目标系统、通道、URL、超时、失败阈值、连续失败、状态和版本 |
| 接口 | `POST /api/integration/v1/endpoints`；`GET /api/integration/v1/endpoints`；`POST /api/integration/v1/endpoints/{endpointNo}/disable` |
| 事件/补偿 | HTTP 投递仍由 `dispatch-runs` 驱动；2xx 为成功，非 2xx/异常为失败；失败继续沿用消息重试、死信和人工重放链路；熔断端点不会丢弃消息载荷 |
| 测试 | `ConfiguredIntegrationTransportAdapterTest` 覆盖 HTTP 请求头契约和熔断；`IntegrationApplicationServiceTest` 覆盖端点管理；`IntegrationControllerTest` 覆盖端点接口委托；`mvn -q -pl integration-service -am test` 已通过 |
| 风险/待办 | 当前已具备不依赖外部 SDK 的 HTTP/OpenAPI 通道；RocketMQ Producer、Dubbo 泛化调用、ERP/税务/支付沙箱契约测试需要外部中间件和服务地址，列入 `INT-REQ-004` |
| 交接提示 | 集成中心在当前仓库可验收范围内已具备路由、接入、运行面、HTTP 传输、失败补偿、死信和重放；下一步必须先准备 MQ/Dubbo/外部系统测试环境 |

### INT-LOG-004 | 已完成 | MQ 与 Dubbo 通道契约预检

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/INT-REQ-004-MQ与Dubbo通道契约预检.md`；`docs/08-系统实现/01-技术选型与系统架构总览.md` 中 RocketMQ、Dubbo、超时和契约测试要求 |
| 新增 | `IntegrationEndpointContractApplicationService`；端点预检接口；RocketMQ、Dubbo、HTTP/OpenAPI、LOCAL 通道契约校验；契约预检应用服务测试 |
| 修改 | `IntegrationController` 增加 `POST /api/integration/v1/endpoints/{endpointNo}/verify`；接口测试补充端点预检委托 |
| 删除 | 无 |
| 数据迁移 | 无，复用 `V3__integration_endpoint_transport.sql` 的 `int_endpoint` |
| 接口 | `POST /api/integration/v1/endpoints/{endpointNo}/verify`，返回 `endpointNo`、`targetSystem`、`channelType`、`valid`、`message`、`metadata` |
| 事件/补偿 | 本切片不直接派发消息；用于投递前和上线前检查端点配置，避免 MQ/Dubbo 配置错误导致运行时批量失败 |
| 测试 | `IntegrationEndpointContractApplicationServiceTest` 覆盖 RocketMQ nameserver/topic/tag、Dubbo service/method/version、HTTP URL 和非法 MQ scheme；`IntegrationControllerTest` 覆盖接口委托；`mvn -q -pl integration-service -am test` 已通过 |
| 风险/待办 | 当前完成的是通道契约与预检，不伪造真实 MQ/Dubbo 成功；真实 RocketMQ Producer、Dubbo 泛化调用、ERP/税务/支付沙箱联调需 `INT-REQ-005` 在具备中间件和外部系统地址后实施 |
| 交接提示 | 继续开发前先准备 RocketMQ nameserver、Nacos/Dubbo 注册中心、目标服务接口或泛化调用方案，以及 ERP/税务/支付沙箱地址和验签资料 |

## BMS 系统

### BMS-LOG-001 | 已完成 | 计费对象、计费规则与费用来源

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/BMS-REQ-001-计费对象规则与费用来源.md`；`docs/09-开发计划/01-开发计划/07-BMS系统接口级开发计划.md` 的 `BMS-API-001/002` |
| 新增 | `bms-service` Maven 模块；`BmsServiceApplication`；`BmsDomain.BillingObjectAggregate`、`BillingRuleAggregate`、`ChargeSourceAggregate`、`ChargeDetailAggregate`；`BmsApplicationService`；`BmsController`；`BmsMapper`；领域/应用/接口测试 |
| 修改 | 根 `project/backend/pom.xml` 注册 `bms-service` 模块；系统复核中将 BMS 从“尚未形成独立服务”和 P1 未完成项移除 |
| 删除 | 无 |
| 数据迁移 | `bms-service/src/main/resources/db/migration/V1__bms_core.sql` 新增计费对象、计费规则、费用来源、费用明细、Outbox、Inbox 和审计表 |
| 接口 | `POST/GET /api/bms/v1/billing-subjects`；`POST /billing-subjects/{objectCode}/enable|disable`；`POST/GET /api/bms/v1/billing-rules`；`POST /billing-rules/{ruleNo}/publish`；`POST /openapi/bms/v1/charge-sources`；`GET /api/bms/v1/charge-sources`；`POST /charge-sources/{sourceNo}/replay` |
| 事件 | 生产 `BillingObjectCreated/Enabled/Disabled`、`BillingRulePublished`、`ChargeSourceAccepted/Failed`、`ChargeCalculated`，写入 `bms_domain_event`；费用来源按 `sourceSystem + idempotencyKey` 幂等 |
| 测试 | `BmsDomainTest` 覆盖规则计费、已确认费用不可重算和金额约束；`BmsApplicationServiceTest` 覆盖来源幂等、失败重放、规则发布后生成费用明细；`mvn -q -pl bms-service -am test` 已通过 |
| 风险/待办 | 当前首轮按本地规则快照计算费用，未接真实税务规则引擎、价格审批流和来源系统签名验签；后续归入生产化联调与安全增强 |
| 交接提示 | 继续阅读 `BMS-LOG-002`，对账账单发票财务能力已在同模块完成 |

### BMS-LOG-002 | 已完成 | 费用调整、对账、账单、发票与财务交接

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/BMS-REQ-002-费用调整对账账单发票财务.md`；`docs/09-开发计划/01-开发计划/07-BMS系统接口级开发计划.md` 的 `BMS-API-003/004` |
| 新增 | `ChargeAdjustmentAggregate`、`ReconciliationAggregate`、`BillAggregate`、`InvoiceAggregate`、`FinanceHandoverAggregate`；费用重算/作废、调整执行、对账确认、账单确认、发票开具、财务交接应用服务方法与接口 |
| 修改 | `BmsApplicationService` 增加金额守恒、对账确认、账单状态推进、发票金额上限和财务过账状态推进；费用确认后禁止直接重算，只允许调整单补差 |
| 删除 | 无 |
| 数据迁移 | `V1__bms_core.sql` 同步新增 `bms_adjustment`、`bms_reconciliation`、`bms_bill`、`bms_invoice`、`bms_finance_handover` |
| 接口 | `GET /api/bms/v1/charges`；`POST /charges/{chargeNo}/recalculate|void`；`POST /charge-adjustments`；`POST /charge-adjustments/{adjustmentNo}/execute`；`POST /reconciliation-statements`；`POST /reconciliation-statements/{reconciliationNo}/confirm|difference`；`POST /bills`；`POST /bills/{billNo}/confirm`；`POST /invoices`；`POST /invoices/{invoiceNo}/issue`；`POST /financial-handovers`；`POST /financial-handovers/{handoverNo}/post|fail` |
| 事件 | 生产 `ChargeRecalculated/Voided/Adjusted`、`ReconciliationIssued/Confirmed/DifferenceRaised`、`BillGenerated/Confirmed`、`InvoiceRequested/Issued`、`FinanceHandoverRequested`、`FinancialPosted/FinancialPostFailed` |
| 测试 | `BmsApplicationServiceTest` 覆盖费用重算、负向调整、对账确认、账单生成/确认、开票、财务过账和 Outbox 事件；`mvn -q -pl bms-service -am test` 已通过 |
| 风险/待办 | 财务交接首轮以本地回调接口推进状态，尚未接真实 ERP ACL、发票税控系统、差异审批流和附件存储 |
| 交接提示 | 下一条日志记录退款、报表和内部事件入口；生产化前需补 ERP/税务/支付契约测试 |

### BMS-LOG-003 | 已完成 | 退款结算、报表与通用事件入口

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/BMS-REQ-003-退款结算报表与事件入口.md`；`docs/09-开发计划/01-开发计划/07-BMS系统接口级开发计划.md` 的 `BMS-API-005` |
| 新增 | `RefundSettlementAggregate`、结算汇总读模型、BMS 内部事件 Inbox；退款结算、报表和通用事件入口接口 |
| 修改 | `BmsApplicationService.consumeEvent` 按 `sourceSystem + sourceEventId` 幂等登记外部事实，生产 `BmsExternalEventConsumed` |
| 删除 | 无 |
| 数据迁移 | `V1__bms_core.sql` 新增 `bms_refund_settlement`、`bms_event_consume_log`，复用 `bms_domain_event` |
| 接口 | `POST /api/bms/v1/refund-settlements`；`POST /refund-settlements/{refundNo}/finish|fail`；`GET /api/bms/v1/reports/settlement-summary`；`POST /internal/bms/v1/events` |
| 事件 | 生产 `RefundSettlementRequested/Finished/Failed`、`BmsExternalEventConsumed`；Inbox 支持重复事件幂等返回 |
| 测试 | `BmsApplicationServiceTest` 覆盖退款创建/完成、结算汇总和事件 Inbox；`BmsControllerTest` 覆盖接口委托；`mvn -q -pl bms-service -am test` 已通过 |
| 风险/待办 | 退款首轮按账单金额作为可退上限，尚未扣减历史退款累计、未接 OMS 售后单和支付/ERP 退款回执；这些列为真实集成二期 |
| 交接提示 | BMS 系统接口级计划首轮完成；下一优先级转向跨系统真实 MQ/Dubbo/ERP/税务/支付联调、前端页面和生产化治理 |

## 主数据系统

### MDM-LOG-001 | 已完成 | 主数据类型、字段模板与编码规则

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/MDM-REQ-001-主数据类型模板与编码规则.md`；`docs/09-开发计划/01-开发计划/08-主数据系统接口级开发计划.md` 的 `MDM-API-001` |
| 新增 | `mdm-service` Maven 模块；`MdmServiceApplication`；`MasterDataTypeAggregate`、`FieldTemplateAggregate`、`CodeRuleAggregate`；`MdmApplicationService`；`MdmController`；`MdmMapper`；领域/应用服务测试 |
| 修改 | 根 `project/backend/pom.xml` 注册 `mdm-service` |
| 删除 | 无 |
| 数据迁移 | `V1__mdm_type_template_code_rule.sql` 新增主数据类型、字段模板、编码规则、Outbox 和操作日志表 |
| 接口 | `POST/GET /api/mdm/v1/master-data-types`；`POST /master-data-types/{typeCode}/enable`；`POST /master-data-types/{typeCode}/disable`；`POST/GET /field-templates`；`POST /field-templates/{templateCode}/publish`；`POST /field-templates/{templateCode}/disable`；`POST/GET /code-rules`；`POST /code-rules/{ruleCode}/enable`；`POST /code-rules/{ruleCode}/disable`；`POST /code-rules/{ruleCode}/generate` |
| 事件 | 生产 `MasterDataTypeCreated/Enabled/Disabled`、`FieldTemplateCreated/Published/Disabled`、`CodeRuleCreated/Enabled/Disabled`、`MasterDataCodeGenerated`，写入 MDM Outbox |
| 测试 | `MdmAggregateTest` 覆盖类型启停、字段唯一、模板发布前置、编码规则状态和递增；`MdmApplicationServiceTest` 覆盖类型、模板、编码规则端到端、Outbox 和审计；全量 `mvn test -q` 已通过 |
| 风险/待办 | MDM 首切片已完成；仍缺主数据记录创建/审核/版本、发布订阅、导入导出、数据质量治理、OpenAPI 快照和真实 MySQL/Flyway 集成测试 |
| 交接提示 | 下一步按排序进入 OMS 销售订单与履约；若继续补主数据，应实施 `MDM-API-002` 主数据记录创建、审核与状态切换 |

### MDM-LOG-002 | 已完成 | 主数据记录审核、状态切换与版本生成

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/MDM-REQ-002-主数据记录审核版本闭环.md`；`docs/09-开发计划/01-开发计划/08-主数据系统接口级开发计划.md` 的 `MDM-API-002` |
| 新增 | `MasterDataRecordAggregate`、`MasterDataVersionAggregate`、`MasterDataRecordApplicationService`、`MasterDataRecordController`、`MasterDataRecordMapper`；领域、应用和接口测试 |
| 修改 | MDM 在既有类型/模板/编码规则基础上增加主数据记录事实源；记录创建前校验类型存在，按 `typeCode + dataCode` 保证唯一 |
| 删除 | 无 |
| 数据迁移 | `V2__mdm_record_version.sql` 新增 `mdm_master_data_record`、`mdm_master_data_version` |
| 接口 | `POST/GET /api/mdm/v1/master-data-records`；`PUT /master-data-records/{recordNo}`；`POST /submit-review`、`/approve`、`/reject`、`/freeze`、`/disable`；`GET /master-data-records/{recordNo}/versions` |
| 事件 | 生产 `MasterDataDraftCreated`、`MasterDataChanged`、`MasterDataSubmitted`、`MasterDataEnabled`、`MasterDataRejected`、`MasterDataFrozen`、`MasterDataDisabled`、`MasterDataVersionGenerated`，写入 MDM Outbox |
| 测试 | `MasterDataRecordLifecycleTest` 覆盖记录状态机和版本生成；`MasterDataRecordApplicationServiceTest` 覆盖创建、提交、批准、版本、重复编码、Outbox 和审计；`MasterDataRecordControllerTest` 覆盖接口委托 |
| 风险/待办 | 动态字段模板校验、审批流 IAM 回调、引用完整性校验和真实 MySQL/Flyway 集成测试仍需增强；当前批准时立即生成版本，尚未区分“批准”和“发布到订阅系统”的业务时点 |
| 交接提示 | 下一步已进入 `MDM-API-003` 发布订阅；发布任务以已批准版本作为不可变事实来源 |

### MDM-LOG-003 | 已完成 | 发布订阅、消费者回执与失败重试

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/MDM-REQ-003-发布订阅回执与重试.md`；`docs/09-开发计划/01-开发计划/08-主数据系统接口级开发计划.md` 的 `MDM-API-003` |
| 新增 | `PublicationSubscriptionAggregate`、`PublicationAggregate`、`MdmPublicationApplicationService`、`MdmPublicationController`、`MdmPublicationReceiptOpenApiController`、`MdmPublicationMapper`；发布订阅领域/应用/接口测试 |
| 修改 | MDM 发布从 `MasterDataVersionAggregate` 读取版本快照并按启用订阅生成发布任务；外部回执通过 Inbox 幂等 claim 处理 |
| 删除 | 无 |
| 数据迁移 | `V3__mdm_publication_subscription.sql` 新增 `mdm_publication_subscription`、`mdm_publication_log`、`mdm_event_inbox` |
| 接口 | `POST/GET /api/mdm/v1/publication-subscriptions`；`POST /publication-subscriptions/{subscriptionNo}/disable`；`POST/GET /api/mdm/v1/publications`；`POST /publications/{publicationNo}/retry`；`POST /openapi/mdm/v1/publication-receipts` |
| 事件 | 生产 `PublicationSubscriptionCreated`、`PublicationSubscriptionDisabled`、`MasterDataPublished`、`MasterDataPublishConfirmed`、`MasterDataRepublished`，写入 MDM Outbox；消费发布回执写入 MDM Inbox |
| 测试 | `MdmPublicationAggregateTest` 覆盖发布失败、重试、确认和订阅乐观锁；`MdmPublicationApplicationServiceTest` 覆盖发布、回执幂等、失败回执、重试、Outbox/Inbox；`MdmPublicationControllerTest` 覆盖管理接口和 OpenAPI 回执入口 |
| 风险/待办 | 真实 MQ Producer/Consumer、订阅系统身份验签、发布过滤规则解析和失败任务定时重试仍待生产化；发布失败目前保留在发布任务状态中，尚未自动生成运维工单 |
| 交接提示 | 下一步继续完成 `MDM-API-004/005`：导入导出、数据质量治理、OpenAPI 快照查询和事件入口；完成后 MDM 系统接口级计划可进入首轮完成状态 |

### MDM-LOG-004 | 已完成 | 导入导出与数据质量治理

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/MDM-REQ-004-导入导出与数据质量治理.md`；`docs/09-开发计划/01-开发计划/08-主数据系统接口级开发计划.md` 的 `MDM-API-004` |
| 新增 | `ImportTaskAggregate`、`DataQualityIssueAggregate`、`MdmImportQualityApplicationService`、`MdmImportQualityController`、`MdmImportQualityMapper`；导入/质量领域、应用和接口测试 |
| 修改 | MDM 在记录/发布能力基础上增加批量维护任务表、导入错误明细、导出任务和质量治理待办；导入任务按 `typeCode + fileHash + importMode` 幂等 |
| 删除 | 无 |
| 数据迁移 | `V4__mdm_import_quality.sql` 新增 `mdm_import_task`、`mdm_import_error`、`mdm_export_task`、`mdm_data_quality_issue` |
| 接口 | `POST/GET /api/mdm/v1/import-tasks`；`POST /import-tasks/{importTaskNo}/validate`、`/execute`、`/cancel`；`GET /import-tasks/{importTaskNo}/errors`；`GET /import-templates/{typeCode}`；`POST /records/export`；`GET /exports`；`POST/GET /data-quality-issues`；`POST /data-quality-issues/{issueNo}/assign|fix|verify|close` |
| 事件 | 生产 `ImportTaskCreated`、`ImportFileValidated`、`ImportTaskExecuted`、`ImportTaskCompleted`、`ImportTaskCancelled`、`MasterDataExportTaskCreated`、`DataQualityIssueRaised/Assigned/Fixed/Verified/Closed`，写入 MDM Outbox |
| 测试 | `MdmImportQualityAggregateTest` 覆盖导入任务和质量问题状态机；`MdmImportQualityApplicationServiceTest` 覆盖导入幂等、错误明细、执行结果、Outbox 和质量治理闭环；`MdmImportQualityControllerTest` 覆盖接口入口 |
| 风险/待办 | 当前首轮不解析真实对象存储文件，导入校验结果由命令提交；后续应接 CSV/Excel 解析器、错误文件生成器、对象存储上传和真实异步任务执行器 |
| 交接提示 | 下一步已进入 `MDM-API-005`：OpenAPI 快照、批量校验和内部事件入口 |

### MDM-LOG-005 | 已完成 | OpenAPI 快照、批量校验与内部事件入口

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/MDM-REQ-005-OpenAPI快照与事件入口.md`；`docs/09-开发计划/01-开发计划/08-主数据系统接口级开发计划.md` 的 `MDM-API-005` |
| 新增 | `MdmOpenApiApplicationService`、`MdmMasterDataOpenApiController`、`MdmInternalEventController`、`MdmOpenApiMapper`；OpenAPI 应用和接口测试 |
| 修改 | 内部事件入口复用 `mdm_event_inbox`；发布回执事件委托既有发布回执服务，资料变更/承运服务确认类事件可生成数据质量问题 |
| 删除 | 无 |
| 数据迁移 | 复用 `V3__mdm_publication_subscription.sql` 的 `mdm_event_inbox`；OpenAPI 查询直接读取 `mdm_master_data_record` 当前快照 |
| 接口 | `POST /openapi/mdm/v1/master-data/query`；`POST /openapi/mdm/v1/master-data/validate`；`GET /openapi/mdm/v1/master-data/{typeCode}/{dataCode}`；`POST /internal/mdm/v1/events` |
| 事件/Inbox | 内部事件写入 `mdm_event_inbox`，状态：处理中、成功、失败、忽略；成功消费生产 `MdmExternalEventConsumed`；发布回执继续生产 `MasterDataPublishConfirmed` |
| 测试 | `MdmOpenApiApplicationServiceTest` 覆盖已启用主数据校验、缺失失败、事件 Inbox 幂等和质量问题联动；`MdmOpenApiControllerTest` 覆盖 OpenAPI 与内部事件入口委托 |
| 风险/待办 | OpenAPI 首轮仍缺真实 appCode/签名验签、字段白名单、数据范围过滤、供应商/仓库专用投影和缓存；内部事件审批回调目前先登记/忽略，尚未接真实审批流推进记录聚合 |
| 交接提示 | MDM 系统接口级计划首轮完成；下一系统按未完成排序进入 IAM/集成生产化或 BMS 计费结算 |

## 权限系统

### IAM-LOG-001 | 已完成 | 登录会话、用户角色、权限点、数据范围、审批日志和安全策略

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/IAM-REQ-001-权限系统核心闭环.md`；`docs/09-开发计划/01-开发计划/09-权限系统接口级开发计划.md` 的 `IAM-API-001` 至 `IAM-API-005` |
| 新增 | `iam-service` Maven 模块；`IamServiceApplication`；`UserAggregate`、`RoleAggregate`、`SessionTokenAggregate`；`IamApplicationService`；`IamController`；`IamMapper`；领域/应用服务测试 |
| 修改 | 根 `project/backend/pom.xml` 注册 `iam-service` |
| 删除 | 无 |
| 数据迁移 | `V1__iam_core.sql` 新增用户、会话、角色、用户角色、权限点、角色权限、数据权限、审批、操作日志和安全策略表 |
| 接口 | `/auth/login`、`/auth/refresh`、`/auth/logout`、`/me`、`/users`、`/roles`、`/permissions`、`/data-scopes`、`/openapi/iam/v1/data-scope`、`/approval-instances`、`/operation-logs`、`/security-policies` |
| 事件 | 本切片先实现本地授权事实源；权限快照失效 Outbox、SSO、MFA、Redis 缓存后续增强 |
| 测试 | `UserAggregateTest` 覆盖登录失败锁定、启用和停用；`IamApplicationServiceTest` 覆盖登录刷新登出、me、角色授权、数据范围、审批、日志、安全策略；全量 `mvn test -q` 已通过 |
| 风险/待办 | 权限系统接口级计划已完成一轮；仍缺 JWT 真签名、Redis TokenCache、SSO/MFA、审批流引擎、真实权限快照缓存和 Web/API 集成测试 |
| 交接提示 | 下一系统按优先级进入 MDM 主数据；若继续打磨 IAM，应先补 Token 签名与权限快照缓存 |

### IAM-LOG-002 | 已完成 | JWT 令牌、Token 校验与权限快照

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/IAM-REQ-002-JWT令牌与权限快照生产化.md`；`docs/09-开发计划/01-开发计划/09-权限系统接口级开发计划.md` 的 `IAM-API-001`、`IAM-API-004` |
| 新增 | `IamJwtService`、`IamPermissionOpenApiApplicationService`、`IamPermissionOpenApiController`、`IamPermissionOpenApiMapper`；JWT、权限快照和 OpenAPI 测试 |
| 修改 | `IamApplicationService` 登录/刷新改为签发 HMAC-SHA256 JWT；`me` 和刷新会校验 JWT 签名，仍以 `iam_session` 作为撤销事实源 |
| 删除 | 无 |
| 数据迁移 | `V2__iam_jwt_permission_snapshot.sql` 扩容 `iam_session.access_token/refresh_token`，新增 `iam_permission_snapshot` 和 `iam_outbox_event` |
| 接口 | `POST /openapi/iam/v1/tokens/validate`；`GET /openapi/iam/v1/users/me/permissions`；`POST /openapi/iam/v1/permissions/check`；`POST /openapi/iam/v1/data-scopes/resolve` |
| 事件 | 权限快照失效生产 `PermissionSnapshotInvalidated`，写入 IAM Outbox |
| 测试 | `IamJwtServiceTest` 覆盖签名验签和篡改拒绝；`IamPermissionOpenApiApplicationServiceTest` 覆盖 Token 校验、撤销拒绝、权限快照、权限检查和数据范围解析；扩展 `IamApplicationServiceTest` 校验登录返回 JWT |
| 风险/待办 | 当前 TokenCache 以 MySQL 快照表承接，未接真实 Redis；JWT secret 仍为本地默认值，生产需接配置中心/密钥轮换；字段级权限脱敏策略仍需业务系统细化 |
| 交接提示 | 下一切片进入应用、菜单、SSO 和事件入口，完成后 IAM 接口级计划首轮闭环 |

### IAM-LOG-003 | 已完成 | 应用菜单、SSO 客户端与内部事件入口

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/IAM-REQ-003-应用菜单SSO与事件入口.md`；`docs/09-开发计划/01-开发计划/09-权限系统接口级开发计划.md` 的 `IAM-API-003`、`IAM-API-005` |
| 新增 | `IamApplicationAggregate`、`IamSsoClientAggregate`、`IamAdminApplicationService`、`IamAdminController`、`IamInternalEventController`、`IamAdminMapper`；领域、应用和接口测试 |
| 修改 | 权限系统补齐应用注册/启停、菜单注册/停用、SSO 配置/密钥重置和内部事件 Inbox 幂等消费；应用停用和菜单停用会生产权限快照失效事件 |
| 删除 | 无 |
| 数据迁移 | `V3__iam_app_menu_sso_inbox.sql` 新增 `iam_app`、`iam_menu`、`iam_sso_client`、`iam_event_inbox` |
| 接口 | `POST/GET /api/iam/v1/apps`；`PUT /api/iam/v1/apps/{appCode}`；`POST /apps/{appCode}/toggle`；`POST/GET /api/iam/v1/menus`；`POST /menus/{menuCode}/disable`；`POST/GET /api/iam/v1/sso-clients`；`POST /sso-clients/{ssoCode}/reset-secret`；`POST /internal/iam/v1/events` |
| 事件/Inbox | 生产 `IamAppCreated/Changed/Enabled/Disabled`、`IamMenuCreated/Disabled`、`SsoClientConfigured/SecretReset`、`PermissionSnapshotInvalidated`、`IamExternalEventConsumed`；内部事件 Inbox 支持成功、忽略和幂等命中 |
| 测试 | `IamAdminAggregateTest` 覆盖应用和 SSO 聚合版本规则；`IamAdminApplicationServiceTest` 覆盖应用、菜单、SSO、Outbox 和事件 Inbox 幂等；`IamAdminControllerTest`、`IamPermissionOpenApiControllerTest` 覆盖接口层入口 |
| 风险/待办 | SSO 首轮仅配置客户端和密钥生命周期，未实现完整 OAuth/OIDC 授权码流；内部事件目前只登记 API 扫描、主数据发布和安全风险，审批流引擎仍是轻量审批实例 |
| 交接提示 | IAM 系统接口级计划首轮完成；下一优先级进入统一集成生产化或 BMS 计费结算 |

## 计划治理

### SEC-LOG-001 | 已完成 | 统一后端认证授权公共契约

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/SEC-REQ-001-统一后端认证授权公共契约.md`；今日计划 `SEC-REQ-001` |
| 新增 | `ScmAccessContext`、`ScmJwtAuthenticationConverter`、`ScmSecurityProperties`、`ScmSecurityConfiguration` 及 3 个公共安全测试类 |
| 修改 | `scm-common/pom.xml` 增加 Spring Security、OAuth2 Resource Server、自动配置和测试依赖 |
| 删除 | 无 |
| 安全契约 | JWT 验签后转换操作人、应用、权限和数据范围；支持精确权限、全局权限和命名空间权限；数据范围默认拒绝；HMAC 密钥至少 32 字节 |
| DDD 边界 | 公共安全模块只建立认证授权上下文，不访问业务数据库、不修改业务聚合；业务状态规则仍由各限界上下文保护 |
| 测试 | 先确认缺失类型导致测试编译失败，再实现至 `mvn -q -pl scm-common test` 和后端全量 `mvn -q test` 通过；后端共 228 项测试、0 失败 |
| 风险/待办 | 各业务服务尚未导入公共安全配置；IAM 令牌权限和数据范围 Claims、业务接口权限映射由 `SEC-REQ-002/003` 接续 |
| 交接提示 | 下一项 `SEC-REQ-002`：采购、WMS、中央库存接入统一鉴权 |

### PLAN-LOG-001 | 已完成 | 2026-07-13 遗漏项开发计划与状态看板

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | 2026-07-13 功能完成度、未完成范围和开发计划遗漏审计 |
| 新增 | `docs/09-开发计划/03-今日开发计划-2026-07-13.md`、`tasks/plan.md`、`tasks/todo.md` |
| 修改 | `docs/09-开发计划/01-开发计划.md` 增加当前执行看板入口和“首轮完成/生产完成”口径 |
| 删除 | 无 |
| 计划内容 | 新增统一鉴权、采购持久化幂等与集成分发、调拨闭环、售后逆向、退款累计、Web/MySQL 集成测试、完整前端权限、计划索引、真实外部联调、运维治理和统一报表任务 |
| 状态规则 | `待开发`、`进行中`、`已完成`、`阻塞`；任务完成必须同步正式计划、`tasks/todo.md` 和开发日志 |
| 验收 | 计划包含依赖、优先级、状态、核心验收和阶段检查点；外部环境任务明确标记为阻塞 |
| 风险/待办 | `INT-REQ-005B`、`OPS-REQ-001` 仍依赖真实中间件、注册中心、外部沙箱和测试数据；其余任务可按依赖顺序推进 |
| 交接提示 | 下一项为 `SEC-REQ-001`：统一后端认证授权公共契约 |

## 单条日志模板

```markdown
### <系统缩写>-LOG-<序号> | <状态> | <功能切片>

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/...` |
| 新增 | 类、接口、表、事件、页面 |
| 修改 | 原类/接口/表及变化原因 |
| 删除 | 删除项与迁移/兼容策略 |
| 数据迁移 | Flyway 文件、回滚/数据修复说明 |
| 接口 | HTTP/Dubbo 契约、权限、幂等键、调用方 |
| 事件 | 生产/消费、载荷版本、Outbox/Inbox/重放 |
| 测试 | 命令、结果、覆盖的规则 |
| 风险/待办 | 已知限制、补偿、后续任务编号 |
| 交接提示 | 下一位开发者应先读的文件和下一步 |
```

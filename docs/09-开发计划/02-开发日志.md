# 供应链系统开发日志

## 使用规则

- 每完成一个可验收功能切片，立即在对应系统下新增一条记录；不得只在聊天中说明完成。
- 记录“新增、修改、删除、迁移、接口、事件、测试、已知问题”，保证其他模型可无缝接手。
- 不删除历史记录；修复通过新增日志关联原编号完成。
- 状态仅允许：`计划中`、`开发中`、`已完成`、`已回滚`、`阻塞`。

## 供应商系统

### SUP-LOG-001 | 已完成 | 供应商服务第一阶段至部分运营能力

| 项目    | 内容                                                                    |
| ----- | --------------------------------------------------------------------- |
| 新增    | 准入、资质、联系人/账号、供应商商品、报价、合同、采购确认、ASN、质量、退供、对账、发票、评分、运营、Outbox/Inbox 基础实现 |
| 修改    | 增加跨系统主动命令、采购订单变更/取消/关闭、入站真实重放、合同草稿版本、ASN 行级收货和 TMS 轨迹                 |
| 删除    | 无                                                                     |
| 数据迁移  | `V1` 至 `V24` 供应商服务迁移，包含集成命令、收件箱载荷、合同历史、质量证据、ASN收货/轨迹等                 |
| 接口/事件 | 见 `docs/06/01`、`docs/07/01`；新增接口必须在后续日志逐项登记                           |
| 验证    | JDK 21 离线 `mvn test -q` 已通过；尚缺真实 MySQL/RocketMQ/Dubbo 集成测试            |
| 待办    | 其余系统尚未创建独立后端服务；供应商服务仍需持续补齐 API 测试、导出、对账和全量运营报表                        |

### SUP-LOG-002 | 已完成 | 供应商系统工作台、导出任务与运营报表补齐

| 项目      | 内容                                                                                                                                                                                                                                                                                                                                                                              |
| ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 需求/设计来源 | `docs/09-开发计划/01-开发计划/01-供应商系统接口级开发计划.md` 的 `SUP-API-001`、`SUP-API-008`，以及供应商系统缺口检查结论                                                                                                                                                                                                                                                                                           |
| 新增      | `SupplierWorkbenchController`、`SupplierWorkbenchApplicationService`、`SupplierWorkbenchMapper`；`SupplierReportController`、`SupplierReportApplicationService`、`SupplierReportMapper`；运营导出任务接口和 `OperationViews.ExportTask`                                                                                                                                                        |
| 修改      | `SupplierOperationsApplicationService` 增加导出任务创建、查询、完成、失败能力；`SupplierOperationsController` 增加 `/exports` 相关接口；`SupplierOperationsMapper` 增加导出任务持久化方法                                                                                                                                                                                                                             |
| 删除      | 无                                                                                                                                                                                                                                                                                                                                                                               |
| 数据迁移    | `supplier-service/src/main/resources/db/migration/V25__supplier_workbench_export.sql`，新增 `sup_export_task` 导出任务表                                                                                                                                                                                                                                                                |
| 接口      | `GET /api/supplier/v1/workbench/summary`；`GET /api/supplier/v1/reports/fulfillment`；`GET /api/supplier/v1/reports/exceptions`；`POST /api/supplier/v1/operations/exports`；`GET /api/supplier/v1/operations/exports`；`GET /api/supplier/v1/operations/exports/{id}`；`POST /api/supplier/v1/operations/exports/{id}/complete`；`POST /api/supplier/v1/operations/exports/{id}/fail` |
| 事件      | 本切片不新增领域事件；工作台和报表读取供应商本地投影，导出任务写操作记录审计                                                                                                                                                                                                                                                                                                                                          |
| 测试      | 新增 `SupplierWorkbenchApplicationServiceTest`、`SupplierOperationsExportTest`，覆盖供应商范围收敛、统计天数校验、导出类型校验、导出任务创建和完成                                                                                                                                                                                                                                                                   |
| 风险/待办   | 供应商系统业务面已基本补齐；仍缺真实 MySQL/Flyway 集成测试、真实 RocketMQ/Dubbo 联调测试、Controller 权限/参数 Web 测试，以及导出文件生成器/对象存储上传实现                                                                                                                                                                                                                                                                          |
| 交接提示    | 下一步建议进入采购系统基础设施补齐：采购 Outbox 投递、Inbox 重放、WMS/TMS/BMS/IAM 事件消费和 Dubbo ACL 客户端                                                                                                                                                                                                                                                                                                     |

## 采购系统

### PUR-LOG-001 | 已完成 | 采购请购单纵向切片

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/01-开发计划/02-采购系统接口级开发计划.md` 的 `PUR-API-001`、`PUR-API-002` |
| 新增 | `purchase-service` Maven 模块；`PurchaseRequisitionController`；`PurchaseRequisitionApplicationService`；`PurchaseRequisitionQueryApplicationService`；`PurchaseRequisitionAggregate`、`PurchaseRequisitionLine`、`PurchaseRequisitionStatus`；MyBatis 请购资源库、读模型、Outbox、操作日志 |
| 修改 | 根 `pom.xml` 增加 `purchase-service` 模块 |
| 删除 | 无 |
| 数据迁移 | `purchase-service/src/main/resources/db/migration/V1__purchase_requisition.sql`，新增请购头、请购行、采购 Outbox、采购操作日志 |
| 接口 | `GET /api/purchase/v1/requisitions`、`GET /api/purchase/v1/requisitions/{id}`、`POST /api/purchase/v1/requisitions`、`PUT /api/purchase/v1/requisitions/{id}`、`POST /submit`、`POST /approve`、`POST /reject`、`POST /convert`；写接口要求 `X-Idempotency-Key`，支持 `X-Purchase-Org-Id` 数据范围 |
| 事件 | 生产 `PurchaseRequisitionCreated`、`PurchaseRequisitionChanged`、`PurchaseRequisitionSubmitted`、`PurchaseRequisitionApproved`、`PurchaseRequisitionRejected`、`PurchaseRequisitionConverted`，写入 `purchase_outbox_event` |
| 测试 | 新增 `PurchaseRequisitionAggregateTest` 覆盖重复 SKU+需求日期、提交审批转采购状态机、超批准数量转采购 |
| 风险/待办 | 幂等当前为内存实现，后续需迁移到 Redis/MySQL 幂等表；主数据 SKU、预算和审批 ACL 暂未真正调用；Outbox 投递任务、Inbox 审批回调将在采购后续接口切片补齐 |
| 交接提示 | 下一步实现 `PUR-API-003` RFQ 询价发布与截标，需复用本模块的 Outbox、审计、命令上下文和领域事件模式 |

### PUR-LOG-002 | 已完成 | RFQ 询价发布与截标纵向切片

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/01-开发计划/02-采购系统接口级开发计划.md` 的 `PUR-API-003`，以及 `docs/03-核心业务模型/02-采购领域模型/03-询价单聚合CQRS设计.md` |
| 新增 | `RfqController`；`RfqApplicationService`；`RfqQueryApplicationService`；`RfqAggregate`、`RfqLine`、`RfqInvitation`、`RfqStatus`；MyBatis RFQ 资源库、读模型、查询 Mapper |
| 修改 | `CommandContextFactory` 本地默认权限补充 `purchase:rfq:create/publish/close/read` |
| 删除 | 无 |
| 数据迁移 | `purchase-service/src/main/resources/db/migration/V2__purchase_rfq.sql`，新增 RFQ 头、询价行、邀请供应商三张表 |
| 接口 | `GET /api/purchase/v1/rfqs`、`GET /api/purchase/v1/rfqs/{rfqNo}`、`POST /api/purchase/v1/rfqs`、`POST /api/purchase/v1/rfqs/{rfqNo}/publish`、`POST /api/purchase/v1/rfqs/{rfqNo}/close`；写接口要求 `X-Idempotency-Key`，支持 `X-Purchase-Org-Id` 数据范围 |
| 事件 | 生产 `RfqCreated`、`RfqPublished`、`RfqBiddingClosed`；`RfqPublished` 按邀请供应商拆分事件写入 `purchase_outbox_event`，便于供应商系统创建对应报价待办 |
| 测试 | 新增 `RfqAggregateTest` 覆盖重复供应商、发布按供应商拆分事件、截标关闭供应商待办、重复发布状态冲突 |
| 风险/待办 | 供应商可协同、SKU 可采购、品类资质校验暂未真正调用主数据/供应商 ACL；RFQ 修改、取消、系统自动到期截标、供应商报价消费将在后续报价/比价切片补齐 |
| 交接提示 | 下一步实现 `PUR-API-004` 比价、定标与采购价格，需消费供应商报价并以 RFQ 截标状态作为比价前置条件 |

### PUR-LOG-003 | 已完成 | 比价、定标与采购价格纵向切片

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/01-开发计划/02-采购系统接口级开发计划.md` 的 `PUR-API-004`，`docs/03-核心业务模型/02-采购领域模型/05-比价结果聚合CQRS设计.md`，`06-采购价格聚合CQRS设计.md` |
| 新增 | `BidComparisonController`、`PurchasePriceController`；比价/采购价格应用服务、查询服务、读模型端口；`BidComparisonAggregate`、`BidCandidate`、`PurchasePriceAggregate`；MyBatis 比价与价格资源库/读模型 |
| 修改 | `CommandContextFactory` 本地默认权限补充 `purchase:comparison:*`、`purchase:price:*` |
| 删除 | 无 |
| 数据迁移 | `purchase-service/src/main/resources/db/migration/V3__comparison_and_price.sql`，新增比价结果、比价候选快照、采购价格三张表 |
| 接口 | `GET/POST /api/purchase/v1/bid-comparisons`、`GET /api/purchase/v1/bid-comparisons/{compareNo}`、`POST /api/purchase/v1/bid-comparisons/{compareNo}/award`、`GET/POST /api/purchase/v1/purchase-prices`、`GET /api/purchase/v1/purchase-prices/{priceNo}`、`POST /api/purchase/v1/purchase-prices/{priceNo}/disable` |
| 事件 | 生产 `CompareResultGenerated`、`CompareResultAwarded`、`PurchasePriceActivated`、`PurchasePriceDisabled`，写入采购 Outbox；定标时可按请求直接生成采购价格 |
| 测试 | 新增 `BidComparisonAggregateTest`、`PurchasePriceAggregateTest`，覆盖候选数量、定标候选校验、单一中标、价格含税价计算、有效期校验、价格停用 |
| 风险/待办 | 供应商报价仍由接口传入快照，尚未接入供应商报价事件消费；供应商合同/协议有效性、TMS 历史运输表现、BMS 价格结算约束暂未通过远程 ACL 校验 |
| 交接提示 | 下一步实现 `PUR-API-005` 采购订单发布、变更与取消，需要复用采购价格作为 PO 取价来源，并接入供应商合同/价格协议校验 |

### PUR-LOG-004 | 已完成 | 采购订单发布、变更与取消纵向切片

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/01-开发计划/02-采购系统接口级开发计划.md` 的 `PUR-API-005`，`docs/03-核心业务模型/02-采购领域模型/07-采购订单聚合CQRS设计.md` |
| 新增 | `PurchaseOrderController`、`PurchaseOrderChangeController`；采购订单/变更单应用服务、查询服务、读模型端口；`PurchaseOrderAggregate`、`PurchaseOrderLine`、`PurchaseOrderChangeAggregate`；MyBatis 订单与变更单资源库/读模型 |
| 修改 | `CommandContextFactory` 本地默认权限补充 `purchase:po:*`、`purchase:po-change:*` |
| 删除 | 无 |
| 数据迁移 | `purchase-service/src/main/resources/db/migration/V4__purchase_order.sql`，新增采购订单、采购订单行、采购订单变更单三张表 |
| 接口 | `GET/POST /api/purchase/v1/purchase-orders`、`GET /api/purchase/v1/purchase-orders/{orderNo}`、`POST /submit`、`POST /approve`、`POST /publish`、`POST /cancel`、`GET/POST /api/purchase/v1/order-changes`、`GET /api/purchase/v1/order-changes/{changeNo}`、`POST /api/purchase/v1/order-changes/{changeNo}/approve` |
| 事件 | 生产 `PurchaseOrderCreated`、`PurchaseOrderSubmitted`、`PurchaseOrderApproved`、`PurchaseOrderRejected`、`PurchaseOrderPublished`、`PurchaseOrderCancelled`、`PurchaseOrderChangeCreated`、`PurchaseOrderChangeEffective`、`PurchaseOrderChangeRejected` |
| 测试 | 新增 `PurchaseOrderAggregateTest`、`PurchaseOrderChangeAggregateTest`，覆盖提交审批发布状态机、已入库不可取消、变更生效更新版本、变更单重复审批冲突 |
| 风险/待办 | 供应商合同/价格协议、采购价格取价、仓库/SKU主数据、供应商协同配置暂未通过远程 ACL 校验；发布后的供应商/WMS/BMS/TMS 主动命令暂由 Outbox 事件承接，后续需补集成命令和失败补偿 |
| 交接提示 | 下一步实现 `PUR-API-006` 到货跟踪与退供申请，并补齐 PO 发布后供应商确认、WMS/TMS/BMS 事件消费 |

### PUR-LOG-005 | 已完成 | 到货跟踪与退供申请纵向切片

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/01-开发计划/02-采购系统接口级开发计划.md` 的 `PUR-API-006`，`docs/03-核心业务模型/02-采购领域模型/09-入库跟踪聚合CQRS设计.md`，`10-退供申请聚合CQRS设计.md` |
| 新增 | `InboundTrackingController`、`SupplierReturnController`；到货跟踪/退供申请应用服务、查询服务、读模型端口；`InboundTrackingAggregate`、`SupplierReturnAggregate`、`SupplierReturnLine`；MyBatis 到货跟踪与退供申请资源库/读模型 |
| 修改 | `CommandContextFactory` 本地默认权限补充 `purchase:inbound:*`、`purchase:supplier-return:*` |
| 删除 | 无 |
| 数据迁移 | `purchase-service/src/main/resources/db/migration/V5__inbound_and_supplier_return.sql`，新增采购到货跟踪、退供应商申请、退供应商申请行三张表 |
| 接口 | `GET /api/purchase/v1/inbounds`、`GET /api/purchase/v1/inbounds/{inboundNo}`、`POST /api/purchase/v1/inbounds/asns`、`POST /api/purchase/v1/inbounds/{inboundNo}/sync-wms`、`GET /api/purchase/v1/supplier-returns`、`GET /api/purchase/v1/supplier-returns/{returnNo}`、`POST /api/purchase/v1/supplier-returns`、`POST /api/purchase/v1/supplier-returns/{returnNo}/submit`、`POST /api/purchase/v1/supplier-returns/{returnNo}/approve`、`POST /api/purchase/v1/supplier-returns/{returnNo}/notify-execution` |
| 事件 | 生产 `PurchaseAsnRecorded`、`PurchaseGoodsReceived`、`PurchaseInspectionCompleted`、`PurchaseGoodsPutawayCompleted`、`SupplierReturnCreated`、`SupplierReturnSubmitted`、`SupplierReturnApproved`、`SupplierReturnRejected`、`SupplierReturnExecutionNotified`，统一写入采购 Outbox |
| 测试 | 新增 `InboundTrackingAggregateTest`、`SupplierReturnAggregateTest`，覆盖 ASN 记录、WMS 回传上架、超 ASN 数量拦截、退供数量约束、提交审批通知执行状态机 |
| 风险/待办 | 目前已具备采购侧到货/退供业务壳和 Outbox 事件；尚未真正主动调用 WMS 创建预约/出库、TMS 建运输任务、中央库存锁定/释放、BMS 冲减，也尚未接入 WMS/TMS/BMS RocketMQ 消费器 |
| 交接提示 | 下一步建议进入采购系统事件消费与跨系统集成命令切片，优先补 `PUR-EVT-001` 采购订单变更/取消/关闭消费、`PUR-EVT-002` WMS 收货/质检/上架消费、`PUR-EVT-003` TMS 运输轨迹消费 |

### PUR-LOG-006 | 已完成 | 采购系统事件投递、入站消费与跨系统命令补齐

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | 采购系统缺口检查结论；`docs/09-开发计划/01-开发计划/02-采购系统接口级开发计划.md` 的 `PUR-API-003` 至 `PUR-API-006` 事件消费和跨系统交互要求 |
| 新增 | 采购 Outbox 投递端口、投递应用服务、定时投递任务、RocketMQ 适配器；采购 Inbox 入站事件日志、原始载荷保存、人工重放服务；统一外部事件入口 `PurchaseExternalEventController`；供应商报价/确认、WMS入库、TMS运输、BMS应付事实投影；采购集成命令写入器 |
| 修改 | `PurchaseServiceApplication` 启用调度；`EventPersistenceMapper` 增加 Outbox claim/发布/失败方法；`PurchaseOrderApplicationService.publish` 发布后写供应商/WMS/BMS 集成命令；`SupplierReturnApplicationService.notifyExecution` 通知执行后写库存/WMS/TMS/BMS 集成命令；`InboundTrackingApplicationService` 增加 WMS 事件同步方法 |
| 删除 | 无 |
| 数据迁移 | `V6__purchase_inbox_and_integration.sql` 新增 `purchase_inbox_event`、`purchase_integration_command`；`V7__purchase_external_facts.sql` 新增供应商报价事实、供应商确认事实、WMS入库事实、TMS运输事实、BMS应付事实 |
| 接口 | `POST /internal/purchase/v1/events` 接收供应商/WMS/TMS/BMS/IAM 等外部事件；`GET /api/purchase/v1/operations/failed-events` 查询失败入站事件；`POST /api/purchase/v1/operations/failed-events/{id}/replay` 人工重放失败事件 |
| 事件 | Outbox 支持 RocketMQ 投递和失败重试；Inbox 支持幂等 claim、成功/忽略/失败状态、原始载荷保存和人工重放；统一消费 `SupplierQuoteSubmitted/Changed/Voided/Adopted`、`PurchaseOrderConfirmed/Rejected/DifferenceSubmitted`、`WmsReceiptCompleted/WmsQualityInspectionCompleted/WmsPutawayCompleted`、`TmsTransport*`、`BmsPayable*/BmsSettlementCompleted` |
| 测试 | 新增 `OutboxDispatchApplicationServiceTest`、`PurchaseExternalEventConsumerApplicationServiceTest`，覆盖 Outbox 成功/失败投递、外部报价事件幂等消费和事实投影写入 |
| 风险/待办 | 当前已具备可靠事件和命令箱骨架；尚未实现 `purchase_integration_command` 的 Dubbo 分发器和真实远程 ACL；供应商确认事实暂未反写采购订单状态，后续应给采购订单聚合补确认/差异状态迁移方法后接入 |
| 交接提示 | 下一步建议实现采购集成命令分发器：按 `target_system + command_type` 调 WMS/TMS/BMS/供应商系统 Dubbo/OpenAPI，并补失败重试、熔断和补偿 |

### PUR-LOG-007 | 已完成 | 供应商订单确认事件驱动采购订单状态

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/01-开发计划/02-采购系统接口级开发计划.md` 的 `PUR-API-005` 供应商确认消费要求；`docs/03-核心业务模型/02-采购领域模型/07-采购订单聚合CQRS设计.md` 的供应商确认状态机和 Inbox 消费规则 |
| 新增 | `PurchaseOrderApplicationService.recordSupplierResponse`；采购订单供应商确认、拒绝、差异反馈三个聚合行为；采购订单状态 `SUPPLIER_REJECTED`；迁移 `V8__purchase_order_supplier_rejected_status.sql`；`PurchaseOrderSupplierResponseApplicationServiceTest` |
| 修改 | `PurchaseExternalEventConsumerApplicationService` 支持供应商系统实际投递的 `PurchaseOrderConfirmedBySupplier`、`PurchaseOrderRejectedBySupplier`、`PurchaseOrderDifferenceReportedBySupplier`，并兼容原事件别名；消费后先写确认事实投影，再推进采购订单聚合并写本地 Outbox、审计日志 |
| 删除 | 无 |
| 数据迁移 | `V8__purchase_order_supplier_rejected_status.sql`，补充采购订单状态码 `12=供应商已拒绝` 的数据库字段说明；既有数据无需修复 |
| 接口 | 复用 `POST /internal/purchase/v1/events`；确认类事件必须提供 `X-Source-System`、`X-Event-Code`、`eventType`、`orderNo` 或 `businessNo`、`supplierId`；拒绝事件额外要求 `reason` |
| 事件 | 外部事件先进入 `purchase_inbox_event` 幂等处理；本地生产 `SupplierOrderConfirmationRecorded`、`SupplierOrderRejectionRecorded`、`SupplierOrderDifferenceRecorded`，写入 `purchase_outbox_event` |
| 测试 | 扩展 `PurchaseOrderAggregateTest` 覆盖确认、拒绝、差异状态机和禁止迁移；新增应用服务测试覆盖供应商确认后订单状态、审计和 Outbox 领域事件；JDK 21 离线 `mvn test -q` 已通过 |
| 风险/待办 | 供应商确认差异的行级数量、价格、交期仍保留在外部事实载荷中，尚未形成采购侧可操作的差异处理聚合；PO 发布时产生的 WMS/TMS 命令缺少 ASN、仓库 ID 等 Dubbo 合同字段，不能直接分发，需在 ASN 创建和退供审批切片补齐后再接入远程 ACL |
| 交接提示 | 下一步先实现供应商确认差异处理单（接受差异、重新协商、取消订单），再补采购订单变更、取消、关闭事件向供应商/WMS/BMS 的同步与补偿 |

### PUR-LOG-008 | 已完成 | 供应商确认差异处理页与采购订单协商闭环

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/06-子系统接口设计/02-采购系统接口设计.md` 的 5.9 供应商确认处理页；采购订单状态机“供应商差异 -> 供应商已确认/待供应商确认/已取消” |
| 新增 | `SupplierConfirmController`、`SupplierConfirmApplicationService`、`SupplierConfirmCommands`、`SupplierConfirmView`、`SupplierConfirmMapper`；迁移 `V9__purchase_supplier_confirm_processing.sql` 为供应商确认事实增加处理状态、处理人、处理时间、处理版本 |
| 修改 | `PurchaseOrderAggregate` 增加接受差异、重新协商行为；`PurchaseOrderApplicationService` 增加对应应用服务入口；`CommandContextFactory` 增加 `purchase:supplier_confirm:*` 权限点；采购订单领域测试增加协商状态机覆盖 |
| 删除 | 无 |
| 数据迁移 | `V9__purchase_supplier_confirm_processing.sql`，处理状态：1待处理、2已接受差异、3协商中、4已取消订单；以 `process_version` 做确认事实的乐观锁 |
| 接口 | `GET /api/purchase/v1/supplier-confirms`；`GET /api/purchase/v1/supplier-confirms/{confirmId}`；`POST /{confirmId}/accept-diff`；`POST /{confirmId}/renegotiate`；`POST /{confirmId}/cancel-order`。写接口继承公共契约，要求幂等键、数据范围和版本号 |
| 事件 | 接受差异生产 `SupplierOrderDifferenceAccepted`；重新协商生产 `SupplierOrderRenegotiationRequested`；取消沿用 `PurchaseOrderCancelled`；均与采购订单、审计和 Outbox 同事务保存 |
| 测试 | 扩展 `PurchaseOrderAggregateTest`，覆盖接受差异、重新协商和状态迁移限制；JDK 21 离线 `mvn test -q` 已通过 |
| 风险/待办 | 当前差异的行级数量/价格/交期仍保存在供应商原始事件载荷，尚未形成独立的采购订单变更单；重新协商事件已写 Outbox，但供应商系统需在后续消费 `SupplierOrderRenegotiationRequested` 或标准化 `PurchaseOrderChanged` 后创建新待办 |
| 交接提示 | 下一步实现采购订单关闭剩余数量及对供应商、WMS、BMS 的终止/关闭同步；随后补 ASN 创建后基于完整字段的 WMS 预约和 TMS 发运命令 |

### PUR-LOG-009 | 已完成 | 采购订单关闭剩余数量

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/03-核心业务模型/02-采购领域模型/01-采购系统领域模型.md` 的采购订单状态机；`docs/06-子系统接口设计/02-采购系统接口设计.md` 的采购关闭约束 |
| 新增 | `PurchaseOrderCommands.Close`、`POST /api/purchase/v1/purchase-orders/{orderNo}/close`、权限点 `purchase:po:close` |
| 修改 | `PurchaseOrderAggregate.closeRemaining` 仅允许供应商已确认、部分入库、供应商差异或供应商拒绝状态关闭，并要求关闭原因；关闭时递增业务版本、进入 `CLOSED`、生产 `PurchaseOrderClosed`。`PurchaseOrderApplicationService` 统一执行组织范围、幂等、乐观锁、审计、Outbox 保存 |
| 删除 | 无 |
| 数据迁移 | 无，复用采购订单既有状态 `10=已关闭` |
| 接口 | 请求体：`version`、`reason`；调用页面：采购订单详情/到货跟踪的“关闭剩余”操作；权限：`purchase:po:close`；写请求必须提供 `X-Idempotency-Key` |
| 事件 | 生产 `PurchaseOrderClosed`，供供应商协同、WMS、BMS 订阅关闭未执行义务；本地 Outbox 支持后续投递与人工重试 |
| 测试 | 扩展 `PurchaseOrderAggregateTest`，覆盖草稿禁止关闭、供应商确认后关闭、关闭事件；JDK 21 离线 `mvn test -q` 已通过 |
| 风险/待办 | 关闭事件的跨系统投递契约仍需补事件信封和供应商/WMS/BMS 消费端映射；已收货/已上架数量由 WMS 和中央库存事实源判定，采购侧当前尚未按行汇总后自动限制关闭数量 |
| 交接提示 | WMS-API-001 将优先实现入库单创建/取消；采购侧随后接入该入库单回执、收货、质检、上架事件，形成采购入库主闭环 |

## WMS 系统

### WMS-LOG-001 | 已完成 | 外部创建/取消入库单纵向切片

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/01-开发计划/03-WMS系统接口级开发计划.md` 的 `WMS-API-001`；`docs/05-子系统数据库设计/03-WMS系统数据库设计.md` 的 `wms_inbound` |
| 新增 | `wms-service` Maven 模块；`WmsServiceApplication`；`InboundOrderAggregate`、`InboundOrderStatus`、`InboundOrderRepository`；`InboundOrderApplicationService`；MyBatis 入库单 Mapper/资源库；`InboundOrderController`；领域测试 |
| 修改 | 根 `pom.xml` 注册 `wms-service` 模块 |
| 删除 | 无 |
| 数据迁移 | `wms-service/src/main/resources/db/migration/V1__wms_inbound_order.sql`，创建 `wms_inbound`，以来源类型、来源单号、仓库组成幂等唯一约束 |
| 接口 | `POST /openapi/wms/v1/inbound-orders`：校验 `X-Source-System` 与 `sourceType`、`X-Idempotency-Key`；`POST /api/wms/v1/inbound-orders/{id}/cancel`：按 `X-Warehouse-Id` 校验仓库范围、版本与取消原因 |
| 事件 | 本切片先固化入库单状态机和来源幂等；WMS Outbox/Inbox 与 `WmsInboundOrderCreated/Cancelled` 事件投递将在 PDA 收货切片统一接入，避免同一入库单拆成两套事件表 |
| 测试 | `InboundOrderAggregateTest` 覆盖待到货入库单取消、取消原因必填；JDK 21 离线 `mvn test -q` 已通过 |
| 风险/待办 | 尚未实现入库行、预约、收货单、扫码流水、Inbox/Outbox；当前创建接口使用来源+来源单号+仓库作为业务幂等边界，后续 ASN 行级变更需通过版本化变更命令处理 |
| 交接提示 | 下一步 `WMS-API-002`：新增收货单/收货行/PDA 扫码与提交，完成后由 Outbox 发送 `WmsArrivalRegistered`、`WmsReceiptCompleted` 给采购、供应商和中央库存 |

### WMS-LOG-002 | 已完成 | 收货、质检、上架核心聚合与数量不变量

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `WMS-API-002`、`WMS-API-003`；WMS 领域模型中“收货不等于质检、质检不等于上架、上架不等于中央库存可售”的边界 |
| 新增 | `ReceiptAggregate`、`ReceiptStatus`、`InspectionAggregate`、`PutawayTaskAggregate`；对应收货、质检、上架领域测试 |
| 修改 | 无 |
| 删除 | 无 |
| 数据迁移 | 无。本切片先落地领域数量规则，下一切片与收货/质检/上架表、扫码流水、库存流水一并持久化，避免中途表结构与聚合边界不一致 |
| 接口 | 后续接入 `POST /api/wms/v1/pda/receipts/scan`、`POST /api/wms/v1/receipts/{receiptNo}/submit`、`POST /api/wms/v1/inspections/{inspectionNo}/result`、`POST /api/wms/v1/pda/putaway/scan` |
| 事件 | 领域规则已明确为收货完成、质检完成、上架完成三类独立事实；Outbox 载荷、生产和消费将在持久化切片实现 |
| 测试 | `ReceiptAggregateTest`、`InspectionAggregateTest`、`PutawayTaskAggregateTest`，覆盖拒收原因、收货数量平账、质检数量平账、上架不超合格数量；JDK 21 离线 `mvn test -q` 已通过 |
| 风险/待办 | 本切片尚未接入数据库、PDA 权限、设备、仓库库位容量、库存流水、Outbox/Inbox，不可独立作为生产接口发布 |
| 交接提示 | 继续实施 WMS 收货持久化与 PDA 接口，并在同一事务创建质检单、上架任务、事件发布表和库存流水 |

### WMS-LOG-003 | 已完成 | 收货持久化、PDA 扫码幂等与收货完成事件

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/WMS-REQ-001-收货持久化与PDA扫码.md`；`docs/09-开发计划/01-开发计划/03-WMS系统接口级开发计划.md` 的 `WMS-API-002` |
| 新增 | `ReceivingController`；`ReceivingApplicationService`；`ReceiptRepository` MyBatis 实现；`wms_receipt`、`wms_receipt_scan`、`wms_operation_event` 相关 Mapper；应用服务测试 `ReceivingApplicationServiceTest` |
| 修改 | 整理 `ReceivingApplicationService`、`ReceivingController`、`ReceiptAggregate`、`ReceiptScanMapper`、`MyBatisWmsEventPublisher` 为正常 Java 多行格式；保留既有状态机和持久化口径 |
| 删除 | 无 |
| 数据迁移 | `V2__wms_operation_tables.sql` 创建 `wms_receipt`、`wms_operation_event`；`V4__wms_receipt_scan.sql` 创建 PDA 扫码幂等流水 |
| 接口 | `POST /api/wms/v1/receipts`；`POST /api/wms/v1/pda/receipts/scan`；`POST /api/wms/v1/receipts/{receiptNo}/submit`。扫码接口要求 `X-Idempotency-Key`，提交接口用收货单版本防并发 |
| 事件 | 打开收货单发布 `WmsArrivalRegistered`；提交收货发布 `WmsReceiptCompleted`，写入 WMS Outbox 表 `wms_operation_event` |
| 测试 | 新增 `ReceivingApplicationServiceTest`，覆盖打开收货、扫码、提交、事件发布、扫码幂等、缺少幂等键和版本冲突；全量 `mvn test -q` 已通过 |
| 风险/待办 | 当前仍未做真实 PDA 用户/设备/仓库数据权限校验；收货模型暂按单 SKU 收货单处理，多行 ASN 需在后续入库行/收货行切片补齐；质检/上架持久化和库存流水记账仍待开发 |
| 交接提示 | 下一步实施 `WMS-API-003`：质检结果、上架任务持久化、库位/库存流水、`QualityInspectionCompleted` 与 `PutawayCompleted` 事件 |

### WMS-LOG-004 | 已完成 | 质检结果、上架任务与仓内库存流水

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/WMS-REQ-002-质检上架与仓内库存流水.md`；`docs/09-开发计划/01-开发计划/03-WMS系统接口级开发计划.md` 的 `WMS-API-003` |
| 新增 | `InspectionApplicationServiceTest`、`PutawayApplicationServiceTest`，覆盖质检完成事件、上架库存流水、上架完成事件和版本冲突 |
| 修改 | 整理 `InspectionAggregate`、`PutawayTaskAggregate`、`InspectionApplicationService`、`PutawayApplicationService`、`InspectionController`、`PutawayController`、`InspectionMapper`、`PutawayMapper`、`StockLedgerMapper` 为正常 Java 多行格式 |
| 删除 | 无 |
| 数据迁移 | 复用 `V2__wms_operation_tables.sql` 的 `wms_inspection`、`wms_putaway_task`、`wms_stock_ledger`、`wms_operation_event` |
| 接口 | `POST /api/wms/v1/inspections`；`POST /api/wms/v1/inspections/{no}/result`；`POST /api/wms/v1/putaway-tasks`；`POST /api/wms/v1/pda/putaway/scan` |
| 事件 | 质检完成发布 `WmsQualityInspectionCompleted`；上架任务完成发布 `WmsPutawayCompleted`；上架扫描写 `wms_stock_ledger`，交易类型 `PUTAWAY_IN` |
| 测试 | `InspectionApplicationServiceTest`、`PutawayApplicationServiceTest` 加应用服务覆盖；全量 `mvn test -q` 已通过 |
| 风险/待办 | 上架扫描暂未做独立幂等键；库位容量、温层、混放、不合格区和中央库存账户同步仍待后续切片；WMS Outbox 仍缺真实投递任务和失败重试 |
| 交接提示 | 下一步按排序实施 `WMS-REQ-003`：WMS Outbox/Inbox 投递、入站命令、失败重放；随后进入中央库存 `INV-01` |

### WMS-LOG-005 | 已完成 | WMS Outbox 投递、失败查询与人工重试

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/WMS-REQ-003-Outbox投递与失败重试.md`；`docs/09-开发计划/01-开发计划/03-WMS系统接口级开发计划.md` 的 WMS 事件可靠性要求 |
| 新增 | `WmsOutboxDispatchApplicationService`、`WmsMessageBrokerPort`、`LoggingWmsMessageBrokerAdapter`、`WmsOperationsController`、`WmsOutboxDispatchApplicationServiceTest` |
| 修改 | `WmsEventMapper` 增加待投递查询、成功标记、失败标记、失败列表、人工重试方法 |
| 删除 | 无 |
| 数据迁移 | 复用 `V2__wms_operation_tables.sql` 的 `wms_operation_event`，状态：1 待投递、2 已投递、3 投递失败 |
| 接口 | `POST /api/wms/v1/operations/outbox/dispatch`；`GET /api/wms/v1/operations/outbox/failed-events`；`POST /api/wms/v1/operations/outbox/failed-events/{eventId}/retry` |
| 事件 | 覆盖所有 WMS 业务事件的 Outbox 投递骨架；当前消息端口为日志适配器，后续可替换 RocketMQ Producer |
| 测试 | `WmsOutboxDispatchApplicationServiceTest` 覆盖成功投递、失败标记、失败查询和人工重试；全量 `mvn test -q` 已通过 |
| 风险/待办 | 尚未实现真实 RocketMQ Producer、定时任务调度、Inbox 入站消费和死信重放；接口权限目前仍依赖后续 IAM |
| 交接提示 | 下一步实现 WMS Inbox 入站事件/命令消费；之后进入 `WMS-API-004` 外部创建出库单与库存分配 |

### WMS-LOG-006 | 已完成 | WMS Inbox 入站命令、失败查询与人工重放

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/WMS-REQ-004-Inbox入站命令与失败重放.md`；`WMS-API-001`、`WMS-API-004` 的外部命令入站幂等要求 |
| 新增 | `WmsInboxMapper`、`WmsInboundEventApplicationService`、`WmsInboundEventController`、`WmsInboundEventApplicationServiceTest` |
| 修改 | 无 |
| 删除 | 无 |
| 数据迁移 | 复用 `V3__wms_inbox_and_audit.sql` 的 `wms_inbox_event`，状态：1处理中、2成功、3失败 |
| 接口 | `POST /internal/wms/v1/events`；`GET /api/wms/v1/operations/inbox/failed-events`；`POST /api/wms/v1/operations/inbox/failed-events/{inboxId}/replay` |
| 事件 | 支持 `CreateInboundOrderRequested` 分发到入库单创建；支持 `CreateOutboundOrderRequested` 分发到出库单创建；以 `sourceSystem + eventCode` 做 Inbox 幂等 |
| 测试 | `WmsInboundEventApplicationServiceTest` 覆盖入库命令幂等、出库命令创建、失败事件查询与人工重放；全量 `mvn test -q` 已通过 |
| 风险/待办 | 当前入站入口为 HTTP 内部接口，尚未接真实 RocketMQ Listener 和签名验签；仅覆盖入库/出库创建命令，取消/变更命令将在出库分配切片继续扩展 |
| 交接提示 | 下一步进入 `WMS-API-004`：外部创建出库单、库存分配、取消释放和出库异常 |

### WMS-LOG-007 | 已完成 | 外部创建出库单、分配与取消

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/WMS-REQ-005-外部出库单与库存分配.md`；`docs/09-开发计划/01-开发计划/03-WMS系统接口级开发计划.md` 的 `WMS-API-004` |
| 新增 | `OutboundApplicationServiceTest`；扩展 `OutboundOrderAggregateTest` 覆盖取消原因和重复取消 |
| 修改 | 整理 `OutboundOrderAggregate`、`OutboundApplicationService`、`OutboundMapper`、`OutboundOrderController` 为正常 Java 多行格式；取消状态机增加重复取消拦截 |
| 删除 | 无 |
| 数据迁移 | 复用 `V2__wms_operation_tables.sql` 的 `wms_outbound` |
| 接口 | `POST /openapi/wms/v1/outbound-orders`；`POST /api/wms/v1/outbound-orders/allocate`；`POST /openapi/wms/v1/outbound-orders/cancel` |
| 事件 | 创建发布 `WmsOutboundOrderCreated`；分配发布 `WmsOutboundAllocated`；取消发布 `WmsOutboundCancelled`，统一进入 WMS Outbox |
| 测试 | `OutboundApplicationServiceTest` 覆盖创建、重复创建幂等、分配、取消、事件发布、版本冲突；全量 `mvn test -q` 已通过 |
| 风险/待办 | 当前出库单仍缺明细行、真实库位/批次分配、中央库存预占校验和取消释放库存；这些进入 `WMS-API-005` 波次拣货前补齐 |
| 交接提示 | 下一步实施 `WMS-API-005`：波次、拣货、容器与复核包装 |

### WMS-LOG-008 | 已完成 | 波次释放与拣货任务

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/WMS-REQ-006-波次释放与拣货任务.md`；`docs/09-开发计划/01-开发计划/03-WMS系统接口级开发计划.md` 的 `WMS-API-005` 前半段 |
| 新增 | `WaveMapper`、`PickTaskMapper`、`WavePickingApplicationService`、`WavePickingController`、`WavePickingApplicationServiceTest` |
| 修改 | 整理 `WaveAggregate`、`PickTaskAggregate` 和领域测试为正常 Java 多行格式 |
| 删除 | 无 |
| 数据迁移 | 复用 `V2__wms_operation_tables.sql` 的 `wms_wave`、`wms_pick_task` |
| 接口 | `POST /api/wms/v1/waves`；`POST /api/wms/v1/waves/release`；`POST /api/wms/v1/pick-tasks`；`POST /api/wms/v1/pda/pick-tasks/scan` |
| 事件 | 创建波次发布 `WmsWaveCreated`；释放波次发布 `WmsWaveReleased`；拣货完成发布 `WmsPickCompleted` |
| 测试 | `WavePickingApplicationServiceTest` 覆盖波次创建/释放、拣货任务创建、分批拣货完成、重复创建幂等、版本冲突；全量 `mvn test -q` 已通过 |
| 风险/待办 | 尚未实现容器绑定、复核包装、短拣异常、路径推荐和真实库存占用释放；`WMS-API-005` 仍需继续完成后半段 |
| 交接提示 | 下一步继续 `WMS-API-005`：容器绑定、复核包装、短拣异常；完成后进入 `WMS-API-006` 发货交接、盘点、仓内异常 |

### WMS-LOG-009 | 已完成 | 容器绑定、封箱与复核包装

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/WMS-REQ-007-容器绑定与复核包装.md`；`docs/09-开发计划/01-开发计划/03-WMS系统接口级开发计划.md` 的 `WMS-API-005` 后半段 |
| 新增 | `ContainerAggregate`、`PackingAggregate`、`ContainerMapper`、`PackingMapper`、`PackingApplicationService`、`PackingController`、`PackingApplicationServiceTest` |
| 修改 | 无 |
| 删除 | 无 |
| 数据迁移 | `V5__wms_container_and_packing.sql` 新增 `wms_container`、`wms_packing` |
| 接口 | `POST /api/wms/v1/containers/bind`；`POST /api/wms/v1/containers/seal`；`POST /api/wms/v1/packing`；`POST /api/wms/v1/packing/verify` |
| 事件 | 容器绑定发布 `WmsContainerBound`；封箱发布 `WmsContainerSealed`；复核包装发布 `WmsPackingVerified` |
| 测试 | `PackingApplicationServiceTest` 覆盖容器绑定、封箱、复核包装创建/确认、重复创建幂等、版本冲突；全量 `mvn test -q` 已通过 |
| 风险/待办 | 尚未实现包裹明细、称重、面单打印、短拣异常和 TMS 运单联动；进入 `WMS-API-006` 发货交接时继续补 |
| 交接提示 | 下一步实施 `WMS-API-006`：发货交接、盘点、仓内异常 |

### WMS-LOG-010 | 已完成 | 发货交接、盘点差异与仓内异常

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/WMS-REQ-008-发货交接盘点仓内异常.md`；`docs/09-开发计划/01-开发计划/03-WMS系统接口级开发计划.md` 的 `WMS-API-006` |
| 新增 | `ShipmentHandoverAggregate`、`StocktakeAggregate`、`WarehouseExceptionAggregate`；对应 Mapper、`WmsOperationApplicationService`、`WmsOperationController`、`WmsOperationApplicationServiceTest` |
| 修改 | 无 |
| 删除 | 无 |
| 数据迁移 | `V6__wms_handover_stocktake_exception.sql` 新增 `wms_shipment_handover`、`wms_stocktake`、`wms_warehouse_exception` |
| 接口 | `/handovers` 创建/确认；`/stocktakes` 创建和确认差异；`/warehouse-exceptions` 创建和关闭 |
| 事件 | `WmsShipmentHandedOver`、`WmsStocktakeDifferenceConfirmed`、`WmsWarehouseExceptionCreated`、`WmsWarehouseExceptionClosed` |
| 测试 | `WmsOperationApplicationServiceTest` 覆盖交接、盘点、异常创建/确认/关闭、事件发布、重复创建幂等；全量 `mvn test -q` 已通过 |
| 风险/待办 | WMS 计划功能已形成端到端代码骨架和测试基线；仍缺真实 RocketMQ、Dubbo/TMS/中央库存联调、Web 权限、真实 MySQL/Flyway 集成测试和更细的出入库行级模型 |
| 交接提示 | WMS 系统当前接口级计划已完成一轮；下一步按排序进入中央库存 `INV-01` 或补 WMS 集成测试/联调 |

## 2026-07-12 功能盘点与后续需求排序

### 已完成/基本完成

| 系统 | 完成情况 | 证据 |
| --- | --- | --- |
| 供应商系统 | 业务面基本补齐，仍缺真实 MySQL/Flyway、RocketMQ/Dubbo 联调和 Web 权限测试 | `SUP-LOG-*`，`supplier-service` 代码 |
| 采购系统 | 请购、RFQ、比价/价格、PO、到货、退供、Outbox/Inbox、供应商确认差异、关闭剩余已完成 | `PUR-LOG-001` 至 `PUR-LOG-009`，`purchase-service` 测试通过 |
| WMS 系统 | 接口级计划 `WMS-API-001` 至 `WMS-API-006` 已完成一轮代码骨架、迁移、事件和应用测试；剩余为联调/权限/行级模型增强 | `WMS-LOG-001` 至 `WMS-LOG-010`，`wms-service` 测试通过 |
| 中央库存系统 | 库存账户、流水、预占、冻结/调整、库存事件、快照和对账已完成一轮；剩余为真实 MQ/权限/集成测试增强 | `INV-LOG-001` 至 `INV-LOG-003`，`inventory-service` 测试通过 |
| 权限系统 | 登录会话、用户角色、权限点、数据范围、审批日志和安全策略已完成一轮；剩余为 JWT/缓存/SSO/MFA/权限快照增强 | `IAM-LOG-001`，`iam-service` 测试通过 |
| 主数据系统 | 主数据类型、字段模板、编码规则、编码生成、Outbox 和审计已完成首轮；剩余为记录审核、发布订阅、导入导出和 OpenAPI | `MDM-LOG-001`，`mdm-service` 测试通过 |
| OMS 系统 | 渠道订单、审单、履约分仓/换仓/拆分、库存预占/释放、WMS 出库命令和结果消费已完成首轮；取消售后和外部查询代码已补首轮，待测试验收 | `OMS-LOG-001`、`OMS-LOG-002`、`OMS-LOG-003` |

### 未开发/未完成排序

| 优先级 | 系统/功能 | 状态 | 排序理由 | 对应需求单/下一步 |
| --- | --- | --- | --- | --- |
| P0 | OMS-04 取消、售后、退款与异常 | 开发中/待测试 | 履约主链已形成，必须补齐发货前取消、退货入库、退款和异常处理，才能完成 OMS 核心闭环 | OMS-API-004、OMS-REQ-003 |
| P0 | OMS-05 外部订单查询与事件入口 | 开发中/待测试 | 渠道和客服需要看到履约/物流结果，且 WMS/TMS/BMS 事实必须能幂等回写 OMS | OMS-API-005、OMS-REQ-004 |
| P1 | TMS-01 运输任务与运单 | 未开始 | WMS 发货交接和采购到货需要运输任务事实源，OMS 外部履约轨迹也依赖 TMS | TMS-API-001、TMS-API-002 |
| P1 | MDM-02 主数据记录与发布订阅 | 未开始 | OMS、采购、WMS 的 SKU、仓库、客户和供应商校验仍缺稳定主数据事实源 | MDM-API-002、MDM-API-003 |
| P1 | IAM/集成生产化 | 未完成 | JWT、缓存、真实权限范围、Dubbo/消息分发、死信和人工补偿仍未通过真实联调 | IAM-API-003/004/005、INT-API-001 |
| P2 | BMS-01 计费、对账、发票 | 未开始 | 结算闭环依赖采购、WMS、TMS、OMS 的业务事实稳定，排在交易和履约之后 | BMS-API-001 至 BMS-API-005 |
| P3 | 报表、前端与上线治理 | 未完成 | 需要业务事实沉淀后建设统一分析、全量页面、压测、告警和上线演练 | RPT-API-001 |

## 中央库存系统

### INV-LOG-001 | 已完成 | 库存账户、流水与预占底座

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/INV-REQ-001-库存账户流水与预占.md`；`docs/09-开发计划/01-开发计划/04-中央库存系统接口级开发计划.md` 的 `INV-API-001`、`INV-API-002`、`INV-API-003` 和 WMS 记账入口基础 |
| 新增 | `inventory-service` Maven 模块；`InventoryServiceApplication`；`InventoryAccountAggregate`、`ReservationAggregate`；`InventoryApplicationService`；`InventoryController`；`InventoryMapper`；领域/应用服务测试 |
| 修改 | 根 `project/backend/pom.xml` 注册 `inventory-service` |
| 删除 | 无 |
| 数据迁移 | `V1__inventory_account_reservation.sql` 新增 `inv_stock_balance`、`inv_stock_ledger`、`inv_reservation` |
| 接口 | `GET /api/inventory/v1/stocks`；`GET /api/inventory/v1/stock-ledgers`；`POST /openapi/inventory/v1/wms/inbound`；`POST /openapi/inventory/v1/wms/outbound`；`POST /openapi/inventory/v1/reservations`；`POST /openapi/inventory/v1/reservations/{reservationNo}/release`；`POST /api/inventory/v1/freezes`；`POST /api/inventory/v1/freezes/unfreeze`；`POST /api/inventory/v1/adjustments` |
| 事件 | 本切片先写账户和不可变流水，Outbox 事件表和投递将在后续库存事件切片补齐 |
| 测试 | `InventoryAccountAggregateTest` 覆盖账户数量不变量；`InventoryApplicationServiceTest` 覆盖入库、预占、释放、冻结、解冻、调整和预占幂等 |
| 风险/待办 | 仍缺库存 Outbox/Inbox、冻结单独立审批聚合、调整单聚合、快照对账、WMS 事件消费信封、真实 MySQL/Flyway 集成测试和权限数据范围 |
| 交接提示 | 下一步实施 `INV-API-004`：冻结审批、调整单和库存事件 Outbox；随后补 `INV-API-005` 快照、对账与 WMS 事件入口 |

### INV-LOG-002 | 已完成 | 库存 Outbox/Inbox 与 WMS 入站记账

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/INV-REQ-002-库存事件与WMS入站记账.md`；`INV-API-005` 的 WMS 记账入口和事件可靠性要求 |
| 新增 | `InventoryEventMapper`、`InventoryEventApplicationService`、`InventoryEventController` |
| 修改 | 无 |
| 删除 | 无 |
| 数据迁移 | `V2__inventory_outbox_inbox.sql` 新增 `inv_outbox_event`、`inv_inbox_event` |
| 接口 | `POST /internal/inventory/v1/events`；`POST /api/inventory/v1/operations/outbox/dispatch` |
| 事件 | 消费 `WmsPutawayCompleted`、`WmsShipmentHandedOver`、`WmsStocktakeDifferenceConfirmed`；生产 `InventoryChanged` |
| 测试 | 后续与快照对账切片一并补应用服务组合测试；当前全量编译测试需通过 |
| 风险/待办 | 当前 payload parser 仅支持简单扁平 JSON；真实 MQ、死信和事件版本兼容待后续增强 |
| 交接提示 | 下一步补快照、对账与操作测试，完成中央库存系统接口级闭环 |

### INV-LOG-003 | 已完成 | 库存快照与对账

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/INV-REQ-003-库存快照与对账.md`；`INV-API-005` 的快照、对账与补偿要求 |
| 新增 | `InventorySnapshotMapper`、`InventorySnapshotApplicationService`、`InventorySnapshotController`、`InventorySnapshotApplicationServiceTest` |
| 修改 | 无 |
| 删除 | 无 |
| 数据迁移 | `V3__inventory_snapshot_reconcile.sql` 新增 `inv_stock_snapshot`、`inv_stock_reconcile` |
| 接口 | `POST /api/inventory/v1/snapshots/generate`；`GET /api/inventory/v1/snapshots`；`POST /api/inventory/v1/inventory-reconciliations`；`POST /api/inventory/v1/inventory-reconciliations/{reconcileNo}/confirm`；`GET /api/inventory/v1/inventory-reconciliations` |
| 事件 | 本切片不新增事件；对账确认后的差异自动调整留给后续增强 |
| 测试 | `InventorySnapshotApplicationServiceTest` 覆盖快照生成、对账差异计算和确认；全量 `mvn test -q` 已通过 |
| 风险/待办 | 中央库存系统接口级计划已完成一轮；仍缺真实 RocketMQ、权限数据范围、真实 MySQL/Flyway 集成测试、复杂事件载荷解析和冻结/调整独立审批工作流 |
| 交接提示 | 下一系统按优先级进入 IAM 或 MDM；若继续打磨库存，应补集成测试和事件载荷版本化 |

## OMS 系统

### OMS-LOG-001 | 已完成 | 渠道订单接入与销售订单审核

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/OMS-REQ-001-渠道订单接入与销售订单审核.md`；`docs/09-开发计划/01-开发计划/05-OMS系统接口级开发计划.md` 的 `OMS-API-001` |
| 新增 | `oms-service` Maven 模块；`OmsServiceApplication`；`SalesOrderAggregate`；`OmsApplicationService`；`OmsController`；`OmsMapper`；领域/应用服务测试 |
| 修改 | 根 `project/backend/pom.xml` 注册 `oms-service` |
| 删除 | 无 |
| 数据迁移 | `V1__oms_channel_order_sales_order.sql` 新增销售订单、渠道订单映射、Outbox 和操作日志表 |
| 接口 | `POST /api/oms/v1/channel-orders`；`GET /api/oms/v1/channel-orders`；`GET /api/oms/v1/sales-orders`；`GET /api/oms/v1/sales-orders/{orderNo}`；`POST /api/oms/v1/sales-orders/{orderNo}/review` |
| 事件 | 生产 `ChannelOrderReceived`、`SalesOrderCreated`、`SalesOrderReviewed`、`SalesOrderIntercepted`，写入 OMS Outbox |
| 测试 | `SalesOrderAggregateTest` 覆盖接单金额汇总、订单行校验、审核状态机和拦截原因；`OmsApplicationServiceTest` 覆盖渠道订单幂等接入、审核、Outbox 和审计；全量 `mvn test -q` 已通过 |
| 风险/待办 | OMS 首切片已完成；仍缺履约单、分仓、库存预占、WMS 出库下发、取消售后、外部订单查询和事件入口 |
| 交接提示 | 下一步实施 `OMS-API-002` 和 `OMS-API-003`，从销售订单审核通过后创建履约单、请求库存预占，并下发 WMS 出库命令 |

### OMS-LOG-002 | 已完成 | 分仓、库存预占与 WMS 出库下发

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/OMS-REQ-002-分仓预占与WMS出库下发.md`；`docs/09-开发计划/01-开发计划/05-OMS系统接口级开发计划.md` 的 `OMS-API-002`、`OMS-API-003`；履约单和出库单聚合设计 |
| 新增 | `FulfillmentAggregate`、`OutboundAggregate`；`FulfillmentApplicationService`；`FulfillmentMapper`；`FulfillmentController`、`OmsInventoryController`、`OmsOutboundController`、`OmsInternalEventController`；领域/应用服务测试 |
| 修改 | `OutboundAggregate` 增加 WMS 下发重推；OMS 外部事件入口按 `/internal/oms/v1/events` 契约接入；WMS 取消成功后触发库存释放补偿命令 |
| 删除 | 无 |
| 数据迁移 | `V2__oms_fulfillment_reservation_outbound.sql` 新增履约单、库存预占引用、出库单、集成命令 Outbox、事件 Inbox |
| 接口 | `POST /api/oms/v1/sales-orders/{salesOrderNo}/fulfillments`；`POST /api/oms/v1/fulfillments/{fulfillmentNo}/change-warehouse`；`POST /api/oms/v1/fulfillments/{fulfillmentNo}/split`；预占/释放；出库创建/下发/取消/重推；`POST /internal/oms/v1/events` |
| 事件 | 生产 `FulfillmentOrderCreated`、`FulfillmentWarehouseAllocated`、`FulfillmentWarehouseChanged`、`FulfillmentSplit`、`StockReservationRequested`、`StockReservationReleaseRequested`、`OutboundOrderCreated`、`OutboundInstructionIssued`、`OutboundCancelRequested`；消费库存预占和 WMS 出库事实 |
| 跨系统命令 | 预占/释放写 `oms_integration_command` 指向中央库存；创建/取消/重推 WMS 出库写同表指向 WMS；本期不直接修改外部数据库 |
| 测试 | `FulfillmentAggregateTest`、`OutboundAggregateTest` 覆盖状态机、数量守恒和取消前置；`FulfillmentApplicationServiceTest` 覆盖分仓、预占、事件 Inbox 幂等、WMS 接单/发货和 Outbox；全量 `mvn test -q` 已通过 |
| 风险/待办 | 当前跨系统仍是命令 Outbox，尚未接入真实中央库存/WMS ACL；分仓规则、正式订单行表、真实 MySQL/Flyway/消息联调仍待补；OMS 取消售后和外部查询事件投影未完成 |
| 交接提示 | 下一步继续实现 `OMS-API-004` 取消、售后、退款与异常，再实现 `OMS-API-005` 外部订单查询和事件入口 |

### OMS-LOG-003 | 开发中 | 取消申请、售后退款与外部查询首轮

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/OMS-REQ-003-取消申请与售后退款基础闭环.md`、`docs/09-开发计划/需求单/OMS-REQ-004-外部订单查询与事件入口.md`；OMS-API-004、OMS-API-005 |
| 新增 | `CancellationRequestAggregate`、`AfterSaleAggregate`；取消/售后应用服务；`CancellationMapper`；取消、售后、外部查询 Controller；OMS 统一事件入口路由 |
| 数据迁移 | `V3__oms_cancel_after_sale.sql` 新增取消申请和售后单表，复用 OMS Outbox/Inbox/集成命令表 |
| 接口/事件 | 新增取消申请、取消处理、售后创建/审核/退款/完成、外部订单/履约查询；统一消费 `WmsOutboundCancelled`、`StockReleased`、`RefundCompleted` |
| 测试 | 已新增取消/售后领域测试和应用服务测试文件；本轮 Maven 测试因执行环境授权额度耗尽尚未运行，不能标记已完成 |
| 风险/待办 | 需要重新获得 Maven 测试执行权限后验证编译、状态机和全量回归；真实 WMS/库存/BMS ACL、退货入库、TMS 拦截仍未实现 |
| 交接提示 | 当前继续保留在 OMS 开发中；测试通过后补 `OMS-LOG-003` 完成记录，再进入 TMS-API-001 |

## TMS 系统

_尚未开始独立服务开发。首项：TMS-01 运输任务与运单。_

## 2026-07-12 系统现状复核

### AUDIT-20260712-001 | 已完成 | 已完成功能与未完成范围复核

| 项目 | 结论 |
| --- | --- |
| 已完成/首轮完成 | 供应商、采购、WMS、中央库存、权限、主数据均已有首轮代码、迁移、领域/应用测试；OMS 已完成渠道订单、履约分仓、库存预占和 WMS 出库首轮闭环。 |
| 尚未形成独立服务 | TMS、BMS 尚无后端服务模块；统一集成中心也尚未独立建设。 |
| P0 | OMS 取消、售后、退款、异常，以及外部订单状态/履约轨迹/事件入口。 |
| P1 | TMS 运输任务与运单；MDM 主数据记录与发布订阅；IAM 真 JWT/缓存/SSO/MFA；真实跨系统命令分发与联调。 |
| P2 | BMS 计费结算；供应商/采购/WMS/库存生产化集成测试和权限 Web 测试；前端业务页面。 |
| P3 | 统一报表分析、压测、告警、容灾和上线演练。 |
| 本轮动作 | 已完成 `OMS-API-002/003` 并生成 `OMS-REQ-003/004`，继续实现 OMS-API-004/005；Maven 测试因环境授权额度暂未执行。 |
| 验证基线 | 现有各服务 Surefire 报告无失败记录；本轮新增代码完成后重新运行 `mvn test -q`。 |

## BMS 系统

_尚未开始独立服务开发。首项：BMS-01 计费对象与计费规则。_

## 主数据系统

### MDM-LOG-001 | 已完成 | 主数据类型、字段模板与编码规则

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/MDM-REQ-001-主数据类型模板与编码规则.md`；`docs/09-开发计划/01-开发计划/08-主数据系统接口级开发计划.md` 的 `MDM-API-001` |
| 新增 | `mdm-service` Maven 模块；`MdmServiceApplication`；`MasterDataTypeAggregate`、`FieldTemplateAggregate`、`CodeRuleAggregate`；`MdmApplicationService`；`MdmController`；`MdmMapper`；领域/应用服务测试 |
| 修改 | 根 `project/backend/pom.xml` 注册 `mdm-service` |
| 删除 | 无 |
| 数据迁移 | `V1__mdm_type_template_code_rule.sql` 新增主数据类型、字段模板、编码规则、Outbox 和操作日志表 |
| 接口 | `POST/GET /api/mdm/v1/master-data-types`；`POST /master-data-types/{typeCode}/enable`；`POST /master-data-types/{typeCode}/disable`；`POST/GET /field-templates`；`POST /field-templates/{templateCode}/publish`；`POST /field-templates/{templateCode}/disable`；`POST/GET /code-rules`；`POST /code-rules/{ruleCode}/enable`；`POST /code-rules/{ruleCode}/disable`；`POST /code-rules/{ruleCode}/generate` |
| 事件 | 生产 `MasterDataTypeCreated/Enabled/Disabled`、`FieldTemplateCreated/Published/Disabled`、`CodeRuleCreated/Enabled/Disabled`、`MasterDataCodeGenerated`，写入 MDM Outbox |
| 测试 | `MdmAggregateTest` 覆盖类型启停、字段唯一、模板发布前置、编码规则状态和递增；`MdmApplicationServiceTest` 覆盖类型、模板、编码规则端到端、Outbox 和审计；全量 `mvn test -q` 已通过 |
| 风险/待办 | MDM 首切片已完成；仍缺主数据记录创建/审核/版本、发布订阅、导入导出、数据质量治理、OpenAPI 快照和真实 MySQL/Flyway 集成测试 |
| 交接提示 | 下一步按排序进入 OMS 销售订单与履约；若继续补主数据，应实施 `MDM-API-002` 主数据记录创建、审核与状态切换 |

## 权限系统

### IAM-LOG-001 | 已完成 | 登录会话、用户角色、权限点、数据范围、审批日志和安全策略

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/09-开发计划/需求单/IAM-REQ-001-权限系统核心闭环.md`；`docs/09-开发计划/01-开发计划/09-权限系统接口级开发计划.md` 的 `IAM-API-001` 至 `IAM-API-005` |
| 新增 | `iam-service` Maven 模块；`IamServiceApplication`；`UserAggregate`、`RoleAggregate`、`SessionTokenAggregate`；`IamApplicationService`；`IamController`；`IamMapper`；领域/应用服务测试 |
| 修改 | 根 `project/backend/pom.xml` 注册 `iam-service` |
| 删除 | 无 |
| 数据迁移 | `V1__iam_core.sql` 新增用户、会话、角色、用户角色、权限点、角色权限、数据权限、审批、操作日志和安全策略表 |
| 接口 | `/auth/login`、`/auth/refresh`、`/auth/logout`、`/me`、`/users`、`/roles`、`/permissions`、`/data-scopes`、`/openapi/iam/v1/data-scope`、`/approval-instances`、`/operation-logs`、`/security-policies` |
| 事件 | 本切片先实现本地授权事实源；权限快照失效 Outbox、SSO、MFA、Redis 缓存后续增强 |
| 测试 | `UserAggregateTest` 覆盖登录失败锁定、启用和停用；`IamApplicationServiceTest` 覆盖登录刷新登出、me、角色授权、数据范围、审批、日志、安全策略；全量 `mvn test -q` 已通过 |
| 风险/待办 | 权限系统接口级计划已完成一轮；仍缺 JWT 真签名、Redis TokenCache、SSO/MFA、审批流引擎、真实权限快照缓存和 Web/API 集成测试 |
| 交接提示 | 下一系统按优先级进入 MDM 主数据；若继续打磨 IAM，应先补 Token 签名与权限快照缓存 |

## 单条日志模板

```markdown
### <系统缩写>-LOG-<序号> | <状态> | <功能切片>

| 项目 | 内容 |
| --- | --- |
| 需求/设计来源 | `docs/...` |
| 新增 | 类、接口、表、事件、页面 |
| 修改 | 原类/接口/表及变化原因 |
| 删除 | 删除项与迁移/兼容策略 |
| 数据迁移 | Flyway 文件、回滚/数据修复说明 |
| 接口 | HTTP/Dubbo 契约、权限、幂等键、调用方 |
| 事件 | 生产/消费、载荷版本、Outbox/Inbox/重放 |
| 测试 | 命令、结果、覆盖的规则 |
| 风险/待办 | 已知限制、补偿、后续任务编号 |
| 交接提示 | 下一位开发者应先读的文件和下一步 |
```

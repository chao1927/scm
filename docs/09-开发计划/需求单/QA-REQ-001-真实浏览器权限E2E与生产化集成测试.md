# QA-REQ-001 真实浏览器权限 E2E 与生产化集成测试

## 1. 背景

前端业务页面、基础菜单权限规则和路由拆包已完成。需要补真实浏览器 E2E 权限验证，并明确生产化集成测试所需外部条件。

## 2. 已完成范围

| 范围 | 结果 |
| --- | --- |
| Playwright 安装 | 已安装 `@playwright/test` 和 Chromium 运行时 |
| E2E 配置 | 已新增 `playwright.config.js`，使用独立端口 `5174` |
| 菜单权限 E2E | 已覆盖开发态全菜单可用、存在权限快照时未授权菜单禁用 |
| 验证 | `npm run test:e2e` 已通过，2 条 Chromium 测试通过 |

## 3. 剩余生产化集成测试

| 测试项 | 所需条件 | 当前状态 |
| --- | --- | --- |
| 前后端真实 API 联调 | 后端服务、数据库、测试数据、统一网关或代理配置 | 阻塞 |
| RocketMQ/Dubbo 联调 | nameserver、topic、tag、ACL、Nacos/Dubbo 注册中心、目标接口 | 阻塞 |
| ERP/税务/支付沙箱 | 沙箱地址、凭证、验签资料、样例载荷、错误码 | 阻塞 |
| 权限快照联调 | 登录账号、权限点、菜单授权、Token 签发配置 | 阻塞 |

## 4. 结论

当前仓库内可完成的前端 E2E 权限测试已完成；生产化集成测试必须等待外部服务、中间件和测试数据准备完成后继续。

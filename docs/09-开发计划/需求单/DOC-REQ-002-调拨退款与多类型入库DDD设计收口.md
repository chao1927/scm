# DOC-REQ-002 调拨、退款与多类型入库 DDD 设计收口

> 状态：已完成（2026-07-30）。该需求只收口设计与计划事实，不以重新实现已完成代码为目标。

## 1. 背景

调拨、退款和售后退货已经有需求、接口或实现证据，但部分能力没有完整回写到聚合索引、状态机和接口级开发计划，形成“代码存在、领域事实源不完整”的设计债务。

## 2. 收口范围

| 上下文 | 必须补齐 | 核心不变量 |
| --- | --- | --- |
| 中央库存 | 调拨单聚合、`INV-API-006`、在途/差异/取消补偿 | 申请、预占、出库、在途、入库、差异数量可守恒；调入仓上架前不增加可用量 |
| BMS | 退款结算聚合或其明确归属、累计退款和支付回执状态机 | 累计退款不超过可退金额；同一支付回执只应用一次 |
| WMS | 入库单业务类型规则：采购、调拨、售后退货 | 来源业务键唯一；不同类型使用明确的收货、质检、上架和库存处置规则 |

## 3. 文档验收

- 聚合索引、领域模型、接口级计划、事件和测试清单相互引用且术语一致。
- 明确聚合根、命令、事件、状态机、幂等键、资源库、读模型、权限和异常补偿。
- 已有实现若与设计不一致，登记影响和后续实现任务，不得通过修改文字掩盖差异。

## 继续上下文

当前结论：这是设计事实源收口，不改变九个限界上下文。

关键假设：WMS 复用入库聚合，通过业务类型表达差异。

待决问题：BMS 退款是独立聚合还是财务交接聚合内部实体，由不变量边界决定。

下一步：设计差异已分别转入并执行 `INV-NEXT-002`、`WMS-NEXT-002`、`BMS-NEXT-002`。
